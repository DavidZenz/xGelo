---
phase: 11-hybrid-ml-and-contextual-priors
plan: 06
type: execute
wave: 4
depends_on:
  - 11-05
files_modified:
  - _targets.R
  - R/benchmark/hybrid_runner.R
  - R/evaluation/challenger_selection.R
  - outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/
  - tests/testthat/test_hybrid_targets.R
autonomous: true
requirements:
  - HYBRID-01
  - HYBRID-02
  - HYBRID-03
  - HYBRID-04
  - HYBRID-05
must_haves:
  truths:
    - "Phase 11 publishes a research-only benchmark bundle with exact 630 open and 609 rich denominators, G=40, checksums, mode labels, and sealed-WC2026 flags."
    - "The final output is a shortlist/evidence bundle for Phase 12, not a promotion or release decision."
  artifacts:
    - "outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/run_manifest.csv"
    - "outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/manifests/checksum_manifest.csv"
    - "outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/selection/hybrid_shortlist.csv"
  key_links:
    - "Phase 11 registries -> targets DAG -> runner -> scores/comparisons -> research-only bundle"
---

<objective>
Integrate Phase 11 into targets and publish the research-only evidence bundle.

Purpose: Make all Phase 11 candidates reproducible through the project DAG while preserving Phase 12 as the only calibration/promotion/release authority.
Output: Phase 11 target chain, bundle validator, comparisons, shortlist, and durable artifacts.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md
@.planning/phases/10-statistical-goal-model-challengers/10-VERIFICATION.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@R/benchmark/challenger_runner.R
@R/benchmark/runner.R
@R/evaluation/challenger_selection.R
@_targets.R
@tests/testthat/test_hybrid_targets.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 11 runner, comparisons, and research shortlist</name>
  <files>R/benchmark/hybrid_runner.R, R/evaluation/challenger_selection.R, tests/testthat/test_hybrid_targets.R</files>
  <read_first>R/benchmark/challenger_runner.R; R/benchmark/runner.R; R/evaluation/challenger_selection.R; outputs/benchmarks/rolling_tournaments/phase10-statistical-challengers/selection/shortlist.csv</read_first>
  <behavior>
    - D-04: RF/context/structural/xG-gated candidates are accepted only through existing rolling folds and proper-score comparisons.
    - D-15 / D-16: Open, enriched, and external modes are reported separately; only open mode appears in open-default candidate evidence.
  </behavior>
  <action>Complete `run_hybrid_challenger_benchmark()`, `write_hybrid_challenger_bundle()`, `validate_hybrid_challenger_bundle()`, and a research-only shortlist builder such as `build_hybrid_research_shortlist()`. Compare open-mode Phase 11 candidates against the Phase 10 statistical handoff and inherited baselines using paired fold deltas; include mode-specific companion evidence for enriched/external paths without pooling leaderboards. The shortlist must contain evidence hashes and labels without crossing the Phase 12 decision boundary.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_targets.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Bundle validation confirms exact panels, G=40 distributions, mode labels, feature coverage, checksum parents, `research_only=TRUE`, `wc2026_sealed=TRUE`, and no Phase 12 decision authority.</acceptance_criteria>
  <done>Phase 11 runner can publish validated research evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Targets chain and canonical bundle publication</name>
  <files>_targets.R, outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/, tests/testthat/test_hybrid_targets.R</files>
  <read_first>_targets.R; tests/testthat/test_statistical_targets.R; R/benchmark/challenger_runner.R</read_first>
  <behavior>
    - HYBRID-01..05: `_targets.R` has Phase 11 registry-files, registries, predictions, scores, comparisons, and bundle-files targets.
    - The target DAG is downstream-only from Phase 09/10 artifacts and local/manual Phase 11 inputs; it does not invoke dashboard publication, external collection, or WC2026 retrospective outputs.
  </behavior>
  <action>Source the new Phase 11 files in `_targets.R` after their dependencies. Add targets named `benchmark_phase11_registry_files`, `benchmark_phase11_registries`, `benchmark_phase11_predictions`, `benchmark_phase11_scores`, `benchmark_phase11_comparisons`, and `benchmark_phase11_bundle_files`, mirroring the Phase 10 six-node chain. Include Phase 09 registries, accepted Phase 10 bundle files, Phase 11 protocol manifests, goal training feature file, optional local squad aggregate file, and optional manual market manifest as explicit file parents. Publish to `outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers/` only after validation.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_targets.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
    <automated>Rscript -e 'targets::tar_make(benchmark_phase11_bundle_files)'</automated>
    <automated>Rscript -e 'validate_hybrid_challenger_bundle("outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers")'</automated>
  </verify>
  <acceptance_criteria>Phase 11 targets run to a validated bundle; bundle manifests include exact source snapshots, data vintages, licensing fields, missingness, active/inactive status, mode/panel identity, checksums, and research-only/sealed flags.</acceptance_criteria>
  <done>All HYBRID requirements are represented in a durable Phase 11 research-only benchmark bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 11 candidate outputs -> persisted bundle | Predictions and evidence become durable research artifacts. |
| Targets DAG -> protected outputs | Pipeline must not mutate dashboards, WC2026 retrospective outputs, or sealed holdout state. |
| Research shortlist -> Phase 12 handoff | Evidence must not become a release decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-22 | Tampering | `write_hybrid_challenger_bundle()` | high | mitigate | Use staged write, pre/post validation, checksums, row counts, and parent graph validation inherited from Phase 10. |
| T-11-23 | Information Disclosure | `_targets.R` | high | mitigate | Reject target ancestry that invokes WC2026 retrospective outputs or external data collection. |
| T-11-24 | Repudiation | Phase 11 run manifest | medium | mitigate | Record source git SHA, dirty flag, package versions, source snapshots, vintages, mode/panel identity, and checksum parents. |
| T-11-25 | Tampering | Research shortlist | high | mitigate | Validate shortlist vocabulary excludes promotion/release/final-holdout authority and contains evidence hashes only. |
| T-11-SC | Tampering | Bundle/registry drift | high | mitigate | Validate every persisted artifact against registered hashes and exact panel/G=40 contracts before publication. |
</threat_model>

<verification>
Run `Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_targets.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'`, `Rscript -e 'targets::tar_make(benchmark_phase11_bundle_files)'`, and `Rscript -e 'validate_hybrid_challenger_bundle("outputs/benchmarks/rolling_tournaments/phase11-hybrid-challengers")'`.
</verification>

<success_criteria>
Phase 11 is complete when the validated research bundle exists, all focused Phase 11 tests pass, exact 630/609/G=40 invariants hold, optional modes are separated, xG inactive status is explicit, and Phase 12 remains the only promotion path.
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-06-SUMMARY.md` when done.
</output>
