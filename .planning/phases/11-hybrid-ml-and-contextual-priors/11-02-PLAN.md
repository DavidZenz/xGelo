---
phase: 11-hybrid-ml-and-contextual-priors
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - R/benchmark/hybrid_protocol.R
  - R/forecast/hybrid_rf.R
  - R/benchmark/hybrid_adapters.R
  - R/benchmark/hybrid_runner.R
  - data/benchmark/phase11/model_registry.csv
  - data/benchmark/phase11/feature_contract.csv
  - tests/testthat/test_hybrid_random_forest.R
autonomous: true
requirements:
  - HYBRID-01
must_haves:
  truths:
    - "A real open-mode RF candidate runs one end-to-end path from registry through features, fit/predict, NB G=40 distribution, panel-aware scoring, and persisted evidence per D-01 through D-04."
    - "The RF candidate uses independently estimated fold-local Phase 10 dynamic attack/defence and Elo inputs, not a direct 1X2 classifier."
  artifacts:
    - "R/benchmark/hybrid_protocol.R"
    - "R/forecast/hybrid_rf.R"
    - "R/benchmark/hybrid_adapters.R"
    - "R/benchmark/hybrid_runner.R"
    - "data/benchmark/phase11/model_registry.csv"
    - "data/benchmark/phase11/feature_contract.csv"
  key_links:
    - "phase11 model registry -> hybrid adapter dispatch -> RF means -> NB score grid -> benchmark scorer"
---

<objective>
Implement the tracer-first RF challenger path for HYBRID-01.

Purpose: Prove Phase 11 can extend the frozen benchmark architecture end to end before broader feature and mode expansion.
Output: Phase 11 RF protocol files, two-goal RF fit/predict helpers, adapter dispatch, and a runner path that produces scored research evidence.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-RESEARCH.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-PATTERNS.md
@R/benchmark/challenger_protocol.R
@R/benchmark/challenger_runner.R
@R/benchmark/challengers.R
@R/benchmark/baselines.R
@R/benchmark/contracts.R
@R/forecast/dynamic_goal_ability.R
@tests/testthat/test_hybrid_random_forest.R
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end open RF candidate through common benchmark contract</name>
  <files>R/benchmark/hybrid_protocol.R, R/forecast/hybrid_rf.R, R/benchmark/hybrid_adapters.R, R/benchmark/hybrid_runner.R, data/benchmark/phase11/model_registry.csv, data/benchmark/phase11/feature_contract.csv, tests/testthat/test_hybrid_random_forest.R</files>
  <read_first>R/benchmark/challenger_protocol.R; R/benchmark/challengers.R; R/benchmark/challenger_runner.R; R/benchmark/baselines.R; R/benchmark/contracts.R; R/forecast/dynamic_goal_ability.R</read_first>
  <behavior>
    - D-01: `fit_hybrid_two_goal_rf()` fits separate home-goal and away-goal regression forests.
    - D-02: `hybrid_rf_nb_score_distributions()` converts RF means through registered/tuned NB marginals and emits the existing complete `0:40` grid schema.
    - D-03: RF feature rows include Phase 10 dynamic attack/defence state fields and `elo_diff` with point-in-time evidence companions.
    - D-04: `run_hybrid_challenger_benchmark()` scores the RF on the identical rolling folds and shared proper-score framework, with `research_only=TRUE`.
  </behavior>
  <action>Implement the thinnest production RF path: one open candidate ID such as `phase11_rf_dynamic_elo_open`, canonical protocol constructors/writers/validators, a strict adapter allowlist entry, RF fit/predict helpers using `ranger` when installed and failing with a clear dependency preflight otherwise, NB dispersion settings registered in `model_registry.csv`, and a runner wrapper that can score a small deterministic fixture slice through `score_benchmark_fixtures()`. Use existing `benchmark_one_distribution()`, `derive_benchmark_markets()`, `validate_benchmark_score_distributions()`, `build_registered_feature_coverage()`, and Phase 10 dynamic ability helpers. Keep the implementation to the locked RF goal-distribution path, research-only output, and sealed-holdout boundary.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_random_forest.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The RF tracer produces valid predictions and distributions for the test fixture set; every distribution has 1681 cells at G=40, probabilities reconcile to markets, manifests include registration/settings hashes, and feature coverage contains dynamic ability plus Elo provenance.</acceptance_criteria>
  <done>A real Phase 11 RF candidate can traverse registry -> features -> fit/predict -> NB score distribution -> scoring -> persisted-style evidence for one path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RF fold tuning and deterministic package provenance</name>
  <files>R/benchmark/hybrid_protocol.R, R/forecast/hybrid_rf.R, data/benchmark/phase11/model_registry.csv, tests/testthat/test_hybrid_random_forest.R</files>
  <read_first>.planning/phases/11-hybrid-ml-and-contextual-priors/11-RESEARCH.md; data/benchmark/phase10/tuning_grid.csv; data/benchmark/phase10/model_registry.csv</read_first>
  <behavior>
    - D-04: RF hyperparameters and NB dispersion choices are registered before evaluation and shared between frozen/updating tracks for the same outer fold.
    - HYBRID-01: `ranger` dependency status, version, and fallback rejection are visible in model manifests.
  </behavior>
  <action>Add Phase 11 RF settings fields for `num.trees`, `mtry`, `min.node.size`, seed policy, RF feature set ID, home/away tuning relationship, and NB dispersion source. Freeze a small deterministic grid and validation that rejects unregistered runtime settings. Record `ranger` version when available; if absent, the preflight must fail before benchmark execution rather than silently switching engines. The researched `randomForest` fallback may remain documented as a blocked fallback, but do not make it the primary path unless the tests and registry explicitly declare that changed adapter identity.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_random_forest.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>RF settings hashes are stable, unregistered RF settings are rejected, package provenance is recorded or fail-closed, and frozen/updating tracks share one fold-local setting identity.</acceptance_criteria>
  <done>HYBRID-01 has a registered, deterministic, chronology-safe RF implementation surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 10 dynamic ability -> RF feature matrix | Fold-local independent ability evidence enters the forest. |
| RF model -> benchmark adapter | Model-specific means become common validated score distributions. |
| Registry/settings -> runtime execution | Registered settings constrain what the runner may fit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Tampering | `R/forecast/hybrid_rf.R` | high | mitigate | Split home/away RF means per D-01 and validate no direct 1X2-primary output satisfies HYBRID-01. |
| T-11-07 | Tampering | `R/benchmark/hybrid_adapters.R` | high | mitigate | Route means through registered NB marginals and inherited G=40 validators per D-02. |
| T-11-08 | Information Disclosure | RF training history | high | mitigate | Use `guard_benchmark_purpose(..., "candidate_selection")`, Phase 10 dynamic state cutoffs, and source dates strictly before boundaries per D-03/D-04. |
| T-11-09 | Repudiation | `data/benchmark/phase11/model_registry.csv` | medium | mitigate | Hash settings and registration rows; manifests must carry the same hashes across folds. |
| T-11-SC | Tampering | CRAN `ranger` dependency | medium | mitigate | Require explicit package preflight and version/provenance manifest before RF execution; no npm/pip/cargo install occurs in this plan. |
</threat_model>

<verification>
Run `Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_random_forest.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'`.
</verification>

<success_criteria>
HYBRID-01 is runnable as a production-quality tracer under the existing benchmark contracts, while final selection and promotion remain absent.
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-02-SUMMARY.md` when done.
</output>
