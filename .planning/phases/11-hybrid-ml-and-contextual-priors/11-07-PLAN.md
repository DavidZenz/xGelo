---
phase: 11-hybrid-ml-and-contextual-priors
plan: 07
type: execute
wave: 0
depends_on: []
files_modified:
  - R/benchmark/challenger_preflight.R
  - data/benchmark/phase11/ranger_provenance.csv
  - data/cache/phase11-cran/ranger_0.18.0.tar.gz
  - data/cache/phase11-library/ranger/
  - _targets.R
autonomous: true
requirements:
  - HYBRID-01
must_haves:
  truths:
    - "Official CRAN `ranger` 0.18.0 is available from a checksum-verified retained archive in the project-local Phase 11 library before 11-02 executes."
    - "Offline replay verifies repository/index identity, archive checksum, dependency inventory, installed-content hash, exact version, and `.libPaths()` wiring without network access."
    - "HYBRID-01 RF execution fails closed when the verified local `ranger` runtime cannot be proven."
  artifacts:
    - "data/benchmark/phase11/ranger_provenance.csv"
    - "data/cache/phase11-cran/ranger_0.18.0.tar.gz"
    - "data/cache/phase11-library/ranger/"
  key_links:
    - "CRAN provenance -> retained archive -> project-local library -> RF preflight -> 11-02 tracer"
---

<objective>
Provision the approved Phase 11 `ranger` dependency before RF implementation.

Purpose: Make HYBRID-01 executable in clean and offline replay environments instead of letting the RF path fail at runtime.
Output: Checksum-backed `ranger` provenance, retained source archive, local installed library, preflight verification, and target package wiring.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-02-PLAN.md
@R/benchmark/challenger_preflight.R
@_targets.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify CRAN ranger provenance and install local Phase 11 library</name>
  <files>R/benchmark/challenger_preflight.R, data/benchmark/phase11/ranger_provenance.csv, data/cache/phase11-cran/ranger_0.18.0.tar.gz, data/cache/phase11-library/ranger/, _targets.R</files>
  <read_first>R/benchmark/challenger_preflight.R; .planning/phases/10-statistical-goal-model-challengers/10-02-PLAN.md; .planning/phases/11-hybrid-ml-and-contextual-priors/11-RESEARCH.md; _targets.R</read_first>
  <behavior>
    - HYBRID-01 / D-01 / D-04: RF execution cannot start unless exact `ranger` 0.18.0 is installed from an approved CRAN source artifact in the project-local Phase 11 library.
    - HYBRID-01: `require_hybrid_environment()` verifies local-library availability, exact package version, selected archive SHA-256, official repository/index checksum, dependency inventory, and installed-content hash before the 11-02 tracer runs.
  </behavior>
  <action>Extend the existing Phase 10 preflight pattern with Phase 11 functions such as `capture_ranger_package_provenance()`, `verify_ranger_package_archive()`, `hash_installed_package_contents("ranger")`, and `require_hybrid_environment()`. Fetch official CRAN metadata for `ranger` 0.18.0, persist repository URL, repository snapshot/index identity, index SHA-256, package row metadata, selected source archive name, official checksum when available, local archive SHA-256, dependency inventory, installed-content SHA-256, and exact installed version in `data/benchmark/phase11/ranger_provenance.csv`. Retain the verified archive at `data/cache/phase11-cran/ranger_0.18.0.tar.gz`, install only that archive into `data/cache/phase11-library`, add the Phase 11 library path before RF tests and targets, and add `ranger` to the target package declarations. Offline replay must use only the retained archive, provenance CSV, and installed-content hash; if any proof is absent or mismatched, fail before RF benchmark execution. Do not install unrelated packages or silently fall back to `randomForest`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'source("R/benchmark/challenger_preflight.R"); p &lt;- capture_ranger_package_provenance(package = "ranger", version = "0.18.0", install = TRUE, output_path = "data/benchmark/phase11/ranger_provenance.csv", archive_path = "data/cache/phase11-cran/ranger_0.18.0.tar.gz", library_path = "data/cache/phase11-library"); stopifnot(isTRUE(p$valid)); .libPaths(c(normalizePath("data/cache/phase11-library", mustWork = TRUE), .libPaths())); stopifnot(identical(as.character(utils::packageVersion("ranger")), "0.18.0"), isTRUE(verify_ranger_package_archive(p)), length(inventory_cran_dependencies(p)$unexpected) == 0L, grepl("^[0-9a-f]{64}$", hash_installed_package_contents("ranger", library_path = "data/cache/phase11-library"))); x &lt;- require_hybrid_environment(); stopifnot(isTRUE(x$valid)); targets::tar_manifest()'</automated>
    <automated>rtk Rscript --vanilla -e 'source("R/benchmark/challenger_preflight.R"); .libPaths(c(normalizePath("data/cache/phase11-library", mustWork = TRUE), .libPaths())); x &lt;- require_hybrid_environment(offline = TRUE); stopifnot(isTRUE(x$valid), identical(as.character(utils::packageVersion("ranger")), "0.18.0"))'</automated>
  </verify>
  <acceptance_criteria>Exact `ranger` 0.18.0 is installed from the verified retained official-CRAN archive into `data/cache/phase11-library`; provenance includes repository/index, package metadata, dependency inventory, archive checksum/SHA-256, installed-content SHA-256, and exact version; offline verification succeeds without network access; RF tests and targets can call `require_hybrid_environment()` before model fitting.</acceptance_criteria>
  <done>Plan 11-02 has a concrete verified `ranger` runtime dependency to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRAN package metadata -> local archive | Registry metadata and package bytes enter the reproducible model runtime. |
| Local archive -> installed library | Installed code becomes executable by the RF challenger. |
| Preflight -> target DAG | Runtime verification gates whether Phase 11 RF targets may execute. |

## Phase-Wide Risk Coverage

| Risk | Applicability | Plan Treatment |
|------|---------------|----------------|
| Historical leakage | applicable | `require_hybrid_environment()` only provisions runtime code and must not read fold outcomes, WC2026 outputs, or feature snapshots. |
| Silent activation | applicable | RF execution must fail closed until verified `ranger` provenance and exact local-library version are present. |
| Restricted-data redistribution | not applicable | This plan handles only an official CRAN package; no squad, bookmaker, or restricted sports data is read or published. |
| Registry/bundle drift | applicable | Repository/index, archive, dependency, installed-content, and target package declarations are hashed and replay-checked. |
| Manual snapshot contamination | not applicable | This plan creates no structural, geography, squad, or market snapshot; those are handled in later registry plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-26 | Tampering | CRAN `ranger` dependency | high | mitigate | Verify official CRAN metadata, selected archive checksum/SHA-256, dependency inventory, exact version, and installed-content hash before RF execution. |
| T-11-27 | Repudiation | `ranger_provenance.csv` | medium | mitigate | Persist repository URL, index identity, package metadata, archive path/hash, dependency inventory hash, installed-content hash, and verification timestamp. |
| T-11-28 | Denial of Service | Offline RF replay | medium | mitigate | Retain the verified archive and local-library install so replay does not require live CRAN network access after provisioning. |
| T-11-SC | Tampering | Package-manager installs | high | mitigate | Install only the approved CRAN `ranger` artifact and block unrelated package updates or undeclared fallback engines. |
</threat_model>

<verification>
Run both provenance commands in Task 1. The first command may use network access only to freeze official CRAN metadata and the selected archive; the second command must validate offline replay from local artifacts only.
</verification>

<success_criteria>
HYBRID-01 RF work is executable because `ranger` has a concrete project-local, checksum-backed, offline-replayable runtime before Plan 11-02.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| REQ | HYBRID-01 | Covered by provisioning the `ranger` RF dependency needed before 11-02. |
| RESEARCH | `ranger` missing locally but required for RF challenger | Covered by exact CRAN provenance, retained archive, local install, and offline preflight. |
| CONTEXT | D-01, D-04 | Covered by enabling the two-goal RF implementation and common benchmark execution path without fallback engine drift. |

No deferred ideas are implemented.
</source_audit>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-07-SUMMARY.md` when done.
</output>
