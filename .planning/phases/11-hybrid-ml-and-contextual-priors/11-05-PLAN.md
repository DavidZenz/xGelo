---
phase: 11-hybrid-ml-and-contextual-priors
plan: 05
type: execute
wave: 3
depends_on:
  - 11-03
  - 11-04
files_modified:
  - R/forecast/external_market.R
  - R/benchmark/hybrid_protocol.R
  - R/benchmark/hybrid_adapters.R
  - data/benchmark/phase11/mode_registry.csv
  - data/benchmark/phase11/manual_market_manifest.csv
  - tests/testthat/test_hybrid_modes.R
autonomous: true
requirements:
  - HYBRID-05
user_setup:
  - service: manual_bookmaker_snapshot
    why: "External market benchmark is allowed only from a manually frozen local snapshot with source, timestamp, and licensing metadata."
    env_vars: []
    dashboard_config:
      - task: "Provide a local CSV snapshot only if legally permitted; live collection is outside Phase 11."
        location: "data/manual/bookmaker/"
must_haves:
  truths:
    - "Open default, enriched squad, and external market modes are separately labelled, licensed, and panel-bound."
    - "Only open-data mode can compete for open-default promotion in later phases; enriched and external outputs are research/reference only."
  artifacts:
    - "R/forecast/external_market.R"
    - "data/benchmark/phase11/mode_registry.csv"
    - "data/benchmark/phase11/manual_market_manifest.csv"
  key_links:
    - "Transfermarkt derived squad aggregates -> enriched mode -> feature_rich panel"
    - "Manual bookmaker snapshot -> external mode -> labelled reference output"
---

<objective>
Implement enriched squad and external market mode boundaries for HYBRID-05.

Purpose: Allow optional evidence to be evaluated without weakening the open-data product identity or redistributing restricted inputs.
Output: Mode registry, manual market validator, enriched/external adapter dispatch, and mode tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@R/transfermarkt/squad_strength.R
@R/forecast/features.R
@R/benchmark/contracts.R
@R/benchmark/challenger_runner.R
@tests/testthat/test_hybrid_modes.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode registry and enriched squad boundary</name>
  <files>R/benchmark/hybrid_protocol.R, R/benchmark/hybrid_adapters.R, data/benchmark/phase11/mode_registry.csv, tests/testthat/test_hybrid_modes.R</files>
  <read_first>R/transfermarkt/squad_strength.R; R/forecast/features.R; data/benchmark/phase09/panel_fixtures.csv; tests/testthat/test_transfermarkt_benchmark.R</read_first>
  <behavior>
    - D-13: Squad information is a separately labelled point-in-time enriched mode using locally derived aggregates only.
    - D-15 / D-16: Enriched mode is not blended into the open default and cannot replace open-mode promotion eligibility.
  </behavior>
  <action>Add `canonical_phase11_mode_registry()` and `validate_hybrid_mode_registry()` with rows for `open_default`, `enriched_squad`, and `external_market`. Wire enriched-squad adapter metadata to existing Transfermarkt aggregate features and `feature_rich` panel only. Store vintage, source artifact checksum, derived-only publication rule, license class, active/inactive status, and promotion boundary. Do not read raw Transfermarkt rows in benchmark outputs and do not automate Transfermarkt collection.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_modes.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Mode registry validates; enriched squad mode is labelled `feature_rich`, carries local snapshot provenance, publishes derived aggregates only, and has open-default promotion disabled.</acceptance_criteria>
  <done>Enriched squad mode is implemented as a separate research mode per HYBRID-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual external market snapshot validator</name>
  <files>R/forecast/external_market.R, R/benchmark/hybrid_protocol.R, data/benchmark/phase11/manual_market_manifest.csv, tests/testthat/test_hybrid_modes.R</files>
  <read_first>R/transfermarkt/squad_strength.R; R/benchmark/contracts.R; .planning/phases/11-hybrid-ml-and-contextual-priors/11-RESEARCH.md</read_first>
  <precondition>External market benchmarking requires a legally usable manually frozen local snapshot; if absent, the external mode remains registered inactive with an explicit unavailable reason.</precondition>
  <behavior>
    - D-14: Bookmaker consensus is a manually frozen point-in-time external benchmark with timestamp, source, and licensing metadata.
    - D-15 / D-16: External market mode is labelled reference/research output and excluded from open candidate promotion.
  </behavior>
  <action>Create `R/forecast/external_market.R` with `validate_manual_market_snapshot()`, `read_manual_market_snapshot()`, and `market_probabilities_to_benchmark_predictions()` for permitted 1X2 probabilities plus metadata. Register required columns in `manual_market_manifest.csv`: snapshot_id, fixture_id, captured_at_utc, source_name, source_url_or_label, license_class, redistribution_allowed, manual_freeze_operator, source_sha256, row_sha256, and active_status. Reject missing timestamps, non-normalized probabilities, post-cutoff snapshots, redistributed raw restricted rows, and any live collection command/path. If no snapshot exists, keep the mode inactive but schema-valid.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_modes.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Manual market snapshots validate only when source/licensing/cutoff fields are complete; absent snapshots produce explicit inactive mode evidence; external rows cannot enter the open candidate pool.</acceptance_criteria>
  <done>External bookmaker mode is implemented as a manual, labelled, non-promotional reference path per HYBRID-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Transfermarkt snapshot -> enriched aggregate features | Restricted raw data must not be redistributed. |
| Manual bookmaker snapshot -> external benchmark | Licensed/manual external probabilities enter reference outputs only. |
| Mode registry -> promotion boundary | Optional modes must not alter open-default eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Information Disclosure | Enriched squad mode | high | mitigate | Publish derived squad aggregates and metadata only; keep raw restricted data out of committed outputs per D-13. |
| T-11-19 | Information Disclosure | External market mode | high | mitigate | Require manual snapshot licensing fields and reject redistribution-disallowed raw rows per D-14. |
| T-11-20 | Tampering | `mode_registry.csv` | high | mitigate | Validate mode-specific panel, active status, and promotion boundaries per D-15/D-16. |
| T-11-21 | Repudiation | Manual snapshots | medium | mitigate | Require timestamp, source label, operator, source checksum, and row hashes. |
| T-11-SC | Tampering | External collectors | high | mitigate | Tests and validators reject live collection paths for restricted or externally licensed sources. |
</threat_model>

<verification>
Run `test_hybrid_modes.R` and `test_transfermarkt_benchmark.R`.
</verification>

<success_criteria>
HYBRID-05 is satisfied with separately labelled open, enriched, and external modes and explicit licensing/provenance boundaries.
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-05-SUMMARY.md` when done.
</output>
