---
phase: 14-shared-competition-state-and-forecast-layer
plan: "18"
type: execute
wave: 17
depends_on: ["14-17", "14-22"]
files_modified:
  # Canonical production transaction: exactly these eleven NL artifacts.
  - outputs/competition/uefa_nations_league_2026_27/state/canonical_matches.csv
  - outputs/competition/uefa_nations_league_2026_27/state/standings.csv
  - outputs/competition/uefa_nations_league_2026_27/state/competition_form.csv
  - outputs/competition/uefa_nations_league_2026_27/state/all_international_form.csv
  - outputs/competition/uefa_nations_league_2026_27/state/model_form.csv
  - outputs/competition/uefa_nations_league_2026_27/state/forecast_status.csv
  - outputs/competition/uefa_nations_league_2026_27/state/forecasts.csv
  - outputs/competition/uefa_nations_league_2026_27/state/forecast_top10.csv
  - outputs/competition/uefa_nations_league_2026_27/audit/standings_reconciliation.csv
  - outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
  - outputs/competition/uefa_nations_league_2026_27/local/score_distributions.rds
  # Narrow construction/test support for the same transaction; no other production targets.
  - R/competition/state_bundle.R
  - R/competition/forecast_layer.R
  - tests/testthat/test_phase14_forecast_layer.R
  - tests/testthat/test_phase14_plan18_transaction_probe.R
  # Canonical planning evidence written after the transaction succeeds.
  - .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, FORECAST-01, FORECAST-02, FORECAST-03]
must_haves:
  truths:
    - "The accepted official bundle nl-2026-27-official-uefa-v2 produces a durable Nations League state/forecast directory containing exactly the eleven declared artifacts."
    - "The durable NL inventory preserves all 156 official fixtures, 14 official groups, and 54 official teams with no silent fixture drop between accepted input, canonical_matches, forecast_status, and available forecast/grid outputs."
    - "Every durable status, forecast, top-10, and manifest row carries the complete approved resolver release/model/calibrator identity and SHA-256 lineage in explicit fields, model_data_cutoff 2026-06-10, and the direct output assertion proves the primary calibrated_1x2 view; available fixtures also carry identical strict pre-kickoff feature_cutoff_utc values across status and forecasts."
    - "National-team xG is explicitly unavailable/NA and audited under the active Elo-only release; the synthetic Austria/Germany xG-active suppression contract remains unit-test evidence only and is not treated as production inventory."
    - "Candidate validation, atomic promotion, injected-failure rollback, and durable read-back prove that partial or mixed NL output cannot replace the last accepted directory."
  artifacts:
    - path: outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
      provides: "Self-hashed eleven-artifact NL state graph with row counts, parent hashes, release identity, cutoff lineage, predictor audit, and validation status"
    - path: .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md
      provides: "Canonical completion record for the official NL promotion"
  key_links:
    - from: scripts/build_competition_state.R
      to: R/competition/state_bundle.R
      via: "phase14_build_competition_state_main -> phase14_build_competition_state_batch -> phase14_validate_competition_state_bundle"
    - from: outputs/releases/approved_release.csv
      to: outputs/competition/uefa_nations_league_2026_27
      via: "phase14_resolve_approved_release selector authority and exact model_data_cutoff propagation"
    - from: data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv
      to: outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
      via: "five-row accepted/official provenance, artifact/edition consistency, official URL/raw/source hashes, fixture inventory, and parent/content hashes"
    - from: outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
      to: .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md
      via: "actual manifest_sha256, machine-checked durable artifact count, validator result, rollback result, and durable read-back result"
    - from: temporary eleven-target transaction harness
      to: outputs/competition/uefa_nations_league_2026_27
      via: "same-filesystem staging/promotion, failure injection after indices 1..11, exact byte restoration, residue cleanup, and successful durable read-back"
  prohibitions:
    - statement: "NL promotion must not publish dashboard artifacts, introduce an external API, fabricate national-team xG, use Austria/Germany as a production fixture, or implement Phase 15, 16, or 17 behavior."
      status: unverified
      verification: judgment
---

<objective>
Close the only remaining Phase 14 gap by building, validating, atomically promoting, and reading back the official `nl-2026-27-official-uefa-v2` Nations League state/forecast bundle. Purpose: make the official 156-fixture production output durable so all seven Phase 14 requirements have an accepted artifact path.
Output: the exact eleven NL artifacts plus the canonical `14-18-SUMMARY.md`; no production API or unrelated source behavior changes. A narrow top-10 lineage-column addition is permitted only if the existing construction boundary lacks the required resolver fields.
</objective>

<scope_rationale>
This is one narrow gap-closure plan because candidate validation, the exact eleven-file inventory, atomic promotion, and injected-failure rollback must remain one transaction boundary. No production API, dashboard, or competition-rules behavior is being added; the only possible source edit is the narrow top-10 lineage-column addition and test-only transaction harness required to make the already-defined official NL durable output contract directly verifiable.

Scope accounting: the eleven output paths are the only durable production files promoted. `R/competition/state_bundle.R` is limited to binding the accepted five-row provenance digest into the existing state-manifest construction boundary; `R/competition/forecast_layer.R` is limited to the existing top-10 construction boundary when resolver lineage is absent; `tests/testthat/test_phase14_forecast_layer.R` receives only the direct production assertions; and `tests/testthat/test_phase14_plan18_transaction_probe.R` is a committed test-only harness whose staging, backup, lock, restore, and sibling-sentinel paths are temporary runtime files outside the production inventory. The summary is documentation, not a twelfth production artifact.
</scope_rationale>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-RESEARCH.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-PATTERNS.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/COVERAGE.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-VERIFICATION.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-UAT.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-18-DATA-CORRECTION-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-17-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-19-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-20-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-22-SUMMARY.md
@R/competition/state_bundle.R
@scripts/build_competition_state.R
@R/competition/publication_transaction.R
@tests/testthat/test_phase14_state_bundle.R
@tests/testthat/test_phase14_forecast_layer.R
@data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv
</context>

## Decision Coverage

The executor must preserve the already-implemented locked contracts D-01 through D-21 while closing this durable-output gap. In particular, D-01 through D-08 govern canonical lifecycle, score, standings, and same-bundle reconciliation; D-09 through D-12 govern separate form products and exclusive cutoffs; D-13 through D-17 govern eligibility, approved calibrated/raw views, G=40 support, and uncertainty; D-18 through D-21 govern shared-input allowlisting, stable identity, D-20 lineage, edition isolation, fan-out, and rollback boundaries. The closure must cite and verify these contracts rather than replace them.

## Artifacts this phase produces

- `state/canonical_matches.csv`
- `state/standings.csv`
- `state/competition_form.csv`
- `state/all_international_form.csv`
- `state/model_form.csv`
- `state/forecast_status.csv`
- `state/forecasts.csv`
- `state/forecast_top10.csv`
- `audit/standings_reconciliation.csv`
- `audit/state_manifest.csv`
- `local/score_distributions.rds`
- `.planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md`

The first eleven paths are the complete production inventory. Do not add dashboard/static-site files, an external API surface, registry/release mutations, or Phase 15/16/17 outputs. Preserve the API coverage decision in `COVERAGE.md` unchanged.

<tasks>

<task type="tracer">
  <name>Task 14-18-01: Build, validate, and atomically promote the official NL eleven-artifact bundle</name>
  <read_first>
    - R/competition/state_bundle.R
    - scripts/build_competition_state.R
    - R/competition/publication_transaction.R
    - data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv
    - data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
    - data/competition/accepted/uefa_nations_league_2026_27/groups.csv
    - data/competition/accepted/uefa_nations_league_2026_27/results.csv
    - data/competition/accepted/uefa_nations_league_2026_27/standings.csv
    - data/competition/accepted/uefa_nations_league_2026_27/status.csv
    - data/competition/registries/competition_editions.csv
    - data/competition/registries/team_identity.csv
    - data/competition/registries/national_team_xg_sources.csv
    - outputs/releases/approved_release.csv
    - outputs/releases/phase14-open-nb-incumbent-calibrated-v1/release_manifest.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
    - tests/testthat/test_phase14_state_bundle.R
    - tests/testthat/test_phase14_forecast_layer.R
  </read_first>
  <requirements>STATE-01, STATE-02, STATE-03, STATE-04, FORECAST-01, FORECAST-02, FORECAST-03</requirements>
  <must_haves>The accepted five-row official source manifest is validated with explicit artifact IDs, accepted/raw paths, URLs, hashes, and edition/bundle lineage before promotion; all eleven durable files are staged and read back; every required lineage field and hash is bound row-wise to the approved resolver and candidate/state-manifest provenance digest; the 156-fixture inventory has non-empty available coverage with a real representative fixture carried through status, forecast, grid, and top-10 outputs; national-team xG remains an explicit unavailable/no-imputation product; and each injected failure after promotion indices 1 through 11 restores the exact incumbent bytes.</must_haves>
  <scope_prohibitions>Do not widen the target vector beyond the eleven NL artifacts, reuse the Phase 13 fourteen-file transaction as evidence for this NL graph, modify release authority or registries, promote dashboard files, or treat synthetic Austria/Germany evidence as production input.</scope_prohibitions>
  <files>outputs/competition/uefa_nations_league_2026_27/state/canonical_matches.csv, outputs/competition/uefa_nations_league_2026_27/state/standings.csv, outputs/competition/uefa_nations_league_2026_27/state/competition_form.csv, outputs/competition/uefa_nations_league_2026_27/state/all_international_form.csv, outputs/competition/uefa_nations_league_2026_27/state/model_form.csv, outputs/competition/uefa_nations_league_2026_27/state/forecast_status.csv, outputs/competition/uefa_nations_league_2026_27/state/forecasts.csv, outputs/competition/uefa_nations_league_2026_27/state/forecast_top10.csv, outputs/competition/uefa_nations_league_2026_27/audit/standings_reconciliation.csv, outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv, outputs/competition/uefa_nations_league_2026_27/local/score_distributions.rds, R/competition/state_bundle.R, R/competition/forecast_layer.R, tests/testthat/test_phase14_forecast_layer.R, tests/testthat/test_phase14_plan18_transaction_probe.R</files>
  <action>
    Use the existing `phase14_build_competition_state_main`, `phase14_build_competition_state_batch`, `phase14_state_bundle_attach_manifest`, `phase14_validate_competition_state_bundle`, and `phase14_state_bundle_expected_inventory` contracts to build the explicit `uefa_nations_league_2026_27` candidate from the accepted bundle ID `nl-2026-27-official-uefa-v2`. Honor D-01 through D-21 as existing contracts, including edition-local inputs (D-18), stable fixture identity (D-19), complete D-20 lineage, and D-21 failure isolation. Resolve authority only through `phase14_resolve_approved_release("outputs/releases/approved_release.csv", "outputs/releases")`; require release `phase14-open-nb-incumbent-calibrated-v1`, calibrated primary 1X2 lineage, immutable active predictor `elo_diff`, dropped national-team xG/form predictors, and resolver `model_data_cutoff` `2026-06-10` in status, forecasts, top-10, and manifest rows. Verify the official input inventory before promotion: 156 fixture rows, 14 group IDs, 54 non-NA resolved team IDs, 156 matching result fixture IDs with source status `UPCOMING`/scheduled, edition-level `competition_status=scheduled`, the accepted source bundle ID on all production lineage, and no production Austria/Germany fixture. Select a real official scheduled fixture that is actually `available` after the candidate build as the representative availability assertion; the Austria/Germany case may remain only in explicitly synthetic unit tests. Require every source fixture ID to appear exactly once in canonical matches and forecast status, with fixture IDs exactly equal between accepted fixtures and scheduled/upcoming results, canonical matches and status, and available status rows and forecasts/grid/top-10 rows. Require non-empty available coverage, complete G=40 grids, and compact top-10 rows. Audit `model_form` and forecast/status lineage for national-team xG status unavailable/NA, source `national_team_xg_sources.csv`, sample count zero, no accepted point-in-time artifact, and no imputed xG values. Re-run the existing synthetic xG-active contract and require `feature_evidence_unavailable` suppression without treating it as production evidence. Stage all eleven files in a sibling temporary output root, validate the candidate and staged files, then use the existing guarded snapshot/stage/promote/rollback transaction pattern with same-filesystem rename. Inject failure after each target promotion and prove exact restoration of prior target bytes, unrelated siblings, selector/release files, and transaction residue; only after post-promotion validation may the staged directory become the durable target. Do not modify production code beyond the narrow top-10 lineage-field addition and state-manifest provenance binding at their existing construction boundaries; do not modify `COVERAGE.md`, release authority, edition registries, or any output outside the eleven declared NL paths.
    Before any promotion, read `data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv` and require exactly five rows for fixtures, groups, results, standings, and status. Build an explicit artifact-type map and require `source_artifact_id` to equal `nl-2026-27-official-uefa-v2-${artifact_type}`, the accepted CSV path `data/competition/accepted/uefa_nations_league_2026_27/${artifact_type}.csv` to exist, the manifest `relative_local_raw_path` to exist, and every `source_url` and `source_url_lineage` value to be non-empty and byte-for-byte checked against the accepted row map. Require one edition and bundle ID, `bundle_status=accepted`, `acceptance_state=accepted`, `fallback_status=official`, `artifact_count=5`, and `required_resource_count=5`; compare each accepted CSV's edition column (`edition_id` or `source_edition_id`) to `uefa_nations_league_2026_27`, compare its `bundle_id`/`source_bundle_id` when present to `nl-2026-27-official-uefa-v2`, and otherwise bind its exact `source_artifact_id` to the corresponding manifest row and bundle ID. Require the fixture/result cross-artifact IDs to map to the fixtures artifact and require all five rows' source-bundle, artifact-manifest, canonical/raw, and per-artifact hashes to match their referenced files. Extend only the existing `phase14_state_bundle_manifest_rows` construction boundary in `R/competition/state_bundle.R` to carry the canonical five-row digest on every state-manifest row as `source_bundle_id`, `source_artifact_ids`, `source_artifact_paths`, `source_urls`, `source_url_lineage`, `source_bundle_sha256`, `artifact_manifest_sha256`, and `raw_sha256`; then bind this digest to the candidate and `state_manifest.csv` through exact edition, source-bundle, source-artifact, accepted-path, URL-lineage, and hash comparisons before promotion. An absent state-manifest provenance field is a construction-boundary defect to fix in that bounded source change, not a reason to weaken the assertion. Cross-check this manifest as official accepted provenance before promotion. Require complete resolver identity fields and hashes row-wise in status, forecasts, top-10, and state manifest: release ID, release manifest SHA-256, selector SHA-256, model ID and SHA-256, calibrator ID and SHA-256, and model cutoff. If the current top-10 schema lacks model/calibrator fields, add those explicit fields at its existing construction boundary and compare them to the joined forecast row before writing the durable artifact. Read `state/model_form.csv` directly and assert `form_scope=national_team_xg`, unavailable status/reason, `sample_count=0`, empty or NA xGF/xGA/xGD and related EWMA fields, and scan `availability_reason`, `evidence_basis`, `source_scope`, `source_id`, and every related metadata field for forbidden imputation markers; require the explicit unavailable/NA contract, matching `national_team_xg_sources.csv` evidence with `acceptance_status=not_accepted`, and no accepted point-in-time artifact.
    The production code has no eleven-target transaction callback. Create the bounded, committed test-only harness `tests/testthat/test_phase14_plan18_transaction_probe.R` with function `phase14_run_temporary_eleven_target_transaction_probe`; it must serialize the candidate into the exact eleven relative paths, snapshot incumbent bytes, use the existing transaction byte/hash/write/injected-failure/cleanup primitives and same-filesystem `file.rename` boundary, and promote in `phase14_state_bundle_expected_inventory()` order without changing the production API. Run it once for each failure index 1 through 11, injecting failure immediately after that target promotion; each run must restore every incumbent target byte and SHA-256, preserve an unrelated sibling sentinel plus selector/release bytes, and leave no stage, backup, lock, or restore residue. Run one successful promotion, validate staged and durable trees with `phase14_validate_competition_state_bundle()`, then reopen every CSV/RDS artifact from disk and compare bytes/content hashes, row counts, parent hashes, provenance digest, and the actual `state_manifest.csv` manifest hash to the candidate. The probe's staging, backup, lock, restore, and sibling-sentinel paths are temporary runtime paths outside the eleven production artifacts. Treat builder `durable_mutation=FALSE` as expected for dry-run only; the successful test harness is the durable promotion proof. Keep all writes within the eleven declared NL paths and do not modify `COVERAGE.md`, release authority, or edition registries.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_state_bundle.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase14_forecast_layer.R", stop_on_failure=TRUE); source("R/competition/state_bundle.R"); stopifnot(isTRUE(phase14_validate_competition_state_bundle("outputs/competition/uefa_nations_league_2026_27")))' </automated>
    <automated>Rscript --vanilla -e 'f &lt;- utils::read.csv("data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv", stringsAsFactors=FALSE, check.names=FALSE); g &lt;- utils::read.csv("data/competition/accepted/uefa_nations_league_2026_27/groups.csv", stringsAsFactors=FALSE, check.names=FALSE); r &lt;- utils::read.csv("data/competition/accepted/uefa_nations_league_2026_27/results.csv", stringsAsFactors=FALSE, check.names=FALSE); t &lt;- utils::read.csv("data/competition/registries/team_identity.csv", stringsAsFactors=FALSE, check.names=FALSE); m &lt;- utils::read.csv("outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv", stringsAsFactors=FALSE, check.names=FALSE); c &lt;- utils::read.csv("outputs/competition/uefa_nations_league_2026_27/state/canonical_matches.csv", stringsAsFactors=FALSE, check.names=FALSE); s &lt;- utils::read.csv("outputs/competition/uefa_nations_league_2026_27/state/forecast_status.csv", stringsAsFactors=FALSE, check.names=FALSE); stopifnot(nrow(f)==156L, length(unique(g$source_group_id))==14L, length(unique(c(f$home_team_id, f$away_team_id)))==54L, nrow(r)==156L, nrow(c)==156L, nrow(s)==156L, !any((f$home_display_name=="Austria" & f$away_display_name=="Germany") | (f$home_display_name=="Germany" & f$away_display_name=="Austria")), identical(as.character(m$artifact_path), phase14_state_bundle_expected_inventory()), setequal(as.character(f$fixture_id), as.character(c$fixture_id)), setequal(as.character(f$fixture_id), as.character(s$fixture_id)), all(as.character(m$model_data_cutoff)=="2026-06-10"), all(as.character(s$model_data_cutoff)=="2026-06-10"), all(as.character(t$source_bundle_id[t$team_id %in% unique(c(f$home_team_id, f$away_team_id))])=="nl-2026-27-official-uefa-v2"))'</automated>
    <automated>Rscript --vanilla -e 'coverage &lt;- ".planning/phases/14-shared-competition-state-and-forecast-layer/COVERAGE.md"; before &lt;- digest::digest(file=coverage, algo="sha256"); source("tests/testthat/test_phase14_match_state.R", local=.GlobalEnv); source("tests/testthat/test_phase14_standings.R", local=.GlobalEnv); match_matrix &lt;- phase14_match_state_test_run_v2_rollback_matrix(); standings_matrix &lt;- phase14_standings_test_run_v2_rollback_matrix(); after &lt;- digest::digest(file=coverage, algo="sha256"); stopifnot(isTRUE(match_matrix$passed), isTRUE(standings_matrix$passed), match_matrix$failure_count==match_matrix$target_count, standings_matrix$failure_count==standings_matrix$target_count, identical(before, after))'</automated>
    <automated>Rscript --vanilla -e 'root &lt;- "outputs/competition/uefa_nations_league_2026_27"; read_csv &lt;- function(path) utils::read.csv(file.path(root, path), stringsAsFactors=FALSE, check.names=FALSE, na.strings=""); status &lt;- read_csv("state/forecast_status.csv"); forecasts &lt;- read_csv("state/forecasts.csv"); top10 &lt;- read_csv("state/forecast_top10.csv"); manifest &lt;- read_csv("audit/state_manifest.csv"); grid &lt;- readRDS(file.path(root, "local/score_distributions.rds")); required_status &lt;- c("fixture_id","kickoff_utc","feature_cutoff_utc","forecast_status","release_calibration_status","model_release_id","model_data_cutoff","source_bundle_id","national_team_xg_status","national_team_xg_source_id","national_team_xg_sample_count","national_team_xg_availability_reason"); required_forecasts &lt;- c("fixture_id","kickoff_utc","feature_cutoff_utc","model_release_id","model_id","calibrator_id","model_data_cutoff","primary_probability_view","source_bundle_id","score_distribution_id","score_support_max","score_cell_count"); required_top10 &lt;- c("fixture_id","score_distribution_id","rank","model_release_id","release_manifest_sha256","model_data_cutoff"); required_manifest &lt;- c("artifact_path","model_release_id","model_id","calibrator_id","model_data_cutoff","national_team_xg_status","national_team_xg_source_id","national_team_xg_sample_count","national_team_xg_availability_reason"); stopifnot(all(required_status %in% names(status)), all(required_forecasts %in% names(forecasts)), all(required_top10 %in% names(top10)), all(required_manifest %in% names(manifest)), anyDuplicated(status$fixture_id)==0L, anyDuplicated(forecasts$fixture_id)==0L); available &lt;- as.character(status$fixture_id[status$forecast_status=="available"]); forecast_ids &lt;- as.character(forecasts$fixture_id); grid_ids &lt;- sub("__score$", "", unique(as.character(grid$score_distribution_id))); top_ids &lt;- unique(as.character(top10$fixture_id)); stopifnot(setequal(available, forecast_ids), setequal(available, grid_ids), setequal(available, top_ids)); stopifnot(all(vapply(available, function(id) { rows &lt;- top10[top10$fixture_id==id,,drop=FALSE]; identical(as.integer(rows$rank), 1:10) &amp;&amp; nrow(rows)==10L }, logical(1)))); stopifnot(all(vapply(available, function(id) { rows &lt;- grid[sub("__score$", "", as.character(grid$score_distribution_id))==id,,drop=FALSE]; nrow(rows)==1681L &amp;&amp; identical(sort(unique(as.integer(rows$home_goals))), 0:40) &amp;&amp; identical(sort(unique(as.integer(rows$away_goals))), 0:40) }, logical(1)))); parse_utc &lt;- function(x) as.POSIXct(x, format="%Y-%m-%dT%H:%M:%SZ", tz="UTC"); stopifnot(all(as.character(status$feature_cutoff_utc[match(available,status$fixture_id)])==as.character(forecasts$feature_cutoff_utc[match(available,forecasts$fixture_id)])), all(parse_utc(forecasts$feature_cutoff_utc) &lt; parse_utc(forecasts$kickoff_utc))); release &lt;- unique(as.character(forecasts$model_release_id)); model &lt;- unique(as.character(forecasts$model_id)); calibrator &lt;- unique(as.character(forecasts$calibrator_id)); stopifnot(length(release)==1L, release=="phase14-open-nb-incumbent-calibrated-v1", length(model)==1L, nzchar(model), length(calibrator)==1L, nzchar(calibrator), all(as.character(status$model_release_id)==release), all(as.character(top10$model_release_id)==release), all(as.character(manifest$model_release_id)==release), all(as.character(manifest$model_id)==model), all(as.character(manifest$calibrator_id)==calibrator), all(as.character(status$model_data_cutoff)=="2026-06-10"), all(as.character(forecasts$model_data_cutoff)=="2026-06-10"), all(as.character(top10$model_data_cutoff)=="2026-06-10"), all(as.character(manifest$model_data_cutoff)=="2026-06-10"), all(as.character(forecasts$primary_probability_view)=="calibrated_1x2"), all(as.character(status$release_calibration_status)=="fitted"), all(as.character(status$source_bundle_id)=="nl-2026-27-official-uefa-v2"), all(as.character(forecasts$source_bundle_id)=="nl-2026-27-official-uefa-v2"), all(as.character(status$national_team_xg_status)=="inactive_optional_unavailable"), all(as.character(forecasts$national_team_xg_status)=="inactive_optional_unavailable"), all(as.character(manifest$national_team_xg_status)=="inactive_optional_unavailable"), all(as.character(status$national_team_xg_source_id)=="national_team_xg_sources.csv"), all(as.character(forecasts$national_team_xg_source_id)=="national_team_xg_sources.csv"), all(as.character(manifest$national_team_xg_source_id)=="national_team_xg_sources.csv"), all(as.integer(status$national_team_xg_sample_count)==0L), all(as.integer(forecasts$national_team_xg_sample_count)==0L), all(as.integer(manifest$national_team_xg_sample_count)==0L), all(!grepl("imput", tolower(as.character(status$national_team_xg_availability_reason)))), all(!grepl("imput", tolower(as.character(forecasts$national_team_xg_availability_reason)))), all(!grepl("imput", tolower(as.character(manifest$national_team_xg_availability_reason)))), all(grepl("unavailable|inactive", tolower(as.character(status$national_team_xg_availability_reason)))), all(grepl("unavailable|inactive", tolower(as.character(forecasts$national_team_xg_availability_reason)))), all(grepl("unavailable|inactive", tolower(as.character(manifest$national_team_xg_availability_reason)))))'</automated>
    <automated>Rscript --vanilla -e 'source("scripts/build_competition_state.R"); result &lt;- phase14_build_competition_state_main(args=c("--edition-id","uefa_nations_league_2026_27","--replay-check")); stopifnot(isTRUE(result$replay_check), isTRUE(result$replay$verified), isTRUE(result$durable_mutation==FALSE))'</automated>
    <automated>Rscript --vanilla -e 'm &lt;- utils::read.csv("data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv", stringsAsFactors=FALSE, check.names=FALSE); stopifnot(nrow(m)==5L, length(unique(m$bundle_id))==1L, unique(m$bundle_id)=="nl-2026-27-official-uefa-v2", length(unique(m$edition_id))==1L, unique(m$edition_id)=="uefa_nations_league_2026_27", all(m$bundle_status=="accepted"), all(m$acceptance_state=="accepted"), all(m$fallback_status=="official"), all(m$artifact_count==5L), all(m$required_resource_count==5L), setequal(m$artifact_type, c("fixtures","groups","results","standings","status")), length(unique(m$source_url_lineage))==1L, grepl("^https://match\\.uefa\\.com/", m$source_url[[1L]]), length(unique(m$source_bundle_sha256))==1L, length(unique(m$artifact_manifest_sha256))==1L, length(unique(m$raw_sha256))==1L))'</automated>
    <automated>Rscript --vanilla -e 'root &lt;- "data/competition/accepted/uefa_nations_league_2026_27"; bundle &lt;- "nl-2026-27-official-uefa-v2"; edition &lt;- "uefa_nations_league_2026_27"; types &lt;- c("fixtures","groups","results","standings","status"); rd &lt;- function(path) utils::read.csv(path, stringsAsFactors=FALSE, check.names=FALSE, na.strings=""); p &lt;- rd(file.path(root,"source_bundle_manifest.csv")); expected_id &lt;- setNames(paste0(bundle,"-",types),types); stopifnot(nrow(p)==5L, setequal(as.character(p$artifact_type),types), all(as.character(p$source_artifact_id)==unname(expected_id[as.character(p$artifact_type)])), all(file.exists(file.path(root,paste0(types,".csv")))), all(file.exists(as.character(p$relative_local_raw_path))), all(nzchar(as.character(p$source_url))), all(nzchar(as.character(p$source_url_lineage))), all(as.character(p$source_url)==as.character(p$source_url_lineage))); for (kind in types) { d &lt;- rd(file.path(root,paste0(kind,".csv"))); ec &lt;- if ("edition_id" %in% names(d)) "edition_id" else "source_edition_id"; stopifnot("source_artifact_id" %in% names(d), all(as.character(d$source_artifact_id)==expected_id[[kind]]), all(as.character(d[[ec]])==edition)); if ("bundle_id" %in% names(d)) stopifnot(all(as.character(d$bundle_id)==bundle)); if ("source_bundle_id" %in% names(d)) stopifnot(all(as.character(d$source_bundle_id)==bundle)); if (kind=="results") stopifnot(all(as.character(d$fixture_source_artifact_id)==expected_id[["fixtures"]])) }; sm &lt;- rd("outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv"); required &lt;- c("edition_id","source_bundle_id","source_artifact_ids","source_artifact_paths","source_urls","source_url_lineage","source_bundle_sha256","artifact_manifest_sha256","raw_sha256"); stopifnot(all(required %in% names(sm)), all(as.character(sm$edition_id)==edition), all(as.character(sm$source_bundle_id)==bundle), all(as.character(sm$source_artifact_ids)==paste(unname(expected_id[types]),collapse="|")), all(as.character(sm$source_artifact_paths)==paste(file.path(root,paste0(types,".csv")),collapse="|")), all(as.character(sm$source_urls)==paste(as.character(p$source_url[match(types,p$artifact_type)]),collapse="|")), all(as.character(sm$source_url_lineage)==paste(as.character(p$source_url_lineage[match(types,p$artifact_type)]),collapse="|")), all(as.character(sm$source_bundle_sha256)==paste(unique(as.character(p$source_bundle_sha256)),collapse="|")), all(as.character(sm$artifact_manifest_sha256)==paste(unique(as.character(p$artifact_manifest_sha256)),collapse="|")), all(as.character(sm$raw_sha256)==paste(as.character(p$raw_sha256[match(types,p$artifact_type)]),collapse="|")))'</automated>
    <automated>Rscript --vanilla -e 'root &lt;- "outputs/competition/uefa_nations_league_2026_27"; rd &lt;- function(path) utils::read.csv(file.path(root,path), stringsAsFactors=FALSE, check.names=FALSE, na.strings=""); f &lt;- rd("state/canonical_matches.csv"); s &lt;- rd("state/forecast_status.csv"); y &lt;- rd("state/forecasts.csv"); top &lt;- rd("state/forecast_top10.csv"); grid &lt;- readRDS(file.path(root,"local/score_distributions.rds")); ids &lt;- as.character(f$fixture_id); available &lt;- as.character(s$fixture_id[as.character(s$forecast_status)=="available"]); stopifnot(length(available)&gt;=1L, anyDuplicated(ids)==0L, anyDuplicated(as.character(s$fixture_id))==0L, all(!is.na(f$home_team_id)), all(!is.na(f$away_team_id)), all(nzchar(as.character(f$home_team_id))), all(nzchar(as.character(f$away_team_id))), setequal(ids,as.character(s$fixture_id)), setequal(available,as.character(y$fixture_id)), setequal(available,sub("__score$","",unique(as.character(grid$score_distribution_id)))), setequal(available,unique(as.character(top$fixture_id)))); rep_id &lt;- available[[1L]]; rs &lt;- s[as.character(s$fixture_id)==rep_id,,drop=FALSE]; ry &lt;- y[as.character(y$fixture_id)==rep_id,,drop=FALSE]; rg &lt;- grid[as.character(grid$score_distribution_id)==paste0(rep_id,"__score"),,drop=FALSE]; rt &lt;- top[as.character(top$fixture_id)==rep_id,,drop=FALSE]; stopifnot(nrow(rs)==1L, as.character(rs$forecast_status)=="available", nrow(ry)==1L, nrow(rg)==1681L, identical(sort(unique(as.integer(rg$home_goals))),0:40), identical(sort(unique(as.integer(rg$away_goals))),0:40), nrow(rt)==10L, identical(sort(as.integer(rt$rank)),1:10), all(as.character(rs$feature_cutoff_utc)==as.character(ry$feature_cutoff_utc)), as.POSIXct(ry$feature_cutoff_utc,format="%Y-%m-%dT%H:%M:%SZ",tz="UTC")&lt;as.POSIXct(ry$kickoff_utc,format="%Y-%m-%dT%H:%M:%SZ",tz="UTC")))'</automated>
    <automated>Rscript --vanilla -e 'base &lt;- "data/competition/accepted/uefa_nations_league_2026_27"; f &lt;- utils::read.csv(file.path(base,"fixtures.csv"), stringsAsFactors=FALSE, check.names=FALSE); r &lt;- utils::read.csv(file.path(base,"results.csv"), stringsAsFactors=FALSE, check.names=FALSE); e &lt;- utils::read.csv(file.path(base,"status.csv"), stringsAsFactors=FALSE, check.names=FALSE); stopifnot(nrow(f)==156L, nrow(r)==156L, anyDuplicated(f$fixture_id)==0L, anyDuplicated(r$fixture_id)==0L, setequal(as.character(f$fixture_id),as.character(r$fixture_id)), all(tolower(trimws(as.character(f$source_status)))%in%c("upcoming","scheduled")), all(tolower(trimws(as.character(r$source_status)))%in%c("upcoming","scheduled")), nrow(e)==1L, tolower(trimws(as.character(e$competition_status)))=="scheduled")'</automated>
    <automated>Rscript --vanilla -e 'root &lt;- "outputs/competition/uefa_nations_league_2026_27"; mf &lt;- utils::read.csv(file.path(root,"state/model_form.csv"), stringsAsFactors=FALSE, check.names=FALSE, na.strings=""); required &lt;- c("form_scope","availability_status","availability_reason","evidence_basis","source_scope","source_id","sample_count"); stopifnot(all(required %in% names(mf)), all(as.character(mf$form_scope)=="national_team_xg"), all(as.character(mf$availability_status)%in%c("unavailable","inactive_optional_unavailable")), all(grepl("unavailable|inactive",tolower(as.character(mf$availability_reason)))), all(as.integer(mf$sample_count)==0L), all(grepl("national.team|senior",tolower(as.character(mf$source_scope)))), all(nzchar(as.character(mf$evidence_basis))), all(is.na(mf$source_id)|as.character(mf$source_id)=="national_team_xg_sources.csv")); xg_cols &lt;- grep("^(xgf|xga|xgd)(_|$)",names(mf),value=TRUE); stopifnot(length(xg_cols)&gt;=3L, all(vapply(mf[xg_cols],function(x) all(is.na(x)|trimws(as.character(x))==""),logical(1)))); metadata_cols &lt;- grep("availability|evidence|source|reason|basis|scope|status|artifact",names(mf),value=TRUE); metadata_text &lt;- tolower(paste(unlist(mf[metadata_cols],use.names=FALSE),collapse="|")); stopifnot(!grepl("imput|backfill|fill_na|mean_fill",metadata_text))'</automated>
    <automated>Rscript --vanilla -e 'root &lt;- "outputs/competition/uefa_nations_league_2026_27"; read_csv &lt;- function(path) utils::read.csv(file.path(root, path), stringsAsFactors=FALSE, check.names=FALSE, na.strings=""); status &lt;- read_csv("state/forecast_status.csv"); forecasts &lt;- read_csv("state/forecasts.csv"); top10 &lt;- read_csv("state/forecast_top10.csv"); model_form &lt;- read_csv("state/model_form.csv"); manifest &lt;- read_csv("audit/state_manifest.csv"); required &lt;- c("model_release_id","release_manifest_sha256","release_selector_sha256","model_id","model_sha256","calibrator_id","calibrator_sha256","model_data_cutoff"); stopifnot(all(required %in% names(status)), all(required %in% names(forecasts)), all(required %in% names(top10)), all(required %in% names(manifest))); expected &lt;- setNames(lapply(required, function(field) unique(as.character(forecasts[[field]]))), required); stopifnot(all(vapply(expected, function(value) length(value)==1L &amp;&amp; nzchar(value[[1L]]), logical(1))), all(vapply(required, function(field) all(as.character(status[[field]])==expected[[field]]), logical(1))), all(vapply(required, function(field) all(as.character(top10[[field]])==expected[[field]]), logical(1))), all(vapply(required, function(field) all(as.character(manifest[[field]])==expected[[field]]), logical(1))), all(as.character(forecasts$primary_probability_view)=="calibrated_1x2"), all(as.character(forecasts$model_data_cutoff)=="2026-06-10")); stopifnot(all(c("form_scope","availability_status","availability_reason","sample_count","xgf","xga","xgd","xgf_ewma","xga_ewma","xgd_ewma","source_id") %in% names(model_form)), all(as.character(model_form$form_scope)=="national_team_xg"), all(as.character(model_form$availability_status) %in% c("unavailable","inactive_optional_unavailable")), all(as.integer(model_form$sample_count)==0L), all(is.na(model_form$xgf) | model_form$xgf==""), all(is.na(model_form$xga) | model_form$xga==""), all(is.na(model_form$xgd) | model_form$xgd==""), all(is.na(model_form$xgf_ewma) | model_form$xgf_ewma==""), all(is.na(model_form$xga_ewma) | model_form$xga_ewma==""), all(is.na(model_form$xgd_ewma) | model_form$xgd_ewma=="")); xg &lt;- utils::read.csv("data/competition/registries/national_team_xg_sources.csv", stringsAsFactors=FALSE, check.names=FALSE); xg &lt;- xg[xg$source_id=="phase14-national-team-xg-unavailable",,drop=FALSE]; stopifnot(nrow(xg)==1L, xg$acceptance_status=="not_accepted", is.na(xg$accepted_artifact_path) | xg$accepted_artifact_path=="")'</automated>
    <automated>Rscript --vanilla -e 'source("tests/testthat/test_phase14_plan18_transaction_probe.R"); stopifnot(isTRUE(phase14_run_temporary_eleven_target_transaction_probe("outputs/competition/uefa_nations_league_2026_27", phase14_state_bundle_expected_inventory(), failure_indices=seq_len(11L), successful_readback=TRUE)))'</automated>
  </verify>
  <acceptance_criteria>The accepted five-row official manifest is provenance-valid with explicit source-artifact IDs, accepted/raw paths, every URL, edition/bundle lineage, and hashes bound into the candidate/state manifest; all eleven artifacts have complete resolver identity and hashes including explicit top-10 model/calibrator lineage; the 156-fixture inventory has non-empty available coverage and one real representative fixture verified across status, forecast, G=40 grid, and top-10; `model_form.csv` proves national-team xG is unavailable with zero samples, empty/NA xG, explicit metadata, and no imputation markers; the temporary eleven-target harness passes all 11 injected-failure rollback cases and one successful durable read-back; and no sibling, selector, release, registry, or transaction-residue bytes change.</acceptance_criteria>
  <done>The official NL candidate and staged directory validate against the accepted five-row official source manifest, exact eleven-artifact inventory, 156/14/54 counts, fixture/result ID equality, scheduled status, non-NA resolved team IDs, non-vacuous representative availability, complete resolver identity/hash lineage including top-10, direct model_form unavailable/no-imputation metadata evidence, G=40 forecast contract, and a temporary eleven-target transaction probe that passes all 11 injected-failure rollback cases plus successful durable read-back without residue or sibling mutation.</done>
</task>

<task type="auto">
  <name>Task 14-18-02: Record canonical completion evidence for the official NL promotion</name>
  <read_first>
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-VERIFICATION.md
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-UAT.md
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-DATA-CORRECTION-SUMMARY.md
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-20-SUMMARY.md
    - outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
    - outputs/competition/uefa_nations_league_2026_27/state/forecast_status.csv
    - outputs/competition/uefa_nations_league_2026_27/state/forecasts.csv
  </read_first>
  <requirements>STATE-01, STATE-02, STATE-03, STATE-04, FORECAST-01, FORECAST-02, FORECAST-03</requirements>
  <must_haves>The summary records the actual unique state-manifest hash, machine-checkable inventory counts, validator and rollback results, durable read-back result, and the Phase 13 deferred failure boundary.</must_haves>
  <scope_prohibitions>Do not write a second completion artifact, copy stale UAT prose as evidence, claim full-suite green, add an API, or alter outputs, release authority, registries, `COVERAGE.md`, or the correction summary.</scope_prohibitions>
  <files>.planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md</files>
  <action>Write the canonical `14-18-SUMMARY.md` only after Task 14-18-01's fresh validator, eleven-target transaction probe, and durable read-back pass. Record machine-checkable lines for `source_bundle_id=nl-2026-27-official-uefa-v2`, `source_fixture_count=156`, `group_count=14`, `team_count=54`, `durable_artifact_count=11`, `state_manifest_sha256=<actual unique manifest_sha256 from audit/state_manifest.csv>`, `validator_result=TRUE`, `rollback_probe_result=11/11 passed`, `durable_readback_result=TRUE`, and `phase13_deferred_failure_boundary=outside Phase 14 acceptance gate`. Also record the exact eleven durable artifact paths, complete approved release/model/calibrator identity and hashes, resolver `model_data_cutoff=2026-06-10`, representative official fixture evidence, direct national-team xG unavailable/NA model_form audit, synthetic-only Austria/Germany test boundary, deterministic replay result, and the exact focused commands run. Explicitly mark UAT case 45 as superseded by filesystem plus `phase14_validate_competition_state_bundle()` evidence, and explicitly record the repository-wide Phase 13 fixture-seed failure as outside the Phase 14 acceptance gate. Do not claim full-suite green, do not add a new API surface, and do not describe the correction artifact as the canonical completion summary.</action>
  <verify>
    <automated>Rscript --vanilla -e 'p &lt;- ".planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md"; x &lt;- paste(readLines(p, warn=FALSE), collapse="\n"); required &lt;- c("nl-2026-27-official-uefa-v2", "156", "14 groups", "54 teams", "state/canonical_matches.csv", "state/standings.csv", "state/competition_form.csv", "state/all_international_form.csv", "state/model_form.csv", "state/forecast_status.csv", "state/forecasts.csv", "state/forecast_top10.csv", "audit/standings_reconciliation.csv", "audit/state_manifest.csv", "local/score_distributions.rds", "manifest hash", "phase14-open-nb-incumbent-calibrated-v1", "model_data_cutoff 2026-06-10", "national-team xG", "unavailable/NA", "synthetic", "Austria/Germany", "atomic", "rollback", "case 45", "superseded", "outside the Phase 14 acceptance gate", "Phase 13", "fixture-seed"); stopifnot(file.exists(p), all(vapply(required, grepl, logical(1), x=x, fixed=TRUE)))'</automated>
    <automated>Rscript --vanilla -e 'm &lt;- utils::read.csv("outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv", stringsAsFactors=FALSE, check.names=FALSE); stopifnot(nrow(m) &gt; 0L, any(grepl("state_manifest.csv", m$artifact_path, fixed=TRUE)), any(grepl("sha256|hash", names(m), ignore.case=TRUE)), any(grepl("phase14-open-nb-incumbent-calibrated-v1", unlist(m), fixed=TRUE)), any(grepl("2026-06-10", unlist(m), fixed=TRUE)))'</automated>
    <automated>Rscript --vanilla -e 'p &lt;- ".planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md"; x &lt;- paste(readLines(p, warn=FALSE), collapse="\n"); m &lt;- utils::read.csv("outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv", stringsAsFactors=FALSE, check.names=FALSE); actual &lt;- unique(as.character(m$manifest_sha256)); stopifnot(file.exists(p), length(actual)==1L, grepl(paste0("state_manifest_sha256=", actual[[1L]]), x, fixed=TRUE), grepl("source_fixture_count=156", x, fixed=TRUE), grepl("group_count=14", x, fixed=TRUE), grepl("team_count=54", x, fixed=TRUE), grepl("durable_artifact_count=11", x, fixed=TRUE), grepl("validator_result=TRUE", x, fixed=TRUE), grepl("rollback_probe_result=11/11 passed", x, fixed=TRUE), grepl("durable_readback_result=TRUE", x, fixed=TRUE), grepl("phase13_deferred_failure_boundary=outside Phase 14 acceptance gate", x, fixed=TRUE))'</automated>
    <automated>rg -q '156|14 groups|54 teams' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md &amp;&amp; rg -q 'national-team xG.*(unavailable|NA)|(unavailable|NA).*national-team xG' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md &amp;&amp; rg -q 'Austria/Germany.*(synthetic|production)|(synthetic|production).*Austria/Germany' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md &amp;&amp; rg -q 'atomic.*rollback|rollback.*atomic' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md &amp;&amp; rg -q 'case 45.*superseded|superseded.*case 45' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md &amp;&amp; rg -q 'repository-wide Phase 13 fixture-seed|Phase 13 fixture-seed.*outside' .planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>The summary's recorded `state_manifest_sha256` equals the actual unique manifest hash in `audit/state_manifest.csv`, and its machine-checkable counts and validator/rollback/read-back result lines are present alongside the official-versus-synthetic and Phase 13 boundary evidence.</acceptance_criteria>
  <done>The canonical Plan 14-18 summary exists, binds its recorded hash to the durable state manifest, distinguishes official production evidence from synthetic tests, records validator/rollback/read-back proof and the excluded Phase 13 failure, and does not alter code, API coverage, release authority, registries, or any artifact outside the exact eleven NL outputs.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted source bundle -> state builder | Official CSV inputs cross into derived state and forecast construction. |
| Approved release selector -> forecast outputs | Release identity, model/calibrator hashes, and cutoff cross into durable lineage. |
| Staged NL directory -> durable output directory | Untrusted or incomplete candidate files could replace the accepted state unless promotion is atomic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-18-01 | Tampering | `state_manifest.csv` and parent graph | high | mitigate | Validate exact eleven paths, row counts, content hashes, parent hashes, self-hash, and durable read-back before acceptance. |
| T-14-18-02 | Tampering | Accepted official fixture inventory | high | mitigate | Compare accepted/canonical/status fixture ID sets and assert 156 official rows with no silent drop. |
| T-14-18-03 | Information Disclosure | Edition output root | medium | mitigate | Restrict writes to the NL edition directory and prove unrelated outputs, release files, registries, and `COVERAGE.md` are byte-identical. |
| T-14-18-04 | Denial of Service | Partial promotion or injected failure | high | mitigate | Snapshot exact bytes, promote staged files in deterministic order, inject failure after each target, restore atomically, and verify no residue. |
| T-14-18-05 | Repudiation | Release/cutoff and xG availability lineage | high | mitigate | Resolve through the self-hashed selector, require `2026-06-10`, preserve D-20 identities, and record explicit national-team xG unavailable/NA evidence. |
| T-14-18-SC | Tampering | R package/test execution inputs | high | mitigate | Use the existing focused test and validator commands; no package installation or new dependency is introduced by this closure. |
</threat_model>

<verification>
Run the two focused Phase 14 test files, the fresh durable bundle validator, the accepted five-row official source-manifest/provenance and 156/14/54 inventory assertion, the direct status/forecast/top-10/model_form/grid/manifest lineage assertion, the temporary eleven-target transaction probe with failure indices 1 through 11 plus successful durable read-back, the `--replay-check` dry-run, and the focused command that invokes both existing every-index rollback matrices while comparing the pre/post SHA-256 of `COVERAGE.md`. Confirm the canonical summary records the actual `state_manifest.csv` `manifest_sha256`, machine-checkable counts, validator result, rollback result, durable read-back result, and the Phase 13 deferred boundary. Do not use the repository-wide Phase 13 fixture-seed failure as a Phase 14 acceptance failure; it remains separately tracked in the existing deferred-items ledger.
</verification>

<success_criteria>
The official NL bundle is durable and validator-approved with exactly eleven artifacts; the accepted five-row official source manifest is provenance- and edition-consistent; all 156 accepted fixtures are represented without silent drop; approved release/model/calibrator identities and SHA-256 hashes are directly read row-wise from status, forecasts, top-10, and manifest using explicit fields; `model_data_cutoff` is exact; national-team xG remains honestly unavailable/NA with zero samples, empty/NA xG values, no imputation, and non-accepted registry evidence; G=40 and uncertainty outputs validate; the temporary eleven-target promotion probe restores exact bytes after each injected failure and successful durable read-back matches the manifest; both existing every-index rollback matrices restore exact bytes without changing `COVERAGE.md`; the stale Austria/Germany production must-have and stale UAT filesystem claim are replaced by official-inventory evidence; all seven requirement IDs remain represented; and the canonical `14-18-SUMMARY.md` binds its recorded manifest hash to disk.
</success_criteria>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Both editions reuse an edition-aware state/form/forecast engine without future leakage | 14-18 | COVERED | Durable NL completion closes the only missing production truth; EURO remains the completed pre_draw sibling. |
| REQ | STATE-01 | Shared state and official fixture-backed durable state | 14-18 | COVERED | Eleven-artifact NL state and validator evidence. |
| REQ | STATE-02 | Canonical lifecycle/score semantics | 14-18 | COVERED | Existing state builder/test contracts are revalidated. |
| REQ | STATE-03 | Cutoff-safe competition and all-senior form | 14-18 | COVERED | Form artifacts, model form, and strict cutoff lineage are read back. |
| REQ | STATE-04 | Edition isolation and shared-input audit | 14-18 | COVERED | NL-only promotion, replay, D-18/D-21 and rollback checks. |
| REQ | FORECAST-01 | Approved calibrated forecast authority | 14-18 | COVERED | Selector-resolved release and exact cutoff lineage. |
| REQ | FORECAST-02 | Forecast probabilities, G=40 grids, top-10, uncertainty | 14-18 | COVERED | Durable forecast artifacts and focused forecast tests. |
| REQ | FORECAST-03 | Auditability and deterministic replay | 14-18 | COVERED | D-20 lineage, replay-check, manifest, and rollback evidence. |
| RESEARCH | - | Candidate -> validate -> promote, fail closed, provenance, strict cutoffs, bounded G=40 | 14-18 | COVERED | Uses existing Phase 14 builder/validator and guarded promotion pattern. |
| CONTEXT | D-01 through D-21 | Locked state, forecast, lineage, isolation, and rollback contracts | 14-18 | COVERED | Decision Coverage section and Task 14-18-01 action. |
| CONTEXT | Deferred Ideas | Phase 15/16/17 rules, topology, simulation, dashboard, hourly publication | NONE | EXCLUDED | Explicitly outside this gap closure per CONTEXT.md. |
| API COVERAGE | - | Preserve existing UEFA match API integration decision | NONE | PRESERVED | `COVERAGE.md` is read-only and unchanged. |
</source_audit>

<output>
Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md` when done.
</output>
