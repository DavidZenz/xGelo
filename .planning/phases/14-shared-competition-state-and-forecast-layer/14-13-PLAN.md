---
phase: 14-shared-competition-state-and-forecast-layer
plan: "13"
type: execute
wave: 10
depends_on: ["14-12"]
files_modified: [R/competition/match_state.R, data/competition/registries/match_identity.csv, tests/testthat/test_phase14_match_state.R]
autonomous: true
requirements: [STATE-02, STATE-04]
must_haves:
  truths:
    - "D-19 uses one durable source-to-canonical crosswalk whose minting inputs exclude score, status, row order, and score-bearing hashes."
    - "D-01 through D-04 semantics validate the full production batch, including D-02 orthogonal lifecycle/completion axes, not only a tracer row."
    - "Source corrections preserve match_id while changing state row hashes and retaining both source lineages."
  artifacts:
    - path: R/competition/match_state.R
      provides: "Canonical match builder and validators"
    - path: data/competition/registries/match_identity.csv
      provides: "Durable competition/history canonical-match crosswalk"
  key_links:
    - from: data/competition/registries/match_identity.csv
      to: R/competition/match_state.R
      via: "one-to-one source lookup before semantics"
      pattern: "match_id"
  prohibitions:
    - statement: "Canonical match identity must not depend on mutable scores, lifecycle/status, row order, or score-bearing hashes."
      status: unverified
      verification: judgment
---
<objective>Build the durable canonical-match crosswalk and complete match-state engine. Purpose: establish stable identity and truthful D-01 through D-04 semantics before reducers. Output: crosswalk, production module, passing tests.</objective>
<execution_context>@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/14-shared-competition-state-and-forecast-layer/14-12-SUMMARY.md
@R/competition/team_identity.R
@data/processed/martj42_historical_normalized.csv
@tests/testthat/test_phase14_match_state.R</context>

## Artifacts this phase produces

- New production file/functions: `R/competition/match_state.R` with `phase14_build_match_identity_crosswalk()`, `phase14_validate_match_identity_crosswalk()`, `phase14_build_canonical_matches()`, and `phase14_validate_canonical_matches()`.
- New durable registry: `data/competition/registries/match_identity.csv` with schema/version, canonical `match_id`, source namespace/ID, edition/team/schedule/venue minting projection and hash, competition/history lineage IDs, review state, and row hash.
- Canonical match fields/enums: `source_status`; D-02 `match_status`; D-02 `completion_method`; regulation/final/shootout scores; `winner_team_id`; evidence availability time; `counts_for_standings`; `counts_for_form`; edition/source lineage; row/table hashes; and collision/review status.
- Existing `tests/testthat/test_phase14_match_state.R` gains production crosswalk, correction-stability, complete lifecycle-score matrix, accepted-batch, and foreign-link coverage.

<tasks>
<task type="auto" tdd="true">
  <name>Task 14-13-01: Build and validate the durable canonical-match crosswalk</name>
  <read_first>
    - R/competition/team_identity.R
    - data/processed/martj42_historical_normalized.csv
    - data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
    - tests/testthat/test_phase14_match_state.R
  </read_first>
  <files>R/competition/match_state.R, data/competition/registries/match_identity.csv, tests/testthat/test_phase14_match_state.R</files>
  <behavior>
    - "Competition and historical source IDs map one-to-one to canonical match_id."
    - "Minting uses stable source namespace/ID, edition, teams, date/time, and neutral/venue context only."
    - "Collisions require explicit reviewed rows; corrections cannot remint identity."
  </behavior>
  <action>Implement `phase14_build_match_identity_crosswalk()` and `phase14_validate_match_identity_crosswalk()` per D-19 and the resolved research policy. Persist schema/version, canonical/source IDs, minting projection/hash, lineage IDs, review state, and row hash. Prefer accepted competition records for competition fields while retaining historical lineage. Activate crosswalk/correction tests.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_match_state.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Every real input match has one canonical ID; duplicate/collision ambiguity fails; score/status corrections preserve ID.</acceptance_criteria>
  <done>The durable crosswalk exists, validates, and is correction-stable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 14-13-02: Implement the complete canonical lifecycle and score engine</name>
  <read_first>
    - R/competition/match_state.R
    - R/competition/team_identity.R
    - tests/fixtures/phase14/match_lifecycle_cases.csv
    - tests/testthat/test_phase14_match_state.R
  </read_first>
  <files>R/competition/match_state.R, tests/testthat/test_phase14_match_state.R</files>
  <behavior>
    - "Per D-02, match_status lifecycle and completion_method are orthogonal; source_status remains auditable."
    - "Regulation, final football, shootout, winner, and separate count flags obey D-03/D-04."
    - "Unknown/incomplete states fail closed or remain unresolved."
  </behavior>
  <action>Implement `phase14_build_canonical_matches()` and `phase14_validate_canonical_matches()` for every accepted fixture/result plus historical crosswalk row. Per D-02, validate lifecycle and completion method independently and prevent source adapters from silently manufacturing completed. Enforce paired scores, penalty tie, awarded semantics, evidence time, stable IDs, edition/source lineage, canonical ordering, and row/table hashes.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_match_state.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase13_competition_registry.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>The complete D-02 lifecycle/completion cross-product and accepted production rows pass; invalid combinations, silently invented completion, and foreign links fail.</acceptance_criteria>
  <done>Canonical match state is production-complete and ready for standings/form reducers.</done>
</task>
</tasks>
<threat_model asvs_level="1" block_on="high">Unresolved high threats block execution.
| Threat ID | STRIDE | Severity | Disposition | Control | Test |
|---|---|---|---|---|---|
| T-14-13-S | Spoofing | high | mitigate | explicit source/canonical enums and reviewed crosswalk | match-state test |
| T-14-13-T | Tampering | high | mitigate | stable minting projection, one-to-one keys, hashes | match-state test |
</threat_model>
<verification>Canonical match and Phase 13 identity regressions pass.</verification>
<success_criteria>Stable match identity and complete match semantics exist for all production inputs.</success_criteria>
<output>Create `14-13-SUMMARY.md` when done.</output>
