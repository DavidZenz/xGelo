---
phase: 14-shared-competition-state-and-forecast-layer
plan: "21"
type: execute
wave: 3
depends_on: ["14-05"]
files_modified:
  - R/release/calibration_remediation.R
  - tests/testthat/test_phase14_calibration_remediation.R
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/remediation_contract.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_selection.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibrator.rds
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibrated_predictions.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
autonomous: true
requirements: [FORECAST-01]
must_haves:
  truths:
    - "Each outer tournament chooses raw identity, shrunk scalar temperature, or regularized class-specific vector scaling using nested strictly earlier tournament folds only."
    - "The warm-up, scalar-shrinkage, and vector-penalty grids are frozen exactly as approved before any authoritative outer evaluation."
    - "Insufficient inner history/class support or no eligible improvement selects explicit raw_fallback for that outer tournament without suppressing forecast availability."
    - "Each outer tournament persists the exact strictly-prior outer training tournament IDs, every inner validation ID and its strictly-prior training-ID map, row/class support, selected family/hyperparameters, deterministic optimizer seed/convergence, fallback reason, and fitted identity/scalar/vector parameters sufficient to reproduce that outer prediction."
    - "The same 630-fixture outer panel is evaluated by the unchanged Phase 12 support, coverage, identity, RPS, Brier, log-loss, fold-stability, and strict calibration-improvement vetoes."
    - "A final fitted calibrator exists only after an actual outer pass; failure remains immutable candidate evidence with raw authority and no WC2026 labels."
  artifacts:
    - path: R/release/calibration_remediation.R
      provides: "Frozen family grid, nested selector, outer evaluator, final fitter, artifact writer, and producer validator"
    - path: tests/testthat/test_phase14_calibration_remediation.R
      provides: "Leakage, fallback, grid, scoring, hash, holdout, and authority-mutation regression coverage"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
      provides: "Self-hashed graph binding every remediation input/output, including per-outer fit evidence"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv
      provides: "Unchanged authoritative outer-gate decision"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
      provides: "One replay-sufficient, hash-bound selected fit record per outer tournament"
  key_links:
    - from: R/release/calibration_remediation.R
      to: R/calibration/calibration_selection.R
      via: "phase12_selection_decision over the complete outer predictions"
      pattern: "phase12_selection_decision"
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_selection.csv
      to: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
      via: "exact nested lineage, support, selected family/hyperparameters, deterministic seed, convergence, fallback, and fitted parameters per outer tournament"
      pattern: "outer_edition_id"
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
      to: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibrated_predictions.csv
      via: "replay-sufficient outer fit parameters applied to the held-out outer tournament"
      pattern: "fit_record_sha256"
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv
      to: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
      via: "exact file SHA-256 and manifest self-hash"
      pattern: "calibration_gate_sha256"
---

<objective>
Implement the locked nested calibration-remediation contract after the first candidate's empirical block, preserving D-14/D-15 and all Phase 12 release authority.

Purpose: Produce leakage-safe outer evidence for identity, shrunk temperature, and regularized class-specific vector scaling without using exploratory figures as promotion evidence.
Output: A tested remediation module and one immutable, separately rooted candidate graph that honestly records either the unchanged authoritative pass or block.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CALIBRATION-REVISION.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-04-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-05-SUMMARY.md
@R/release/calibration_revision.R
@R/calibration/probability_calibration.R
@R/calibration/calibration_selection.R
@data/benchmark/phase12/calibration_recipe.json
@data/benchmark/phase09/promotion_protocol.json
@data/benchmark/phase12/freeze_manifest.csv
</context>

## Artifacts this phase produces

- `R/release/calibration_remediation.R` with exported APIs `phase14_calibration_remediation_contract()`, `phase14_select_nested_calibrator()`, `phase14_build_calibration_remediation()`, and `phase14_validate_calibration_remediation()`.
- `tests/testthat/test_phase14_calibration_remediation.R` covering exact grids, identifiable transforms, nested chronology, raw fallback, complete 630-row evaluation, unchanged outer vetoes, hash integrity, no holdout labels, and no authority mutation.
- Separate immutable root `outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/` containing `remediation_contract.csv`, `outer_fold_selection.csv`, `outer_fold_fits.csv`, `calibrator.rds`, `calibrated_predictions.csv`, `calibration_gate.csv`, and `calibration_revision_manifest.csv`.

## Explicit prohibitions

- Do not modify Plan 14-04, its source artifacts, its summary, or its blocked gate/manifest bytes.
- Do not alter Phase 12 selector logic, veto thresholds/reason order, G=40 score distribution, recipe authority, protocol authority, or either competition registry pin.
- Do not inspect or encode WC2026 labels anywhere in tuning, nested selection, outer evaluation, final fitting, tests, or artifacts.
- Do not treat the exploratory feasibility rows in `14-CALIBRATION-REVISION.md` as hyperparameter choices or promotion evidence.
- Do not fit the final calibrator before the complete authoritative outer decision passes; a failed run retains raw authority.
- Do not change public suppression or forecast-availability semantics; explicit raw fallback preserves earliest-possible availability.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 14-21-01: Prove one nested outer-tournament path across all frozen families</name>
  <read_first>
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-CALIBRATION-REVISION.md
    - R/release/calibration_revision.R
    - R/calibration/probability_calibration.R
    - R/calibration/calibration_selection.R
    - data/benchmark/phase12/calibration_recipe.json
    - data/benchmark/phase09/promotion_protocol.json
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/calibration_remediation.R, tests/testthat/test_phase14_calibration_remediation.R</files>
  <behavior>
    - "The contract enumerates raw identity; warm-up rows 60, 128, 256, 400 crossed with scalar shrinkage 0.25, 0.50, 0.75, 1.00; and the same warm-up rows crossed with vector penalties 0.001, 0.01, 0.05, 0.10, 0.50, 1.00, 5.00."
    - "Shrunk temperature interpolates the bounded Phase 12 log-temperature toward log(1); vector scaling uses three class-specific log-probability slopes bounded to 0.25 through 4 and two free offsets bounded to -2 through 2 with the third offset constrained to the negative sum, minimizing multiclass log-loss plus the frozen penalty toward slopes 1 and offsets 0 from an identity start."
    - "For one outer edition, every inner validation tournament is trained only on tournaments completed strictly earlier than that inner tournament, and all inner evidence predates the outer edition."
    - "A candidate is eligible only when its nested inner predictions beat raw under frozen inner coverage/identity/support, non-worse RPS, Phase 12 Brier/log-loss limits, fold-regression limit, and strict calibration-improvement criteria; ties resolve by RPS, calibration error, log-loss, Brier, then frozen lower-complexity order."
    - "Nested evidence is sufficient only with at least two valid inner validation tournaments whose own strictly prior fit rows meet the candidate warm-up and unchanged minimum 10 examples per observed class; otherwise, or when optimization is invalid or no candidate is eligible, selection returns auditable raw_fallback."
    - "The contract fixes a seed base and canonical outer/inner/family/hyperparameter seed derivation; the selected outer fit record stores the resulting seed, optimizer method, convergence code/message, objective value, identity/scalar/vector parameters, and a canonical fit-record hash."
  </behavior>
  <action>Write failing tests first, then implement a production-quality single-outer-fold path using base R only. Freeze family ordering, all grids, and a deterministic integer seed schedule keyed by canonical outer index, inner validation index, family, and hyperparameter tuple in one canonical contract. Derive observed classes only from registered development fixtures; construct tournament-complete inner folds whose fit rows are strictly earlier than each inner validation tournament and strictly earlier than the outer tournament; set the derived seed immediately before every optimizer invocation; fit each candidate without consulting outer outcomes; and return the selected family, exact hyperparameters, ordered outer training tournament IDs, ordered inner validation IDs, the canonical inner-validation-to-training-ID map, row/home/draw/away support, score tuple, optimizer method/seed/convergence/objective, explicit fallback reason, and fitted parameters. Represent raw fallback with identity slopes/offsets plus explicit `not_run` optimizer status. For scalar fits retain the final temperature; for vector fits retain all three slopes and all three identifiable offsets, including the derived zero-sum offset, so the outer probabilities can be regenerated without the producer object. Canonically hash the fit record. Keep raw probabilities and score-distribution identity intact per D-14, and source the unchanged Phase 12 scoring/veto primitives instead of copying or relaxing them.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_remediation.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>A real outer tournament runs end to end through frozen contract, nested selection, replay-sufficient fit/apply, and audit output; adversarial tests fail outer-fold leakage, same-tournament evidence, altered training/validation IDs, undersupported classes, invalid simplex, non-converged optimization, and non-identifiable vector parameters.</acceptance_criteria>
  <done>One production outer-fold slice proves every family and the fail-closed fallback without changing any original evidence or release authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-21-02: Evaluate all outer folds and persist the immutable remediation graph</name>
  <read_first>
    - R/release/calibration_remediation.R
    - tests/testthat/test_phase14_calibration_remediation.R
    - R/release/calibration_revision.R
    - R/calibration/calibration_selection.R
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv
    - data/benchmark/phase09/fixtures.csv
  </read_first>
  <files>R/release/calibration_remediation.R, tests/testthat/test_phase14_calibration_remediation.R, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/remediation_contract.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_selection.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibrator.rds, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibrated_predictions.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv</files>
  <behavior>
    - "All 12 outer tournaments and exactly 630 registered fixtures receive nested-selected or raw-fallback probabilities with unique unchanged fixture identity and complete audit lineage."
    - "outer_fold_fits.csv has exactly one canonical row per outer tournament, including warm-up fallbacks, and its ordered tournament lineage, support, selected recipe, seed/convergence, fallback, parameters, and fit-record hash reproduce that outer tournament's probabilities."
    - "The authoritative decision is recomputed by phase12_selection_decision with unchanged reason ordering and veto semantics over the complete outer predictions."
    - "Only a zero-reason outer pass selects the final family/hyperparameters by the same frozen full-development nested tournament evidence and then fits that recipe on all 630 development rows; otherwise calibrator.rds records raw_fallback and raw_1x2 remains primary."
    - "The manifest hashes every frozen input, the original blocked gate/manifest, new code, contract, selection table, calibrator, predictions, new gate, and its own canonical row."
  </behavior>
  <action>Add failing complete-panel, post-pass-fit, outer-fit replay, artifact-integrity, and authority-nonmutation tests first. Then expand the proven slice to all registered outer tournaments in canonical chronology and write the complete candidate graph atomically to the separate remediation root. Expand `outer_fold_selection.csv` so each outer row exposes the ordered outer training tournament IDs, ordered inner validation IDs, canonical inner-validation-to-strictly-prior-training map, date cutoffs, row/home/draw/away support, chosen family/hyperparameters, frozen ranking metrics, and fallback reason. Write `outer_fold_fits.csv` with exactly one row for each of the 12 outer tournaments: repeat the selected lineage/support/recipe/fallback identity and persist optimizer method, deterministic seed, convergence code/message, objective value, scalar temperature or all vector slopes/offsets, identity parameters for raw fallback, source-panel hash, and canonical `fit_record_sha256`. Reapply only those persisted parameters in a test-only replay and require all outer probabilities to equal the just-produced probabilities at absolute tolerance `1e-12`. Evaluate the exact 630-row outer predictions through `compare_phase12_raw_calibrated()` and `phase12_selection_decision()` with the existing protocol/freeze and unchanged distribution/auxiliary identity. After a zero-reason decision, rerun the frozen nested selector across all 12 development tournaments to choose the final recipe by the same eligibility/ranking rule, then fit that chosen recipe on all development rows; otherwise persist a raw-fallback calibrator and blocked gate. Include `outer_fold_fits.csv` and its canonical content hash in `calibration_revision_manifest.csv`, bind every input/output plus manifest self-hash, assert the original evidence and release/registry bytes are unchanged, and reject any development identity outside the 12 registered editions. Add tamper tests proving forged training IDs, convergence fields, fitted parameters, or fit-record hashes fail producer validation.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_remediation.R", stop_on_failure=TRUE); source("R/release/calibration_remediation.R"); root &lt;- "outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2"; stopifnot(isTRUE(phase14_validate_calibration_remediation(root, require_promoted=FALSE)))'</automated></verify>
  <acceptance_criteria>The separate root is complete and self-consistent; all 12 replay-sufficient outer fit rows are manifest-bound and reproduce all 630 probabilities at `1e-12`; its decision is either an unchanged-veto pass with a post-pass fitted calibrator or a hash-bound block with raw fallback, while original evidence, selectors, registries, holdout sealing, and public suppression remain unchanged.</acceptance_criteria>
  <done>The locked remediation protocol has produced immutable, chronology-safe outer evidence ready for independent Plan 14-22 acceptance.</done>
</task>

</tasks>

<threat_model asvs_level="1">
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| development fixtures to nested selector | Outcome labels are permitted only after their tournament becomes an inner or outer validation fold. |
| exploratory evidence to authoritative evaluation | Observed feasibility figures must not control frozen family/hyperparameter selection. |
| candidate root to release authority | Candidate files are untrusted until independently hash- and gate-validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-21-S | Spoofing | family/grid identity | high | mitigate | Canonical contract rows and hashes bind exact family names, grids, transforms, and ranking order. |
| T-14-21-T | Tampering | nested selection, fitted parameters, and artifact graph | high | mitigate | Persist exact training/validation lineage, support, deterministic seed/convergence, replay-sufficient parameters, fit-record hashes, every input/output hash, and manifest self-hash. |
| T-14-21-I | Information Disclosure | WC2026 labels | high | mitigate | Restrict identities to the 12 registered development editions and test holdout rejection on every path. |
| T-14-21-E | Elevation of Privilege | final fit and release authority | high | mitigate | Fit only after a zero-reason outer decision and never mutate selector or registry authority in this plan. |
| T-14-21-R | Repudiation | fallback and hyperparameter choice | medium | mitigate | Persist support, inner folds, score tuple, tie-break order, chosen parameters, and reason for every outer fold. |
| T-14-21-D | Denial of Service | invalid/undersupported optimization | medium | mitigate | Bound all frozen grids and deterministically return raw fallback for insufficient or non-finite evidence. |
</threat_model>

<verification>
Run the focused remediation tests and producer validator in fresh R processes; verify exact grid/seed identity, nested date/tournament inequalities, 12 replay-sufficient fit rows, all 630 probabilities at `1e-12`, unchanged Phase 12 decision output, complete hashes, holdout exclusion, and unchanged authority bytes.
</verification>

<success_criteria>
- Three frozen families are evaluated without outer-fold leakage.
- Every outer fold has an auditable selected recipe or explicit raw fallback.
- Every outer fold has exact strictly-prior lineage, support, optimizer state, and fitted parameters sufficient for independent replay.
- The same 630 fixtures reach the unchanged authoritative gate.
- The separate immutable graph validates without using WC2026 labels or mutating authority.
</success_criteria>

<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-21-SUMMARY.md` when done.</output>
