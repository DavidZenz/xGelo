---
phase: 14-shared-competition-state-and-forecast-layer
plan: "17"
type: execute
wave: 16
depends_on: ["14-16"]
files_modified: [R/competition/forecast_layer.R, R/competition/state_bundle.R, scripts/build_competition_state.R, tests/testthat/test_phase14_forecast_layer.R, tests/testthat/test_phase14_state_bundle.R]
autonomous: true
requirements: [STATE-04, FORECAST-01, FORECAST-02, FORECAST-03]
must_haves:
  truths:
    - "Every production fixture receives available or suppressed status; no lifecycle, identity, evidence, or release case is silently dropped."
    - "Every available forecast carries all D-20 source/state/registry/ruleset/release/model/calibrator/identity/form/cutoff/support/method/seed/generation lineage."
    - "Batch prediction loads release once, predicts all eligible fixtures once, keeps full grids local, and emits compact top-10/audit tables."
    - "Failure of a shared input required by the immutable release active-predictor set invalidates both candidates; inactive optional xG unavailability is audited, and edition-specific failure invalidates only that diagnostic candidate per D-21."
    - "Every batch uses the canonical-match adapter and immutable release manifest: evidence is required only for active predictors, current Elo-only forecasts retain audited unavailable/NA xG, and xG-active releases suppress uncovered fixtures."
    - "The build script sets one fixed startup seed before sourcing build callbacks, and repeated dry-run/replay invocations with identical inputs produce identical candidate rows and hashes."
  artifacts:
    - path: scripts/build_competition_state.R
      provides: "Explicit one/both-edition dry-run/build entrypoint with fixed startup seed"
    - path: R/competition/state_bundle.R
      provides: "Complete batch candidate validation"
  key_links:
    - from: R/competition/forecast_layer.R
      to: R/competition/state_bundle.R
      via: "complete fixture status/forecast/grid/lineage and active-predictor evidence tables"
      pattern: "suppression_reason"
    - from: outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
      to: R/competition/forecast_layer.R
      via: "immutable active/dropped predictor contract for batch eligibility"
      pattern: "active_predictors"
  prohibitions:
    - statement: "Batch expansion must not silently drop fixtures, relabel inactive/missing inputs as evidence, emit qualitative confidence labels, publish Phase 17 dashboards, or copy state between editions."
      status: unverified
      verification: judgment
---
<objective>Expand the tracer to complete production-batch forecasting, suppression, lineage, validation, and edition-aware build orchestration. Purpose: deliver the full Phase 14 backend capability before durable edition outputs. Output: complete modules, script, and green batch tests.</objective>
<execution_context>@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/14-shared-competition-state-and-forecast-layer/14-16-SUMMARY.md
@R/benchmark/baselines.R
@R/benchmark/contracts.R
@R/competition/publication_transaction.R
@scripts/acquire_uefa_snapshot.R</context>

## Artifacts this phase produces

- Expanded production functions: vectorized `phase14_build_fixture_forecasts()`, complete `phase14_build_competition_state_candidate()`, new `phase14_build_competition_state_batch()`, and new `phase14_validate_competition_state_bundle()`.
- New script: `scripts/build_competition_state.R` with explicit `--edition-id <edition_id|both>` and `--dry-run` flags, script-relative root resolution, `set.seed(14017L)` at startup before callback/build execution, injectable callbacks, and fail-closed exit statuses; it never discovers editions or releases by recency.
- Complete forecast/suppression reason enum: available, pre_draw, kickoff_unconfirmed, identity_unresolved, feature_evidence_unavailable, release_not_calibrated, status_ineligible, and explicit approved-release field-unavailable reasons.
- Complete candidate tables/fields: fixture status, forecasts, full local G=40 grids, compact top-10 rows, standings/form outputs, national-team xG availability/source/sample/cutoff audit, active/dropped predictor identity, audit rows, and manifests carrying `model_data_cutoff`, `feature_cutoff_utc`, D-20 lineage, exact parents/rows/hashes/warnings/validation statuses, and shared versus edition-local failure scope.
- Existing Phase 14 forecast/state tests gain full-batch no-silent-drop, canonical adapter, model-once/batch-once, exact resolver API, immutable active/dropped predictor handling, current Elo-only forecast with unavailable xG, synthetic xG-active suppression/fan-out, script dry-run, inventory, and manifest-validator coverage.

<tasks>
<task type="auto" tdd="true">
  <name>Task 14-17-01: Expand complete suppression, G=40 uncertainty, and D-20 lineage</name>
  <read_first>
    - R/competition/forecast_layer.R
    - R/benchmark/baselines.R
    - R/benchmark/contracts.R
    - R/forecast/features.R
    - data/competition/registries/team_identity.csv
    - data/competition/registries/national_team_xg_sources.csv
    - data/processed/rolling_form.csv
    - data/benchmark/phase10/feature_contract.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
    - tests/testthat/test_phase14_forecast_layer.R
  </read_first>
  <files>R/competition/forecast_layer.R, tests/testthat/test_phase14_forecast_layer.R</files>
  <behavior>
    - "Input fixture IDs equal the union of available and suppressed fixture IDs with no duplicates or omissions."
    - "All eligibility reasons and D-20 fields, including model_data_cutoff and feature_cutoff_utc, are complete and hash-bound."
    - "Full grids are G=40; top-10/entropy/intervals/modal/expected goals derive deterministically from unchanged grids."
  </behavior>
  <action>Expand `phase14_build_fixture_forecasts()` from tracer to vectorized production batches. Resolve the selector and hash-bound model manifest once, adapt all canonical fixtures once, and call `phase14_build_release_features()` once before model prediction. Require strictly prior evidence for each immutable `active_predictors` entry; retain `dropped_predictors_with_reason` and internal registered missingness for inactive predictors. Under the current `elo_diff`-only incumbent, permit available forecasts while national-team xG stays unavailable/NA in public form/status lineage; under an xG-active synthetic manifest, suppress uncovered fixtures as `feature_evidence_unavailable`. Implement the full D-13 suppression enum, dual-simplex checks, all D-17 quantitative/unavailable fields, exact D-20 lineage, model-once/batch-once execution, deterministic top-10 ordering, compact limits, and explicit input/status equality validation. Forecast, suppression-status, and candidate-manifest rows must expose exact release predictor identity, `model_data_cutoff`, and per-fixture `feature_cutoff_utc`.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_forecast_layer.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>All lifecycle/suppression/G=40/lineage cases pass and fixture input/status equality is exact.</acceptance_criteria>
  <done>Forecast expansion covers every production fixture without silent loss.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 14-17-02: Build complete one/both-edition candidates with scoped failure semantics</name>
  <read_first>
    - R/competition/state_bundle.R
    - R/competition/publication_transaction.R
    - scripts/acquire_uefa_snapshot.R
    - data/competition/registries/national_team_xg_sources.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
    - tests/testthat/test_phase14_state_bundle.R
  </read_first>
  <files>R/competition/state_bundle.R, scripts/build_competition_state.R, tests/testthat/test_phase14_state_bundle.R</files>
  <behavior>
    - "Shared release, identity, history, and every release-active predictor preflight occurs before edition work and invalidates both on failure."
    - "Optional inactive national-team xG unavailability is audited without invalidating either edition; an xG-active release makes that evidence required."
    - "Edition-local failure records only that edition invalid; the other may complete for diagnosis."
    - "Candidate manifests enumerate exact output files, rows, hashes, parents, warnings, and validation statuses."
    - "Two identical script dry-runs and replay checks start from the same fixed seed and produce byte-identical canonical candidate tables and manifest hashes."
  </behavior>
  <action>Complete `phase14_build_competition_state_candidate()` and add `phase14_build_competition_state_batch()`, `phase14_validate_competition_state_bundle()`, plus `scripts/build_competition_state.R`. The validator must enforce the exact inventory, schemas, keys, parent hashes, row hashes, release/model/calibrator identities, immutable active/dropped predictor set, `model_data_cutoff`, feature cutoffs, national-team xG availability/source/sample/cutoff audit, suppression coverage, G=40/local versus compact boundaries, and edition isolation used by Plans 14-18/19. Follow acquisition script root/argument/callback/dry-run patterns. In the script, call `set.seed(14017L)` once during startup before any sourced injectable callback or build operation; extend the state-bundle test to execute identical isolated dry-runs/replays twice and compare canonical rows, manifests, and hashes exactly. Accept explicit edition ID or `both`, never discover by recency; pass a selector path to the canonical resolver and never pass a release manifest as a selector. Before edition work, validate shared release/identity/history and only the evidence required by the active predictor set. Assert failure fan-out to both editions for those required active inputs, audit inactive optional xG without fan-out, build isolated sibling candidates, preserve previous accepted outputs on failure, and leave Phase 17 all-or-nothing public promotion untouched.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_state_bundle.R", stop_on_failure=TRUE); source("R/competition/state_bundle.R"); stopifnot(is.function(phase14_build_competition_state_batch))'</automated></verify>
  <acceptance_criteria>Full temporary NL/EURO candidates validate; shared and edition-local failures have exact D-21 scope; the script has fixed startup seed `14017L`; repeated dry-runs/replays are byte/hash identical and mutate no durable files.</acceptance_criteria>
  <done>The complete production batch engine is ready for separate edition-local promotion.</done>
</task>
</tasks>
<threat_model asvs_level="1" block_on="high">Unresolved high threats block execution.
| Threat ID | STRIDE | Severity | Disposition | Control | Test |
|---|---|---|---|---|---|
| T-14-17-T | Tampering | high | mitigate | no-silent-drop equality, complete lineage/manifests/hashes, fixed startup seed, and deterministic replay | forecast/state tests |
| T-14-17-I | Information Disclosure | high | mitigate | shared allowlist and isolated roots | state test |
| T-14-17-D | Denial of Service | medium | mitigate | release-once, batch-once, fixed G=40, compact output limits | forecast test |
</threat_model>
<verification>Both canonical batch test files pass; the script declares the fixed startup seed and repeated dry-runs/replays are byte-identical and mutation-free.</verification>
<success_criteria>All lifecycle, suppression, lineage, batch, and failure-scope behavior is production-complete.</success_criteria>
<output>Create `14-17-SUMMARY.md` when done.</output>
