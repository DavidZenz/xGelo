---
phase: 14-shared-competition-state-and-forecast-layer
plan: "08"
type: execute
wave: 5
depends_on: ["14-07"]
files_modified:
  - R/release/calibration_revision.R
  - tests/testthat/test_phase14_calibration_release.R
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv
autonomous: true
requirements: [FORECAST-01]
must_haves:
  truths:
    - "The selector candidate is a one-row, self-hashed, trusted-root-contained pointer to the exact immutable release manifest."
    - "Selector proof is non-authoritative: neither `outputs/releases/approved_release.csv` nor competition registry pins move in this plan."
  artifacts:
    - path: R/release/calibration_revision.R
      provides: "Selector row builder/validator and authority transaction inputs"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv
      provides: "Validated non-authoritative selector candidate"
  key_links:
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv
      to: outputs/releases/phase14-open-nb-incumbent-calibrated-v1/release_manifest.csv
      via: "release ID, relative path, manifest SHA-256, selector self-hash"
      pattern: "manifest_sha256"
  prohibitions:
    - statement: "Selector construction must not discover latest releases or move durable authority before both registry rows can move atomically."
      status: unverified
      verification: judgment
---

<objective>
Implement and validate the explicit approved-release selector as a non-authoritative candidate.

Purpose: Separate selector semantics from the later selector-plus-dual-registry authority transaction.
Output: Selector code, tests, and one validated candidate row under the canonical benchmark evidence root.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-07-SUMMARY.md
@R/release/calibration_revision.R
@R/release/release_contract.R
@tests/testthat/test_phase14_calibration_release.R
</context>

## Artifacts this phase produces

- New functions in `R/release/calibration_revision.R`: `phase14_build_release_selector_row()` and `phase14_validate_release_selector()`.
- New non-authoritative durable evidence file: `outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv`.
- Selector schema fields: `release_id`, trusted-root-relative `release_manifest_path`, `manifest_sha256`, `approved_at_utc`, and selector `row_sha256`/self-hash. Selector validation reasons cover traversal, symlink, unknown release, stale manifest hash, multiple rows, and self-hash forgery.
- Existing `tests/testthat/test_phase14_calibration_release.R` gains candidate-path/hash/authority-nonmutation coverage. `outputs/releases/approved_release.csv` is not created by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-08-01: Build and validate the selector contract</name>
  <read_first>
    - R/release/calibration_revision.R
    - R/release/release_contract.R
    - outputs/releases/phase14-open-nb-incumbent-calibrated-v1/release_manifest.csv
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/calibration_revision.R, tests/testthat/test_phase14_calibration_release.R</files>
  <behavior>
    - "Selector has exact schema, one row, canonical relative path, manifest hash, release ID, approved_at_utc, and self-hash."
    - "Traversal, symlink, unknown release, stale hash, multiple rows, and self-hash forgery fail."
  </behavior>
  <action>Implement `phase14_build_release_selector_row()` and `phase14_validate_release_selector()` in `R/release/calibration_revision.R`, delegating release trust to selector-aware preflight. Use canonical CSV hashing and explicit trusted-root containment. Activate the selector section of the canonical release test.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Only one exact immutable release can satisfy the selector contract; all path/hash ambiguity fails before object loading.</acceptance_criteria>
  <done>The selector API is production-ready and tested without moving durable authority.</done>
</task>

<task type="auto">
  <name>Task 14-08-02: Persist and revalidate the selector candidate</name>
  <read_first>
    - R/release/calibration_revision.R
    - outputs/releases/phase14-open-nb-incumbent-calibrated-v1/release_manifest.csv
    - data/competition/registries/competition_editions.csv
  </read_first>
  <files>outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv</files>
  <action>Write the candidate selector under the calibration evidence root, validate it in a fresh R process, and snapshot/assert that the durable selector is absent or byte-identical and both edition rows still pin the incumbent raw release. This row is input to Plan 14-09, not runtime authority.</action>
  <verify><automated>Rscript --vanilla -e 'source("R/release/calibration_revision.R"); s &lt;- read.csv("outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv", stringsAsFactors=FALSE); stopifnot(isTRUE(phase14_validate_release_selector(s, trusted_root="outputs/releases")), nrow(s)==1L)'</automated></verify>
  <acceptance_criteria>The candidate validates against the immutable release while durable selector and registry bytes remain unchanged.</acceptance_criteria>
  <done>A validated non-authoritative selector candidate is ready for atomic dual repin.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-08-T | Tampering | high | mitigate | Self-hash plus exact manifest SHA and fresh preflight. | canonical release test |
| T-14-08-E | Elevation of Privilege | high | mitigate | Trusted-root relative path and explicit release allowlist. | canonical release test |
| T-14-08-R | Repudiation | medium | mitigate | Approved-at timestamp and immutable release/evidence identities. | selector validator |
</threat_model>

<verification>Canonical release tests and fresh candidate validation pass; durable authority is unchanged.</verification>
<success_criteria>Selector semantics are proven independently from promotion.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-08-SUMMARY.md` when done.</output>
