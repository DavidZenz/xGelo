---
phase: 14-shared-competition-state-and-forecast-layer
plan: "14"
type: execute
wave: 11
depends_on: ["14-13"]
files_modified: [R/competition/standings.R, tests/testthat/test_phase14_standings.R]
autonomous: true
requirements: [STATE-01]
must_haves:
  truths:
    - "D-06 computes all universal metrics and invokes an edition ruleset adapter when supplied; otherwise ordering is explicitly provisional."
    - "D-07 snapshots are fully keyed by edition_id/group_id/state_cutoff_utc/source_bundle_id and use only eligible results completed by cutoff."
    - "D-08 aggregate mismatch blocks and retains prior state; rank-only warns; official absence publishes provisional."
  artifacts:
    - path: R/competition/standings.R
      provides: "Universal reducer, adapter seam, reconciliation"
  key_links:
    - from: R/competition/match_state.R
      to: R/competition/standings.R
      via: "counts_for_standings plus final football scores"
      pattern: "counts_for_standings"
  prohibitions:
    - statement: "Phase 14 standings must not claim provisional ordering is official or implement Phase 15/16 competition-specific ordering."
      status: unverified
      verification: judgment
---
<objective>Implement complete cutoff-safe universal standings and official reconciliation. Purpose: satisfy STATE-01 and D-05 through D-08 without downstream competition rules. Output: `standings.R` and green tests.</objective>
<execution_context>@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/14-shared-competition-state-and-forecast-layer/14-13-SUMMARY.md
@R/visualization/worldcup_dashboard.R
@tests/testthat/test_phase14_standings.R</context>

## Artifacts this phase produces

- New production file/functions: `R/competition/standings.R` with `phase14_compute_standings()`, `phase14_reconcile_standings()`, and `phase14_validate_standings_snapshot()`.
- Standings schema fields: `edition_id`, `group_id`, `state_cutoff_utc`, `source_bundle_id`, played/W/D/L/GF/GA/GD/points, computed order/status, ruleset-adapter ID, official rank/points/aggregate fields, reconciliation status/reason, warning/block status, prior-state retention instruction, and canonical row/table hashes.
- Reconciliation reason enum: exact, rank-only warning, aggregate mismatch blocked, partial official blocked, official absent provisional, and foreign source bundle rejected.
- Existing `tests/testthat/test_phase14_standings.R` gains complete production reducer, cutoff/key, adapter-seam, reconciliation, and fail-closed validation coverage. No durable standings output is written in this plan.

<tasks>
<task type="auto" tdd="true">
  <name>Task 14-14-01: Compute fully keyed cutoff-safe universal standings</name>
  <read_first>
    - R/competition/match_state.R
    - R/visualization/worldcup_dashboard.R
    - tests/fixtures/phase14/standings_reconciliation_cases.csv
    - tests/testthat/test_phase14_standings.R
  </read_first>
  <files>R/competition/standings.R, tests/testthat/test_phase14_standings.R</files>
  <behavior>
    - "Only eligible completed evidence before/equal-to state cutoff enters standings as defined by evidence completion availability."
    - "Rows carry the exact four-part snapshot key and all universal metrics."
    - "No adapter yields deterministic provisional order; supplied adapter result is separately identified."
  </behavior>
  <action>Implement `phase14_compute_standings()` per D-06/D-07, reusing only universal reducer arithmetic from the dashboard analog. Group by exact edition/group, filter `counts_for_standings` and cutoff availability, calculate played/W/D/L/GF/GA/GD/points, expose `ruleset_adapter` and `ordering_status`, and hash canonical keyed snapshots.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_standings.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Universal metrics and four-part keys are exact; foreign bundle/edition rows and late evidence fail.</acceptance_criteria>
  <done>Universal standings work for full production groups with explicit provisional ordering.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 14-14-02: Reconcile same-bundle official standings and block aggregates</name>
  <read_first>
    - R/competition/standings.R
    - tests/fixtures/phase14/standings_reconciliation_cases.csv
    - tests/testthat/test_phase14_standings.R
  </read_first>
  <files>R/competition/standings.R, tests/testthat/test_phase14_standings.R</files>
  <behavior>
    - "Exact agreement publishes exact; rank-only mismatch warns; aggregate/partial mismatch blocks."
    - "Official absence is provisional; foreign source bundle cannot reconcile."
  </behavior>
  <action>Implement `phase14_reconcile_standings()` and `phase14_validate_standings_snapshot()`. Preserve computed and official fields side-by-side, reason enums, prior accepted retention instruction, and warning/block status. Compare only same accepted source bundle.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_standings.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>All reconciliation fixture states have exact disposition and no aggregate mismatch can become accepted.</acceptance_criteria>
  <done>Standings reconciliation is complete and fail-closed.</done>
</task>
</tasks>
<threat_model asvs_level="1" block_on="high">Unresolved high threats block execution.
| Threat ID | STRIDE | Severity | Disposition | Control | Test |
|---|---|---|---|---|---|
| T-14-14-T | Tampering | high | mitigate | keyed cutoff filter and same-bundle aggregate reconciliation | standings test |
| T-14-14-S | Spoofing | high | mitigate | official/provisional fields and statuses remain separate | standings test |
</threat_model>
<verification>Run `test_phase14_standings.R` after each task.</verification>
<success_criteria>STATE-01 is complete for production batches with an explicit later-rules adapter seam.</success_criteria>
<output>Create `14-14-SUMMARY.md` when done.</output>
