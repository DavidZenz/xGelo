---
phase: 14-shared-competition-state-and-forecast-layer
plan: "04"
type: execute
wave: 1
depends_on: ["14-02"]
files_modified:
  - R/release/calibration_revision.R
  - tests/testthat/test_phase14_calibration_release.R
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibrator.rds
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibrated_predictions.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
  - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv
autonomous: true
requirements: [FORECAST-01]
must_haves:
  truths:
    - "Calibration filters the 1,260 open_nb_incumbent rows to model_id=open_nb_incumbent, track_id=updating, panel_id=open_core and then requires exactly 630 rows and 630 unique historical development fixtures in strict prior-edition order; it never reads WC2026 labels."
    - "The empirical gate applies existing support, coverage, score-identity, RPS, Brier, log-loss, fold-stability, and strict calibration-improvement vetoes unchanged."
    - "Pass or block is durable: failure writes CALIBRATION_RELEASE_BLOCKED evidence and performs no release, selector, or registry mutation."
  artifacts:
    - path: R/release/calibration_revision.R
      provides: "Chronology-safe incumbent calibration and empirical gate"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
      provides: "Single empirical pass-or-block disposition"
  key_links:
    - from: R/release/calibration_revision.R
      to: R/calibration/calibration_selection.R
      via: "phase12_selection_decision with unchanged protocol"
      pattern: "phase12_selection_decision"
  prohibitions:
    - statement: "Calibration must not weaken thresholds, reopen final labels, emit raw-as-calibrated output, or create split edition pins."
      status: unverified
      verification: judgment
---

<objective>
Build and empirically evaluate the incumbent calibration candidate without changing release authority.

Purpose: Resolve D-15 by evidence: calibrated forecasting proceeds only if frozen development vetoes pass unchanged.
Output: `R/release/calibration_revision.R` plus four hash-bound artifacts under the canonical Plan-04 benchmark root.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-RESEARCH.md
@.planning/milestones/v2.0-phases/12-calibration-promotion-and-model-release/12-10-SUMMARY.md
@R/calibration/probability_calibration.R
@R/calibration/calibration_selection.R
@tests/testthat/test_phase14_calibration_release.R
</context>

## Artifacts this phase produces

- New production file/functions: `R/release/calibration_revision.R` with `phase14_build_incumbent_development_panel()`, `phase14_fit_rolling_incumbent_calibration()`, `phase14_evaluate_incumbent_calibration()`, and `phase14_validate_calibration_revision()`.
- New durable evidence: `calibrator.rds`, `calibrated_predictions.csv`, `calibration_gate.csv`, and `calibration_revision_manifest.csv` under `outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/`.
- New gate fields/enums: model/track/panel identity; 630-row and unique-fixture counts; chronology/support/coverage/score-identity/RPS/Brier/log-loss/fold-stability/calibration-improvement booleans; ordered `reason_codes`; `fit_status`; `primary_probability_view`; `calibration_promoted`; `model_data_cutoff`; calibration evidence cutoff; and `CALIBRATION_RELEASE_BLOCKED` disposition.
- Existing `tests/testthat/test_phase14_calibration_release.R` gains active calibration-panel, chronology, lineage, gate, and manifest-hash tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-04-01: Build chronology-safe incumbent calibration evidence</name>
  <read_first>
    - R/calibration/probability_calibration.R
    - R/calibration/calibration_selection.R
    - data/benchmark/phase09/fixtures.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/calibration_revision.R, tests/testthat/test_phase14_calibration_release.R, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibrator.rds, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibrated_predictions.csv</files>
  <behavior>
    - "Each evaluation edition fits from strictly earlier registered editions."
    - "The final fitted candidate uses development rows only after rolling evaluation completes."
    - "The source table is filtered by model_id=open_nb_incumbent, track_id=updating, panel_id=open_core before asserting exactly 630 rows and 630 unique fixture IDs."
    - "Fixture, distribution, model, recipe, protocol, and chronology identities are retained."
  </behavior>
  <action>Implement `phase14_build_incumbent_development_panel()` and `phase14_fit_rolling_incumbent_calibration()` in canonical `R/release/calibration_revision.R`. From the 1,260 `open_nb_incumbent` source rows, filter `model_id == "open_nb_incumbent"`, `track_id == "updating"`, and `panel_id == "open_core"`; fail unless the result has exactly 630 rows and 630 unique fixture IDs before any fit. Reuse `fit_phase12_1x2_calibrator()` and bind the exact incumbent model hash, filtered 630-prediction bytes, fixtures, recipe/protocol, code commit, G=40, model data cutoff, and max evidence date. Extend the canonical test file so this section is active and rejects wrong slice cardinality, duplicate fixtures, equality/future folds, and any WC2026 label path.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase12_calibration.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>The exact `open_nb_incumbent`/`updating`/`open_core` slice has 630 rows and 630 unique fixtures; rolling calibrated predictions cover that same slice, are chronology-safe, and preserve all score/distribution identities.</acceptance_criteria>
  <done>The candidate calibrator and calibrated development predictions exist with complete immutable lineage and no authority mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-04-02: Apply unchanged vetoes and persist pass-or-block</name>
  <read_first>
    - R/release/calibration_revision.R
    - R/calibration/calibration_selection.R
    - data/benchmark/phase09/promotion_protocol.json
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/calibration_revision.R, tests/testthat/test_phase14_calibration_release.R, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv, outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv</files>
  <behavior>
    - "Every existing veto reason remains in Phase 12 order."
    - "Zero reasons is the only promoted outcome; any reason is CALIBRATION_RELEASE_BLOCKED."
    - "Both outcomes validate as honest evidence without mutating release authority."
  </behavior>
  <action>Implement `phase14_evaluate_incumbent_calibration()` and `phase14_validate_calibration_revision()`. Compare raw versus rolling calibrated 1X2 on identical fixtures and call `phase12_selection_decision()` without threshold or operator changes. Persist one gate row plus a manifest hashing every input/output. Make the automated test pass for either a valid pass or valid blocked disposition, while separately asserting that only pass can satisfy downstream preconditions.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE); source("R/release/calibration_revision.R"); stopifnot(isTRUE(phase14_validate_calibration_revision("outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision")))'</automated></verify>
  <acceptance_criteria>The disposition is reproducible from frozen hashes; blocked evidence cannot be converted to pass without changing frozen evidence or protocol.</acceptance_criteria>
  <done>The empirical gate and revision manifest are durable, valid, and ready for the blocking checkpoint.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-04-T | Tampering | high | mitigate | Frozen input/output hashes and unchanged selector veto code. | canonical calibration test |
| T-14-04-S | Spoofing | high | mitigate | Fitted status and zero ordered reasons are required for pass. | canonical calibration test |
| T-14-04-I | Information Disclosure | high | mitigate | WC2026 label paths/identities are rejected from fit and selection lineage. | canonical calibration test |
</threat_model>

<verification>Run both focused calibration files; the task succeeds for a valid pass or valid block, never for malformed evidence.</verification>
<success_criteria>The empirical calibration gate is complete, hash-bound, and authority-neutral.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-04-SUMMARY.md` when done.</output>
