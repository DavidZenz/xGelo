---
phase: 14-shared-competition-state-and-forecast-layer
plan: "09"
type: execute
wave: 6
depends_on: ["14-08"]
files_modified:
  - R/release/calibration_revision.R
  - R/competition/edition_registry.R
  - tests/testthat/test_phase14_calibration_release.R
  - outputs/releases/approved_release.csv
  - data/competition/registries/competition_editions.csv
autonomous: true
requirements: [FORECAST-01, STATE-04]
must_haves:
  truths:
    - "Selector and exactly two edition rows either all move to the same calibrated release or all retain prior bytes."
    - "Both edition revisions increment exactly once while source bundle, lifecycle, ruleset, and edition-local output lineage remain unchanged."
    - "Registry and runtime release resolution use only phase14_resolve_approved_release(selector_path, trusted_release_root); a manifest is never passed as selector_path, and split pins are invalid."
  artifacts:
    - path: outputs/releases/approved_release.csv
      provides: "Durable one-row release authority"
    - path: data/competition/registries/competition_editions.csv
      provides: "Atomically dual-repinned edition registry"
  key_links:
    - from: outputs/releases/approved_release.csv
      to: data/competition/registries/competition_editions.csv
      via: "one locked snapshot/stage/validate/promote/rollback transaction"
      pattern: "phase14_promote_calibrated_release"
  prohibitions:
    - statement: "Authority promotion must not create split edition pins, partial selector state, or an override path after failed validation."
      status: unverified
      verification: judgment
---

<objective>
Atomically promote the explicit selector and both competition-edition release pins.

Purpose: Establish one calibrated authority for both editions without split state or partial promotion.
Output: Durable selector and revisioned two-row registry, with rollback-safe implementation and tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-08-SUMMARY.md
@R/competition/edition_registry.R
@R/release/calibration_revision.R
@data/competition/registries/competition_editions.csv
@tests/testthat/test_phase13_competition_registry.R
</context>

## Artifacts this phase produces

- New functions: `phase14_repin_both_competition_releases()` in `R/competition/edition_registry.R` and `phase14_promote_calibrated_release()` in `R/release/calibration_revision.R`.
- New durable authority file: `outputs/releases/approved_release.csv`, using the selector schema from Plan 14-08.
- Revised durable registry: `data/competition/registries/competition_editions.csv`, with both edition rows sharing the calibrated `model_release_id`, each `registry_revision` incremented once, and audit event/time/operator plus refreshed row hashes while source/lifecycle/ruleset/output lineage remains unchanged.
- Transaction reasons/statuses: staged, promoted, rollback-restored, split-pin rejected, missing-edition rejected, selector/release mismatch rejected, and post-validation failed. Existing `tests/testthat/test_phase14_calibration_release.R` gains every-rename failure injection and byte-rollback coverage.

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-09-01: Implement and failure-test the two-authority transaction</name>
  <read_first>
    - R/competition/edition_registry.R
    - R/release/calibration_revision.R
    - tests/testthat/test_phase14_calibration_release.R
    - tests/testthat/test_phase13_competition_registry.R
  </read_first>
  <files>R/release/calibration_revision.R, R/competition/edition_registry.R, tests/testthat/test_phase14_calibration_release.R</files>
  <behavior>
    - "Both rows update in memory, validate against one selector/release, then promote with the selector under one lock."
    - "Failure before/after every rename restores exact selector and registry bytes."
    - "A split pin, missing edition, wrong revision delta, or changed edition-local lineage fails."
  </behavior>
  <action>Implement `phase14_repin_both_competition_releases()` in `edition_registry.R` and `phase14_promote_calibrated_release()` in `calibration_revision.R`. Snapshot the selector and registry, stage complete replacements, validate the staged selector by calling `phase14_resolve_approved_release(selector_path, trusted_release_root)`, validate the exact two-edition set and one-release invariant against its returned identity/cutoffs, promote deterministically, post-validate through the same selector-path API, and restore both snapshots on any failure. Registry code must never pass a release manifest path in the selector argument. Activate all transaction/failure cases in the canonical test.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase13_competition_registry.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Every injected failure and split-pin case leaves exact prior bytes; success advances both revisions and selector together.</acceptance_criteria>
  <done>The atomic authority transaction is proven against temporary roots.</done>
</task>

<task type="auto">
  <name>Task 14-09-02: Promote the durable selector and dual pins once</name>
  <read_first>
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/approved_release_candidate.csv
    - outputs/releases/phase14-open-nb-incumbent-calibrated-v1/release_manifest.csv
    - data/competition/registries/competition_editions.csv
    - R/release/calibration_revision.R
    - R/competition/edition_registry.R
  </read_first>
  <reversibility rating="costly">Undo requires another audited dual-row revision and selector transaction; in-place history rewriting is forbidden.</reversibility>
  <files>outputs/releases/approved_release.csv, data/competition/registries/competition_editions.csv</files>
  <action>Invoke the proven transaction once on durable files using the validated candidate selector. Reopen in a fresh R process, call `phase14_resolve_approved_release(selector_path="outputs/releases/approved_release.csv", trusted_release_root="outputs/releases")`, validate exactly two registry rows, require both release IDs equal the resolver identity `phase14-open-nb-incumbent-calibrated-v1`, require the resolver's non-missing `model_data_cutoff`, and require each prior registry revision plus one with unchanged source/lifecycle/ruleset/output fields.</action>
  <verify><automated>Rscript --vanilla -e 'source("R/release/release_contract.R"); source("R/competition/source_contracts.R"); source("R/competition/team_identity.R"); source("R/competition/edition_registry.R"); x &lt;- phase14_resolve_approved_release(selector_path="outputs/releases/approved_release.csv", trusted_release_root="outputs/releases"); r &lt;- load_competition_edition_registries(project_root="."); stopifnot(nrow(r$registries)==2L, length(unique(r$registries$model_release_id))==1L, unique(r$registries$model_release_id)=="phase14-open-nb-incumbent-calibrated-v1", identical(x$release_identity$release_id,"phase14-open-nb-incumbent-calibrated-v1"), !is.na(as.Date(x$model_data_cutoff)))'</automated></verify>
  <acceptance_criteria>Selector and both rows point to the calibrated release, revisions advance once, and no partial/split authority state exists.</acceptance_criteria>
  <done>One explicit calibrated release is now authoritative for both editions.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-09-T | Tampering | high | mitigate | Lock, byte snapshots, complete staging, post-check, rollback after every failure point. | canonical release test |
| T-14-09-E | Elevation of Privilege | high | mitigate | Exactly two allowlisted editions and one validated release selector. | canonical release test |
| T-14-09-R | Repudiation | medium | mitigate | Revision+1, audit event/time/operator, preserved prior lineage. | registry tests |
</threat_model>

<verification>Canonical release and Phase 13 registry tests pass; fresh selector/registry resolution proves one authority.</verification>
<success_criteria>The selector and two pins are atomically calibrated and rollback-safe.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-09-SUMMARY.md` when done.</output>
