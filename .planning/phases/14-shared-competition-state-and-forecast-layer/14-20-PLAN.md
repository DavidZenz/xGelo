---
phase: 14-shared-competition-state-and-forecast-layer
plan: "20"
type: execute
wave: 19
depends_on: ["14-19"]
files_modified: [R/competition/state_bundle.R, scripts/build_competition_state.R, tests/testthat/test_phase14_state_bundle.R]
autonomous: true
requirements: [STATE-04, FORECAST-03]
must_haves:
  truths:
    - "Normal/reversed-input and repeated production runs produce identical canonical rows, manifests, and durable hashes."
    - "Failures of shared inputs required by the active release invalidate both; optional inactive national-team xG unavailability is audited without fan-out; NL-only/EURO-only failures do not copy or corrupt the other edition."
    - "No-silent-drop equality holds for every source match/fixture and every output manifest inventory."
    - "Replay verifies model_data_cutoff equality across resolver, every forecast/status row, both manifests, and durable read-back."
    - "Phase 15/16 rules and Phase 17 dashboard/hourly atomic public promotion remain absent."
  artifacts:
    - path: tests/testthat/test_phase14_state_bundle.R
      provides: "Final isolation, rollback, equality, and replay gate"
  key_links:
    - from: outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
      to: outputs/competition/uefa_euro_2028_qualifying/audit/state_manifest.csv
      via: "shared-input identity equality plus edition-local output inequality"
      pattern: "edition_id"
  prohibitions:
    - statement: "Final replay must not silently drop rows, copy state, mutate outputs on dry-run/failure, or perform Phase 17 all-or-nothing public promotion."
      status: unverified
      verification: judgment
---
<objective>Close Phase 14 with full production isolation, rollback, no-silent-drop, and deterministic replay evidence. Purpose: prove complete post-tracer expansion, not only examples. Output: final validator/script/test hardening and green Phase 14/full regressions.</objective>
<execution_context>@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/14-shared-competition-state-and-forecast-layer/14-18-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-19-SUMMARY.md
@R/competition/publication_transaction.R
@tests/testthat/test_phase13_publication_transaction.R</context>

## Artifacts this phase produces

- New script flag: `scripts/build_competition_state.R --replay-check`, which builds isolated normal/reversed/repeated both-edition candidates, compares canonical row/manifests, and never promotes.
- Expanded functions/validators in `R/competition/state_bundle.R`: shared failure fan-out, every-eleven-target edition rollback, foreign-edition scans, no-silent-drop row/inventory equality, durable `model_data_cutoff`/feature-cutoff read-back, and deterministic replay checks. No separately named public API is added.
- Existing `tests/testthat/test_phase14_state_bundle.R` gains complete production isolation, failure injection, byte snapshot, normal/reversed/repeated replay, cutoff-lineage, compact/G=40, and durable-manifest comparison coverage.
- No new durable state artifact is created; Plans 14-18/19 outputs must remain byte-identical during replay/failure checks.

<tasks>
<task type="auto" tdd="true">
  <name>Task 14-20-01: Enforce shared-failure fan-out and edition-specific isolation</name>
  <read_first>
    - R/competition/state_bundle.R
    - tests/fixtures/phase14/edition_isolation_cases.csv
    - tests/testthat/test_phase14_state_bundle.R
    - tests/testthat/test_phase13_refresh_failure.R
  </read_first>
  <files>R/competition/state_bundle.R, tests/testthat/test_phase14_state_bundle.R</files>
  <behavior>
    - "Shared identity/release/history or active-predictor evidence failure invalidates both before edition work."
    - "Inactive optional national-team xG unavailability remains audited and does not invalidate either edition; the xG-active synthetic release does fan out when evidence is absent."
    - "Edition-specific failure invalidates only that candidate and cannot change/copy the other."
    - "Injected promotion failure restores exact prior bytes and leaves unrelated paths untouched."
  </behavior>
  <action>Complete D-21 validation and failure injection across all eleven edition targets. Derive shared requiredness from the immutable release active-predictor set: release/identity/history and missing active evidence fan out to both editions, inactive optional national-team xG unavailability remains an audit state, and the synthetic xG-active case fans out when accepted evidence is absent. Add explicit shared/edition-local error scopes, prior-byte snapshots, cross-edition foreign-key scans, and exact unrelated-path checks. Keep Phase 17 public two-site atomic withholding outside this transaction.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_state_bundle.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase13_refresh_failure.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Every isolation case has exact invalid editions and byte outcomes; no failed path changes accepted state.</acceptance_criteria>
  <done>D-21 failure and isolation semantics are complete for production inventories.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 14-20-02: Prove deterministic full-batch replay and equality</name>
  <read_first>
    - scripts/build_competition_state.R
    - R/competition/state_bundle.R
    - tests/testthat/test_phase14_state_bundle.R
    - outputs/competition/uefa_nations_league_2026_27/audit/state_manifest.csv
    - outputs/competition/uefa_euro_2028_qualifying/audit/state_manifest.csv
  </read_first>
  <files>scripts/build_competition_state.R, R/competition/state_bundle.R, tests/testthat/test_phase14_state_bundle.R</files>
  <behavior>
    - "Normal, reversed, and repeated both-edition dry-runs produce identical canonical hashes."
    - "Input fixture/match IDs equal output/suppression coverage IDs; manifest rows equal actual inventory; model_data_cutoff equals the resolver and all forecast/status/manifest rows."
    - "Durable bytes remain unchanged after dry-run replay."
  </behavior>
  <action>Add explicit `--replay-check` support that builds isolated normal/reversed candidates, compares canonical manifests and row sets, verifies no-silent-drop equalities, resolver/forecast/status/manifest `model_data_cutoff` equality, per-fixture cutoff lineage, and compact/G=40 boundaries, and never promotes. Extend final tests to compare against both durable manifests and assert exact byte snapshots before/after.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_dir("tests/testthat")'</automated></verify>
  <acceptance_criteria>The full repository `tests/testthat` suite passes; replay is order-independent; every row/artifact and cutoff is accounted for; durable bytes do not change.</acceptance_criteria>
  <done>The complete Phase 14 backend is deterministic, isolated, rollback-safe, and ready for Phase 15.</done>
</task>
</tasks>
<threat_model asvs_level="1" block_on="high">Unresolved high threats block execution.
| Threat ID | STRIDE | Severity | Disposition | Control | Test |
|---|---|---|---|---|---|
| T-14-20-T | Tampering | high | mitigate | full inventory/row equality, hash replay, rollback | state test |
| T-14-20-I | Information Disclosure | high | mitigate | cross-edition foreign-key scan and isolated roots | state test |
| T-14-20-D | Denial of Service | medium | mitigate | bounded inventories/G=40/compact limits | Phase 14 suite |
</threat_model>
<verification>Run the explicit automated phase gate `Rscript --vanilla -e 'testthat::test_dir("tests/testthat")'`; it includes all Phase 14 files and affected Phase 12/13 regressions.</verification>
<success_criteria>Production batch expansion, isolation, cutoff propagation, and replay are complete with no silent loss, and the full repository suite is green.</success_criteria>
<output>Create `14-20-SUMMARY.md` when done.</output>
