---
phase: 14-shared-competition-state-and-forecast-layer
plan: "06"
type: execute
wave: 3
depends_on: ["14-05"]
files_modified:
  - R/release/release_contract.R
  - R/release/release_bundle.R
  - tests/testthat/test_phase14_calibration_release.R
autonomous: true
requirements: [FORECAST-01]
must_haves:
  truths:
    - "The sole registry/runtime API is phase14_resolve_approved_release(selector_path, trusted_release_root); it reads approved_release.csv, resolves the immutable release directory, internally validates release_manifest_path plus model/calibrator bytes and identities, and never discovers authority by recency or sole-directory assumption."
    - "A calibrated release is valid only when model, fitted calibrator, revision gate, chronology, support, model_data_cutoff, and unchanged score-distribution identities agree before RDS use."
  artifacts:
    - path: R/release/release_contract.R
      provides: "Selector-aware metadata-first release preflight"
    - path: R/release/release_bundle.R
      provides: "Calibrated revision bundle validation/staging contract"
  key_links:
    - from: R/release/release_contract.R
      to: R/release/release_bundle.R
      via: "metadata-only preflight before object loading"
      pattern: "preflight.*load_models"
  prohibitions:
    - statement: "Release resolution must not scan for newest/latest authority or accept a raw fallback as a calibrated release."
      status: unverified
      verification: judgment
---

<objective>
Extend the existing release trust boundary for fitted calibration revisions and explicit selector authority.

Purpose: Make multiple immutable releases unambiguous before staging the new bundle.
Output: Updated canonical `R/release/` contracts and passing adversarial tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-05-SUMMARY.md
@.planning/milestones/v2.0-phases/12-calibration-promotion-and-model-release/12-09-SUMMARY.md
@.planning/milestones/v2.0-phases/12-calibration-promotion-and-model-release/12-10-SUMMARY.md
@R/release/release_contract.R
@R/release/release_bundle.R
@tests/testthat/test_phase12_release.R
</context>

## Artifacts this phase produces

- New public runtime function: `phase14_resolve_approved_release(selector_path, trusted_release_root)` in `R/release/release_contract.R`.
- Resolver return contract: `release_dir`, `release_manifest_path`, `release_identity`, `model_identity`, `calibrator_identity`, `model_data_cutoff`, `calibration_data_cutoff`, `support_max`, `primary_probability_view`, and hash-validated loaded `model`/`calibrator` objects.
- Extended internal functions: selector-aware metadata preflight in `preflight_phase12_approved_release()` and calibrated revision handling in `validate_phase12_release_bundle()` / `stage_phase12_release_bundle()`; direct manifest paths remain internal staging/install inputs, never selector inputs for registry/runtime callers.
- Extended test fields/reasons in `tests/testthat/test_phase14_calibration_release.R`: selector path/hash/self-hash, trusted-root and symlink failures, manifest/object identity drift, model/calibration cutoffs, raw/fitted status, final-label rejection, and calibrated-bundle gate reasons. No durable release or authority artifact is written.

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-06-01: Add exact selector-aware metadata preflight</name>
  <precondition>The Plan 14-05 checkpoint completed with `calibration-gate-passed`.</precondition>
  <read_first>
    - R/release/release_contract.R
    - tests/testthat/test_phase12_release.R
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/release_contract.R, tests/testthat/test_phase14_calibration_release.R</files>
  <behavior>
    - "An explicit selector row binds release_id, relative manifest path, manifest SHA-256, and selector self-hash."
    - "Trusted-root containment, symlink rejection, exact topology, and fresh preflight precede RDS reads."
    - "phase14_resolve_approved_release(selector_path, trusted_release_root) returns the resolved immutable paths, identities, model_data_cutoff/calibration cutoff, and validated objects; callers never pass a manifest as selector_path."
  </behavior>
  <action>Implement the one canonical registry/runtime API `phase14_resolve_approved_release(selector_path, trusted_release_root)`. It must read the one-row `approved_release.csv` at `selector_path`, validate its self-hash and trusted-root-contained relative manifest path, internally call metadata-only `preflight_phase12_approved_release()` on that resolved manifest, validate manifest/artifact hashes before RDS reads, load and identity-check the model/calibrator, and return exactly the resolved release directory/manifest path, release/model/calibrator identities, `model_data_cutoff`, calibration data cutoff, G=40 support/view metadata, and validated objects. Preserve existing Phase 12 direct-manifest APIs only as lower-level install/legacy internals; registry and runtime tests must call the Phase 14 API with a selector path. Reject ambiguous roots, traversal/symlinks, forged/stale selectors, contract path swaps, and selector/manifest identity disagreement.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Two immutable release directories remain unambiguous only through `phase14_resolve_approved_release(selector_path, trusted_release_root)`; it returns exact identity/cutoff fields after object-hash validation, while lower-level direct-manifest internals remain fail-closed.</acceptance_criteria>
  <done>Metadata-first release authority supports exact selection without loading model bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-06-02: Validate and stage calibrated revision bundles</name>
  <read_first>
    - R/release/release_bundle.R
    - R/calibration/probability_calibration.R
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv
    - tests/testthat/test_phase14_calibration_release.R
  </read_first>
  <files>R/release/release_bundle.R, tests/testthat/test_phase14_calibration_release.R</files>
  <behavior>
    - "calibrated_1x2 requires fitted calibrator, passing gate, chronology-safe revision, exact model identity, model_data_cutoff, and G=40 unchanged distribution."
    - "Raw 1X2 remains a separately named audit view."
  </behavior>
  <action>Extend `validate_phase12_release_bundle()` and `stage_phase12_release_bundle()` for a Phase 14 calibration-revision manifest and gate. Validate canonical artifact paths, hashes, source release, candidate/track/panel, exact `model_data_cutoff`, calibration evidence cutoff, labels_embedded=false, fitted calibrator identity, and score-distribution invariance. Ensure the staged contract exposes those cutoffs to `phase14_resolve_approved_release()`. Do not install a durable bundle in this plan.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Raw, failed, chronology-unsafe, forged, or final-label-derived revision candidates fail before model use; the passing candidate validates in a temporary root.</acceptance_criteria>
  <done>The release contract and bundle builder are ready to stage exactly one immutable calibrated release.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-06-T | Tampering | high | mitigate | Selector/manifest/artifact hashes and fresh metadata preflight. | Phase 12/14 release tests |
| T-14-06-E | Elevation of Privilege | high | mitigate | Trusted-root containment, symlink rejection, explicit release allowlist. | Phase 12/14 release tests |
| T-14-06-S | Spoofing | high | mitigate | Fitted/gate/chronology/view identities required together. | canonical calibration test |
</threat_model>

<verification>Both canonical Phase 12 and Phase 14 release tests pass after each task.</verification>
<success_criteria>Exact release authority and calibrated revision validation are production-ready without durable mutation.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-06-SUMMARY.md` when done.</output>
