---
phase: 14-shared-competition-state-and-forecast-layer
plan: "10"
type: execute
wave: 9
depends_on: ["14-01", "14-09"]
files_modified:
  - R/competition/source_contracts.R
  - R/competition/team_identity.R
  - R/competition/publication_hashes.R
  - scripts/acquire_uefa_snapshot.R
  - tests/testthat/test_phase14_match_state.R
  - tests/testthat/test_phase14_standings.R
autonomous: true
requirements: [STATE-01, STATE-02]
must_haves:
  truths:
    - "Accepted fixture/result/standings schema v2 retains every D-01 through D-08 input, including D-02 orthogonal match_status and completion_method axes, without changing stable fixture/team identity or raw provenance."
    - "Optional source fields fail closed: absent completion time, kickoff confirmation, split scores, group, or official aggregate remains typed missing/unresolved."
    - "EURO pre_draw resource tables remain exact zero-row schema-complete tables."
  artifacts:
    - path: R/competition/source_contracts.R
      provides: "Source-shaped Phase 14 v2 schemas"
    - path: R/competition/team_identity.R
      provides: "Normalized fixture/result/standings v2 projections"
    - path: R/competition/publication_hashes.R
      provides: "Schema-aware canonical hashing"
  key_links:
    - from: scripts/acquire_uefa_snapshot.R
      to: R/competition/team_identity.R
      via: "source handoff normalization before staged hash regeneration"
      pattern: "stage_normalized_edition"
  prohibitions:
    - statement: "Source adapters must not invent completed state, kickoff confirmation, split scores, official totals, or non-empty EURO pre_draw structures."
      status: unverified
      verification: judgment
---

<objective>
Evolve accepted competition source and normalized contracts to the state-ready v2 schema without durable promotion.

Purpose: Remove the schema information bottleneck before canonical state builders consume accepted data.
Output: Versioned source/normalization/hash/handoff interfaces with passing Phase 13 regressions.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-PATTERNS.md
@.planning/phases/13-source-contracts-and-competition-registry/13-11-SUMMARY.md
@.planning/phases/13-source-contracts-and-competition-registry/13-12-SUMMARY.md
@R/competition/source_contracts.R
@R/competition/team_identity.R
@R/competition/publication_hashes.R
</context>

## Artifacts this phase produces

- New schema IDs: `phase14-normalized-fixture-v2`, `phase14-normalized-result-v2`, and `phase14-normalized-standings-v2` in the existing source/identity/hash dispatch contracts.
- New function: `phase14_normalize_accepted_standings_rows()` in `R/competition/team_identity.R`; existing source/fixture/result normalization and publication-hash dispatch gain v2 branches.
- Fixture fields: group identity, source status, confirmed kickoff/time, source-artifact and stable fixture/team identity fields. Result fields: D-02 `match_status`, D-02 `completion_method`, regulation/final/shootout score pairs, `winner_team_id`, evidence completion time, and separate standings/form count flags. Standings fields: mapped group/team, nullable official rank, played, W/D/L, GF/GA/GD, points, source bundle/artifact lineage, warnings, and row hash.
- Existing `scripts/acquire_uefa_snapshot.R` gains v2 handoff dispatch only; no script flag or live client is added. Existing Phase 14 match/standings tests gain accepted-boundary and schema/hash assertions. No durable accepted CSV is changed in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 14-10-01: Extend source and normalized match contracts</name>
  <read_first>
    - R/competition/source_contracts.R
    - R/competition/team_identity.R
    - tests/testthat/test_phase14_match_state.R
    - tests/testthat/test_phase13_source_contracts.R
  </read_first>
  <files>R/competition/source_contracts.R, R/competition/team_identity.R, tests/testthat/test_phase14_match_state.R</files>
  <behavior>
    - "Fixture v2 retains group, source status, kickoff confirmation/time, source artifact, and stable fixture/team identity."
    - "Result v2 retains source/canonical inputs, completion method, regulation/final/shootout scores, winner, evidence time, and count flags."
    - "Missing optional fields remain typed missing/false/unresolved."
  </behavior>
  <action>Implement D-01, D-02, D-03, and D-04 at the accepted boundary using explicit `phase14-normalized-fixture-v2` and `phase14-normalized-result-v2` branches. Per D-02, preserve `match_status` and `completion_method` as separately validated fields, preserve `source_status` verbatim, and reject source mappings that silently invent completed. Extend source-shaped and normalized schemas together, preserve Phase 13 identity/source-artifact guards, and activate the accepted-boundary section of `test_phase14_match_state.R`. Final semantic validation remains Plan 14-13's responsibility.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_match_state.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase13_source_contracts.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Every D-01 through D-04 input survives normalization; D-02 lifecycle/completion axes remain independent; missing source evidence stays missing; v1 fixture replay remains green.</acceptance_criteria>
  <done>Accepted match schemas are state-ready without inventing semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 14-10-02: Add official standings normalization and hash parity</name>
  <read_first>
    - R/competition/team_identity.R
    - R/competition/publication_hashes.R
    - tests/testthat/test_phase14_standings.R
    - tests/testthat/test_phase13_publication_hashes.R
  </read_first>
  <files>R/competition/team_identity.R, R/competition/publication_hashes.R, tests/testthat/test_phase14_standings.R</files>
  <behavior>
    - "Standings v2 retains mapped group/team plus nullable official rank, played, W/D/L, GF/GA/GD, and points."
    - "Writer and hash refresher use one exact schema order and reject partial/foreign/forged links."
  </behavior>
  <action>Implement `phase14-normalized-standings-v2` and `phase14_normalize_accepted_standings_rows()`. Preserve source team/group IDs, mapped IDs, official fields independently, mapping warnings, source artifact, row hash, and source bundle linkage. Dispatch publication hashing by explicit schema version and activate accepted standings tests. Do not add competition-specific ordering.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_standings.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase13_publication_hashes.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Official aggregate absence remains NA, complete rows hash deterministically, and v1 hash regressions pass.</acceptance_criteria>
  <done>Accepted standings expose the complete same-bundle reconciliation surface.</done>
</task>

<task type="auto">
  <name>Task 14-10-03: Carry schema v2 through the source handoff normalizer</name>
  <read_first>
    - scripts/acquire_uefa_snapshot.R
    - R/competition/publication_hashes.R
    - R/competition/publication_manifests.R
    - tests/testthat/test_phase13_publication_integration.R
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R</files>
  <action>Update only the source-handoff-to-normalized staging path to emit explicit fixture/result/standings v2. Preserve raw bytes, raw SHA, parser commit, source URLs, artifact IDs, fallback state, isolated one-edition replay, and complete dual-edition transaction routing. Missing optional fields map to missing/unresolved values. Add no live API client.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_integration.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase14_match_state.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase14_standings.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>A temporary handoff emits hash-valid v2 tables, truthful EURO empties, and immutable raw provenance.</acceptance_criteria>
  <done>The accepted schema can flow into a staged candidate without durable data mutation.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-10-S | Spoofing | high | mitigate | Source wording retained; missing/unknown fields cannot manufacture completion. | match-state tests |
| T-14-10-T | Tampering | high | mitigate | Exact versioned schemas, row/content hashes, artifact foreign keys. | Phase 13 hash + Phase 14 tests |
| T-14-10-I | Information Disclosure | medium | mitigate | Edition/source-bundle keys reject foreign joins. | standings tests |
</threat_model>

<verification>Run the task-specific Phase 14 file and listed Phase 13 regression file after each task.</verification>
<success_criteria>State-ready v2 schemas are explicit, truthful, hash-compatible, and not yet durable.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-10-SUMMARY.md` when done.</output>
