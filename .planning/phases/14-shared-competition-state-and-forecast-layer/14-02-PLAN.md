---
phase: 14-shared-competition-state-and-forecast-layer
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/phase14/raw_release/model_contract.json
  - tests/fixtures/phase14/raw_release/release_manifest.csv
  - tests/fixtures/phase14/calibrated_release/model_contract.json
  - tests/fixtures/phase14/calibrated_release/release_manifest.csv
  - tests/testthat/helper_phase14_release.R
  - tests/testthat/test_phase14_calibration_release.R
autonomous: true
requirements: [FORECAST-01]
must_haves:
  truths:
    - "Raw fallback and fitted calibration fixtures are distinguishable by exact release/calibrator identity and probability-view fields."
    - "Wave 0 release tests pass now using descriptor and temporary-root checks; later behavior assertions activate as named production APIs appear."
    - "Backend-only scope excludes Phase 15/16 rules and Phase 17 dashboard/public-batch work."
  artifacts:
    - path: tests/testthat/test_phase14_calibration_release.R
      provides: "Canonical Phase 14 calibration/release/selector/dual-pin test surface"
    - path: tests/testthat/helper_phase14_release.R
      provides: "Temporary trusted-root and byte-snapshot helpers"
  key_links:
    - from: tests/fixtures/phase14/calibrated_release/model_contract.json
      to: tests/testthat/test_phase14_calibration_release.R
      via: "hash-complete temporary release root"
      pattern: "calibrated_1x2.*fitted"
  prohibitions:
    - statement: "Raw probabilities must not be labelled calibrated, calibration thresholds must not be weakened, and WC2026 final labels must not be reopened for fit or selection."
      status: unverified
      verification: judgment
flagged_assumptions:
  - requirement_id: FORECAST-01
    category: unclassified
    status: unresolved
    statement: "The deterministic probe supplied no FORECAST-01 shape; release authority remains ineligible until selector, manifest, objects, gate, and both edition pins are all proven."
---

<objective>
Create the canonical release fixture and test surface used by every Phase 14 calibration/release plan.

Purpose: Make empirical pass-or-block, immutable release validation, explicit selector authority, and dual-pin rollback executable without path drift.
Output: Four descriptors, one helper, and `tests/testthat/test_phase14_calibration_release.R`.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-PATTERNS.md
@R/release/release_contract.R
@R/release/release_bundle.R
@tests/testthat/helper_phase12_release.R
@tests/testthat/test_phase12_release.R
</context>

## Artifacts this phase produces

- New descriptor files: `tests/fixtures/phase14/raw_release/model_contract.json`, `tests/fixtures/phase14/raw_release/release_manifest.csv`, `tests/fixtures/phase14/calibrated_release/model_contract.json`, and `tests/fixtures/phase14/calibrated_release/release_manifest.csv`.
- New helper/test files: `tests/testthat/helper_phase14_release.R` and `tests/testthat/test_phase14_calibration_release.R`.
- New test helper functions: `phase14_materialize_release_fixture_root(descriptor_dir, trusted_root)`, `phase14_validate_release_fixture_root(trusted_root)`, and `phase14_assert_no_binary_release_fixtures(fixture_root)`.
- Descriptor fields: release/model/calibrator IDs and hashes, relative manifest path and hash, `support_max`, `fit_status`, `primary_probability_view`, calibration-gate identity, selector fields/self-hash, and model/calibration cutoff identities. No binary release object becomes a committed fixture.

<tasks>

<task type="auto">
  <name>Task 14-02-01: Create raw and fitted release descriptors</name>
  <read_first>
    - outputs/releases/phase12-wc2026-incumbent-retained-v1/model_contract.json
    - outputs/releases/phase12-wc2026-incumbent-retained-v1/release_manifest.csv
    - tests/testthat/helper_phase12_release.R
  </read_first>
  <files>tests/fixtures/phase14/raw_release/model_contract.json, tests/fixtures/phase14/raw_release/release_manifest.csv, tests/fixtures/phase14/calibrated_release/model_contract.json, tests/fixtures/phase14/calibrated_release/release_manifest.csv, tests/testthat/helper_phase14_release.R</files>
  <action>Create compact descriptor-only fixtures under `tests/fixtures/phase14`. Implement `phase14_materialize_release_fixture_root(descriptor_dir, trusted_root)` to parse one descriptor pair, copy the incumbent model into a temporary trusted root, construct the declared raw-fallback or fitted synthetic calibrator object, regenerate hashes through existing Phase 12 helpers, and return the validated identities. Implement `phase14_validate_release_fixture_root(trusted_root)` and `phase14_assert_no_binary_release_fixtures(fixture_root)`. Include release ID, exact manifest path/hash, G=40, model/calibrator identities, probability views, calibration gate identity, selector self-hash, and cutoff identities. Exclude final-label rows and directory-recency selection.</action>
  <verify><automated>Rscript --vanilla -e 'source("tests/testthat/helper_phase14_release.R"); root &lt;- tempfile("phase14-release-"); dir.create(root); raw &lt;- phase14_materialize_release_fixture_root("tests/fixtures/phase14/raw_release", file.path(root,"raw")); fitted &lt;- phase14_materialize_release_fixture_root("tests/fixtures/phase14/calibrated_release", file.path(root,"fitted")); stopifnot(isTRUE(phase14_validate_release_fixture_root(raw$trusted_root)), isTRUE(phase14_validate_release_fixture_root(fitted$trusted_root)), !identical(raw$release_id,fitted$release_id), !identical(raw$calibrator_sha256,fitted$calibrator_sha256), !identical(raw$primary_probability_view,fitted$primary_probability_view), isTRUE(phase14_assert_no_binary_release_fixtures("tests/fixtures/phase14")))'</automated></verify>
  <acceptance_criteria>Temporary roots are complete and hash-valid; raw and fitted identities cannot be confused; no binary fixture copy is committed.</acceptance_criteria>
  <done>The canonical release descriptors and helper exist and load successfully.</done>
</task>

<task type="auto">
  <name>Task 14-02-02: Seed the single calibration/release test surface</name>
  <read_first>
    - tests/testthat/test_phase12_calibration.R
    - tests/testthat/test_phase12_release.R
    - R/calibration/calibration_selection.R
    - tests/testthat/helper_phase14_release.R
  </read_first>
  <files>tests/testthat/test_phase14_calibration_release.R</files>
  <action>Create one direct-testthat file with passing descriptor/temp-root tests and guarded behavior sections for `phase14_evaluate_incumbent_calibration`, selector-aware preflight, immutable bundle validation, and atomic dual repin. Each guard skips only when its named API is absent; subsequent owning plans source the API and satisfy the unchanged assertions. Cover failed empirical gate, forged selector/calibrator, ambiguous roots, split pins, and injected rollback.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_calibration_release.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>The file passes immediately and contains one exact guarded section for each later release layer, all using canonical paths.</acceptance_criteria>
  <done>The FORECAST-01 Wave 0 test surface is runnable and cannot silently green a present-but-invalid API.</done>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-02-S | Spoofing | high | mitigate | Fitted calibrator plus calibrated view and passing gate are required together. | `test_phase14_calibration_release.R` |
| T-14-02-T | Tampering | high | mitigate | Temporary roots regenerate and verify manifest/object/selector hashes before load. | `test_phase14_calibration_release.R` |
| T-14-02-I | Information Disclosure | high | mitigate | Final-label identities and paths are rejected from calibration lineage. | `test_phase14_calibration_release.R` |
</threat_model>

<verification>Run the canonical release test file directly; no `tests/testthat.R` config exists.</verification>
<success_criteria>All six artifacts exist, the Wave 0 test passes, and the FORECAST-01 unclassified probe item is explicitly flagged.</success_criteria>
<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-02-SUMMARY.md` when done.</output>
