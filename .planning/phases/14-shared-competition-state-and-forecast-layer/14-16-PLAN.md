---
phase: 14-shared-competition-state-and-forecast-layer
plan: "16"
type: execute
wave: 12
depends_on: ["14-14", "14-15"]
files_modified: [R/competition/forecast_layer.R, R/competition/state_bundle.R, tests/testthat/test_phase14_forecast_layer.R, tests/testthat/test_phase14_state_bundle.R]
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, FORECAST-01, FORECAST-02, FORECAST-03]
must_haves:
  truths:
    - "One real Austria/Germany scheduled path traverses canonical-match adaptation, release-manifest active-predictor resolution, strict pre-kickoff Elo, G=40, calibrated 1X2, uncertainty, lineage, and candidate state."
    - "The same engine emits truthful EURO pre_draw suppression with no fabricated match/group/standing/form/probability rows."
    - "The immutable release manifest decides evidence sufficiency: the current incumbent activates elo_diff and drops xGF/xGA/xGD EWMA, so Austria/Germany can forecast while national-team xG remains audited unavailable/NA."
    - "A synthetic immutable release that activates xGF/xGA/xGD suppresses the same fixture with feature_evidence_unavailable until accepted national-team xG covers both teams before kickoff."
    - "The tracer is permanent production code and precedes all production-batch expansion."
  artifacts:
    - path: R/competition/forecast_layer.R
      provides: "Canonical-match adapter plus release-active-predictor feature and one-fixture forecast path"
    - path: R/competition/state_bundle.R
      provides: "Edition-parameterized candidate orchestration with active-input failure fan-out"
  key_links:
    - from: R/competition/match_state.R
      to: R/competition/forecast_layer.R
      via: "phase14_adapt_matches_for_forecast maps canonical IDs/kickoff/venue to the existing feature-table match contract"
      pattern: "phase14_adapt_matches_for_forecast"
    - from: outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
      to: R/competition/forecast_layer.R
      via: "phase14_build_release_features enforces immutable active_predictors and dropped_predictors_with_reason"
      pattern: "active_predictors"
    - from: R/forecast/features.R
      to: R/competition/forecast_layer.R
      via: "build_forecast_feature_table on adapted matches with registered inactive-predictor missingness"
      pattern: "build_forecast_feature_table"
  prohibitions:
    - statement: "The tracer must not fabricate EURO pre_draw state, relabel club form/goals as national-team xG, or require inactive dropped predictors as forecast evidence."
      status: unverified
      verification: judgment
---
<objective>Prove one production-quality vertical state-to-forecast slice before batch expansion. Purpose: catch cross-layer authority/wiring errors early. Output: permanent forecast/state skeleton and green end-to-end tracer tests.</objective>
<execution_context>@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/14-shared-competition-state-and-forecast-layer/14-14-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-15-SUMMARY.md
@R/competition/team_identity.R
@R/integration/rolling_form.R
@R/forecast/features.R
@data/competition/registries/team_identity.csv
@data/competition/registries/national_team_xg_sources.csv
@data/processed/rolling_form.csv
@data/benchmark/phase10/feature_contract.csv
@outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
@R/benchmark/baselines.R
@R/benchmark/contracts.R
@R/competition/publication_transaction.R</context>

## Artifacts this phase produces

- New production functions in `R/competition/forecast_layer.R`: `phase14_adapt_matches_for_forecast()`, `phase14_build_release_features()`, and `phase14_build_fixture_forecasts()`; `R/competition/state_bundle.R` adds `phase14_build_competition_state_candidate()`.
- Tracer forecast/status fields: edition/fixture/kickoff identity, availability/suppression reason, raw and calibrated 1X2, expected goals/modal score, G=40 distribution identity, top-10/omitted mass, uncertainty, `model_data_cutoff`, `feature_cutoff_utc`, latest evidence time, release/model/calibrator/registry/ruleset/source/state/team/form/history hashes, method/support/seed/count, generation time, and row hashes.
- Canonical adapter contract: `phase14_adapt_matches_for_forecast()` maps canonical `match_id`, home/away team IDs through the accepted team registry into `home_team_canonical`/`away_team_canonical`, uses fixture kickoff for `date`, preserves explicit `kickoff_utc`/`feature_cutoff_utc`, carries `home_score`/`away_score`, and maps `venue`; unresolved/ambiguous IDs or kickoff fail eligibility before feature building.
- Release feature contract: `phase14_build_release_features()` reads and hash-binds the resolved immutable release row's `active_predictors` and `dropped_predictors_with_reason`. Required active predictors must have strictly pre-kickoff evidence. Registered missingness may satisfy the existing wide-table shape for inactive dropped predictors, but the public national-team xG form/status remains unavailable with `NA` values and cannot be labelled xG evidence.
- Existing forecast/state tests gain a production Austria/Germany fixture proving adapter columns, strict-before Elo, current Elo-only G=40 availability with xG unavailable lineage, synthetic xG-active suppression, club-only rolling-form rejection, EURO pre_draw truth, and required-active-input failure fan-out. No durable edition output is promoted.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 14-16-01: Carry one scheduled match and one pre_draw edition end to end</name>
  <read_first>
    - R/competition/team_identity.R
    - R/competition/match_state.R
    - R/competition/standings.R
    - R/competition/form.R
    - R/integration/rolling_form.R
    - R/forecast/features.R
    - data/competition/registries/team_identity.csv
    - data/competition/registries/national_team_xg_sources.csv
    - data/processed/rolling_form.csv
    - data/benchmark/phase10/feature_contract.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/model_manifests.csv
    - R/benchmark/baselines.R
    - R/benchmark/contracts.R
    - R/competition/publication_transaction.R
    - tests/testthat/test_phase14_forecast_layer.R
    - tests/testthat/test_phase14_state_bundle.R
  </read_first>
  <files>R/competition/forecast_layer.R, R/competition/state_bundle.R, tests/testthat/test_phase14_forecast_layer.R, tests/testthat/test_phase14_state_bundle.R</files>
  <behavior>
    - "Austria/Germany adapter rows have exact date, canonical teams, NA scheduled scores, match_id, venue, kickoff, and cutoff fields; Elo evidence resolves strictly before kickoff."
    - "Current Elo-only incumbent yields exact G=40 output while xG is unavailable/NA and never labelled source evidence; a synthetic xG-active release suppresses with feature_evidence_unavailable."
    - "EURO pre_draw yields explicit suppression and zero fabricated structures."
    - "Required active shared-input failures invalidate both candidates; optional inactive national-team xG unavailability is audited and does not invalidate either edition."
  </behavior>
  <action>Start from the production Austria/Germany failing tracer assertions. Implement `phase14_adapt_matches_for_forecast(canonical_matches, team_registry, feature_cutoff_utc)` to produce the exact `build_forecast_feature_table()` columns `date`, `home_team_canonical`, `away_team_canonical`, `home_score`, `away_score`, `match_id`, and `venue`, mapping stable team IDs only through the accepted registry and deriving `date` from confirmed fixture kickoff while retaining UTC kickoff/cutoff columns for leakage checks. Implement `phase14_build_release_features()` to read the hash-verified immutable release manifest's `active_predictors` and `dropped_predictors_with_reason`, require strictly pre-kickoff Elo for the current active `elo_diff`, and require accepted national-team xG for xGF/xGA/xGD only when those predictors are active. Call `build_forecast_feature_table()` at the integration boundary; its registered missingness/zero companions may preserve internal wide-table compatibility only for inactive dropped predictors, while public xG form/status values stay unavailable/NA. Reject the club-only rolling form as `national_team_xg`. Then implement `phase14_build_fixture_forecasts()` and `phase14_build_competition_state_candidate()`, resolve authority only through `phase14_resolve_approved_release(selector_path, trusted_release_root)`, run `predict_registered_baseline(..., support_max=40L)`, and retain all D-20 lineage. Fan out failures only for required active shared inputs; audit inactive optional xG without invalidating either candidate.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_forecast_layer.R", stop_on_failure=TRUE); testthat::test_file("tests/testthat/test_phase14_state_bundle.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>Austria/Germany maps exactly, uses Elo strictly before kickoff, reaches G=40 under the Elo-only release with xG honestly unavailable, suppresses under an xG-active release, rejects club form as national-team xG, and preserves EURO pre_draw plus active-required failure fan-out.</acceptance_criteria>
  <done>The permanent release-active-predictor vertical tracer is green and committed before batch expansion.</done>
</task>
</tasks>
<threat_model asvs_level="1" block_on="high">Unresolved high threats block execution.
| Threat ID | STRIDE | Severity | Disposition | Control | Test |
|---|---|---|---|---|---|
| T-14-16-S | Spoofing | high | mitigate | selector/release/calibrator preflight and honest views | tracer tests |
| T-14-16-T | Tampering | high | mitigate | accepted hashes, cutoff lineage, grid validation | tracer tests |
| T-14-16-I | Information Disclosure | high | mitigate | mandatory edition_id and cross-edition rejection | state-bundle test |
</threat_model>
<verification>Both end-to-end tracer tests pass in one fresh R process.</verification>
<success_criteria>The complete architecture is proven before horizontal production expansion.</success_criteria>
<output>Create `14-16-SUMMARY.md` when done.</output>
