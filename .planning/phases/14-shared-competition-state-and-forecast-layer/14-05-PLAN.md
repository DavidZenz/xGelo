---
phase: 14-shared-competition-state-and-forecast-layer
plan: "05"
type: execute
wave: 2
depends_on: ["14-04"]
files_modified: []
autonomous: false
requirements: [FORECAST-01]
must_haves:
  truths:
    - "Downstream release work starts only when the immutable empirical gate is fitted, chronology-safe, integrity-valid, performance-valid, and promoted."
    - "Any failed assertion ends execution at CALIBRATION_RELEASE_BLOCKED with selector, registry pins, and forecast suppression unchanged."
  artifacts:
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
      provides: "Empirical checkpoint authority"
  key_links:
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
      to: 14-06-PLAN.md
      via: "blocking pass precondition"
      pattern: "calibration_promoted"
  prohibitions:
    - statement: "The checkpoint must not override a failed gate or alter labels, evidence, thresholds, reasons, selector, or registry rows."
      status: unverified
      verification: judgment
---

<objective>
Enforce the empirical calibration result as a non-overridable pass-or-block checkpoint.

Purpose: Keep raw fallback honestly suppressed when calibration does not pass the frozen vetoes.
Output: A pass signal for Plan 14-06 or terminal `CALIBRATION_RELEASE_BLOCKED`.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-04-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
@outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
@outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv
</context>

## Artifacts this phase produces

No new symbols, files, fields, reason enums, script flags, schemas, or durable artifacts. This plan only revalidates the immutable Plan 14-04 evidence in a fresh R process and emits the orchestration resume signal `calibration-gate-passed` or terminal state `CALIBRATION_RELEASE_BLOCKED`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 14-05-01: Resume only on the immutable passing disposition</name>
  <read_first>
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_gate.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision/calibration_revision_manifest.csv
    - R/release/calibration_revision.R
    - tests/testthat/test_phase14_calibration_release.R
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-CONTEXT.md
  </read_first>
  <files>none</files>
  <action>Start a fresh R process, source `R/release/calibration_revision.R`, and run the complete `phase14_validate_calibration_revision()` over the canonical revision root so every manifest row, exact artifact byte hash, model/calibrator/recipe/protocol identity, chronology field, and gate identity is recomputed before reading the disposition. Resume only when that complete validation passes and the single gate row records fitted calibration, calibrated primary view, chronology/integrity/performance pass, and zero veto reasons. If any identity/hash/assertion fails, report `CALIBRATION_RELEASE_BLOCKED` and stop all later plans. Do not edit evidence or authority files.</action>
  <verify><automated>Rscript --vanilla -e 'source("R/release/calibration_revision.R"); root &lt;- "outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-revision"; stopifnot(isTRUE(phase14_validate_calibration_revision(root))); g &lt;- read.csv(file.path(root,"calibration_gate.csv"), stringsAsFactors=FALSE); stopifnot(nrow(g)==1L, isTRUE(g$calibration_promoted[[1L]]), identical(g$fit_status[[1L]],"fitted"), identical(g$primary_probability_view[[1L]],"calibrated_1x2"), isTRUE(g$chronology_safe[[1L]]), isTRUE(g$integrity_veto_passed[[1L]]), isTRUE(g$performance_veto_passed[[1L]]), !nzchar(g$reason_codes[[1L]]))'</automated></verify>
  <acceptance_criteria>Exactly one outcome occurs: `calibration-gate-passed`, or terminal `CALIBRATION_RELEASE_BLOCKED` with all release authority unchanged.</acceptance_criteria>
  <done>The checkpoint either authorizes Plan 14-06 from machine-valid evidence or stops Phase 14 without override.</done>
  <what-built>A chronology-safe calibration candidate and immutable empirical gate from Plan 14-04.</what-built>
  <how-to-verify>Run the fresh-process command; accept only after `phase14_validate_calibration_revision()` re-hashes the four canonical artifacts, validates every manifest identity, and the exact gate assertions pass.</how-to-verify>
  <resume-signal>Enter `calibration-gate-passed` only after the automated assertion succeeds.</resume-signal>
</task>

</tasks>

<threat_model asvs_level="1" block_on="high">
Unresolved high-severity threats block execution.

| Threat ID | STRIDE | Severity | Disposition | Control | Automated Test |
|---|---|---|---|---|---|
| T-14-05-T | Tampering | high | mitigate | Exact one-row gate and immutable manifest hashes. | checkpoint assertion |
| T-14-05-E | Elevation of Privilege | high | mitigate | Only passing machine result permits resume; failure has no override. | checkpoint assertion |
| T-14-05-R | Repudiation | medium | mitigate | Named terminal state and retained evidence. | manifest validation |
</threat_model>

<verification>Run the task assertion. No later plan may run after a failed assertion.</verification>
<success_criteria>Pass is evidence-driven; block preserves suppression and all authority files.</success_criteria>
<output>Create `14-05-SUMMARY.md` only on pass; on failure record `CALIBRATION_RELEASE_BLOCKED` and stop.</output>
