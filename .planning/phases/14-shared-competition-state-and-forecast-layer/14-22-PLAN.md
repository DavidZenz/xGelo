---
phase: 14-shared-competition-state-and-forecast-layer
plan: "22"
type: execute
wave: 4
depends_on: ["14-21"]
files_modified:
  - tests/testthat/helper_phase14_calibration_remediation_acceptance.R
  - tests/testthat/test_phase14_calibration_remediation_acceptance.R
autonomous: false
requirements: [FORECAST-01]
must_haves:
  truths:
    - "An acceptance path that never sources or calls the Phase 14 remediation producer or producer validator independently rebuilds the frozen contract/raw panel, reruns every nested selection, refits every selected strictly-prior outer model, and reproduces all 630 probabilities before scoring or acceptance."
    - "The reference reconstruction compares exact outer training IDs, inner validation/training maps, support counts, selected family/hyperparameters, optimizer seed/convergence, fallback reasons, and fitted parameters to persisted evidence at tight deterministic tolerances."
    - "Only an actual zero-reason authoritative pass with fitted calibrator and calibrated primary view can emit `calibration-v2-gate-passed`."
    - "Any missing, forged, stale, chronology-unsafe, holdout-derived, authority-mutating, raw-fallback, or veto-failing graph remains blocking with no human override."
  artifacts:
    - path: tests/testthat/helper_phase14_calibration_remediation_acceptance.R
      provides: "Independent contract, panel, nested selection, outer fit/apply, and acceptance implementation using frozen raw sources and Phase 12 scoring primitives only"
    - path: tests/testthat/test_phase14_calibration_remediation_acceptance.R
      provides: "Independent reconstruction and adversarial forgery/leakage suite that does not trust producer validation or self-consistent producer hashes"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
      provides: "Candidate graph revalidated before checkpoint presentation"
    - path: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
      provides: "Untrusted per-outer fit evidence compared against independently reconstructed lineage, optimizer state, parameters, and probabilities"
  key_links:
    - from: tests/testthat/test_phase14_calibration_remediation_acceptance.R
      to: tests/testthat/helper_phase14_calibration_remediation_acceptance.R
      via: "reference reconstruction plus direct and self-consistently rehashed adversarial candidate copies"
      pattern: "reference_validate_phase14_calibration_candidate"
    - from: tests/testthat/helper_phase14_calibration_remediation_acceptance.R
      to: R/calibration/calibration_selection.R
      via: "Phase 12 comparison/decision primitives only after independent probabilities match"
      pattern: "phase12_selection_decision"
    - from: outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv
      to: .planning/phases/14-shared-competition-state-and-forecast-layer/14-22-SUMMARY.md
      via: "resume signal exists only after all automated acceptance assertions pass"
      pattern: "calibration-v2-gate-passed"
---

<objective>
Independently accept or block the remediation graph before any release-dependent Phase 14 plan can resume.

Purpose: Separate candidate production from release authorization and preserve D-14/D-15, unchanged vetoes, and fail-closed authority.
Output: An independent acceptance test plus a blocking-human checkpoint exposed only after a machine-proven pass.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-CALIBRATION-REVISION.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-04-SUMMARY.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-21-SUMMARY.md
@R/calibration/calibration_selection.R
@data/benchmark/phase12/calibration_recipe.json
@data/benchmark/phase09/fixtures.csv
@outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv
@data/benchmark/phase09/promotion_protocol.json
@data/benchmark/phase12/freeze_manifest.csv
@outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
</context>

## Artifacts this phase produces

- `tests/testthat/helper_phase14_calibration_remediation_acceptance.R`, a test-only reference implementation that independently defines the predeclared family/grid/transform/seed contract, rebuilds the exact raw 630-row development panel, reruns every nested selection, refits/applies all 12 outer models, and uses only frozen Phase 12 comparison/decision primitives after semantic replay succeeds. It never sources or calls `R/release/calibration_remediation.R` or any producer API/validator; producer code is read only as opaque bytes when its manifest hash is checked.
- `tests/testthat/test_phase14_calibration_remediation_acceptance.R`, an independent verifier and adversarial suite that compares persisted selections/fits/probabilities with that reference reconstruction rather than trusting stored hashes or decision booleans.
- `.planning/phases/14-shared-competition-state-and-forecast-layer/14-22-SUMMARY.md` only after both the automated acceptance and blocking-human pass acknowledgement complete.

## Explicit prohibitions

- Do not call `phase14_validate_calibration_remediation()` as acceptance authority and do not trust persisted pass flags without recomputation.
- Do not source `R/release/calibration_remediation.R`, call `phase14_select_nested_calibrator()`, call `phase14_build_calibration_remediation()`, or reuse any Phase 14 producer fit/apply helper in the acceptance helper or test; producer code may be opened only as opaque bytes for the declared code hash.
- Do not emit a release resume signal for raw fallback, any veto reason, invalid hash/identity/chronology, or absent fitted calibrator.
- Do not permit a human acknowledgement to override a failed automated assertion.
- Do not mutate candidate evidence, Plan 14-04 evidence, Phase 12 selectors/thresholds/release authority, competition registry pins, WC2026 holdout status, or public suppression.

<tasks>

<task type="auto">
  <name>Task 14-22-01: Independently reconstruct the remediation graph and unchanged gate</name>
  <read_first>
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-CALIBRATION-REVISION.md
    - R/calibration/calibration_selection.R
    - data/benchmark/phase12/calibration_recipe.json
    - data/benchmark/phase09/promotion_protocol.json
    - data/benchmark/phase12/freeze_manifest.csv
    - data/benchmark/phase09/fixtures.csv
    - outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/remediation_contract.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_selection.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
  </read_first>
  <files>tests/testthat/helper_phase14_calibration_remediation_acceptance.R, tests/testthat/test_phase14_calibration_remediation_acceptance.R</files>
  <behavior>
    - "The helper independently instantiates raw identity; warm-up rows 60/128/256/400 crossed with scalar shrinkage 0.25/0.50/0.75/1.00; warm-up crossed with vector penalties 0.001/0.01/0.05/0.10/0.50/1.00/5.00; the declared transforms/bounds/tie-breaks; and the fixed canonical optimizer-seed schedule, then compares that reference contract to remediation_contract.csv."
    - "From frozen Phase 09 fixture_predictions.csv and fixtures.csv, independently filter exactly open_nb_incumbent/updating/open_core, reconstruct canonical tournament chronology and the 630-row raw panel, and reject every identity outside the 12 development tournaments before reading candidate selections or fitted probabilities."
    - "For each outer tournament, independently enumerate all strictly earlier outer training tournaments; rebuild every inner validation fold and its own strictly-prior training set; fit/score the full predeclared grid without outer labels; apply frozen eligibility/tie-break rules; and compare selected family, hyperparameters, support, lineage, and fallback reason exactly."
    - "Independently refit each selected outer model from its reconstructed strictly-prior raw rows using the reference transform/optimizer and seed, compare optimizer/convergence metadata exactly and scalar/vector parameters at absolute tolerance 1e-10, apply the reference fit, and compare each p_home/p_draw/p_away for all 630 fixtures at absolute tolerance 1e-12."
    - "Only after all semantic comparisons pass, recompute raw/calibrated score evidence and phase12_selection_decision, compare every gate field/ordered reason/probability view/disposition, and independently recompute the complete contract/input/code/selection/outer-fit/calibrator/predictions/gate/original-evidence/manifest-self hash graph from canonical bytes."
    - "Acceptance requires 630 unique fixtures, unchanged score/auxiliary identities, no holdout labels, no authority mutation, zero reasons, fitted final calibrator, and calibrated_1x2 primary view."
    - "Static acceptance tests fail if the helper/test sources or calls the Phase 14 remediation producer or producer validator."
    - "Adversarial copies with outer-label leakage, forged fitted parameters, forged training IDs, or self-consistently rehashed leaked selections/fits/predictions fail semantic reconstruction even when all producer-side hashes and stored gate fields agree."
  </behavior>
  <action>Create `tests/testthat/helper_phase14_calibration_remediation_acceptance.R` as a genuinely separate reference implementation with public test helper `reference_validate_phase14_calibration_candidate(root, require_promoted = TRUE)`. Do not source or call the Phase 14 remediation producer/module/validator and do not copy a persisted selection, fitted parameter, probability, pass flag, or producer-derived fold list into the expected result. Independently encode the predeclared grid/transforms/bounds/ranking and deterministic seed derivation from `14-CALIBRATION-REVISION.md`; construct the exact raw development panel directly from the frozen Phase 09 predictions/fixtures; source only the frozen Phase 12 comparison/decision primitive and its low-level scoring dependencies. For all 12 outer tournaments, rebuild the full nested candidate search from strictly earlier tournament rows, compare expected selections and exact lineage/support/fallback fields to `outer_fold_selection.csv`, refit the selected recipe, compare expected seed/convergence and fitted identity/scalar/vector fields to `outer_fold_fits.csv` at `1e-10`, apply the independent fit, and compare all three probabilities for every one of the 630 fixtures to `calibrated_predictions.csv` at `1e-12`. Treat raw fallback as an independently derived identity fit, not a trusted producer marker. Only after these comparisons succeed, recompute the unchanged 630-row Phase 12 score evidence/decision and every persisted gate field, then reconstruct the complete byte/canonical manifest graph including `outer_fold_fits.csv`, original blocked evidence, selector, registry, and producer-code opaque hash. In the test file, first assert by call/source inspection that neither acceptance artifact uses producer APIs; then build isolated adversarial candidate copies for: an outer label entering an outer or inner fit, altered fitted scalar/vector parameters, altered outer/inner training IDs, and a leaked output graph whose selections/fits/predictions/gate/manifests have all been consistently rehashed by test-local hash writers. Require each copy to fail for the semantic mismatch before authoritative scoring. End with strict promoted-pass assertions so the command exits nonzero for every blocked disposition, including internally valid candidate evidence that did not earn promotion.</action>
  <verify><automated>Rscript --vanilla -e 'source("tests/testthat/helper_phase14_calibration_remediation_acceptance.R"); root &lt;- "outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2"; stopifnot(isTRUE(reference_validate_phase14_calibration_candidate(root, require_promoted=TRUE))); testthat::test_file("tests/testthat/test_phase14_calibration_remediation_acceptance.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>The independent suite passes only after separately reproducing every nested choice, every selected strictly-prior outer fit, its lineage/support/seed/convergence/parameters, and all 630 three-way probabilities within the declared tolerances; the unchanged Phase 12 decision must then have zero veto reasons, and every direct or self-consistently rehashed leakage/forgery case must fail.</acceptance_criteria>
  <done>The candidate has independent machine acceptance, or execution stops here with release-dependent plans still blocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 14-22-02: Acknowledge only the independently proven passing result</name>
  <read_first>
    - tests/testthat/helper_phase14_calibration_remediation_acceptance.R
    - tests/testthat/test_phase14_calibration_remediation_acceptance.R
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/outer_fold_fits.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_gate.csv
    - outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2/calibration_revision_manifest.csv
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-21-SUMMARY.md
  </read_first>
  <files>None; this checkpoint is read-only.</files>
  <action>Present this checkpoint only after Task 14-22-01 succeeds in the same execution. In a fresh process, source only the independent helper/test artifacts, rerun `reference_validate_phase14_calibration_candidate(root, require_promoted=TRUE)` plus the complete adversarial suite, display the one-row pass disposition and manifest hash, and require human acknowledgement of that already-proven result. If semantic reconstruction, any one of 630 probability comparisons, any adversarial rejection, the unchanged gate, or any hash check fails, do not present an override choice, do not create a summary, and leave Plan 14-06 unreachable.</action>
  <verify><automated>Rscript --vanilla -e 'source("tests/testthat/helper_phase14_calibration_remediation_acceptance.R"); root &lt;- "outputs/benchmarks/rolling_tournaments/phase14-incumbent-calibration-remediation-v2"; stopifnot(isTRUE(reference_validate_phase14_calibration_candidate(root, require_promoted=TRUE))); testthat::test_file("tests/testthat/test_phase14_calibration_remediation_acceptance.R", stop_on_failure=TRUE)'</automated></verify>
  <acceptance_criteria>The checkpoint is reachable only after a fresh independent semantic reconstruction and adversarial pass; the displayed gate has zero reasons, fitted status, calibrated primary view, false holdout/authority flags, exact complete hashes, and all 630 probabilities reproduced independently.</acceptance_criteria>
  <done>Human acknowledgement records the machine-proven pass and emits the sole release resume signal; any failure remains blocking.</done>
  <what-built>An independently verified remediation candidate that passed every unchanged authoritative veto.</what-built>
  <how-to-verify>Run the automated command, inspect the persisted one-row gate and manifest self-hash, and confirm that the result is an actual pass rather than a raw fallback or waived veto.</how-to-verify>
  <resume-signal>Enter `calibration-v2-gate-passed` only after the automated command succeeds; otherwise provide no resume signal.</resume-signal>
</task>

</tasks>

<threat_model asvs_level="1">
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer artifacts to independent verifier | All persisted fields and hashes are untrusted until independently reconstructed. |
| machine acceptance to human checkpoint | Human acknowledgement may confirm a pass but cannot create or override one. |
| accepted candidate to downstream release plans | Only the exact pass signal opens Plan 14-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-22-S | Spoofing | stored pass identity | high | mitigate | Rebuild candidate, panel, fold, selector, registry, and probability-view identities independently. |
| T-14-22-T | Tampering | nested selections, training lineage, fitted parameters, probabilities, and complete hash graph | high | mitigate | Independently rebuild all nested selections and outer fits from frozen raw sources, compare all 630 probabilities before scoring, then recompute every source/output hash and manifest self-hash; self-consistently rehashed leaked graphs remain rejected. |
| T-14-22-E | Elevation of Privilege | resume signal | high | mitigate | Gate checkpoint presentation and signal emission behind a zero-exit fresh independent command. |
| T-14-22-I | Information Disclosure | sealed holdout | high | mitigate | Reject holdout identities/flags and verify the 12-edition development allowlist. |
| T-14-22-R | Repudiation | pass/fail disposition | medium | mitigate | Recompute and compare all ordered reason codes and decision fields; retain summary only on pass. |
</threat_model>

<verification>
The independent helper/test pair is the acceptance authority. Without sourcing or calling Phase 14 producer code, it must reconstruct the frozen contract/raw panel, all nested selections, all selected outer fits, fitted parameters, 630 probability triples, hashes, score evidence, and the unchanged veto decision in a fresh process before the human checkpoint is reachable.
</verification>

<success_criteria>
- The complete remediation graph is independently reconstructed from frozen raw sources, including every nested selection, strictly-prior outer fit, and all 630 probabilities.
- Outer-label leakage, forged fitted parameters, forged training IDs, and self-consistently rehashed leaked outputs are rejected.
- A pass signal exists only for zero veto reasons with a fitted calibrated candidate.
- Any failure leaves Plan 14-06 and all release-dependent work blocked.
</success_criteria>

<output>Create `.planning/phases/14-shared-competition-state-and-forecast-layer/14-22-SUMMARY.md` only after `calibration-v2-gate-passed` is acknowledged.</output>
