---
phase: 17-shared-dashboards-and-atomic-refresh-operations
plan: 03
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - R/dashboard/publication.R
  - scripts/refresh_competition_dashboards.R
  - tests/testthat/test_phase17_dashboards.R
  - docs/competitions/nations-league/index.html
  - docs/competitions/nations-league/payload.json
  - docs/competitions/nations-league/route-manifest.json
  - docs/competitions/nations-league/current.json
  - docs/competitions/euro-qualifying/index.html
  - docs/competitions/euro-qualifying/payload.json
  - docs/competitions/euro-qualifying/route-manifest.json
  - docs/competitions/euro-qualifying/current.json
  - docs/competitions/phase17-batch-manifest.json
  - docs/competitions/current.json
autonomous: true
requirements: [OPS-02, OPS-03, OPS-05]
must_haves:
  truths:
    - "One candidate contains both editions, all exact route/payload/manifest/current paths, and one cross-edition batch manifest before promotion."
    - "Exact inventory, JSON, hashes, byte limits, statuses, lineage, path containment, and symlink checks fail closed."
    - "The ordered source, rules, probability, freshness, replay, browser, regression, envelope, promotion, and read-back gates are recorded and asserted by exact invocation order."
    - "Any collision, interruption, injected failure, or read-back mismatch retains incumbent bytes and does not alter refresh history."
  artifacts:
    - path: "R/dashboard/publication.R"
      provides: "Exact inventory, size limits, lock, atomic promotion, read-back, rollback"
    - path: "scripts/refresh_competition_dashboards.R"
      provides: "Bounded ordered refresh CLI"
    - path: "tests/testthat/test_phase17_dashboards.R"
      provides: "Transaction and exact gate-order tests"
  key_links:
    - "Accepted bundles -> exact candidate inventory -> one root swap -> read-back"
    - "Gate trace -> nonzero failure -> incumbent retention"
  assumptions:
    - "Resolved research fixes the ten routes, compact payloads, 5/20 MiB limits, and blocked-edition incumbent preservation; both editions are required; identical accepted inputs are idempotent; lock collision stops before mutation."
  prohibitions:
    - "Reject refresh_batches, raw inputs, score distributions, logs, unrelated paths, old World Cup scheduler artifacts, and every artifact outside the exact inventory or size limits."
---

<objective>Build the fail-closed cross-edition batch transaction and ordered refresh coordinator that stages, validates, promotes, reads back, and rolls back the complete dashboard envelope.</objective>
## Artifacts this phase produces
- Exact shared public inventory comes only from `phase17_expected_public_inventory()` in `R/dashboard/payload_contract.R`: `docs/competitions/nations-league/index.html`, `docs/competitions/nations-league/payload.json`, `docs/competitions/nations-league/route-manifest.json`, `docs/competitions/nations-league/current.json`, `docs/competitions/euro-qualifying/index.html`, `docs/competitions/euro-qualifying/payload.json`, `docs/competitions/euro-qualifying/route-manifest.json`, `docs/competitions/euro-qualifying/current.json`, `docs/competitions/phase17-batch-manifest.json`, `docs/competitions/current.json`. Plan 03 owns all ten generated public artifacts; Plan 01 owns the provider and shared renderer that generate them.
- Named limits: `phase17_max_public_file_bytes = 5L * 1024L * 1024L`; `phase17_max_batch_bytes = 20L * 1024L * 1024L`.
- Gate sequence: `source`, `rules`, `probability`, `freshness`, `replay`, `browser`, `regression`, `envelope`, `promotion`, `read_back`.
<execution_context>
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-01-PLAN.md
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-02-PLAN.md
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-VALIDATION.md
@R/competition/publication_transaction.R
@R/competition/publication_hashes.R
@scripts/build_nations_league_outcomes.R
@scripts/build_euro_qualifying_outcomes.R
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Implement exact batch inventory, limits, lock, promotion, and rollback</name>
  <files>R/dashboard/publication.R, scripts/refresh_competition_dashboards.R, tests/testthat/test_phase17_dashboards.R, docs/competitions/phase17-batch-manifest.json, docs/competitions/current.json</files>
  <read_first>Read `R/competition/publication_transaction.R`, `R/competition/publication_hashes.R`, Phase 13 transaction/failure tests, and the Wave 0 inventory/limit helpers.</read_first>
  <action>Implement `phase17_validate_batch_envelope()`, `phase17_with_batch_lock()`, `phase17_promote_batch()`, `phase17_rollback_batch()`, and `phase17_batch_identity()`. Call `phase17_expected_public_inventory()` and the named 5/20 MiB constants rather than copying vectors. Require exactly ten paths; reject missing/extra/duplicate paths, refresh_batches, raw inputs, score distributions, logs, symlinks, traversal, invalid JSON, mixed batch IDs, status/lineage errors, SHA-256 mismatches, and over-limit files/batches. After Task 3 stages both route directories, write `docs/competitions/phase17-batch-manifest.json` and `docs/competitions/current.json` with one shared batch identity, ordered route hashes, generated timestamp, source/model/replay lineage, statuses, and exact inventory. Stage on one filesystem, retain incumbent, swap one root, read back every path, restore exact incumbent bytes on every injector, and make identical current content a validated no-op.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="atomic batch|inventory|hash|size|rollback|idempotency", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Tests prove exact ten-path inventory, equal-limit acceptance, over-limit rejection for each named limit, symlink/traversal rejection, common identity, lock collision, cross-edition manifest/current-pointer generation, failure at each promotion/read-back boundary, byte-identical restoration, and unchanged refresh history.</acceptance_criteria>
  <done>The complete cross-edition envelope is atomically promotable and rollback-safe.</done>
</task>
<task type="auto">
  <name>Task 2: Wire the ordered refresh CLI to exact prior-phase callbacks and fail-closed gates</name>
  <files>scripts/refresh_competition_dashboards.R, R/dashboard/publication.R, tests/testthat/test_phase17_dashboards.R</files>
  <read_first>Read the exact production contracts and return values before wiring the coordinator: `R/competition/source_contracts.R::phase13_validate_source_bundle(bundle, artifacts)` returns a validated one-row bundle data frame; `R/competition/edition_registry.R::phase13_validate_accepted_snapshot(accepted_dir, edition_row, source_bundles, source_artifacts, project_root = ".", identity_registry = NULL, raw_root = NULL)` returns `validated_snapshot` with `edition_id`, `bundle_id`, `source_bundle`, `source_artifacts`, `source_bundle_manifest`, `manifest`, `fixtures`, `groups`, `standings`, `results`, and `status`; `phase13_validate_competition_edition_registries(registries, source_bundles = NULL, approved_model_release_ids = phase13_approved_model_release_ids(), trusted_release_root = NULL, selector_path = NULL, resolved_release = NULL, require_complete = NULL, project_root = ".")` returns the validated registry data frame or invisible empty input; `R/competition/state_bundle.R::phase14_state_bundle_shared_preflight(ids, edition_registry, canonical_matches, team_registry, resolved_release, selector_path, trusted_release_root, elo_ratings, national_team_xg_registry, national_team_xg_history, model_manifest, model_manifest_path, historical_matches)` returns `resolved_release`, `manifest`, `audit`, and failure fields; `phase14_build_competition_state_candidate(edition_id, edition_registry = NULL, canonical_matches = NULL, team_registry, resolved_release, selector_path, trusted_release_root, elo_ratings, national_team_xg_registry, national_team_xg_history, model_manifest, model_manifest_path, source_bundle_manifest, source_status, euro_activation, results, groups, standings, competition_form, all_senior_form, historical_matches, generated_at_utc)` returns one edition candidate or a batch candidate; and `R/competition/forecast_layer.R::phase14_build_fixture_forecasts(canonical_matches, team_registry, resolved_release, selector_path, trusted_release_root, elo_ratings, national_team_xg_registry, national_team_xg_history, model_manifest, model_manifest_path, edition_registry, edition_lifecycle_state, rolling_form)` returns `forecasts`, `score_distributions`, and `fixture_status`. Read `R/release/release_contract.R::phase14_resolve_approved_release(selector_path, trusted_release_root)` as the calibrated Phase 12 selector authority; `R/competition/uefa_nations_league_outcomes.R::phase15_build_nl_outcomes_candidate(simulation, rules, topology, source, stage_capture, state_bundle, project_root, generated_at_utc)` returns the candidate with `artifacts`, `state_manifest`, and `simulation_metadata`; `phase15_validate_nl_outcomes_bundle(bundle)` returns invisible TRUE or throws; and `phase15_nl_compare_replays(first, second, label = "replay")` returns the replay comparison. Read `R/competition/uefa_euro_rules.R::phase16_validate_euro_source_bundle(candidate = NULL, config = NULL, incumbent = NULL)`; `R/competition/uefa_euro_outcomes.R::validate_euro_activation(candidate = NULL, groups = NULL, fixtures = NULL, team_registry = NULL, source_bundle = NULL, standings = NULL, results = NULL, raw_snapshot = NULL, registered_manifest = NULL, config = NULL, incumbent = NULL)`; `phase16_build_euro_outcomes_candidate(simulation, rules, topology, source, stage_capture, state_bundle, project_root, generated_at_utc, activation, source_lineage, model_lineage, incumbent)`; `phase16_validate_euro_outcomes_bundle(bundle)` returns `valid`, `failure_reason`, and `status`; and `phase16_compare_euro_outcomes_replays(first, second)` returns `identical`, `differences`, and `valid`. Also read the exact child-process entry point `scripts/build_euro_qualifying_outcomes.R --replay-check`, `docs/wc2026/index.html`, `R/visualization/worldcup_dashboard.R::publish_worldcup_dashboard_pages()`, and the production equivalents in both build scripts and outcome files.</read_first>
  <action>Implement `phase17_refresh_main()` with bounded `--dry-run`, `--fixture-root`, `--public-root`, `--gate-failure`, and test/dry-run-only `--skip-git`. Add `phase17_validate_competition_freshness(bundle, cutoff, thresholds)` and `phase17_validate_probability_inputs(bundle, approved_release)`, each returning `{valid, failure_reason, gate_trace}`. Implement `phase17_detect_browser_capability()`, `phase17_run_browser_gate()`, and `phase17_run_regression_gate()` in this earlier Plan 03 coordinator boundary so Plan 03 can invoke them before Plan 04 exists: use exactly `/System/Cryptexes/App/usr/bin/safaridriver --version`, require Safari `26.5.2`, run automated DOM/ARIA smoke for both generated routes at named desktop/mobile viewports, and execute the exact regression contract below with `PHASE17_IN_REGRESSION_GATE=1`. Bind and record every callback by name, exact arguments, and return contract: pass `bundle` and `artifacts` to `phase13_validate_source_bundle`; pass `accepted_dir`, the one-row `edition_row`, `source_bundles`, `source_artifacts`, `project_root`, `identity_registry`, and `raw_root` to `phase13_validate_accepted_snapshot`; pass all eight named registry arguments to `phase13_validate_competition_edition_registries`; pass all thirteen named shared-preflight arguments to `phase14_state_bundle_shared_preflight`; pass every named state-candidate input through `generated_at_utc` to `phase14_build_competition_state_candidate`; pass all thirteen named fixture-forecast inputs to `phase14_build_fixture_forecasts`; resolve the calibrated release only through `phase14_resolve_approved_release(selector_path, trusted_release_root)`; pass `simulation`, `rules`, `topology`, `source`, `stage_capture`, `state_bundle`, `project_root`, and `generated_at_utc` to `phase15_build_nl_outcomes_candidate`; validate its returned `artifacts` with `phase15_validate_nl_outcomes_bundle(bundle)`; call `phase15_nl_compare_replays(first, second, label = "replay")`; pass `candidate`, `config`, and `incumbent` to `phase16_validate_euro_source_bundle`; pass the exact eleven named inputs to `validate_euro_activation`; pass all twelve named inputs to `phase16_build_euro_outcomes_candidate`; validate its result with `phase16_validate_euro_outcomes_bundle(bundle)`; and call `phase16_compare_euro_outcomes_replays(first, second)`. Record exact labels in order: `phase17_git_preflight` before source/staging in production; Phase 13 source/snapshot/registry validators; Phase 14 shared preflight/state candidate/fixture forecasts and approved Phase 12 calibrated 1X2 selector; Phase 15 builder/validator; Phase 16 source/activation/builder/validator; the two Phase 17 wrappers; Phase 15 replay; Phase 16 replay; child `scripts/build_euro_qualifying_outcomes.R --replay-check`; `phase17_run_browser_gate`; `phase17_run_regression_gate`; envelope validation; promotion; read-back; and final `phase17_git_preflight` immediately before commit. Delegate route materialization and cross-edition pointer writing to the following Plan 03 tasks. `--skip-git` cannot bypass production preflight. Every injector is nonzero and preserves incumbent bytes, and tests assert the recorded argument bindings and return fields rather than accepting implicit or ellipsis arguments.</action>
  <regression_gate_contract>Run these child-process steps in this exact order, recording each command and description: (1) `scripts/build_euro_qualifying_outcomes.R --replay-check` with label `phase16 euro replay`; (2) `testthat::test_file("tests/testthat/test_phase13_publication_hashes.R", desc = "phase13 publication hashes")`; (3) `testthat::test_file("tests/testthat/test_phase13_publication_manifests.R", desc = "phase13 publication manifests")`; (4) `testthat::test_file("tests/testthat/test_phase13_publication_transaction.R", desc = "phase13 publication transaction")`; (5) `testthat::test_file("tests/testthat/test_phase13_publication_integration.R", desc = "phase13 publication integration")`; (6) `testthat::test_file("tests/testthat/test_phase13_refresh_failure.R", desc = "phase13 refresh failure")`; (7) `testthat::test_file("tests/testthat/test_phase15_nations_league.R", desc = "phase15 nations league")`; (8) `testthat::test_file("tests/testthat/test_uefa_nations_league_production.R", desc = "UEFA nations league production")`; (9) `testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", desc = "phase16 euro qualifying")`; and (10) `testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc = "phase17 regression")`. Set `PHASE17_IN_REGRESSION_GATE=1` for the final test file so it skips recursive coordinator invocation; any child nonzero exit aborts the gate.</regression_gate_contract>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="exact gate order|dry run|fail closed|prior phase contracts", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>The test asserts exact ordered callback/script names and arguments, each gate consumes the named Phase 13-16 contract, deterministic replay runs in a child process, and every injected failure is nonzero with no public mutation.</acceptance_criteria>
  <done>The bounded coordinator composes all prior-phase authority and exact ordered Phase 17 gates before atomic publication.</done>
</task>
<task type="auto">
  <name>Task 3: Render and stage both edition routes through the shared renderer</name>
  <files>scripts/refresh_competition_dashboards.R, tests/testthat/test_phase17_dashboards.R, docs/competitions/nations-league/index.html, docs/competitions/nations-league/payload.json, docs/competitions/nations-league/route-manifest.json, docs/competitions/nations-league/current.json, docs/competitions/euro-qualifying/index.html, docs/competitions/euro-qualifying/payload.json, docs/competitions/euro-qualifying/route-manifest.json, docs/competitions/euro-qualifying/current.json</files>
  <read_first>Read `R/dashboard/payload_nations_league.R`, `R/dashboard/payload_euro.R`, `R/dashboard/renderer.R`, `docs/wc2026/index.html`, and `R/visualization/worldcup_dashboard.R::publish_worldcup_dashboard_pages()`.</read_first>
  <action>Implement the route-materialization helper called by the Plan 03 coordinator. Adapt the validated NL and EURO bundles into the neutral payload, call `render_phase17_dashboard()` once per edition, and stage exactly the eight edition-local files: index.html, payload.json, route-manifest.json, and current.json under each named route. Use the provider-backed inventory, deterministic bytes, route-specific metadata and hashes, truthful EURO pre_draw/blocked states, and no raw source, score distributions, or operational logs.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="route rendering|shared renderer|route manifests|current pointers", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Both route directories are emitted by the shared renderer with exact inventory, stable hashes, correct edition lineage, and truthful empty/blocked semantics; route generation is covered by hostile-text, replay, and provider-inventory assertions.</acceptance_criteria>
  <done>The eight edition-local public artifacts are owned, deterministic, and ready for the common batch envelope.</done>
</task>
</tasks>
<threat_model>
## ASVS-1 Threat Model
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Source/rules/probability -> candidate | Payload trust and injection | high | mitigate | Existing validators, typed callbacks, compact allowlists, escaped serialization, hostile fixtures. |
| CLI path args -> filesystem | Path/symlink traversal | high | mitigate | Canonical containment and exact inventory. |
| Candidate -> public root | Publication integrity | critical | mitigate | Lock, same-filesystem swap, exact hashes/sizes, read-back, rollback. |
| Coordinator -> Git | Git mutation | high | mitigate | Production `phase17_git_preflight()` before staging and before commit; test/dry-run-only skip flag cannot bypass it. |
| launchd -> CLI | Launchd environment/path | medium | mitigate | Plan 04 absolute paths and explicit environment. |
| Operator -> gate trace | Operator-secret handling | high | mitigate | Redact env/credential values; traces outside public inventory. |
## STRIDE Threat Register
| ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---:|---|---|
| T-17-07 | Tampering | Candidate envelope | high | mitigate | Exact inventory, lineage, hashes, size, symlink checks. |
| T-17-08 | Tampering/availability | Promotion | critical | mitigate | Lock, one root swap, incumbent snapshot, read-back, rollback. |
| T-17-09 | Denial of service | Oversized artifacts | high | mitigate | Named per-file and batch byte limits. |
| T-17-10 | Repudiation | Gate trace | medium | mitigate | Ordered names, arguments, status, hashes, failure reason. |
</threat_model>
<verification>Run the full Phase 17 suite plus Phase 13 publication transaction/hash/integration, Phase 15 Nations League, and Phase 16 EURO regressions; run bounded dry-run and compare public bytes before/after.</verification>
<success_criteria>Every exact gate runs in order before one atomic cross-edition promotion; invalid, partial, stale, oversized, interrupted, or failed candidates never replace the incumbent.</success_criteria>
<output>Create `.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-03-SUMMARY.md` when done.</output>
