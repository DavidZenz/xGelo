---
phase: 17-shared-dashboards-and-atomic-refresh-operations
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified:
  - scripts/com.xgelo.competition-dashboards.plist
  - scripts/com.xgelo.dashboard-update.plist
  - scripts/refresh_competition_dashboards.R
  - scripts/auto_update_competition_dashboards.sh
  - tests/testthat/test_phase17_dashboards.R
autonomous: true
requirements: [OPS-01, OPS-03, OPS-04, OPS-05]
must_haves:
  truths:
    - "Exactly one enabled launchd user agent runs one bounded hourly Phase 17 entry point; the old World Cup job is explicitly unloaded and disabled or retargeted with tested rationale."
    - "Automated-only Safari WebDriver policy uses /System/Cryptexes/App/usr/bin/safaridriver with Safari 26.5.2 when enabled, records runner/version/status, and fail-closes without promotion when unavailable, disabled, or desktop/mobile DOM smoke fails."
    - "The exact shared public inventory is the only Git allowlist; old scheduler artifacts, raw inputs, refresh_batches, score distributions, logs, unrelated files, and oversized artifacts are rejected."
    - "Commit/push occurs only after promotion, full read-back, final preflight, and clean upstream-aligned status."
  artifacts:
    - path: "scripts/com.xgelo.competition-dashboards.plist"
      provides: "Hourly LaunchAgent with absolute Rscript and explicit logs"
    - path: "scripts/com.xgelo.dashboard-update.plist"
      provides: "Explicit disabled/unloaded old World Cup scheduler state"
    - path: "scripts/auto_update_competition_dashboards.sh"
      provides: "Exact allowlist Git publication wrapper"
    - path: "tests/testthat/test_phase17_dashboards.R"
      provides: "Safari, launchd, Git allowlist, preflight, and no-mutation tests"
  key_links:
    - "launchd -> one refresh CLI -> ordered gates -> atomic batch -> read-back -> exact Git allowlist"
    - "Safari capability/status -> browser gate -> promotion decision"
  assumptions:
    - "Resolved blocked-edition and Git decisions are fail-closed: local HEAD must equal fetched upstream before candidate staging and again before commit; push failure records blocked Git publication; no valid local promotion is retained when production Git preflight is blocked."
  prohibitions:
    - "No manual-release-gate, silent dependency installation, cron, watcher, interactive PATH discovery, secret in plist/log/payload, or descriptor-less browser pass."
---

<objective>Make the validated batch runnable hourly on macOS and enforce the automated-only browser, regression, launchd conflict, and exact Git publication gates.</objective>
## Artifacts this phase produces
- Inventory source: consume `phase17_expected_public_inventory()` and `phase17_expected_git_allowlist()` from Plan 01; Plan 03 owns the ten generated public artifacts, including `docs/competitions/phase17-batch-manifest.json` and `docs/competitions/current.json`. This plan owns only the scheduler, wrapper, CLI integration, and operational tests listed in `files_modified`.
- Explicitly excluded: `scripts/auto_update_worldcup_dashboard.sh`, old scheduler state before its disabled edit, `data/competition/raw/**`, `data/competition/refresh_batches/**`, `outputs/**/score_distributions/**`, `logs/**`, every unlisted path, and every over-limit artifact.
- Browser policy: automated-only Safari WebDriver at `/System/Cryptexes/App/usr/bin/safaridriver`, Safari `26.5.2`; no Playwright/Chromium install or manual gate.
<execution_context>
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-03-PLAN.md
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-VALIDATION.md
@scripts/com.xgelo.dashboard-update.plist
@scripts/auto_update_worldcup_dashboard.sh
@tests/testthat/test_phase13_publication_integration.R
@tests/testthat/test_phase13_refresh_failure.R
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Establish and validate the automated-only Safari and launchd capability policy</name>
  <files>scripts/com.xgelo.competition-dashboards.plist, scripts/com.xgelo.dashboard-update.plist, scripts/refresh_competition_dashboards.R, tests/testthat/test_phase17_dashboards.R</files>
  <read_first>Read `scripts/com.xgelo.dashboard-update.plist`, `scripts/auto_update_worldcup_dashboard.sh`, `17-RESEARCH.md` launchd/browser findings, and `17-VALIDATION.md` Wave 0 requirements.</read_first>
  <action>Consume the Plan 03 implementations of `phase17_detect_browser_capability()` and `phase17_run_browser_gate()`; verify that they probe exactly `/System/Cryptexes/App/usr/bin/safaridriver --version`, require Safari `26.5.2`, record automated-only runner/version/capability/status, and fail closed for unavailable, disabled, mismatch, driver, DOM, or viewport failures. Do not install, enable, or manually approve dependencies. For launchd tests, use a reversible captured launchctl shim and temporary plist/user-agent fixture; assert bootout/disable/bootstrap/print/print-disabled arguments without touching the live user domain. Set Disabled=true on the old plist and configure one absolute hourly new job. Keep actual operator installation/unload as manual verification with restoration instructions, not automated-test coverage.</action>
  <verify><automated>plutil -lint scripts/com.xgelo.competition-dashboards.plist && plutil -lint scripts/com.xgelo.dashboard-update.plist && Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="launchd|Safari policy|browser smoke|scheduler conflict", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Tests assert the exact driver path, Safari version, automated-only policy, recorded statuses, no installation command, nonzero fail-closed behavior for unavailable/disabled/DOM/mobile/desktop failures, valid plist fields, old-job disabled/unloaded state, new-job single-label state, and no competing hourly job.</acceptance_criteria>
  <done>Host capability and scheduler ownership are explicit, tested, automated-only, and fail closed.</done>
</task>
<task type="auto">
  <name>Task 2: Add regression gate and exact allowlist Git publication</name>
  <files>scripts/refresh_competition_dashboards.R, scripts/auto_update_competition_dashboards.sh, tests/testthat/test_phase17_dashboards.R</files>
  <read_first>Read the exact inventory in this plan and Plan 03, `scripts/auto_update_worldcup_dashboard.sh`, Phase 13 dirty/diverged/push-failure tests, and the Phase 17 gate trace contract.</read_first>
  <action>Consume the Plan 03 implementation of `phase17_run_regression_gate()` and verify its child-process guard, exact `testthat::test_file()` order, and Phase 17 recursion guard. Implement the shell wrapper with strict errors, bounded `--dry-run`, and `--skip-push`; consume `phase17_expected_git_allowlist()` via an explicit Rscript emission flag, never a repeated path vector. Require `phase17_git_preflight()` before candidate staging and again immediately before commit; dirty/diverged state stops before staging/promotion, while `--skip-git` is test/dry-run-only. Commit/push only after promotion/read-back, clean upstream-aligned status, exact allowlist equality, 5/20 MiB checks, and final preflight. Record heads, status, allowlist, and push outcome; push failure is nonzero with no retry or unrelated mutation.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="regression|Git preflight|exact allowlist|push failure|no mutation", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Tests assert exact ordered regression invocations, clean/upstream checks before and after promotion, exact allowlist equality, rejection of each excluded category and over-limit artifact, no commit before read-back/final preflight, and nonzero push failure without unrelated mutation.</acceptance_criteria>
  <done>Hourly refresh can publish Git changes only after automated browser/regression success, atomic read-back, and exact repository preflight.</done>
</task>
</tasks>
<threat_model>
## ASVS-1 Threat Model
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Browser payload -> DOM | Payload trust and HTML/JS injection | high | mitigate | Automated DOM/ARIA smoke, escaping, compact schema, hostile text tests. |
| CLI args/fixture root -> filesystem | Path/symlink traversal | high | mitigate | Exact containment and allowlist; reject symlinks and `..`. |
| Candidate -> public/Git | Publication integrity and Git mutation | critical | mitigate | Provider-backed allowlist equality, pre-staging and final pre-commit preflight, promotion/read-back ordering. |
| launchd -> refresh | Launchd environment/path | high | mitigate | Absolute executable/script/log paths, explicit working directory, one label, disabled old label. |
| Operator credential -> process/log | Operator-secret handling | high | mitigate | No secrets in plist/HTML/trace; use existing Git credential helper and redact values. |
## STRIDE Threat Register
| ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---:|---|---|
| T-17-11 | Spoofing/Tampering | Browser capability | high | mitigate | Exact driver path/version/status and fail-closed smoke. |
| T-17-12 | Tampering | Git allowlist | critical | mitigate | Exact set equality and rejected extras. |
| T-17-13 | Availability | launchd conflict | high | mitigate | Explicit bootout/disable/status checks. |
| T-17-14 | Information disclosure | Logs/plist | high | mitigate | Redaction and no secret material. |
</threat_model>
<verification>Run plist lint, captured launchctl state smoke in a temporary user-agent fixture, automated Safari smoke, the exact child-process regression list, and focused Phase 13/15/16 tests. Manual operator verification only: copy the new plist into `~/Library/LaunchAgents`, run bootstrap/print and one bounded trigger, inspect logs, then bootout and restore the prior plist/disabled state; do not treat live-domain mutation as automated coverage.</verification>
<success_criteria>Exactly one launchd job, one automated Safari policy, exact browser/regression evidence, atomic read-back, and exact allowlisted commit/push behavior are proven; failures return nonzero without forbidden mutation.</success_criteria>
<output>Create `.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-04-SUMMARY.md` when done.</output>
