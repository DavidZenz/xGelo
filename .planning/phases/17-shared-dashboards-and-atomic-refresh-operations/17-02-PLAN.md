---
phase: 17-shared-dashboards-and-atomic-refresh-operations
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - R/dashboard/renderer.R
  - R/dashboard/payload_contract.R
  - tests/testthat/test_phase17_dashboards.R
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "Populated dashboards show Overview, Structure, Standings, Fixtures, Results, Form, Match forecasts, and Projected outcomes."
    - "The eight approved UI state categories are explicit and tested: empty/pre_draw, loading/refresh pending, error/refresh blocked, populated, partial, overflow, zero-one-many, and long-text behavior."
    - "Native section, league/group, team, matchday, and fixture-status filters work on desktop and mobile while preserving warnings and lineage."
    - "Keyboard focus, text status equivalents, bounded overflow, wrapping long text, reduced motion, and stable mobile controls are present."
  artifacts:
    - path: "R/dashboard/renderer.R"
      provides: "Shared sections, filters, typed statuses, responsive CSS, ARIA, lineage, credits"
    - path: "tests/testthat/test_phase17_dashboards.R"
      provides: "DOM/static assertions and Wave 0 fixture coverage"
  key_links:
    - "Neutral payload sections -> stable DOM IDs and status surfaces"
    - "Filter controls -> exact predicates -> result count without payload mutation"
  assumptions:
    - "Resolved route, Safari, compact-payload, and blocked-edition decisions are honored; duplicate option IDs collapse only when canonical identity is equal; zero/one/many retain shape; declared stable keys determine ordering; no-match differs from unavailable."
  prohibitions:
    - "No descriptor-less state, color-only status, fabricated probability, nested cards, decorative gradients, marketing hero, or server-backed API."
---

<objective>Complete the shared dashboard shell and prove the UI-SPEC across populated, pre_draw, partial, blocked, filtered, mobile, and long-text states.</objective>
## Artifacts this phase produces
- Updated `R/dashboard/renderer.R`, `R/dashboard/payload_contract.R`, and `tests/testthat/test_phase17_dashboards.R`.
- DOM IDs: `overview`, `structure`, `standings`, `fixtures`, `results`, `form`, `match_forecasts`, `projected_outcomes`, `dashboard-data`, `dashboard-status`, `source-lineage`, `data-credits`, `filter-section`, `filter-league-group`, `filter-team`, `filter-matchday`, `filter-status`, `clear-filters`, and `filter-result-count`.
<execution_context>
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-01-PLAN.md
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-UI-SPEC.md
@.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-VALIDATION.md
@R/visualization/worldcup_dashboard.R
@tests/testthat/test_worldcup_dashboard.R
</execution_context>
<tasks>
<task type="auto">
  <name>Task 1: Render all sections and typed UI states</name>
  <files>R/dashboard/renderer.R, R/dashboard/payload_contract.R, tests/testthat/test_phase17_dashboards.R</files>
  <read_first>Read all entry-point, state-summary, section-navigation, eight section, pre_draw, blocked, warning, lineage, and credits requirements in `17-UI-SPEC.md`, plus the existing World Cup renderer/template.</read_first>
  <action>Extend `render_phase17_dashboard()` and `phase17_render_section()` to emit the eight stable sections in order, with exact typed status/reason envelopes for populated, empty/pre_draw, loading, blocked, partial, overflow, zero/one/many, and long-text cases. Render accepted rows and lifecycle, forecast status, refresh time, batch ID, source confidence, model release/cutoffs, replay lineage, warnings, and collapsed credits. Escape all payload text and script JSON; preserve accepted snapshot identity during pending or blocked states; use the provider functions from Plan 01 for route/inventory assertions and do not introduce a second path vector.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="payload sections|metadata|pre_draw|blocked|credits", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Assertions find all eight section labels in order, every state has a visible text status and exact reason, metadata and credits are present, hostile text is escaped, and EURO pre_draw has no fabricated rows.</acceptance_criteria>
  <done>The shared renderer presents complete football content and truthful state surfaces for both editions.</done>
</task>
<task type="auto">
  <name>Task 2: Add exact filters, accessibility, and responsive regression checks</name>
  <files>R/dashboard/renderer.R, R/dashboard/payload_contract.R, tests/testthat/test_phase17_dashboards.R</files>
  <read_first>Read the UI-SPEC filter/accessibility/layout sections and existing World Cup client predicates, event bindings, table overflow, and status-region patterns.</read_first>
  <action>Implement `phase17_filter_payload()` and native controls for section, league/group, searchable team, matchday, fixture status, and `clear-filters`. Use exact equality predicates, deterministic option/row ordering, keyboard focus, live `filter-result-count`, no-match text distinct from unavailable status, stable mobile geometry, bounded table scroll regions, wrapped hashes/URLs, and reduced-motion CSS. Keep warnings, source/model/replay lineage, and accepted batch identity visible while filtering. Assert desktop/mobile HTML contract in the Wave 0 harness; Plan 04 owns automated browser execution.</action>
  <verify><automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase17_dashboards.R", desc="filters|responsive|accessibility|zero-one-many|long-text", stop_on_failure=TRUE, reporter="summary")'</automated></verify>
  <acceptance_criteria>Each required filter has a labeled native control and clear behavior; exact no-match output is emitted; filtering cannot change metadata or hide warnings; markup passes keyboard, ARIA, overflow, and responsive assertions.</acceptance_criteria>
  <done>DASH-01 through DASH-04 are covered by one shared responsive accessible renderer.</done>
</task>
</tasks>
<threat_model>
## ASVS-1 Threat Model
| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Payload JSON -> DOM | HTML/JS injection | high | mitigate | JSON/HTML escape, compact allowlist, hostile-string tests, no dynamic code generation. |
| Filter input -> client state | Payload trust/tampering | medium | mitigate | Allowlisted dimensions and immutable metadata. |
| Route path -> assets | Path/symlink traversal | high | mitigate | Renderer accepts payload objects; publication validates containment. |
| Rendered output -> publication | Publication integrity | high | mitigate | Plan 03 exact inventory/hash/size/read-back. |
| Test/refresh -> Git | Git mutation | high | mitigate | Plan 04 wrapper only after promotion. |
| launchd -> renderer | Launchd environment/path | medium | mitigate | Plan 04 absolute paths and explicit working directory. |
| Operator -> HTML | Operator-secret handling | high | mitigate | Exclude secrets from metadata, credits, DOM, and traces. |
## STRIDE Threat Register
| ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---:|---|---|
| T-17-04 | Tampering | Client predicates | medium | mitigate | Exact payload-derived options and immutable metadata. |
| T-17-05 | Information disclosure | Embedded payload | medium | mitigate | Compact accepted projection only. |
| T-17-06 | Availability | Long rows/text | low | mitigate | Bounded overflow, wrapping, stable dimensions. |
</threat_model>
<verification>Run both focused renderer commands, the full Phase 17 test file, and `tests/testthat/test_worldcup_dashboard.R`; browser execution is the automated Plan 04 gate.</verification>
<success_criteria>All eight UI-SPEC state truths, required filters, metadata, accessibility semantics, and responsive layout assertions pass from deterministic fixtures.</success_criteria>
<output>Create `.planning/phases/17-shared-dashboards-and-atomic-refresh-operations/17-02-SUMMARY.md` when done.</output>
