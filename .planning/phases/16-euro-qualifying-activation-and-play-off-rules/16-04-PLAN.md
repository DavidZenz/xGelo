---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 04
type: execute
wave: 5
depends_on:
  - 16-03
files_modified:
  - R/competition/uefa_euro_outcomes.R
  - R/competition/state_bundle.R
  - scripts/build_competition_state.R
  - tests/testthat/test_phase16_euro_qualifying.R
autonomous: true
requirements:
  - COMP-03
  - SIM-02
  - SIM-04
must_haves:
  truths:
    - "The in-memory EURO outcome candidate has one exact nine-file schema contract for active, pre_draw, unavailable, unresolved, and revision-blocked states."
    - "The actual Phase 14 production batch path cannot emit active EURO dashboard state from a missing, incomplete, or missing-kickoff activation bundle, while accepted pre_draw and valid active-after-draw zero-result bundles produce schema-valid state."
    - "Phase 14 remains the state authority: the Phase 16 activation gate is an input/validation boundary and does not bypass Phase 14 manifest, cutoff, forecast, or edition isolation checks."
    - "Outcome rows carry accepted source/rules/model/cutoff lineage and preserve candidate/accepted status without exposing an unvalidated candidate."
    - "Phase 14 source manifests and suppression reasons are edition-neutral: EURO lineage is loaded from its own registered accepted path and Phase 16 reasons map explicitly into the accepted Phase 14 state contract."
  artifacts:
    - path: "R/competition/uefa_euro_outcomes.R"
      provides: "Exact nine-file inventory, schemas, candidate builder, validator, registered reader/writer boundary, lineage, and replay-comparison contract"
      contains: "phase16_euro_outcomes_expected_inventory"
    - path: "R/competition/state_bundle.R"
      provides: "Phase 14-owned EURO activation gate, per-edition source-manifest lineage, and Phase 16 reason mapping at the production candidate/batch boundary"
      contains: "phase14_state_bundle_candidate_production"
    - path: "scripts/build_competition_state.R"
      provides: "Existing fresh-process and main state emission path wired to explicit EURO rule loading and registered activation validation"
      contains: "phase14_build_competition_state_main"
    - path: "tests/testthat/test_phase16_euro_qualifying.R"
      provides: "Outcome-schema and Phase 14 state-gate integration tests"
      contains: "missing-kickoff"
  key_links:
    - from: "Plan 03 activation/rules/simulation values"
      to: "phase16_build_euro_outcomes_candidate()"
      via: "validated status, topology, probability, and lineage inputs"
    - from: "phase16_validate_euro_activation()"
      to: "phase14_state_bundle_candidate_production()"
      via: "EURO-specific gate before the Phase 14 production active state branch"
    - from: "phase14_build_competition_state_batch()"
      to: "phase14_state_bundle_candidate_production()"
      via: "actual per-edition candidate invocation around the production batch path"
    - from: "phase14_build_competition_state_load_resource() / phase14_build_competition_state_run() / phase14_build_competition_state_main()"
      to: "phase14_validate_competition_state_bundle()"
      via: "registered resource load, main invocation, state emission, and final schema validation"
    - from: "scripts/build_competition_state.R fresh-process source loader"
      to: "R/competition/uefa_euro_rules.R"
      via: "explicit source/load before state-bundle execution"
---

<objective>
Define the EURO outcomes payload contract and wire the Phase 16 activation gate into the existing Phase 14 state construction authority.

Purpose: Make later CLI/publication work consume one exact state-shaped contract while proving that missing or incomplete EURO inputs cannot become active dashboard state.
Output: In-memory EURO outcomes module and focused Phase 14 state-gate integration coverage; generated files and CLI publication are owned by Plan 05.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-UI-SPEC.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@R/competition/uefa_euro_rules.R
@R/competition/uefa_euro_simulation.R
@R/competition/source_contracts.R
@R/competition/publication_hashes.R
@R/competition/state_bundle.R
@scripts/build_competition_state.R
@R/competition/uefa_nations_league_outcomes.R
@tests/testthat/test_phase14_state_bundle.R
@tests/testthat/test_phase13_publication_hashes.R
@tests/testthat/test_phase15_nations_league.R
</context>

<dependency_graph>
Plan 04 depends on the activation/rules/simulation contracts from Plans 01-03. Task 1 creates the in-memory outcomes interface; Task 2 wires that interface into the exact Phase 14 production batch, fresh-process, main, and validator boundaries, while generalizing per-edition lineage/reasons. Plan 05 depends on both tasks and owns all CLI/publication/generated-output files.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an active and pre_draw candidate through the exact EURO outcomes schema</name>
  <files>R/competition/uefa_euro_outcomes.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read the Phase 15 exact nine-file outcomes inventory, schema, candidate builder, validator, lineage graph, and replay comparator. Read the Phase 14 eleven-artifact state contract and 16-UI-SPEC.md lines 114-190 for empty/active/status-bearing payload requirements. Reuse Plan 01-03 symbols and do not create a second publication protocol.
  </read_first>
  <behavior>
    - Test 1: one active synthetic candidate builds the exact nine named in-memory artifacts, validates schemas/IDs/lineage, and carries the Plan 02 ledger and Plan 03 simulation metadata.
    - Test 2: the active-after-draw candidate has official groups and confirmed fixtures while its zero completed results/standings remain explicit and schema-valid.
    - Test 3: a pre_draw candidate has all nine schema-valid artifacts with explicit lifecycle/forecast status, exact D-16 message/reason fields, and empty structural/projection/probability collections.
    - Test 4: invalid source/rules/topology/handoff inputs produce typed status-bearing candidate metadata that later publication can isolate without converting a valid incumbent into an absent state.
  </behavior>
  <acceptance_criteria>
    - `phase16_euro_outcomes_expected_inventory()` returns exactly `competition_topology.csv`, `stage_slots.csv`, `projected_standings.csv`, `projected_rankings.csv`, `qualification_ledger.csv`, `team_path_probabilities.csv`, `fixture_forecast_form.csv`, `simulation_metadata.csv`, and `outcomes_manifest.csv` under `outcomes/`.
    - `phase16_euro_outcomes_schema()` and `phase16_validate_euro_outcomes_bundle()` reject missing artifacts, duplicate stable IDs, invalid statuses, missing lineage, hash mismatches, unsupported topology, wrong-stage external eligibility, and probabilities without active confirmed-kickoff inputs.
    - `phase16_build_euro_outcomes_candidate()`, `phase16_write_euro_outcomes_bundle()`, and `phase16_read_euro_outcomes_bundle()` expose the validated registered-root boundary that Plan 05 drives through the CLI; the writer does not accept an unvalidated candidate.
    - `phase16_build_euro_outcomes_candidate()` preserves accepted source/rules/model/cutoff/seed/count lineage and carries scenario/status/reason fields required by D-05-D-16.
    - Pre_draw and active-after-draw zero-result candidates remain distinct: pre_draw has empty groups/fixtures, while active-after-draw has real groups/fixtures and empty completed results/standings.
    - The candidate interface is renderer-neutral and does not add Phase 17 dashboard shell, filter, credit, refresh, or browser behavior.
  </acceptance_criteria>
  <action>
    Create `R/competition/uefa_euro_outcomes.R` by adapting the Phase 15 sibling-artifact design to EURO. Define the exact nine-file inventory, schemas, stable row/hash fields, candidate builder, validator, registered-root reader/writer boundary, lineage graph, and replay comparator. Build active, active-after-draw zero-result, pre_draw, unavailable, unresolved, unsupported-topology, and revision-blocked in-memory candidates from Plans 01-03. Preserve exact D-16 message/status fields and D-05-D-12 ledger/simulation lineage. Add tests before implementation for all candidate states and ensure probability rows are admitted only after the activation, kickoff, host/rules, and interim-stage handoff gates pass; Plan 05 will exercise the writer and generated inventory through the CLI.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="outcomes_schema|candidate|pre_draw|active_after_draw|lineage", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>The exact in-memory nine-file EURO outcomes contract validates active, active-after-draw, pre_draw, and typed blocked candidates with complete lineage and no fabricated rows.</done>
  <reversibility rating="reversible">The task defines pure candidate/validation values; filesystem publication remains Plan 05.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire EURO activation through the real Phase 14 production batch, main, and fresh-process paths</name>
  <files>R/competition/state_bundle.R, scripts/build_competition_state.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read `R/competition/state_bundle.R` at `phase14_state_bundle_candidate_production()` around lines 1081-1225, the hardcoded Nations League manifest validation around lines 1308-1360, the closed `reason_enum` around lines 1658-1672, and `phase14_build_competition_state_batch()` around lines 1783-1950 where the production candidate is invoked near line 1890. Read `scripts/build_competition_state.R` at the fresh-process dependency loader around lines 52-72, the per-edition resource/default manifest path around lines 198-256, the `phase14_build_competition_state_run()` invocation around lines 124-136, and `phase14_build_competition_state_main()` around lines 266-345. Read `R/competition/uefa_euro_rules.R` and Phase 14 state tests for candidate status, pre_draw empty artifacts, active validation, and replay invariants.
  </read_first>
  <behavior>
    - Test 1: a real `phase14_build_competition_state_main()` integration invocation with a registered accepted EURO pre_draw input returns schema-valid pre_draw state with empty structural collections and explicit status/reason metadata.
    - Test 2: the same `phase14_build_competition_state_main()` path with a valid active-after-draw bundle containing real groups, stable fixtures, confirmed kickoffs, and zero completed results/standings returns schema-valid active/scheduled state without being rewritten as pre_draw or unavailable.
    - Test 3: through `phase14_build_competition_state_main()` and its `phase14_build_competition_state_batch()` callback, missing activation/source, incomplete groups/fixtures/identity, and missing kickoff inputs produce invalid/blocked state and never emit partial active collections.
    - Test 4: per-edition manifest/status loading accepts the registered EURO manifest instead of the Nations League path and preserves the exact source bundle, source artifact, raw hash, bundle hash, and registry lineage for each edition.
    - Test 5: the Phase 16 reasons `pre_draw`, `unavailable`, `revision_blocked`, `host_place_unresolved`, `unresolved_external_eligibility`, and `unsupported_topology` map to the accepted Phase 14 reason/state contract with their original reason retained; no fabricated active state is emitted for any mapping.
    - Test 6: the fresh child process explicitly loads/sources `R/competition/uefa_euro_rules.R` before state construction, and Phase 14 non-EURO state construction plus manifest/cutoff/forecast validation remain authoritative and unchanged.
  </behavior>
  <acceptance_criteria>
    - `phase14_state_bundle_candidate_production()` invokes the Phase 16 activation validation for `uefa_euro_2028_qualifying` before its active branch, and `phase14_build_competition_state_batch()` carries that gate through the actual candidate call around line 1890 while preserving Phase 14 state construction and forecast authority for all editions.
    - `phase14_validate_competition_state_bundle()` rejects an emitted EURO active candidate whose activation envelope/source/rules/kickoff gate is missing or incomplete, and accepts schema-valid pre_draw and valid active-after-draw candidates.
    - The accepted-manifest validation around lines 1308-1360 derives the expected bundle ID, source artifact IDs, and accepted resource path from the candidate edition/registered manifest; it contains no Nations League-only constants and works for both EURO and Nations League editions.
    - The closed reason enum around lines 1658-1672 is replaced by an edition-neutral accepted mapping that explicitly handles `pre_draw`, `unavailable`, `revision_blocked`, `host_place_unresolved`, `unresolved_external_eligibility`, and `unsupported_topology`, preserving the Phase 16 reason in state/status lineage while retaining Phase 14 candidate/forecast/state semantics.
    - `phase14_build_competition_state_load_resource()` at the existing script path near line 198 loads only registered edition resources; its default manifest path is edition-scoped rather than the hardcoded Nations League path around line 236. `phase14_build_competition_state_run()` and `phase14_build_competition_state_main()` cannot emit active EURO state unless the gate and final Phase 14 validator pass.
    - The fresh-process dependency loader around line 65 explicitly sources/loads `R/competition/uefa_euro_rules.R` and verifies the required EURO symbol before loading the state bundle.
    - A real `phase14_build_competition_state_main()` integration test proves accepted pre_draw and valid active-after-draw zero-result state, rejects missing/incomplete/missing-kickoff bundles, verifies registry/state-path lineage, and preserves non-EURO authority.
    - The integration carries D-01, D-02, D-04, D-13, D-14, D-15, and D-16 status/reason fields without moving source acquisition, publication, refresh, or renderer ownership into Phase 14 or adding Phase 17 shell/filter/credits/browser work.
  </acceptance_criteria>
  <action>
    Update the actual Phase 14 production path: add the narrowly scoped EURO branch to `phase14_state_bundle_candidate_production()` around line 1081, carry it through `phase14_build_competition_state_batch()` around the checker-identified invocation near line 1890, and retain `phase14_validate_competition_state_bundle()` as the final shared validator. Generalize the accepted source-manifest validation around lines 1308-1360 so the expected bundle ID, five artifact IDs, paths, hashes, and status are derived from each edition's registered manifest; remove the Nations League-only constants without weakening the official lineage checks. Replace the closed reason list around lines 1658-1672 with an explicit edition-neutral Phase 14 mapping for `pre_draw`, `unavailable`, `revision_blocked`, `host_place_unresolved`, `unresolved_external_eligibility`, and `unsupported_topology`, preserving the original Phase 16 reason and suppressing derived active/probability state where the mapping is unresolved.

    Update `scripts/build_competition_state.R` at the fresh-process loader around line 65 to explicitly `sys.source()`/load `R/competition/uefa_euro_rules.R` and verify its activation symbol before `state_bundle.R`; generalize the default source-bundle manifest selection around line 236 to the requested edition's accepted directory; and enforce the gate through `phase14_build_competition_state_run()` around line 133 and `phase14_build_competition_state_main()` around lines 266-345. Add a real integration test that invokes `phase14_build_competition_state_main()` with the registered loader/batch/validator path for accepted pre_draw and valid active-after-draw zero-result fixtures, plus missing, incomplete, and missing-kickoff candidates. Assert registry/state-path source and reason lineage, child-process EURO rule loading, no fabricated active collections, and unchanged non-EURO Phase 14 authority. Cite D-01, D-02, D-04, D-13, D-14, D-15, and D-16; do not bypass Phase 14 validation or add Phase 17 dashboard/refresh/browser behavior.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="phase14_build_competition_state_main|state_gate|missing_activation|missing_incomplete|missing_kickoff|phase14|active_after_draw|pre_draw|reason_mapping|manifest_lineage|fresh_process", stop_on_failure=TRUE, reporter="summary")' && Rscript --vanilla scripts/build_competition_state.R --edition-id uefa_euro_2028_qualifying --dry-run && Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_state_bundle.R", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>The actual `phase14_build_competition_state_main()` -> `phase14_build_competition_state_batch()` -> `phase14_state_bundle_candidate_production()` path explicitly loads EURO rules, generalizes per-edition manifest/reason lineage, admits only accepted pre_draw or valid active-after-draw EURO state, rejects missing/incomplete/missing-kickoff bundles, and preserves Phase 14 authority for every other edition.</done>
  <reversibility rating="costly">This is a cross-phase state-boundary integration, so the existing Phase 14 validator and regression suite remain mandatory rollback evidence.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activation/rules/simulation inputs -> outcomes candidate | Upstream values must be schema, status, ID, and lineage valid before aggregation. |
| Phase 16 activation -> Phase 14 state builder | EURO input must pass the explicit gate before the shared state authority emits active state. |
| Phase 14 state candidate -> state validator | Durable state artifacts and manifests remain subject to Phase 14 inventory/cutoff/forecast validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-16 | Tampering | `phase16_validate_euro_outcomes_bundle()` | high | mitigate | Validate exact inventory, schemas, IDs, statuses, lineage, hashes, topology, and probability admission before returning a candidate. |
| T-16-17 | Tampering | `phase14_state_bundle_candidate_production()` EURO branch | critical | mitigate | Invoke the Phase 16 activation gate before active state construction and preserve Phase 14 final validation. |
| T-16-18 | Denial of Service | missing/incomplete/kickoff-invalid state input | high | mitigate | Return a bounded invalid/pre_draw state with explicit reason and no partial active emission. |
| T-16-19 | Information Disclosure | blocked candidate/state collections | high | mitigate | Keep candidate rows and blocked derived rows isolated; expose only accepted or schema-valid empty state. |
| T-16-20 | Repudiation | state/outcomes lineage | medium | mitigate | Carry source/rules/model/cutoff/activation hashes through candidate and Phase 14 manifest validation. |
| T-16-SC | Tampering | R/test dependency installation | high | mitigate | No package install is introduced; existing dependencies are reused and asserted by Wave 0. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Exact EURO state-shaped payload and Phase 14 activation/state emission gate are Tasks 1-2. |
| REQ | COMP-03 and SIM-04 are covered by activation/state admission and empty/suppressed candidates; SIM-02 lineage is carried into the outcomes contract. |
| RESEARCH | The actual Phase 14 production candidate/batch/main path, per-edition manifest/status loading, edition-neutral Phase 16 reason mapping, explicit fresh-process EURO-rules loading, exact nine-file sibling pattern, source/rules/model/cutoff lineage, active zero-results semantics, and fail-closed status contracts are mapped to files/symbols/tests. |
| CONTEXT | D-01, D-02, D-04, D-05-D-16 are represented in candidate/state fields; Phase 17 renderer/refresh/browser scope remains outside this plan. |
| Hooks | No package install, external API/SDK/service, or assumption-delta trigger is introduced. |

<verification>
Run the filtered outcome/state-gate tests after each task. At the wave gate run the focused Phase 16 file, `test_phase14_state_bundle.R`, and relevant Phase 15 outcomes regressions; compare full-suite output to the Wave 0 baseline and report any persistent baseline separately.
</verification>

<success_criteria>
The exact in-memory outcomes interface is ready for publication, and the existing Phase 14 state construction path demonstrably rejects missing/incomplete/missing-kickoff EURO active inputs while accepting truthful pre_draw and active-after-draw zero-result state.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-04-SUMMARY.md` when done.
</output>
