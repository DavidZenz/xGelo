---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 03
type: execute
wave: 4
depends_on:
  - 16-02
files_modified:
  - R/competition/uefa_euro_simulation.R
  - tests/testthat/test_phase16_euro_qualifying.R
autonomous: true
requirements:
  - SIM-02
must_haves:
  truths:
    - "EURO qualification simulation follows the accepted official topology and allocates host capacity before best-runner-up and play-off eligibility."
    - "Nations League-linked eligibility is consumed only from a registered, validated Phase 15 handoff normalized into a canonical interim projection keyed by stable team_id and ranking_stage = interim_overall."
    - "Final-only, wrong-stage, duplicate, missing, or unresolved Phase 15 handoffs produce unresolved_external_eligibility and suppress probabilities."
    - "Single-leg and home-and-away play-off paths use the existing calibrated forecast authority, deterministic seeds, and auditable simulation metadata."
    - "Pre-draw, active zero-results without resolved host/rules inputs, missing kickoff, incomplete handoff, unresolved draw conditions, and unsupported topology produce no qualification probabilities; scenario status remains separate from an actual collapsed bracket."
  artifacts:
    - path: "R/competition/uefa_euro_simulation.R"
      provides: "Seeded EURO simulation, tie resolution, host-capacity, draw-condition, and registered Phase 15 interim-projection adapter functions"
      contains: "uefa_euro_validate_nl_eligibility_handoff"
    - path: "tests/testthat/test_phase16_euro_qualifying.R"
      provides: "All topology fixtures, four-host capacity replay, exact handoff-stage rejection, suppression, RNG, and child-process replay checks"
      contains: "ranking_stage"
  key_links:
    - from: "allocate_euro_places()"
      to: "uefa_euro_simulate_qualification()"
      via: "accepted ledger/topology/draw-condition input before probability generation"
    - from: "registered Phase 15 outcomes manifest/projected_rankings.csv"
      to: "uefa_euro_normalize_nl_interim_projection()"
      via: "registered manifest/artifact validation, preserved or derived interim stage, stable team_id, and source/rules lineage"
    - from: "uefa_euro_normalize_nl_interim_projection()"
      to: "uefa_euro_validate_nl_eligibility_handoff()"
      via: "canonical ranking_scope/ranking_stage interim_overall rows before eligibility admission"
    - from: "Phase 14 calibrated 1X2/score authority"
      to: "single-leg/two-leg resolvers"
      via: "model/release/cutoff lineage and deterministic seeded sampling"
---

<objective>
Implement deterministic EURO qualifying simulation and Nations League-linked play-off eligibility across every accepted official topology.

Purpose: Make active post-draw qualification probabilities reflect host-place capacity, official fallback order, versioned draw conditions, and confirmed schedule state while preserving the fail-closed activation boundary.
Output: Tested seeded simulation and registered Phase 15 eligibility adapter for single-leg, two-leg, host, scenario, and suppression paths.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-UI-SPEC.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@R/competition/uefa_nations_league_rules.R
@R/competition/uefa_nations_league_outcomes.R
@R/competition/uefa_nations_league_simulation.R
@outputs/competition/uefa_nations_league_2026_27/outcomes/projected_rankings.csv
@outputs/competition/uefa_nations_league_2026_27/outcomes/outcomes_manifest.csv
@R/forecast/monte_carlo.R
@R/forecast/output.R
@tests/testthat/test_phase15_nations_league.R
</context>

<precondition>Phase 15 accepted outcomes and the Phase 14 calibrated 1X2/score authority are available through their registered manifests and pass their own validation; the adapter must stop when this precondition is false.</precondition>

<dependency_graph>
Plan 03 depends on the complete rules/topology/allocation contracts from Plan 02. Its tracer first normalizes the registered Phase 15 output into the canonical interim projection before one simulation path; the expansion task completes all branches and replay. Plan 04 consumes the resulting adapter, replay, and suppression contracts before building state/output artifacts.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the registered Phase 15 output through interim-projection normalization and calibrated seeded simulation</name>
  <files>R/competition/uefa_euro_simulation.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read the Phase 15 seeded simulation implementation for caller-RNG save/restore, calibrated outcome sampling, single-leg resolution, and explicit source/rules/model metadata. Read `R/competition/uefa_nations_league_rules.R:1467-1498` where `uefa_nl_rank_interim_overall()` creates the interim stage and `R/competition/uefa_nations_league_simulation.R:1829-1857` where `uefa_nl_sim_rank_capture()` overwrites interim rows from final-ranking stages. Read the registered `outputs/competition/uefa_nations_league_2026_27/outcomes/projected_rankings.csv` and `outcomes_manifest.csv`, including the Phase 15 projected-rankings schema, then use Plan 02 topology/ledger functions and D-09/D-10 as inputs.
  </read_first>
  <behavior>
    - Test 1: the registered Phase 15 reader loads `projected_rankings.csv` plus its manifest and either preserves rows already carrying `ranking_stage = interim_overall` or derives that canonical stage only from a valid interim projection such as `interim_overall_rank` plus `interim_ranking_stage`; the normalized rows retain stable `team_id`, source bundle, manifest, and rules lineage.
    - Test 2: the current registered final-only/blocked projected-rankings output, a final-only variant, and a wrong-stage variant are rejected as `unresolved_external_eligibility`; the simulator returns an empty probability table for each rather than accepting the EURO-side synthetic values.
    - Test 3: one resolved two-host-slot topology with a complete normalized handoff whose rows have stable `team_id`, `ranking_scope = interim_overall`, and `ranking_stage = interim_overall` produces deterministic single-leg path outcomes under a fixed seed.
    - Test 4: repeating the same call with the same seed returns identical qualification counts/probabilities and restores the caller RNG state.
    - Test 5: the active zero-results fixture can report scenario/topology status, but it does not emit collapsed qualification probabilities until completed standings, host usage, draw conditions, and required eligibility inputs resolve.
    - Test 6: missing confirmed kickoff or invalid activation state returns an explicit suppressed/unavailable result with no probability rows.
  </behavior>
  <acceptance_criteria>
    - `uefa_euro_sim_with_seed()` follows the Phase 15 RNG contract and `uefa_euro_run_simulation()` accepts explicit seed/count inputs.
    - `uefa_euro_sample_calibrated_outcome()` consumes the Phase 14 calibrated 1X2/score authority rather than creating independent calibration.
    - `uefa_euro_normalize_nl_interim_projection()` reads the registered Phase 15 `projected_rankings.csv` and manifest, validates artifact identity/content lineage, retains a canonical `ranking_stage = interim_overall` when present, or derives it only from preserved interim fields with non-missing stable ranks; it never promotes a final stage by display name or row order.
    - `uefa_euro_validate_nl_eligibility_handoff()` consumes only the normalized projection, requires unique stable `team_id`, accepted source/rules lineage, `ranking_scope = interim_overall`, and exact `ranking_stage = interim_overall`; final-only, wrong-stage, duplicate, missing, or blocked registered rows are rejected.
    - `uefa_euro_resolve_single_leg()` returns winner, path, source/rules/model/cutoff lineage, and a typed blocked status when inputs are ineligible.
    - `uefa_euro_simulate_qualification()` rejects pre_draw, unresolved active scenario, incomplete handoff, unresolved draw conditions, and missing kickoff before generating qualification probabilities, satisfying D-10, D-14, and D-15.
  </acceptance_criteria>
  <action>
    Create `R/competition/uefa_euro_simulation.R` in the Phase 15 style. First add `uefa_euro_read_registered_nl_handoff()` and `uefa_euro_normalize_nl_interim_projection()` around the registered Phase 15 `projected_rankings.csv`/manifest boundary. Validate the manifest row and full artifact hash, filter by stable `team_id`, preserve `ranking_stage = "interim_overall"` when present, and derive the same canonical stage only from an interim rank plus preserved `interim_ranking_stage` evidence from the Phase 15 rules output; retain source bundle, source bundle hash, ruleset version/hash, and manifest lineage on every normalized row. The actual current registered final-only/blocked output must yield `unresolved_external_eligibility`, while the positive branch must use a Phase 15-native interim projection derived from registered Phase 15 inputs, not a synthetic EURO-side table. Wire `uefa_euro_validate_nl_eligibility_handoff()` to consume only this normalized projection, then wire `uefa_euro_sim_with_seed()`, `uefa_euro_sample_calibrated_outcome()`, `uefa_euro_resolve_single_leg()`, and `uefa_euro_run_simulation()` through one resolved active topology. Add tracer tests against the registered Phase 15 output and manifest, final-only/wrong-stage/duplicate rejection, probability suppression, deterministic replay, caller RNG restoration, calibrated authority lineage, confirmed-kickoff gating, and active zero-results scenario status. Cite D-09, D-10, D-14, and D-15.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="simulation|handoff|interim_adapter|registered_phase15|ranking_stage|rng|suppression", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>The registered Phase 15 output is normalized or rejected from its actual stage evidence, only canonical interim-overall rows reach the EURO handoff, one resolved topology runs through reproducible calibrated simulation, and every invalid handoff suppresses probabilities.</done>
  <reversibility rating="reversible">The simulation functions return values and restore caller RNG state; they do not write or replace outcomes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete host branches, fallback eligibility, draw-condition suppression, and fresh-process replay</name>
  <files>R/competition/uefa_euro_simulation.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read Plan 02's final topology/ledger contracts, Task 1's normalized Phase 15 adapter, the Phase 15 transition and projected-ranking fields, the 16-RESEARCH.md resolved draw-condition and host-scenario rules, and 16-PATTERNS.md fresh-process replay requirement. Read 16-UI-SPEC.md status vocabulary for suppressed/unresolved/unavailable rows.
  </read_first>
  <behavior>
    - Test 1: two consumed host places produce 8 entrants, 2 single-leg paths, and 2 available places; one produces 12/3/3; zero produces 8/4 home-and-away/4.
    - Test 2: a four-host input consumes only the two top-ranked covered host places, preserves remaining slots/capacity, and does not create an extra direct place.
    - Test 3: eligibility follows runner-up, Nations League A-C group winner, League D, then broader ranking fallback, excluding qualified/host teams and preserving stable IDs; only Task 1's normalized interim projection is eligible, and final-only/wrong-stage registered rows remain rejected.
    - Test 4: missing or partial accepted draw conditions return `unresolved_draw_conditions`/`unsupported_topology` and no probabilities; host scenarios remain scenario-keyed until results resolve actual usage.
    - Test 5: one-leg and two-leg outcomes carry identical lineage and replay exactly for a fixed seed in normal, reversed-input, repeated, and fresh child-process runs.
  </behavior>
  <acceptance_criteria>
    - `uefa_euro_allocate_playoff_pool()` consumes the Plan 02 ledger and models selected host-place consumption before eligible play-off entrants.
    - `uefa_euro_build_playoff_pots()` applies the official fallback and potting order, uses only Task 1's validated normalized interim-stage handoff, and emits `unresolved_external_eligibility` when the registered handoff is incomplete or wrong-stage.
    - `uefa_euro_resolve_two_leg_tie()` uses the existing calibrated match authority for both legs and the rules-supplied aggregate/extra-time/penalty inputs.
    - `uefa_euro_simulate_qualification()` supports all three topology branches, records scenario/path/stage probabilities only when their prerequisites resolve, and refuses probability rows for pre_draw, active unresolved scenarios, unconfirmed fixtures, unresolved hosts, unsupported topology, or unavailable draw conditions.
    - D-05, D-07, D-09, D-10, D-11, and D-12 are represented in status, eligibility, topology, scenario, lineage, and deterministic replay hashes.
    - Fresh child-process replay compares complete returned artifact hashes/bytes with the in-process normal/reversed/repeated replay; a mismatch is an explicit failure, not a warning-only result.
  </acceptance_criteria>
  <action>
    Complete the simulation module with `uefa_euro_allocate_playoff_pool()`, `uefa_euro_build_playoff_pots()`, `uefa_euro_resolve_two_leg_tie()`, and `uefa_euro_simulate_qualification()`. Route all eligibility through `uefa_euro_normalize_nl_interim_projection()` and `uefa_euro_validate_nl_eligibility_handoff()`; the registered Phase 15 artifact remains the authority, and a final-only/wrong-stage result must carry `unresolved_external_eligibility` through to an empty probability table. Implement the three official branch cardinalities exactly and consume only the two selected host reservations from Plan 02. Apply the researched eligibility order through the validated interim-stage adapter. Require versioned accepted draw conditions before any topology probability path; return `unresolved_draw_conditions` and `unsupported_topology` together when conditions are unavailable. Preserve scenario IDs rather than collapsing host branches before completed results. Extend focused tests with registered-output normalization, final-only/wrong-stage suppression, four-host allocation, all branches, fallback exclusion, every suppression reason, and normal/reversed/repeated/fresh-child replay with identical hashes. Cite D-05, D-07, D-09, D-10, D-11, and D-12.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="topology|four_host|fallback|draw_conditions|fresh_process|replay", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>All official host-capacity and tie-resolution branches are reproducible, the exact Phase 15 interim-stage handoff is enforced, unresolved conditions suppress probabilities, scenarios are preserved, and fresh-process hashes match.</done>
  <reversibility rating="reversible">Branch selection is derived from immutable topology/eligibility values and remains replayable before the outcomes publication plan.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registered Phase 15 outcomes -> EURO eligibility | External competition outcomes are accepted only through stable IDs, registered manifest/source lineage, completeness, preserved/derived interim evidence, and canonical interim-stage validation. |
| accepted topology/ledger -> simulation | Capacity, host status, draw conditions, and scenario resolution control whether a probability path is admitted. |
| Phase 14 forecast authority -> simulated outcomes | Match probabilities and score distributions must come from the accepted calibrated model lineage. |
| child process -> replay evidence | Fresh-process output must be compared by complete bytes/hashes, not only in-memory objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-11 | Spoofing | `uefa_euro_normalize_nl_interim_projection()` / `uefa_euro_validate_nl_eligibility_handoff()` | high | mitigate | Require registered Phase 15 manifest/source identity, stable IDs, preserved or derived interim evidence, `ranking_scope`, and exact `ranking_stage = interim_overall`; reject display-name or final-only matches. |
| T-16-12 | Tampering | host-capacity/scenario branch selection | critical | mitigate | Derive branch from validated ledger, selected top-two hosts, completed-result resolution, and versioned draw conditions; preserve scenario rows before resolution. |
| T-16-13 | Information Disclosure | probability tables for blocked paths | high | mitigate | Gate simulation on active confirmed-kickoff state, resolved prerequisites, and complete handoff; return typed suppression with no probability rows otherwise. |
| T-16-14 | Repudiation | seeded simulation result | medium | mitigate | Persist seed/count, source/rules/model/cutoff lineage, scenario/draw-policy identity, and deterministic replay hash. |
| T-16-15 | Denial of Service | malformed handoff/draw-condition input | medium | mitigate | Validate bounded schemas before eligibility/pot construction and stop with unresolved status rather than iterating unknown data. |
| T-16-SC | Tampering | R/test dependency installation | high | mitigate | No package install is introduced; existing dependencies are reused and asserted by Wave 0. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Active qualification simulation, host capacity, exact Phase 15 handoff, all topologies, scenario preservation, and fail-closed probability state are Tasks 1-2. |
| REQ | SIM-02 is covered by seeded single/two-leg simulation, official branches, host selection, fallback, and probability suppression. |
| RESEARCH | Phase 15 RNG/model contracts, the actual interim-stage overwrite caveat, registered-output normalization, exact `interim_overall` stage, Phase 14 calibrated authority, official topology, fallback order, versioned draw conditions, and fresh-process replay are mapped to functions/tests. |
| CONTEXT | D-05, D-07, D-09, D-10, D-11, and D-12 are implemented/cited here; activation D-01-D-04 and pre_draw D-13-D-16 are asserted at this boundary. |
| Hooks | No package install, external API/SDK/service, or assumption-delta trigger is introduced. |

<verification>
Run the filtered simulation/handoff/registered-output tests after each task, including a fresh child-process replay. At the wave gate run the focused Phase 16 file plus Phase 14 forecast/state and Phase 15 simulation/handoff regressions; compare the full-suite result to the Wave 0 baseline and report the persistent baseline separately.
</verification>

<success_criteria>
The simulation layer derives or preserves a canonical Phase 15 interim projection from the registered handoff, returns reproducible qualification outcomes for each accepted official topology, enforces the exact interim-stage contract, preserves unresolved scenarios, and returns explicit suppressed/unresolved states for every missing or unsupported prerequisite.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-03-SUMMARY.md` when done.
</output>
