---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - R/competition/uefa_euro_simulation.R
  - tests/testthat/test_phase16_euro_qualifying.R
autonomous: true
requirements:
  - SIM-02
must_haves:
  truths:
    - "EURO qualification simulation follows the accepted official topology and allocates host capacity before best-runner-up and play-off eligibility."
    - "Nations League-linked eligibility is consumed from the accepted Phase15 transition handoff by stable team_id, with the official fallback order and explicit unresolved status."
    - "Single-leg and home-and-away play-off paths use the existing calibrated forecast authority, deterministic seeds, and auditable simulation metadata."
    - "Pre-draw, incomplete-handoff, missing-kickoff, and unsupported-topology inputs produce no qualification probabilities."
  artifacts:
    - "R/competition/uefa_euro_simulation.R contains seeded EURO simulation, tie-resolution, host-capacity, and Nations League eligibility functions."
    - "tests/testthat/test_phase16_euro_qualifying.R contains all three host-capacity topology fixtures, Phase15 handoff fixtures, deterministic replay checks, and suppression assertions."
  key_links:
    - "`uefa_euro_simulate_qualification()` consumes `allocate_euro_places()` output and the Phase15 transition handoff without display-name joins."
    - "Every simulated stage resolves through the Phase14 calibrated 1X2 authority and preserves source/rules/model/cutoff lineage."
---

<objective>
Implement deterministic EURO qualifying simulation and Nations League-linked play-off eligibility across every accepted official topology.

Purpose: Make active post-draw qualification probabilities reflect host-place capacity, official fallback order, and confirmed schedule state while preserving the fail-closed boundary established by Plans 01-02.
Output: Tested seeded simulation and eligibility module for single-leg, two-leg, host, and external-eligibility paths.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-UI-SPEC.md
@R/competition/uefa_nations_league_simulation.R
@R/competition/uefa_nations_league_outcomes.R
@R/forecast/monte_carlo.R
@R/forecast/output.R
@tests/testthat/test_phase15_nations_league.R
</context>

<precondition>Phase15 accepted Nations League transition outcomes and the Phase14 calibrated 1X2/score authority are available to the test fixture and runtime caller as validated inputs.</precondition>

<dependency_graph>
Plan 03 depends on the complete rules/topology and allocation contracts from Plan 02. It modifies only the simulation module and shared Phase16 test file, so Plan 04 waits for its replay and suppression behavior before building outcomes and CLI publication.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one accepted topology through seeded play-off simulation</name>
  <files>R/competition/uefa_euro_simulation.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read the Phase15 seeded simulation implementation for RNG save/restore, calibrated outcome sampling, single-leg resolution, two-leg resolution, and simulation metadata. Read the Phase14 forecast output contract to identify the authoritative 1X2/score inputs. Use the Plan 02 topology/ledger functions and 16-CONTEXT.md decisions D-09 and D-10 as inputs.
  </read_first>
  <behavior>
    - Test 1: one two-host-slot topology with a complete accepted handoff produces deterministic single-leg path outcomes under a fixed seed.
    - Test 2: repeating the same call with the same seed returns identical qualification counts and probabilities while restoring the caller RNG state.
    - Test 3: a missing confirmed kickoff or invalid activation state returns an explicit suppressed/unavailable result with no probability rows.
    - Test 4: a missing or ambiguous Phase15 transition handoff returns unresolved_external_eligibility rather than selecting a team by name.
  </behavior>
  <acceptance_criteria>
    - `uefa_euro_sim_with_seed()` follows the Phase15 RNG contract and `uefa_euro_run_simulation()` accepts an explicit seed and simulation count.
    - `uefa_euro_sample_calibrated_outcome()` consumes the Phase14 calibrated 1X2/score authority rather than creating an independent forecast calibration.
    - `uefa_euro_resolve_single_leg()` returns winner, path, source/rules/model/cutoff lineage, and a typed blocked status when inputs are not eligible.
    - `uefa_euro_simulate_qualification()` rejects pre-draw and incomplete activation envelopes before generating qualification probabilities, satisfying D-14 and D-15.
  </acceptance_criteria>
  <action>
    Create `R/competition/uefa_euro_simulation.R` in the Phase15 style. Wire `uefa_euro_sim_with_seed()`, `uefa_euro_sample_calibrated_outcome()`, `uefa_euro_resolve_single_leg()`, and `uefa_euro_run_simulation()` through one active two-host-slot topology. Add `uefa_euro_validate_nl_eligibility_handoff()` at the boundary and require complete Phase15 transition outcomes keyed by stable `team_id`; use D-10 explicitly and reject display-name matching.

    Add the tracer tests first. Assert deterministic replay, caller RNG restoration, calibrated authority lineage, confirmed-kickoff gating, and `unresolved_external_eligibility` for missing/ambiguous handoff rows. Keep the simulation count bounded in focused tests and use local fixtures only.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", stop_on_failure=TRUE)'</automated>
  </verify>
  <done>One accepted active topology runs through a reproducible seeded simulation, and every precondition failure suppresses probability output with a typed reason.</done>
  <reversibility rating="reversible">The simulation functions return values only and restore caller RNG state; they do not write or replace an outcomes bundle.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete host-capacity branches and Nations League eligibility fallback</name>
  <files>R/competition/uefa_euro_simulation.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read Plan 02's final topology and qualification-ledger contracts, the Phase15 transition eligibility fields, and the 16-RESEARCH.md official Nations League-linked eligibility order. Read 16-UI-SPEC.md for the required ledger status values and the distinction between active, suppressed, unresolved, and unavailable.
  </read_first>
  <behavior>
    - Test 1: two reserved host places produce 8 entrants, 2 single-leg paths, and 2 available places after host/direct allocation.
    - Test 2: one reserved host place produces 12 entrants, 3 single-leg paths, and 3 available places.
    - Test 3: no reserved host places produces 8 entrants and 4 seeded/unseeded home-and-away ties with 4 places.
    - Test 4: eligibility selection follows runner-up, Nations League A-C group winner, League D, then broader ranking fallback, while excluding qualified/host teams and preserving stable IDs.
    - Test 5: known future draw conditions are not inferred; unavailable external eligibility or unsupported topology returns suppressed/unresolved and no probabilities.
    - Test 6: one-leg and two-leg outcomes replay exactly for a fixed seed and carry the same source/rules/model/cutoff lineage.
  </behavior>
  <acceptance_criteria>
    - `uefa_euro_allocate_playoff_pool()` consumes the Plan 02 place ledger and models host-place consumption before creating eligible play-off entrants.
    - `uefa_euro_build_playoff_pots()` applies the official Nations League-linked fallback and potting order, separates host associations as required, and emits `unresolved_external_eligibility` whenever the accepted Phase15 handoff is incomplete.
    - `uefa_euro_resolve_two_leg_tie()` uses the existing calibrated match authority for both legs and the official aggregate/extra-time/penalty resolution inputs supplied by the rules contract.
    - `uefa_euro_simulate_qualification()` supports all three topology branches, records path/stage probabilities, and refuses to create forecast rows for pre_draw, unconfirmed fixtures, unresolved host guarantees, unsupported topology, or unavailable draw conditions.
    - D-05, D-07, D-09, D-10, D-11, and D-12 are represented in status, eligibility, topology, and lineage fields; no fabricated team or fixture is added.
  </acceptance_criteria>
  <action>
    Complete the simulation module with `uefa_euro_allocate_playoff_pool()`, `uefa_euro_build_playoff_pots()`, `uefa_euro_resolve_two_leg_tie()`, and `uefa_euro_simulate_qualification()`. Implement the three official capacity branches exactly: 2 reserved places/8 entrants/2 single-leg paths, 1 reserved place/12 entrants/3 single-leg paths, and 0 reserved places/8 entrants/4 seeded-versus-unseeded home-and-away ties. Enforce D-05 and D-07 so an unresolved host guarantee blocks the affected probability path.

    Consume Phase15 transition outcomes by `team_id` and apply the researched eligibility order: qualifying-group runners-up, Nations League A-C group winners not qualified, League D, then the broader ranking fallback. Keep additional draw conditions source-controlled; return `unresolved_external_eligibility` when they are not available. Preserve D-09, D-10, D-11, and D-12 in stage metadata and deterministic replay hashes. Extend focused tests for all branches, fallback order, host exclusion, two-leg replay, and every suppression reason.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", stop_on_failure=TRUE)'</automated>
  </verify>
  <done>All official host-capacity branches and Nations League-linked eligibility paths are simulation-ready, reproducible, source-lined, and fail closed when the accepted evidence cannot determine a valid entrant or topology.</done>
  <reversibility rating="reversible">Simulation branch selection is derived from immutable topology/eligibility values and remains replayable before Plan 04 writes any artifact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase15 transition outcomes -> EURO eligibility | External competition outcomes are accepted only through stable IDs, completeness, and status validation. |
| accepted topology/ledger -> simulation | Capacity, host status, and draw conditions control whether a probability path is allowed. |
| forecast authority -> simulated outcomes | Match probabilities and score distributions must come from the accepted Phase14 model lineage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-11 | Spoofing | `uefa_euro_validate_nl_eligibility_handoff()` | high | mitigate | Require accepted Phase15 source/status metadata and join only on stable `team_id`; reject ambiguous or display-name-only matches. |
| T-16-12 | Tampering | host-capacity branch selection | critical | mitigate | Derive branch from validated host allocation and official topology metadata; reject unknown counts and preserve capacity conservation. |
| T-16-13 | Information Disclosure | probability tables for blocked paths | high | mitigate | Gate simulation on active confirmed-kickoff state and return typed suppression/unavailable results with no probability rows. |
| T-16-14 | Repudiation | seeded simulation result | medium | mitigate | Persist seed, simulation count, model/source/rules/cutoff lineage, and deterministic replay hash in returned metadata. |
| T-16-15 | Denial of Service | malformed handoff or draw-condition input | medium | mitigate | Validate bounded schemas before eligibility/pot construction and stop with unresolved status rather than iterating unknown data. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Active qualification simulation, host capacity, Nations League eligibility, and fail-closed probability state are Tasks 1-2. |
| REQ | SIM-02 is covered by seeded single/two-leg simulation, topology branches, and probability suppression. |
| RESEARCH | Phase15 RNG/model contracts, Phase14 calibrated authority, official three-branch topology, Nations League fallback order, and no invented draw conditions are mapped to functions/tests. |
| CONTEXT | D-05, D-07, D-09, D-10, D-11, and D-12 are implemented/cited here; activation D-01-D-04 and pre-draw D-13-D-16 originate in Plan 01 and are asserted at this boundary. |
| Hooks | No package install, external API/SDK/service, or assumption-delta trigger is introduced; no hook artifact is required. |

<verification>
Run the focused Phase16 test after each task. Before phase completion, run the Phase13-15 relevant suites and the full `tests/testthat` directory. Keep the focused simulation count small; the validation command remains the one in 16-VALIDATION.md.
</verification>

<success_criteria>
The simulation layer returns reproducible qualification outcomes for each accepted official topology and returns explicit empty/suppressed states for every precondition or external-eligibility failure.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-03-SUMMARY.md` when done.
</output>
