---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test_phase16_euro_qualifying.R
  - .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R
  - .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - SIM-02
  - SIM-04
must_haves:
  truths:
    - "A dedicated Phase 16 smoke command can run independently of the full repository suite and fails fast when the Phase 16 contract regresses."
    - "The known Phase 13 full-suite failure is fingerprinted before Phase 16 implementation and is reported separately from any new Phase 16 failure by a runnable capture/compare helper."
    - "Baseline capture persists its record even when the child full suite exits nonzero; capture success means the record was written, not that the full suite was green."
    - "All later plans share deterministic fixtures for pre_draw, active-after-draw with zero completed results, four covered hosts, topology branches, and the Phase 15 interim-ranking handoff."
  artifacts:
    - path: "tests/testthat/test_phase16_euro_qualifying.R"
      provides: "Repository-root-aware focused harness, phase16_smoke test, and reusable local fixtures"
      contains: "phase16_smoke"
    - path: ".planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md"
      provides: "Captured full-suite exit status, normalized failing identities, output fingerprint, and known-baseline disposition"
      contains: "156 fixture IDs paired with zero-length normalized source columns"
    - path: ".planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R"
      provides: "Runnable child-process full-suite baseline capture with explicit child-status persistence and no-new-failure comparison"
      contains: "--capture"
  key_links:
    - from: "tests/testthat/test_phase16_euro_qualifying.R"
      to: "16-VALIDATION.md"
      via: "phase16_smoke filter and focused test command"
    - from: "16-BASELINE.md"
      to: ".planning/STATE.md"
      via: "known baseline failure signature and separate acceptance disposition"
---

<objective>
Establish the Wave 0 Phase 16 validation harness, deterministic fixtures, and full-suite baseline fingerprint before implementation begins.

Purpose: Give every later plan a fast smoke path, shared adversarial fixtures, and an honest regression baseline so a persistent Phase 13 failure is never misreported as a Phase 16 pass.
Output: `tests/testthat/test_phase16_euro_qualifying.R`, `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R`, and `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md` with the captured baseline.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@tests/testthat/test_phase15_nations_league.R
@tests/testthat/test_phase14_state_bundle.R
</context>

<dependency_graph>
This Wave 0 plan has no implementation dependency. It creates the focused harness, fixtures, and runnable baseline comparator consumed by Plans 01-05; its baseline record is the comparison authority for the known full-suite failure.
</dependency_graph>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the Wave 0 smoke harness through baseline capture</name>
  <files>tests/testthat/test_phase16_euro_qualifying.R, .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R, .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md</files>
  <read_first>
    Read `.planning/STATE.md` around the recorded full-suite blocker, the Phase 15 repository-root-aware test loader, the Phase 14 state-bundle tests, 16-VALIDATION.md's baseline policy, and 16-PATTERNS.md replay requirements. Read D-01, D-04, D-10, D-14, and D-16 before naming fixture statuses.
  </read_first>
  <behavior>
    - Test 1: the filtered `phase16_smoke` test runs with `Rscript --vanilla` and validates a minimal schema/status fixture without sourcing a network or external package.
    - Test 2: `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R --capture --baseline .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md` runs the full suite in a child process and writes the baseline even when that child exits nonzero, retaining the exit status, sorted normalized failing test identities, combined-output SHA-256, capture date, and known-baseline disposition.
    - Test 3: capture mode exits zero after successfully writing the baseline regardless of the child suite exit status, and exits nonzero only for helper argument, launch, normalization, or write failure.
    - Test 4: the same helper's `--compare` mode reruns the full suite, reports its current exit status/failure identities/output hash, returns zero for no failures or exactly the recorded pre-existing failure identity, and exits nonzero only for a new or unparseable failure while leaving a persistent known baseline explicitly non-green.
  </behavior>
  <acceptance_criteria>
    - The focused test file is repository-root-aware, uses the existing source-style test conventions, and contains a named `phase16_smoke` test selected by the fast command in 16-VALIDATION.md.
    - `16-baseline-check.R` is a runnable validation helper at the exact path above with separate capture and comparison modes; it never discards or masks the child process status even when capture itself succeeds.
    - `16-BASELINE.md` records the exact full-suite command, the nonzero child-process exit status when present, sorted normalized failing test identities, combined-output SHA-256, capture date, and the STATE.md failure signature `156 fixture IDs paired with zero-length normalized source columns`.
    - Capture mode writes `16-BASELINE.md` and returns zero when the record is successfully persisted even if the child full suite is nonzero; it returns nonzero only when the helper cannot create a trustworthy record.
    - The baseline record includes a known-baseline disposition that permits only the recorded pre-existing failure identity/signature; comparison ignores output-hash drift as a gate and exits nonzero only for a new or unparseable failure.
    - The baseline record explicitly says a persistent known failure is not a claim that the full suite is green; Phase 16 acceptance is focused/relevant suites passing plus no new full-suite failures.
    - D-14 and D-16 remain testable from the harness without inventing groups, fixtures, standings, or probability rows.
  </acceptance_criteria>
  <action>
    Create the focused test file with a repository-root resolver, source helper, assertion helpers, and the smallest smoke test that future plans can run before their production symbols exist. Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R` as a validation-only CLI with `--capture --baseline <path>` and `--compare --baseline <path>` modes. Its child-process command is exactly `Rscript --vanilla -e 'testthat::test_dir("tests/testthat", reporter="summary")'`; capture combined stdout/stderr, preserve the child exit status, normalize failing identities into sorted `test_file::test_name` entries, hash the complete combined output with the repository's existing SHA-256 convention, and write/read the baseline disposition in `16-BASELINE.md`. Define capture mode to return zero after the baseline record is written even when the child suite is nonzero, and to return nonzero only for a helper/recording failure. Define compare mode to return zero for zero failures or exactly the recorded pre-existing identity/signature, treat the output hash as evidence rather than a standalone gate, and return nonzero only for a new or unparseable failure while reporting a persistent known baseline as non-green. Keep all fixture data local and cite D-01, D-04, D-10, D-14, and D-16 in the test/helper names or assertions.
  </action>
  <verify>
    <automated>set +e; Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="phase16_smoke", stop_on_failure=TRUE, reporter="summary")'; smoke_status=$?; Rscript --vanilla .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-baseline-check.R --capture --baseline .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md; capture_status=$?; if [ "$smoke_status" -ne 0 ] || [ "$capture_status" -ne 0 ]; then exit 1; fi</automated>
  </verify>
  <done>The Wave 0 smoke test and exact-path baseline helper are runnable; the full-suite baseline records status, normalized identities, output hash, and disposition, and the known Phase 13 failure is explicitly separated from new Phase 16 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Seed shared activation, host, topology, and handoff fixtures</name>
  <files>tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read 16-RESEARCH.md resolved open-question contract, 16-UI-SPEC.md pre-draw and active-after-draw payload fields, the Phase 15 `projected_rankings` schema, and 16-PATTERNS.md stable-ID/replay patterns.
  </read_first>
  <behavior>
    - Test fixture: a pre_draw bundle has schema-valid empty collections and exact D-16 message fields.
    - Test fixture: an active-after-draw bundle has real groups, stable teams, confirmed fixture kickoffs, zero completed results, and zero completed standings while remaining distinct from pre_draw.
    - Test fixture: four covered host associations can be ranked, and a Phase 15 handoff includes valid `team_id`, `ranking_scope = interim_overall`, and `ranking_stage = interim_overall` rows plus invalid final-only variants.
    - Test fixture: zero, one, two, and more-than-two host-usage cases and all three topology branch inputs are deterministic and locally replayable.
  </behavior>
  <acceptance_criteria>
    - Every later plan can call local constructors for the active zero-results case, the pre_draw case, the four-host case, each host-usage branch, and valid/wrong-stage/missing Phase 15 handoffs.
    - Fixtures use stable `team_id`, `group_id`, `fixture_id`, slot, source, rules, and kickoff fields; display names are never join keys.
    - The active zero-results fixture asserts `lifecycle_state` is active/scheduled rather than `pre_draw`, groups and confirmed fixtures are populated, and results/standings are empty.
  </acceptance_criteria>
  <action>
    Add deterministic base-R fixture constructors and small invariant helpers to the focused test file. Include explicit D-05/D-06 host-slot fields, four covered hosts with rank evidence, the three official topology inputs, and Phase 15 rows that distinguish the accepted `interim_overall` ranking stage from final-only or unresolved rows. Keep fixtures self-contained, source-lineage-bearing, and small enough for the fast smoke command; do not add production code or Phase 17 renderer/refresh behavior in this Wave 0 plan.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="phase16_smoke|fixture", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>Shared local fixtures cover every checker-requested edge, including active-after-draw with zero results, four hosts, all host branches, and the exact Phase 15 interim-stage handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full repository test process -> baseline record | Test output and exit status are untrusted evidence until captured with a reproducible fingerprint. |
| local synthetic fixture -> later contract tests | Fixture identity, status, source, and ranking-stage values must be explicit so later tests cannot pass through accidental defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-00-01 | Repudiation | `16-BASELINE.md` | medium | mitigate | Record command, exit status, combined output hash, known failure text, and capture date before implementation. |
| T-16-00-02 | Tampering | focused synthetic fixtures | medium | mitigate | Use stable IDs, explicit status fields, and assertions for active/pre_draw separation, host cardinality, and interim ranking stage. |
| T-16-SC | Tampering | test/runtime dependencies | high | mitigate | No package install is planned; the existing R/testthat environment is asserted by the Wave 0 smoke command. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Wave 0 makes truthful pre-draw/active validation and full-suite regression comparison executable. |
| REQ | COMP-03, COMP-04, SIM-02, and SIM-04 receive shared fixture and smoke coverage before implementation. |
| RESEARCH | Resolved source acquisition, draw-condition, host-scenario, Phase 15 handoff, stable-ID, and replay contracts become local fixtures. |
| CONTEXT | D-01, D-04, D-05, D-06, D-10, D-14, and D-16 are cited in task behavior/actions and fixture assertions. |
| Hooks | No package, external service, or network acquisition is introduced. |

<verification>
Run the filtered smoke test after each task. Capture the baseline through the exact-path helper before implementation. At the final phase gate run its comparison mode; a persistent STATE.md baseline failure is reported separately, remains non-green, and does not count as a new Phase 16 failure.
</verification>

<success_criteria>
The focused test harness runs quickly, shared adversarial fixtures are available to every implementation plan, and the known repository baseline is captured and compared by an executable helper without being misclassified as a passing full suite.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-00-SUMMARY.md` when done.
</output>
