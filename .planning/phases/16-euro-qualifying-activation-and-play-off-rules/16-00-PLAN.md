---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test_phase16_euro_qualifying.R
  - .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - SIM-02
  - SIM-04
must_haves:
  truths:
    - "A dedicated Phase 16 smoke command can run independently of the full repository suite and fails fast when the Phase 16 contract regresses."
    - "The known Phase 13 full-suite failure is fingerprinted before Phase 16 implementation and is reported separately from any new Phase 16 failure."
    - "All later plans share deterministic fixtures for pre_draw, active-after-draw with zero completed results, four covered hosts, topology branches, and the Phase 15 interim-ranking handoff."
  artifacts:
    - path: "tests/testthat/test_phase16_euro_qualifying.R"
      provides: "Repository-root-aware focused harness, phase16_smoke test, and reusable local fixtures"
      contains: "phase16_smoke"
    - path: ".planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md"
      provides: "Captured full-suite baseline exit status, output fingerprint, and known-failure signature"
      contains: "156 fixture IDs paired with zero-length normalized source columns"
  key_links:
    - from: "tests/testthat/test_phase16_euro_qualifying.R"
      to: "16-VALIDATION.md"
      via: "phase16_smoke filter and focused test command"
    - from: "16-BASELINE.md"
      to: ".planning/STATE.md"
      via: "known baseline failure signature and separate acceptance disposition"
---

<objective>
Establish the Wave 0 Phase 16 validation harness, deterministic fixtures, and full-suite baseline fingerprint before implementation begins.

Purpose: Give every later plan a fast smoke path, shared adversarial fixtures, and an honest regression baseline so a persistent Phase 13 failure is never misreported as a Phase 16 pass.
Output: `tests/testthat/test_phase16_euro_qualifying.R` with Wave 0 helpers and `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md` with the captured baseline.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@tests/testthat/test_phase15_nations_league.R
@tests/testthat/test_phase14_state_bundle.R
</context>

<dependency_graph>
This Wave 0 plan has no implementation dependency. It creates the focused harness and fixtures consumed by Plans 01-05, and its baseline record is the comparison authority for the known full-suite failure.
</dependency_graph>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the Wave 0 smoke harness through baseline capture</name>
  <files>tests/testthat/test_phase16_euro_qualifying.R, .planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-BASELINE.md</files>
  <read_first>
    Read `.planning/STATE.md` around the recorded full-suite blocker, the Phase 15 repository-root-aware test loader, the Phase 14 state-bundle tests, and 16-PATTERNS.md replay requirements. Read D-01, D-04, D-10, D-14, and D-16 before naming fixture statuses.
  </read_first>
  <behavior>
    - Test 1: the filtered `phase16_smoke` test runs with `Rscript --vanilla` and validates a minimal schema/status fixture without sourcing a network or external package.
    - Test 2: the baseline command runs the full suite in a child process, records exit status plus combined-output SHA-256, and records the known Phase 13 fixture-seed failure as a baseline rather than a green result.
    - Test 3: the baseline record states that later acceptance requires no new failing test signature, while a persistent known baseline is reported separately.
  </behavior>
  <acceptance_criteria>
    - The focused test file is repository-root-aware, uses the existing source-style test conventions, and contains a named `phase16_smoke` test selected by the fast command in 16-VALIDATION.md.
    - `16-BASELINE.md` records the exact full-suite command, exit status, combined-output fingerprint, capture date, and the STATE.md failure signature `156 fixture IDs paired with zero-length normalized source columns`.
    - The baseline record explicitly says a persistent known failure is not a claim that the full suite is green; Phase 16 acceptance is focused/relevant suites passing plus no new full-suite failures.
    - D-14 and D-16 remain testable from the harness without inventing groups, fixtures, standings, or probability rows.
  </acceptance_criteria>
  <action>
    Create the focused test file with a repository-root resolver, source helper, assertion helpers, and the smallest smoke test that future plans can run before their production symbols exist. Add a child-process baseline capture procedure that runs `Rscript --vanilla -e 'testthat::test_dir("tests/testthat", reporter="summary")'`, captures combined output and exit status, hashes the output with the repository's existing SHA-256 convention, and writes the result to `16-BASELINE.md`. Carry the known STATE.md failure as an expected baseline fingerprint, not as a swallowed success. Keep all fixture data local and cite D-01, D-04, D-10, D-14, and D-16 in the test/helper names or assertions.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="phase16_smoke", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>The Wave 0 smoke test is runnable, the full-suite baseline is captured with a reproducible fingerprint, and the known Phase 13 failure is explicitly separated from new Phase 16 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Seed shared activation, host, topology, and handoff fixtures</name>
  <files>tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read 16-RESEARCH.md resolved open-question contract, 16-UI-SPEC.md pre-draw and active-after-draw payload fields, the Phase 15 `projected_rankings` schema, and 16-PATTERNS.md stable-ID/replay patterns.
  </read_first>
  <behavior>
    - Test fixture: a pre_draw bundle has schema-valid empty collections and exact D-16 message fields.
    - Test fixture: an active-after-draw bundle has real groups, stable teams, confirmed fixture kickoffs, zero completed results, and zero completed standings while remaining distinct from pre_draw.
    - Test fixture: four covered host associations can be ranked, and a Phase 15 handoff includes valid `team_id`, `ranking_scope = interim_overall`, and `ranking_stage = interim_overall` rows plus invalid final-only variants.
    - Test fixture: zero, one, two, and more-than-two host-usage cases and all three topology branch inputs are deterministic and locally replayable.
  </behavior>
  <acceptance_criteria>
    - Every later plan can call local constructors for the active zero-results case, the pre_draw case, the four-host case, each host-usage branch, and valid/wrong-stage/missing Phase 15 handoffs.
    - Fixtures use stable `team_id`, `group_id`, `fixture_id`, slot, source, rules, and kickoff fields; display names are never join keys.
    - The active zero-results fixture asserts `lifecycle_state` is active/scheduled rather than `pre_draw`, groups and confirmed fixtures are populated, and results/standings are empty.
  </acceptance_criteria>
  <action>
    Add deterministic base-R fixture constructors and small invariant helpers to the focused test file. Include explicit D-05/D-06 host-slot fields, four covered hosts with rank evidence, the three official topology inputs, and Phase 15 rows that distinguish the accepted `interim_overall` ranking stage from final-only or unresolved rows. Keep fixtures self-contained, source-lineage-bearing, and small enough for the fast smoke command; do not add production code or Phase 17 renderer/refresh behavior in this Wave 0 plan.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="phase16_smoke|fixture", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>Shared local fixtures cover every checker-requested edge, including active-after-draw with zero results, four hosts, all host branches, and the exact Phase 15 interim-stage handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full repository test process -> baseline record | Test output and exit status are untrusted evidence until captured with a reproducible fingerprint. |
| local synthetic fixture -> later contract tests | Fixture identity, status, source, and ranking-stage values must be explicit so later tests cannot pass through accidental defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-00-01 | Repudiation | `16-BASELINE.md` | medium | mitigate | Record command, exit status, combined output hash, known failure text, and capture date before implementation. |
| T-16-00-02 | Tampering | focused synthetic fixtures | medium | mitigate | Use stable IDs, explicit status fields, and assertions for active/pre_draw separation, host cardinality, and interim ranking stage. |
| T-16-SC | Tampering | test/runtime dependencies | high | mitigate | No package install is planned; the existing R/testthat environment is asserted by the Wave 0 smoke command. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Wave 0 makes truthful pre-draw/active validation and full-suite regression comparison executable. |
| REQ | COMP-03, COMP-04, SIM-02, and SIM-04 receive shared fixture and smoke coverage before implementation. |
| RESEARCH | Resolved source acquisition, draw-condition, host-scenario, Phase 15 handoff, stable-ID, and replay contracts become local fixtures. |
| CONTEXT | D-01, D-04, D-05, D-06, D-10, D-14, and D-16 are cited in task behavior/actions and fixture assertions. |
| Hooks | No package, external service, or network acquisition is introduced. |

<verification>
Run the filtered smoke test after each task. The captured full-suite baseline is compared again at the final phase gate; a persistent STATE.md baseline failure is reported separately and does not count as a new Phase 16 failure.
</verification>

<success_criteria>
The focused test harness runs quickly, shared adversarial fixtures are available to every implementation plan, and the known repository baseline is fingerprinted without being misclassified as a passing full suite.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-00-SUMMARY.md` when done.
</output>
