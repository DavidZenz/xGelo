---
phase: 16-euro-qualifying-activation-and-play-off-rules
plan: 02
type: execute
wave: 3
depends_on:
  - 16-01
files_modified:
  - R/competition/uefa_euro_rules.R
  - tests/testthat/test_phase16_euro_qualifying.R
autonomous: true
requirements:
  - COMP-04
must_haves:
  truths:
    - "Group winners, best runners-up, host places, and remaining play-off entrants are resolved in the official order with no double counting."
    - "Host places are explicit conditional slots, and actual host usage becomes deterministic only after required completed standings/results; active scenario rows are never collapsed into one guessed branch."
    - "When more than two host associations are covered, the two highest-ranked covered hosts are selected according to the accepted rules input, only those two places consume host capacity, and remaining/unselected slots remain visible in the ledger."
    - "Group ranking and runner-up comparison use the official tie-break and comparability rules, including Article 23 treatment of groups of five."
    - "Incomplete additional draw conditions or unsupported topology become unresolved_draw_conditions/unsupported_topology rather than an inferred format."
  artifacts:
    - path: "R/competition/uefa_euro_rules.R"
      provides: "Official group-ranking, best-runner-up, host-selection, allocation-ledger, draw-condition, and play-off-topology contracts"
      contains: "allocate_euro_places"
    - path: "tests/testthat/test_phase16_euro_qualifying.R"
      provides: "Mixed-size groups, zero/one/two host branches, four-host top-two selection, conservation, scenario, and unresolved-condition tests"
      contains: "unresolved_draw_conditions"
  key_links:
    - from: "rank_euro_group() and rank_euro_overall()"
      to: "allocate_euro_places()"
      via: "stable-ID ranking rows and Article 15/23 evidence"
    - from: "accepted completed standings/results"
      to: "host selector and topology branch"
      via: "deterministic top-two covered-host selection only after required results"
    - from: "versioned accepted draw_conditions input"
      to: "uefa_euro_playoff_topologies()"
      via: "condition completeness/hash gate before topology or probability eligibility"
---

<objective>
Implement the official UEFA EURO qualifying ranking, host allocation, best-runner-up, and play-off topology contracts.

Purpose: Turn an accepted active competition bundle into a source-lined, capacity-conserving qualification ledger without inventing host usage, draw constraints, or a familiar bracket when official inputs are incomplete.
Output: Expanded EURO rules module with ranking, host selection, allocation, topology, and draw-condition validation plus focused tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-CONTEXT.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-RESEARCH.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-PATTERNS.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-UI-SPEC.md
@.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-VALIDATION.md
@R/competition/uefa_euro_rules.R
@R/competition/uefa_nations_league_rules.R
@R/competition/uefa_nations_league_simulation.R
@R/competition/state_bundle.R
@tests/testthat/test_phase15_nations_league.R
</context>

<dependency_graph>
Plan 02 depends on Plan 01 activation identifiers and the Wave 0 fixtures. It extends the same pure rules module and focused test file, so Plan 03 consumes its final ledger/topology contract only after this plan's tests pass.
</dependency_graph>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one completed group through ranking, host allocation, and place conservation</name>
  <files>R/competition/uefa_euro_rules.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read the Phase 15 ranking/transition functions for typed status rows and evidence lineage, 16-RESEARCH.md Articles 14-17 and 23-24 notes, and 16-UI-SPEC.md ledger/host fields. Use the activation functions from Plan 01 as the input boundary and the Wave 0 active-after-draw fixture; do not recreate identifiers.
  </read_first>
  <behavior>
    - Test 1: one completed group is ranked with points, goal difference, goals scored, and official tie-break evidence IDs.
    - Test 2: one covered host association receives an explicit reserved slot, and a direct qualifier consumes that slot once in the ledger.
    - Test 3: a valid active bundle with zero completed results keeps host usage and topology scenario status unresolved/scenario-keyed rather than choosing one actual host count.
    - Test 4: after required completed results resolve the host association, the same stable input produces a deterministic topology and place-capacity summary with source/rules metadata.
    - Test 5: an unresolved host guarantee produces `host_place_unresolved` and suppresses affected qualification eligibility.
  </behavior>
  <acceptance_criteria>
    - `uefa_euro_playoff_topologies()` returns the three official branches: two reserved places with 8 entrants and 2 single-leg paths, one reserved place with 12 entrants and 3 single-leg paths, and no reserved places with 8 entrants in 4 seeded/unseeded home-and-away ties.
    - `rank_euro_group()` and `rank_euro_overall()` return deterministic, source-lined rows keyed by stable IDs with explicit blocked reasons when evidence is missing.
    - `allocate_euro_places()` emits the ordered ledger for group winners, host reservation/consumption, best-runner-up capacity, and remaining play-off slots without counting a host place twice.
    - D-05, D-06, D-07, and D-08 are visible through host association/slot, `consumes_capacity`, occupied/unused/unresolved status, direct host precedence, and lineage fields.
    - Scenario status is available before completed results, but actual host usage and the current topology branch are deterministic only after required results resolve the host decision.
  </acceptance_criteria>
  <action>
    Extend `R/competition/uefa_euro_rules.R` with `uefa_euro_playoff_topologies()`, `rank_euro_group()`, `rank_euro_overall()`, and `allocate_euro_places()`. Wire one completed synthetic group through the host-aware ledger and separately pass the active zero-results fixture through scenario status. Keep every row keyed by stable `team_id`, `group_id`, or slot ID. Use the official D-05-D-08 metadata and preserve scenario IDs/host usage status until completed results resolve them; do not encode a generic tournament shortcut or infer an unresolved host guarantee. Add source/rules revision fields and assert D-05, D-06, D-07, and D-08 in focused tests.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="ranking|host|scenario|allocation|phase16_smoke", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>One completed group produces a deterministic ranked table and host-aware conserved ledger, while an active zero-results bundle retains explicit unresolved/scenario host state.</done>
  <reversibility rating="reversible">The task adds pure ranking/allocation values and does not write a published bundle or mutate accepted source state.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete mixed-size ranking, four-host selection, draw conditions, and topology validation</name>
  <files>R/competition/uefa_euro_rules.R, tests/testthat/test_phase16_euro_qualifying.R</files>
  <read_first>
    Read the exact Phase 15 ranking conventions and Phase 14 arithmetic authority, 16-RESEARCH.md resolved additional-draw-condition and host-selection decisions, and 16-UI-SPEC.md status vocabulary. Confirm the accepted rules input fields before adding topology branches.
  </read_first>
  <behavior>
    - Test 1: Article 15 tie-breaks resolve a multi-team tie in documented order and retain the evidence IDs used for each comparison.
    - Test 2: Article 23 groups of five compare only eligible positions and exclude fifth-place matches from runner-up ranking evidence.
    - Test 3: best runners-up are selected only after direct and host allocation, with remaining runners-up routed to play-off slots and official lineage.
    - Test 4: zero, one, and two consumed host places select the corresponding official topology, with capacity conservation across every branch.
    - Test 5: a four-host synthetic case selects exactly the two highest-ranked covered hosts according to the accepted rules, marks only those two rows as consuming reserved capacity, and preserves the two remaining/unselected host slots as explicit unused/remaining rows.
    - Test 6: absent, partial, stale, or unrecognised accepted `draw_conditions` returns both `unresolved_draw_conditions` and `unsupported_topology` state and prevents a topology/probability-ready result.
  </behavior>
  <acceptance_criteria>
    - Ranking implements the researched official tie-break sequence and carries counted/excluded match IDs and comparison evidence rather than only final ranks.
    - `select_euro_best_runners_up()` runs after host/direct allocation, is unavailable until all comparison groups are complete, and never treats a host place as an extra direct place.
    - With more than two covered host associations, the official selector orders covered hosts by the accepted rank evidence, selects the two highest-ranked associations, sets `consumes_capacity = TRUE` only for those reserved slots, and preserves every other slot/remaining capacity explicitly.
    - `allocate_euro_places()` emits `host_reserved`, `host_reserved_occupied`, `host_reserved_unused`, `host_place_unresolved`, `direct`, `playoff_eligible`, `unsupported_topology`, and `unavailable` as applicable, with source/rules lineage.
    - Topology validation requires a versioned accepted `draw_conditions` input with source artifact ID and canonical hash. Incomplete conditions return `unresolved_draw_conditions` plus `unsupported_topology`; no guessed bracket is selected.
    - D-09, D-11, and D-12 are enforced by accepted-rules ordering, official branch cardinality, version/hash lineage, and no-selection-on-incomplete-input tests.
  </acceptance_criteria>
  <action>
    Implement the Article 15/23 ranking and best-runner-up selection, then complete `allocate_euro_places()` in the order direct qualifiers, host reservations, best runners-up, and remaining play-off entries. Add a rules-controlled host selector that handles zero, one, two, and more-than-two covered associations: after completed standings exist, retain only the two highest-ranked covered hosts for the two reserved places and keep other host associations/unused capacity in the ledger. Add `draw_conditions_version`, `draw_conditions_sha256`, and accepted source-artifact lineage to the topology input; if conditions are absent or incomplete, return `unresolved_draw_conditions` and `unsupported_topology` and leave derived qualification eligibility unresolved. Extend focused tests with mixed-size groups, the four-host synthetic case, all three official branches, place conservation, scenario preservation, and incomplete-condition suppression. Cite D-05, D-06, D-09, D-11, and D-12 in actions/assertions.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase16_euro_qualifying.R", filter="article15|article23|four_host|topology|draw_conditions|conservation", stop_on_failure=TRUE, reporter="summary")'</automated>
  </verify>
  <done>All official ranking and place-allocation branches are deterministic, four-host selection consumes only the official two reserved places, every slot is conserved, and incomplete draw conditions fail closed.</done>
  <reversibility rating="reversible">The rules remain pure and replayable; publication and simulation consume the versioned result later.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| match evidence -> ranking | Results, group membership, and home/away evidence are untrusted until identity and completeness checks pass. |
| host associations/ranks -> place capacity | More than two covered hosts and direct qualification can conflict; the allocation ledger is the control point. |
| accepted rules/draw conditions -> topology | A changed or incomplete official rule source must not select a topology by convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Tampering | `rank_euro_group()` and `rank_euro_overall()` | high | mitigate | Validate stable IDs, preserve evidence/exclusion IDs, and apply the explicit official tie-break order. |
| T-16-07 | Tampering | `allocate_euro_places()` | critical | mitigate | Apply direct, host, best-runner-up, and play-off capacity in one conservation ledger with explicit top-two host selection. |
| T-16-08 | Spoofing | host association/rank rows | high | mitigate | Require accepted source/rank lineage and expose selected, unused, or unresolved slot status rather than trusting display labels. |
| T-16-09 | Denial of Service | incomplete comparison groups/draw conditions | medium | mitigate | Bound comparisons to declared groups and return typed unresolved/unsupported states when required evidence is absent. |
| T-16-10 | Repudiation | topology and rules revision | medium | mitigate | Attach canonical ruleset/draw-condition/source revisions and deterministic hashes to every topology/allocation result. |
| T-16-SC | Tampering | R/test dependency installation | high | mitigate | No package install is introduced; existing dependencies are reused and asserted by Wave 0. |
</threat_model>

## Source Audit

| Source | Coverage in this plan |
|--------|-----------------------|
| GOAL | Official ranking, host allocation, best-runner-up order, all topology branches, four-host selection, and unresolved draw conditions are Tasks 1-2. |
| REQ | COMP-04 is covered by Articles 15/23 ranking, host capacity, versioned draw conditions, and topology branches. |
| RESEARCH | UEFA Articles 13-17/23-24, mixed-size group evidence, scenario preservation, two-reserved-place top-two host selection, and three capacity topologies are mapped to symbols/tests. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09, D-11, and D-12 are cited in task actions/criteria; D-10 is owned by Plan 03. |
| Hooks | No package install, external API/SDK/service, or assumption-delta trigger is introduced. |

<verification>
Run the filtered ranking/host/topology tests after each task. At the wave gate run the focused file plus Phase 14 standings/state and Phase 15 ranking regressions; compare the full-suite result to the Wave 0 baseline and report any persistent baseline separately.
</verification>

<success_criteria>
The rules module explains every supported EURO qualifying allocation through a source-lined conservation ledger, selects only the official two host places from a four-host case, preserves unresolved scenarios, and refuses incomplete draw conditions.
</success_criteria>

<output>
Create `.planning/phases/16-euro-qualifying-activation-and-play-off-rules/16-02-SUMMARY.md` when done.
</output>
