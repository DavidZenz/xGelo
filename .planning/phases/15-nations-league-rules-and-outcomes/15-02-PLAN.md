---
phase: 15-nations-league-rules-and-outcomes
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - R/competition/uefa_nations_league_rules.R
  - tests/testthat/test_phase15_nations_league.R
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "Group standings use phase14_compute_standings universal played/W/D/L/goals/points arithmetic and a Nations League adapter for the complete Article 15 ordering."
    - "Article 15 recursively reapplies head-to-head criteria to the remaining tied subset, then applies overall goal difference, goals, away goals, wins, away wins, discipline, and access-list position in order; absent discipline or access-list evidence returns ordering_status = blocked and missing_rule_input without fabricating contiguous ranks."
    - "Article 19 individual-league and interim rankings are cardinality-aware: fourth-place results are excluded only for positions 1-3 when that team's group actually has a fourth-place team, while three-team League D groups retain all of their results."
    - "Direct transitions and A/B, B/C, and conditional C/D play-off selectors emit exact rank bands and explicit unresolved status when EURO play-off eligibility is not supplied."
  artifacts:
    - path: R/competition/uefa_nations_league_rules.R
      provides: "Article 15 group rank adapter, Article 19 cross-group rankings, interim rank bands, and transition selectors"
      contains: "uefa_nl_make_standings_adapter, uefa_nl_rank_group, uefa_nl_rank_individual_league, uefa_nl_rank_interim_overall, uefa_nl_select_transition_slots"
    - path: tests/testthat/test_phase15_nations_league.R
      provides: "Adversarial Article 15/19 ranking and transition tests"
  key_links:
    - from: R/competition/standings.R
      to: R/competition/uefa_nations_league_rules.R
      via: "phase14_compute_standings receives a closure from uefa_nl_make_standings_adapter and retains the universal metrics"
      pattern: "uefa_nl_make_standings_adapter"
    - from: R/competition/uefa_nations_league_rules.R
      to: tests/testthat/test_phase15_nations_league.R
      via: "synthetic tied groups, missing inputs, fourth-place filters, and transition rank bands"
      pattern: "uefa_nl_rank_individual_league"
    - from: R/competition/uefa_nations_league_rules.R
      to: R/competition/standings.R
      via: "typed missing-rule-input preflight prevents blocked NA ranks from entering phase14_standings_adapter_ranks(); the state wrapper clears provisional ranks before outcomes"
      pattern: "ordering_status.*blocked"
  assumptions:
    - id: EDGE-COMP-02-01
      status: resolved
      statement: "Ranking selectors describe legal participant slots and do not become official downstream fixture rows until the separate stage capture supplies source lineage."
      resolution: "Transition rows retain selector status and rank lineage; only the Plan 15-01 capture loader can admit official/completed stage IDs."
    - id: EDGE-SIM-01-01
      status: resolved
      statement: "C/D eligibility and other external state are explicit inputs, not inferred from an absent row."
      resolution: "Missing EURO eligibility remains unresolved_external_eligibility and is carried to simulation/outcomes unchanged."
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Never use random or team-name ordering as an Article 15/19 substitute when a required rule input is missing."
    - "[FLAGGED-UNVERIFIED] Never remove fourth-place results from League D or from a fourth-placed team's own comparison."
    - "[FLAGGED-UNVERIFIED] Never infer C/D play-off eligibility from an absent EURO qualifying row."
---

<objective>
Implement the official Nations League ranking and transition adapter on top of Phase 14's universal standings arithmetic.

Purpose: Turn completed league-phase results into deterministic Article 15 group ranks, Article 19 individual/interim ranks, and exact direct/play-off participant selectors without contaminating forecast inputs.
Output: Extended `R/competition/uefa_nations_league_rules.R` and focused adversarial ranking coverage.
</objective>

<dependencies>
Plan `15-01` must provide the immutable ruleset/topology, stage IDs/status vocabulary, and Wave 0 test harness. Phase 14 `phase14_compute_standings()` and its cutoff/source-bundle validation remain the arithmetic authority.
</dependencies>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-nations-league-rules-and-outcomes/15-RESEARCH.md
@.planning/phases/15-nations-league-rules-and-outcomes/15-PATTERNS.md
@.planning/phases/15-nations-league-rules-and-outcomes/15-VALIDATION.md
@R/competition/standings.R
@R/competition/uefa_nations_league_rules.R
@R/competition/match_state.R
@tests/testthat/test_phase14_standings.R
@tests/testthat/test_phase15_nations_league.R
</context>

## Spec-less edge assumptions

- Planning resolution `COMP-02 EDGE-COMP-02-01`: ranking outputs identify legal participant slots and never promote a selector into an official fixture row.
- Planning resolution `SIM-01 EDGE-SIM-01-01`: at the current EURO `pre_draw` state, `uefa_nl_select_transition_slots()` returns `unresolved_external_eligibility` for C/D rather than treating missing evidence as “not eligible.”

## Artifacts this phase produces

- `uefa_nl_make_standings_adapter(match_rows, discipline_points, access_list, group_id, rules)`: closure accepted by `phase14_compute_standings()`.
- `uefa_nl_rank_group(standings, match_rows, discipline_points, access_list, rules)`: Article 15 ordered ranks plus trace/status fields.
- `uefa_nl_group_tiebreak_trace(...)`: deterministic criterion/subset audit rows.
- `uefa_nl_blocked_ordering_result(...)` and `uefa_nl_build_group_standings_state(...)`: typed `ordering_status = blocked`/`missing_rule_input` output and a state wrapper that never passes fake contiguous ranks to `phase14_compute_standings()` and clears provisional ranks before state/outcomes publication.
- `uefa_nl_rank_individual_league(group_standings, match_rows, rules)`: Article 19 per-league ranking with counted/excluded match lineage.
- `uefa_nl_rank_interim_overall(individual_rankings, rules)`: cardinality-aware A 1-16, B 17-32, C 33-48, and D 49 through `48 + nD` rank bands.
- `uefa_nl_select_transition_slots(interim_rankings, euro_playoff_eligibility, rules)`: direct promotion/relegation and A/B, B/C, C/D selector rows.
- Ranking fields: `ranking_scope`, `league`, `group_id`, `team_id`, `group_position`, `computed_rank`, `interim_rank`, `counted_match_ids`, `excluded_match_ids`, `discipline_points`, `access_list_position`, `ordering_status`, `missing_rule_input`, `suppression_reason`, `ruleset_version`, `ruleset_sha256`, and `row_sha256`.
- Transition fields: `stage_id`, `transition_type`, `higher_league_team_id`, `lower_league_team_id`, `team_id`, `higher_league_rank`, `lower_league_rank`, `first_leg_home_team_id`, `eligibility_status`, `selection_status`, `unresolved_reason`, `ruleset_version`, `ruleset_sha256`, and `row_sha256`.

<tasks>

<task type="auto">
  <name>Task 15-02-01: Implement recursive Article 15 group ordering</name>
  <read_first>
    - R/competition/uefa_nations_league_rules.R
    - R/competition/standings.R
    - R/competition/match_state.R
    - tests/testthat/test_phase14_standings.R
    - tests/testthat/test_phase15_nations_league.R
    - .planning/phases/15-nations-league-rules-and-outcomes/15-RESEARCH.md
    - .planning/phases/15-nations-league-rules-and-outcomes/15-PATTERNS.md
    - .planning/phases/14-shared-competition-state-and-forecast-layer/14-14-SUMMARY.md
  </read_first>
  <files>R/competition/uefa_nations_league_rules.R, tests/testthat/test_phase15_nations_league.R</files>
  <action>Add `uefa_nl_group_tiebreak_trace()`, `uefa_nl_rank_group()`, `uefa_nl_blocked_ordering_result()`, `uefa_nl_build_group_standings_state()`, and `uefa_nl_make_standings_adapter()` to the rules module. Follow `phase14_compute_standings()`, `phase14_standings_adapter_ranks()`, and the supplied-adapter test at `tests/testthat/test_phase14_standings.R:240-261`. `uefa_nl_make_standings_adapter()` must close over canonical completed result rows, group ID, discipline points, access-list positions, and the immutable ruleset, attach the exact adapter ID, and return the ordered `team_id`/`computed_rank` result required by `phase14_compute_standings()` only after its preflight proves every required rule input is present. The adapter must consume Phase 14 computed metrics and never calculate a second played/W/D/L/goals/points reducer. Implement Article 15 in the exact sequence: head-to-head points, head-to-head goal difference, head-to-head goals, recursive reapplication to the remaining tied subset, overall goal difference, overall goals, overall away goals, wins, away wins, lower disciplinary points, and higher access-list position. Record each criterion, tied subset, counted match IDs, and decision in `uefa_nl_group_tiebreak_trace()`. Require complete paired scores and evidence already accepted by the Phase 14 cutoff. When discipline points or access-list positions are missing, return a typed `phase15_nl_missing_rule_input` condition and `uefa_nl_blocked_ordering_result()` with every `computed_rank = NA_integer_`, `ordering_status = blocked`, `missing_rule_input` listing the exact missing input names, `block_status = blocked`, and `suppression_reason = missing_rule_input`; `uefa_nl_make_standings_adapter()` must stop before returning ranks, so `phase14_standings_adapter_ranks()` never receives fake contiguous values. `uefa_nl_build_group_standings_state()` may call `phase14_compute_standings()` without a ruleset adapter to retain universal arithmetic, but must immediately clear its provisional `computed_rank` values and emit the blocked ordering result before state/outcomes consume it. Add synthetic four-team tied groups covering recursive subsets and all eleven criteria, plus a three-team group proving the same adapter works without a fourth-place row; use a spy around `phase14_compute_standings()`/`phase14_standings_adapter_ranks()` to prove blocked inputs never pass fabricated ranks and that the returned state/outcomes row is unresolved or suppressed with no rank.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase15_nations_league.R", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>The ranking block passes for a complete four-team tie, recursive tied-subset reapplication, all eleven Article 15 criteria, missing discipline/access-list fail-closed behavior, and a three-team group; the Phase 14 reducer still owns all universal arithmetic.</acceptance_criteria>
  <done>Article 15 group ordering is deterministic, auditable, cutoff-safe, and callable through the existing Phase 14 adapter seam.</done>
</task>

<task type="auto">
  <name>Task 15-02-02: Implement Article 19 rankings and transition selectors</name>
  <read_first>
    - R/competition/uefa_nations_league_rules.R
    - R/competition/standings.R
    - tests/testthat/test_phase15_nations_league.R
    - .planning/phases/15-nations-league-rules-and-outcomes/15-RESEARCH.md
    - .planning/phases/15-nations-league-rules-and-outcomes/15-PATTERNS.md
    - data/competition/accepted/uefa_nations_league_2026_27/groups.csv
    - data/competition/accepted/uefa_nations_league_2026_27/standings.csv
  </read_first>
  <files>R/competition/uefa_nations_league_rules.R, tests/testthat/test_phase15_nations_league.R</files>
  <action>Add `uefa_nl_rank_individual_league()`, `uefa_nl_rank_interim_overall()`, and `uefa_nl_select_transition_slots()`. Follow the cardinality and snapshot-key validation style in `phase14_standings_validate_snapshot()` and the production table fixtures in `tests/testthat/test_phase14_standings.R:377-463`. For each team, derive its own Article 15 group position first, then compare an individual league using position, points, goal difference, goals, away goals, wins, away wins, discipline, and access-list position. Exclude matches against the team's own group fourth-place team only when the team's position is 1, 2, or 3 and that fourth-place team exists; preserve all matches for position 4 and for three-team groups, and record the exact counted and excluded match IDs. Build interim ranks dynamically from the resolved team count rather than requiring rank 55. Emit direct selectors for B winners to A, C winners to B, D winners to C, A fourth places to B, B fourth places to C, and C interim ranks 47/48 to D. Emit A/B pairs from A ranks 9-12 versus B ranks 21-24, B/C pairs from B ranks 25-28 versus C ranks 37-40, and C/D candidate pairs from C ranks 45-46 versus D ranks 51-52. Require an explicit `euro_playoff_eligibility` table for C/D resolution; when it is absent or incomplete, set `selection_status = unresolved`, `eligibility_status = unresolved_external_eligibility`, and `unresolved_reason = euro_playoff_eligibility_missing` while preserving the candidate rank lineage. Set the lower-league participant as first-leg home for all play-off selectors and return deterministic, uniquely keyed rows. If an upstream group state has `ordering_status = blocked`, preserve `computed_rank = NA`, map the affected transition to `selection_status = unresolved` and `unresolved_reason = missing_rule_input`, and do not manufacture a direct or play-off participant. Extend tests for fourth-place exclusion, three-team D rankings, dynamic D rank end, all direct transitions, exact A/B/B/C/C/D bands, the blocked discipline/access-list branch, and the unresolved C/D branch.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase15_nations_league.R", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>Article 19 rankings produce the expected counted/excluded match sets and dynamic rank bands; all direct transitions and A/B/B/C/C/D selector rows are deterministic; C/D remains explicitly unresolved without EURO eligibility evidence.</acceptance_criteria>
  <done>COMP-02 ranking and transition selection is complete without duplicating Phase 14 arithmetic or inventing missing external-state facts.</done>
</task>

</tasks>

<threat_model asvs_level="1">
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical completed results to Article 15 adapter | Scores, evidence time, and team IDs are trusted only after Phase 14 validation. |
| Discipline/access-list inputs to official rank | Missing or forged rule inputs can change promotion/relegation eligibility. |
| Article 19 selectors to stage graph | Rank bands become participant slots and must retain their derivation status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02-S | Spoofing | discipline/access-list identity | high | mitigate | Require unique canonical team IDs, explicit source/evidence fields, and complete inputs before official ordering. |
| T-15-02-T | Tampering | recursive tie-break and cross-group filters | high | mitigate | Persist criterion traces, counted/excluded match IDs, and adversarial tests for recursive subsets and three-team groups. |
| T-15-02-R | Repudiation | rank/transition decisions | medium | mitigate | Hash ranked rows, ruleset identity, and ordered transition selectors; keep blocked/unresolved reasons explicit. |
| T-15-02-I | Information Disclosure | future result influence | high | mitigate | Require Phase 14 cutoff-qualified results and use only the immutable pre-simulation state for ranking inputs. |
| T-15-02-E | Elevation of Privilege | C/D eligibility branch | high | mitigate | Treat absent EURO eligibility as unresolved, never as a permission to contest or cancel the play-off. |
</threat_model>

<verification>
Run the focused Phase 15 file after each task. At the wave gate, run the focused Phase 15 file and `Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase14_standings.R", stop_on_failure=TRUE)'`; run `Rscript --vanilla -e 'testthat::test_dir("tests/testthat")'` before phase verification and record repository failures that predate Phase 15 separately.
</verification>

<success_criteria>
- Article 15 group ranks are exact, recursive, deterministic, and fail closed on missing rule inputs.
- Article 19 comparisons apply fourth-place exclusion only where Article 19 permits it and support current three-team League D groups.
- Interim rank bands and all direct/play-off selectors match the 2026/27 rules, with C/D external eligibility explicit.
- All ranking rows retain ruleset, source, cutoff, and counted/excluded match lineage.
</success_criteria>

<output>
Create `.planning/phases/15-nations-league-rules-and-outcomes/15-02-SUMMARY.md` when executed.
</output>
