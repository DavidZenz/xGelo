---
phase: 15-nations-league-rules-and-outcomes
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/testthat/test_phase15_nations_league.R
autonomous: true
requirements: [COMP-02, SIM-01]
must_haves:
  truths:
    - "A focused Phase 15 test file exists before production tasks and contains deterministic fixtures for topology, rankings, stage results, calibrated sampling, provenance, and replay."
    - "Synthetic three-team and four-team groups, admitted and unresolved access-list/group-formation states, two-leg and single-leg stages, and completed QF/semi/final/play-off captures express the exact fields later production tasks must implement."
    - "The harness can run from the repository root and reports a precise missing-API failure for a later Phase 15 symbol instead of silently skipping the contract."
    - "The test harness can obtain a per-run temporary outcomes root only through phase15_test_output_root(), while production output remains bound to the registered durable competition root."
  artifacts:
    - path: tests/testthat/test_phase15_nations_league.R
      provides: "Wave 0 focused harness, synthetic fixtures, source loader, hash helpers, registered test-root helper, and named test sections for all Phase 15 contracts"
      contains: "phase15_test_three_team_group, phase15_test_four_team_group, phase15_test_access_list, phase15_test_group_formation, phase15_test_completed_stage_capture, phase15_test_output_root, phase15_test_require_api"
  key_links:
    - from: tests/testthat/test_phase15_nations_league.R
      to: R/competition/uefa_nations_league_rules.R
      via: "later tests source the rules module through a repository-root loader and exercise named rules, access-list, and group-formation APIs"
      pattern: "uefa_nl_"
    - from: tests/testthat/test_phase15_nations_league.R
      to: R/competition/uefa_nations_league_simulation.R
      via: "later stage-resolution and sampling blocks use the shared synthetic forecast/stage fixtures"
      pattern: "uefa_nl_sample_calibrated_outcome"
  assumptions:
    - id: EDGE-COMP-02-01
      status: resolved
      statement: "Before official downstream pairings exist, legal topology and draw slots are represented as projected or unresolved, never as official fixtures."
      resolution: "The fixture helpers include projection_run_id and draw_policy_id for projected rows and leave source_fixture_id empty until a separately admitted official capture is loaded."
    - id: EDGE-SIM-01-01
      status: resolved
      statement: "The simulator uses the calibrated 1X2 simplex reconciled with the stored score grid by a tested conditional scoreline policy."
      resolution: "The harness fixes the calibrated-simplex policy ID, seed, and empirical tolerance that Plan 15-03 must satisfy."
---

<objective>
Create the Wave 0 Nyquist harness and synthetic fixtures that make every later Phase 15 contract executable.

Purpose: Give Plans 15-01 through 15-06 one shared, repository-root-aware test surface before any production implementation is added.
Output: `tests/testthat/test_phase15_nations_league.R` with reusable group, stage-capture, forecast, replay, and lineage fixtures.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-nations-league-rules-and-outcomes/15-RESEARCH.md
@.planning/phases/15-nations-league-rules-and-outcomes/15-PATTERNS.md
@.planning/phases/14-shared-competition-state-and-forecast-layer/14-01-SUMMARY.md
@tests/testthat/test_phase14_standings.R
@tests/testthat/test_phase14_forecast_layer.R
@tests/testthat/test_phase14_state_bundle.R
@tests/testthat/test_uefa_nations_league_production.R
</context>

<tasks>

<wave_0_contract>
Wave 0 fixtures must include an admitted Article 13 access-list table with `access_list_position`, `league_id`, `group_id`, `draw_pot`, `group_formation_status`, and `source_artifact_id`, plus a current-source fixture whose positions are `NA_integer_` and whose status is `unresolved_access_list`. The same test file must define `phase15_test_output_root()` as the only temporary-root validator/registration path; production-root validation remains a separate runtime contract and rejects unregistered temporary paths.
</wave_0_contract>

<task type="auto">
  <name>Task 15-00-01: Create the Phase 15 focused harness and synthetic fixtures</name>
  <read_first>
    - tests/testthat/test_phase14_standings.R
    - tests/testthat/test_phase14_forecast_layer.R
    - tests/testthat/test_phase14_state_bundle.R
    - tests/testthat/test_uefa_nations_league_production.R
    - .planning/phases/15-nations-league-rules-and-outcomes/15-RESEARCH.md
    - .planning/phases/15-nations-league-rules-and-outcomes/15-PATTERNS.md
  </read_first>
  <files>tests/testthat/test_phase15_nations_league.R</files>
  <action>Create `tests/testthat/test_phase15_nations_league.R` using the repository-root loader style from `test_uefa_nations_league_production.R`, the synthetic fixture style from `test_phase14_standings.R`, the calibrated forecast fixture shape from `test_phase14_forecast_layer.R`, and the tree/hash snapshot pattern from `test_phase14_state_bundle.R`. Define `phase15_test_require_api()` to fail with the missing function name once a dependent production API is absent, `phase15_test_three_team_group()` and `phase15_test_four_team_group()` with tied standings plus discipline and access-list inputs, `phase15_test_two_leg_pair()` with reciprocal home/away legs and an invalid duplicate-home variant, `phase15_test_completed_stage_capture()` with completed `league_a_quarter_final`, `league_a_semi_final`, `league_a_final`, `a_b_playoff`, `b_c_playoff`, and `c_d_playoff` rows, and `phase15_test_calibrated_forecast()` with calibrated 1X2 values and a bounded score grid. The completed capture fixture must contain the exact fields `regulation_home_goals`, `regulation_away_goals`, `extra_time_home_goals`, `extra_time_away_goals`, `penalty_shootout_home_goals`, `penalty_shootout_away_goals`, `final_home_goals`, `final_away_goals`, and `completed_at_utc`, with nonzero extra-time and shootout examples. Add passing Wave 0 tests for fixture shape, Article 14 reciprocal-leg invariants, Article 17 host-association ordering expectations, exact status/provenance columns, calibrated probability sums, and deterministic row/hash helpers; leave API-specific blocks named for Plans 15-01 through 15-06 to extend without changing fixture names. Keep the file self-contained and do not add separate fixture files.
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase15_nations_league.R", stop_on_failure=TRUE)'</automated>
  </verify>
  <acceptance_criteria>The focused test file exists at the exact required path, the Wave 0 fixture tests pass without production Phase 15 APIs, access-list fixtures distinguish admitted metadata from `unresolved_access_list` without invented positions, `phase15_test_output_root()` is the sole temporary-root registration path, and the fixture builders expose all completed-score, host-order, source/provenance, and calibrated-sampling fields required by later plans.</acceptance_criteria>
  <done>Wave 0 provides one runnable Phase 15 harness and deterministic synthetic inputs for both COMP-02 and SIM-01 before Plan 15-01 begins.</done>
</task>

</tasks>

<threat_model asvs_level="1">
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixtures to future production APIs | Fixture values must exercise the real status, score, ordering, and lineage contracts rather than hiding missing fields. |
| Test loader to repository files | Root resolution must prevent tests from passing because they silently run against a different working directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-00-T | Tampering | synthetic contract fixtures | medium | mitigate | Assert exact columns, reciprocal legs, score semantics, host ordering, and deterministic hashes before dependent plans consume the fixtures. |
| T-15-00-R | Repudiation | focused test harness | medium | mitigate | Use repository-root paths, explicit API guards, fixed seeds, and named test blocks so absent implementations fail visibly. |
</threat_model>

<verification>
Run the focused Wave 0 file from the repository root; later plan APIs must extend this same file and retain its fixture constructors and hash helpers.
</verification>

<success_criteria>
- The exact Wave 0 test file exists before all dependent plans.
- Synthetic groups and stages cover three-team/four-team cardinality, reciprocal two-leg home/away, host association ordering, completed QF/semi/final/play-off score fields, calibrated probabilities, and replay metadata.
- The focused command is runnable in under 30 seconds and fails explicitly when a later named API is missing.
</success_criteria>

<output>
Create `.planning/phases/15-nations-league-rules-and-outcomes/15-00-SUMMARY.md` when executed.
</output>
