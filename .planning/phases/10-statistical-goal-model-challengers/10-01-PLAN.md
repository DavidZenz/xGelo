---
phase: 10-statistical-goal-model-challengers
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/testthat/helper_statistical_challengers.R
  - tests/testthat/test_statistical_penalized_poisson_design.R
  - tests/testthat/test_statistical_penalized_poisson_tuning.R
autonomous: true
requirements: [STAT-01]

must_haves:
  truths:
    - "Executable tests define the penalized-Poisson chronology, identifiability, cold-start, provenance, and exact-panel contracts before production implementation starts."
    - "The tests preserve the Phase 9 authority: 12 World Cups/Euros, 630 open fixtures, 609 rich fixtures, frozen/updating tracks, strict prior-only boundaries, equal-tournament weighting, G=40, shared schemas/scorer, and no WC2026 labels."
    - "Plan 10-03 Task 1 and Task 2 have separate RED files so each intermediate command can pass after only its owning production task is implemented."
  artifacts:
    - path: "tests/testthat/helper_statistical_challengers.R"
      provides: "Synthetic chronology, sparse-team, PMF-oracle, registry, and bundle fixtures"
      exports: ["synthetic_statistical_history", "synthetic_statistical_folds", "synthetic_sparse_teams", "statistical_pmf_oracles", "synthetic_phase10_registries"]
    - path: "tests/testthat/test_statistical_penalized_poisson_design.R"
      provides: "Plan 10-03 Task 1 sparse-design, centering, cold-start, and completeness RED contracts"
    - path: "tests/testthat/test_statistical_penalized_poisson_tuning.R"
      provides: "Plan 10-03 Task 2 nesting, Elo boundary, tuning, poisoning, and pairing RED contracts"
  key_links:
    - from: "tests/testthat/helper_statistical_challengers.R"
      to: "tests/testthat/test_statistical_penalized_poisson_*.R"
      via: "shared deterministic fixtures and exact Phase 9 parent/panel facts"
      pattern: "synthetic_statistical_|statistical_pmf_oracles"
---

<objective>
Create the shared synthetic fixture harness and task-scoped penalized-Poisson Wave 0 contracts for D-01 through D-04.

Purpose: Make STAT-01 chronology, attribution, provenance, and cold-start behavior executable before candidate code exists.
Output: One shared helper and two parseable RED test files owned separately by Plan 10-03 Task 1 and Task 2.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-VALIDATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md

<interfaces>
- Phase 9 synthetic conventions live in `tests/testthat/helper_benchmark.R`.
- Common output validation is exposed by `validate_benchmark_predictions()`, `validate_benchmark_score_distributions()`, `validate_model_manifests()`, `validate_benchmark_feature_evidence()`, and `validate_panel_prediction_coverage()`.
- Shared evaluation is exposed by `score_benchmark_fixtures()`, `aggregate_benchmark_scores()`, `fixed_benchmark_calibration()`, and `make_paired_fold_comparisons()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold shared fixtures and penalized-Poisson contract tests</name>
  <read_first>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md</file>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-VALIDATION.md</file>
    <file>tests/testthat/helper_benchmark.R</file>
    <file>tests/testthat/test_benchmark_baselines.R</file>
    <file>tests/testthat/test_benchmark_cutoffs.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/cutoffs.R</file>
  </read_first>
  <files>tests/testthat/helper_statistical_challengers.R, tests/testthat/test_statistical_penalized_poisson_design.R, tests/testthat/test_statistical_penalized_poisson_tuning.R</files>
  <action>Create deterministic in-memory helpers for two-row goal designs, completed-tournament inner folds, sparse and unseen teams, frozen/updating boundaries, exact Phase 9 panel IDs, and parent hashes. Partition STAT-01 RED behavior by owning task. In `test_statistical_penalized_poisson_design.R`, cover only Plan 10-03 Task 1: ridge-protected fixed full attack/defence indicator blocks, sum-to-zero centering of each fitted block, intercept adjustment preserving raw versus centered predictors, complete fixture retention, one-unseen and two-unseen cold starts, and cold-start/shrinkage evidence per D-02/D-04. In `test_statistical_penalized_poisson_tuning.R`, cover only Plan 10-03 Task 2: nested minimal/augmented means, sparse Elo selection, canonical Elo provenance rejection, prior-edition-only penalties shared by both tracks, assessed-outcome poisoning invariance, and candidate-versus-all-five-baselines fixture keys with 630 rows for open comparisons and 609 for `production_hybrid_nb` per D-01/D-03. Do not source or invoke Plan 10-03 Task 2 functions from the design file.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'files &lt;- c("tests/testthat/helper_statistical_challengers.R", "tests/testthat/test_statistical_penalized_poisson_design.R", "tests/testthat/test_statistical_penalized_poisson_tuning.R"); invisible(lapply(files, parse)); source(files[[1]]); stopifnot(all(vapply(c("synthetic_statistical_history", "synthetic_statistical_folds", "synthetic_sparse_teams", "statistical_pmf_oracles", "synthetic_phase10_registries"), exists, logical(1), mode = "function")))'</automated>
  </verify>
  <acceptance_criteria>
    - All three files parse without warnings and expose the five named helper functions.
    - The design file references only Task 1 APIs; the tuning file owns all Task 2 Elo/tuning/baseline-pairing contracts.
    - Tuning tests poison assessed-tournament labels and require unchanged penalties and frozen predictions; design tests require every sparse/unseen-team fixture to remain forecastable with auditable fallback status.
  </acceptance_criteria>
  <done>STAT-01 behavior is executable as task-scoped RED tests against deterministic synthetic folds.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `tests/testthat/helper_statistical_challengers.R` | `synthetic_statistical_history()`, `synthetic_statistical_folds()`, `synthetic_sparse_teams()`, `statistical_pmf_oracles()`, `synthetic_phase10_registries()` |
| `tests/testthat/test_statistical_penalized_poisson_design.R` | Plan 10-03 Task 1 D-02/D-04 sparse-design and cold-start contracts |
| `tests/testthat/test_statistical_penalized_poisson_tuning.R` | Plan 10-03 Task 2 D-01/D-03 nesting, provenance, tuning, poisoning, and pairing contracts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → production contracts | Weak synthetic data could omit the chronology and sparse-team failures that matter in real folds. |
| Sealed labels → tuning callbacks | Tests must prove assessed and WC2026 outcomes cannot enter settings selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | penalized chronology tests | high | mitigate | Assessed-outcome poisoning, strict-cutoff assertions, and frozen/updating equality for selected settings. |
| T-10-02 | Repudiation | sparse-team evidence tests | high | mitigate | Exact prior-count, shrinkage, cold-start, and parent-hash assertions. |
</threat_model>

<verification>
Parse the helper and both task-scoped files. The design and tuning suites remain RED and are run only after their respective Plan 10-03 production tasks.
</verification>

<success_criteria>
- The three owned Wave 0 files exist and parse.
- D-01 through D-04 and STAT-01 have explicit task-scoped RED coverage.
- No historical benchmark runs in this plan.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-01-SUMMARY.md` when done.
</output>
