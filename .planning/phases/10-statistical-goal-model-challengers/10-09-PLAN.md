---
phase: 10-statistical-goal-model-challengers
plan: "09"
type: execute
wave: 1
depends_on: [10-02]
files_modified:
  - R/benchmark/challenger_protocol.R
  - data/benchmark/phase10/model_registry.csv
  - data/benchmark/phase10/feature_contract.csv
  - data/benchmark/phase10/tuning_editions.csv
  - data/benchmark/phase10/tuning_grid.csv
  - data/benchmark/phase10/ablation_registry.csv
  - data/benchmark/phase10/selection_protocol.json
  - data/benchmark/phase10/storage_preflight.csv
  - tests/testthat/test_statistical_registry_protocol.R
  - tests/testthat/test_statistical_storage_preflight.R
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "Canonical validators recompute every stored row/settings/protocol hash from canonicalized content and reject missing, extra, reordered, or mutated registry values."
    - "Every Phase 10 registration proves the exact accepted Phase 9 bundle SHA-256 `977e119dd17e1212d2bfc57da2e676b6ee9d16bfba8b6c9bdbf1a97d302db069` plus the durable model-registry/checksum parent evidence before any diagnostic or fit."
    - "The exact ordered candidate/dependence definitions, 33-row tuning specification, complete 114-row outer/inner chronology relation, and pre-2002 diagnostic are frozen and executable before 2002-2024 assessment scoring."
    - "Selection validation freezes numerical RPS, practical-equivalence, Brier/log, calibration, fold-breadth, and stability thresholds; validates the complete ablation graph and ordered shortlist slots; and fails closed on any promotion/release/WC2026 decision surface."
    - "A deterministic format-identical 2,118,060-row G=40 pilot locks the explicit free-space projection before model implementation or the expensive benchmark."
  artifacts:
    - path: "R/benchmark/challenger_protocol.R"
      provides: "Canonical Phase 9 parent, Phase 10 registry/chronology/grid/selection/ablation, pre-2002 diagnostic, and storage validators"
      exports: ["phase10_protocol_constants", "canonical_phase10_tuning_grid", "canonical_phase10_tuning_relations", "canonical_phase10_selection_protocol", "validate_phase09_parent_identity", "load_and_validate_challenger_protocol", "run_pre2002_grid_diagnostic", "measure_challenger_partition_storage", "validate_challenger_storage_preflight"]
    - path: "data/benchmark/phase10/model_registry.csv"
      provides: "Seven immutable ordered challenger registrations and exact Phase 9 parent identities"
      contains: "poisson_team_ridge_elo_bivpois"
    - path: "data/benchmark/phase10/selection_protocol.json"
      provides: "Canonical research-only dependence, non-inferiority, fold-breadth, stability, and shortlist rules"
      contains: "dependence_meaningful_rps_delta"
    - path: "tests/testthat/test_statistical_registry_protocol.R"
      provides: "Mutation-complete parent/hash/grid/chronology/pre-2002 validator tests"
    - path: "tests/testthat/test_statistical_storage_preflight.R"
      provides: "Mutation-complete ablation/selection/no-promotion/storage validator tests"
    - path: "data/benchmark/phase10/storage_preflight.csv"
      provides: "Measured exact-cardinality partition footprint and locked free-space threshold"
      contains: "2118060"
  key_links:
    - from: "data/benchmark/phase10/model_registry.csv"
      to: "outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/checksum_manifest.csv"
      via: "exact accepted bundle, model-registry canonical hash, checksum self-hash, and complete parent-graph hash"
      pattern: "phase09_parent_bundle_sha256|phase09_parent_registry_sha256"
    - from: "data/benchmark/phase10/tuning_editions.csv"
      to: "data/benchmark/phase09/tournaments.csv"
      via: "complete exact outer-to-strictly-prior-inner relation"
      pattern: "outer_edition_id|inner_edition_id"
    - from: "tests/testthat/test_statistical_registry_protocol.R"
      to: "R/benchmark/challenger_protocol.R"
      via: "canonical recomputation and deletion/addition/mutation/order rejection"
      pattern: "validate_|canonical_"
---

<objective>
Freeze and fail-closed validate the Phase 10 candidate, feature, chronology, tuning, dependence, ablation, selection, and storage protocols.

Purpose: Candidate fitting must not discover models, grids, thresholds, parent identities, chronology, or resource bounds after assessment outcomes are visible.
Output: One canonical protocol/validator module, seven immutable Phase 10 registry/preflight artifacts, and two task-scoped mutation suites.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-02-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-08-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md

<interfaces>
- Plan 10-02 provides `require_challenger_environment()` and exact verified `glmnet` provenance; this plan validates that preflight but never installs a package.
- Phase 9 row hashing uses `benchmark_row_sha256()` and `benchmark_canonical_scalar()` in `R/benchmark/registry.R`; canonical JSON hashing follows `R/evaluation/promotion.R`.
- Current durable evidence fixes Phase 9 bundle SHA `977e119dd17e1212d2bfc57da2e676b6ee9d16bfba8b6c9bdbf1a97d302db069`, model-registry canonical SHA `a3d21b90568aec86f44cefe2964555cb5565e1ab4e205489f42009a3ec489255`, checksum self SHA `4fe638ab49014c9dbac98fe389709d7668715a9ac99840f52847d0297998c309`, and complete parent-graph SHA `19263239c52ceab8b9c2a345646a6475d103f38137ec5deebbc0993525701584`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical parent, registry, grid, chronology, and diagnostic gates</name>
  <read_first>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md</file>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md</file>
    <file>R/benchmark/challenger_preflight.R</file>
    <file>data/benchmark/phase10/glmnet_provenance.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/tournaments.csv</file>
    <file>data/benchmark/phase09/boundaries.csv</file>
    <file>data/processed/elo_matches.csv</file>
    <file>data/processed/goal_training_features_hybrid.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/run_manifest.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/checksum_manifest.csv</file>
    <file>R/benchmark/registry.R</file>
  </read_first>
  <files>R/benchmark/challenger_protocol.R, data/benchmark/phase10/model_registry.csv, data/benchmark/phase10/feature_contract.csv, data/benchmark/phase10/tuning_editions.csv, data/benchmark/phase10/tuning_grid.csv, tests/testthat/test_statistical_registry_protocol.R</files>
  <behavior>
    - Stored row, settings, relation, and registry hashes are recomputed from canonicalized non-hash fields and compared exactly; a valid-looking 64-character replacement is rejected.
    - Phase 9 parent validation independently reconstructs the accepted bundle identity and requires the four exact durable SHA constants in the context interface.
    - Candidate order is exactly `poisson_team_ridge`, `poisson_team_ridge_elo`, `dynamic_goal_ability`, `dynamic_goal_ability_elo`, `poisson_team_ridge_elo_dc`, `poisson_team_ridge_elo_bivpois`, `open_nb_elo_only_ablation`; dependence IDs/mean parents and complexity order are exact.
    - Tuning specification has exactly 33 ordered rows: 13 ascending `10^seq(-4,2,by=0.5)` team-ridge values, 13 ascending `10^seq(-5,1,by=0.5)` Elo-lasso values, five `c(2,4,8,16,32)` pseudo-exposures with 730-day half-life, then the Dixon-Coles positivity-intersection specification with `1e-8` interior epsilon and the bivariate-q specification `[0,0.95)` with `1e-8` optimizer tolerance.
    - Chronology contains exactly 114 canonical relations: four checked pre-2002 editions (`wc1994` 1994-06-17..1994-07-17, `euro1996` 1996-06-08..1996-06-30, `wc1998` 1998-06-10..1998-07-12, `euro2000` 2000-06-10..2000-07-02) plus every Phase 9 assessment edition whose final precedes each outer opener, sorted by outer opener then inner final date.
    - The pre-2002 diagnostic executes against only those four editions, reports finite reach for every frozen grid/dependence specification, proves no assessment row was read, and returns identical before/after canonical grid hashes.
  </behavior>
  <action>Start with `test_statistical_registry_protocol.R` and make every mutation case RED before implementation. Implement `phase10_protocol_constants()`, `canonical_phase10_tuning_grid()`, `canonical_phase10_tuning_relations()`, `validate_phase09_parent_identity()`, and `load_and_validate_challenger_protocol()` in `R/benchmark/challenger_protocol.R`. The parent validator must reconstruct the bundle identity from the durable Phase 9 run/checksum manifests and checked files, not trust a copied Phase 10 field, then assert the exact bundle/model-registry/self/parent-graph SHAs above. Create the immutable Phase 10 overlay without editing `data/benchmark/phase09/`. Register the exact ordered seven candidates with hard-coded adapter IDs, `open_core`, G=40, exact mean/dependence/tuning/feature IDs, D-01/D-05 nested parents, D-09 shared mean parent, complexity ranks, settings hashes, registration hashes, and every exact Phase 9 parent constant. Extend the inherited feature schema for team prior counts, shrinkage weight, cold-start, dynamic state age/exposure, dependence parameter provenance, and inactive xG/form status without removing or reordering inherited fields. Materialize the exact 114-row relation and 33-row tuning specification described in behavior. Canonical validators must reject missing/extra columns or rows, duplicates, order drift, unknown IDs, parent/foreign-key drift, noncanonical scalar representations, stale stored hashes, and valid-shape hash substitutions. Implement `run_pre2002_grid_diagnostic()` as a read-only environment-validated diagnostic: execute it on only the four exact pre-2002 editions, assert finite numerical reach across all frozen values/specifications, and prove the grid canonical hash is unchanged per D-02/D-03/D-04/D-06/D-08/D-10.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'source("R/benchmark/registry.R"); source("R/benchmark/challenger_preflight.R"); source("R/benchmark/challenger_protocol.R"); testthat::test_file("tests/testthat/test_statistical_registry_protocol.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); d &lt;- run_pre2002_grid_diagnostic(); stopifnot(isTRUE(d$valid), identical(d$edition_ids, c("wc1994", "euro1996", "wc1998", "euro2000")), d$max_evidence_date &lt; as.Date("2002-01-01"), identical(d$grid_sha256_before, d$grid_sha256_after), isTRUE(d$assessment_rows_absent))'</automated>
  </verify>
  <acceptance_criteria>
    - Exact canonical recomputation passes for untouched files and every delete/add/reorder/value/hash mutation fails before any model callback.
    - Parent validation proves the exact accepted Phase 9 bundle SHA and durable registry/checksum graph rather than accepting SHA shape or copied fields.
    - The exact candidate/dependence order, 33 tuning rows, and complete 114 chronology rows validate with no missing or extra relation.
    - The diagnostic executes only pre-2002 evidence, asserts every specification, and cannot alter any frozen grid or hash.
  </acceptance_criteria>
  <done>D-01 through D-10 candidate identities, evidence fields, chronology, exact search spaces, and parent provenance are frozen and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical ablation, selection, no-promotion, and storage gates</name>
  <read_first>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md</file>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md</file>
    <file>R/benchmark/challenger_protocol.R</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/benchmark/phase10/feature_contract.csv</file>
    <file>data/benchmark/phase09/promotion_protocol.json</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/feature_coverage.csv</file>
  </read_first>
  <files>R/benchmark/challenger_protocol.R, data/benchmark/phase10/ablation_registry.csv, data/benchmark/phase10/selection_protocol.json, data/benchmark/phase10/storage_preflight.csv, tests/testthat/test_statistical_storage_preflight.R</files>
  <behavior>
    - The exact ablation graph has root `open_nb_incumbent`, scored child `open_nb_elo_only_ablation`, and ordered non-activated child nodes `attack_xg`, `defence_xg`, `xgd`, `form`, all parented to the full incumbent with zero-coverage reasons; missing, extra, cyclic, reparented, activated, or reordered nodes fail.
    - The exact ordered shortlist slots are `best_proper_score`, `simplest_non_inferior`, `dependence_representative`; no recursive field/value or source path can encode promotion, release, final-holdout eligibility, or WC2026 outcomes.
    - Frozen thresholds are exact: dependence RPS gain `-0.001`, practical tie `0.0005`, simpler non-inferiority `+0.001`, Brier/log relative regression `0.01`, calibration regression `0.01`, maximum fold regression `0.015`, minimum fold wins `8`, World Cup wins `2`, and Euro wins `2`.
    - Selection/ablation JSON/CSV hashes are recomputed from canonical content and all missing/extra/mutated/order-drift cases fail closed.
    - Storage validation recomputes the pilot schema/generator/content hashes and exact cardinality/projection formulas rather than trusting stored pass flags.
  </behavior>
  <action>Start with `test_statistical_storage_preflight.R` and cover a valid fixture plus one-at-a-time deletion, insertion, reordering, parent mutation, threshold mutation, hash substitution, forbidden-field nesting, shortlist mutation, and storage-operand mutation. Implement `canonical_phase10_selection_protocol()` plus ablation/selection validators inside `load_and_validate_challenger_protocol()`. Per D-13/D-14, freeze the exact graph in behavior and preserve formula columns with explicit source/value absent, imputed, inactive evidence. Per D-11/D-12/D-15/D-16, serialize the exact thresholds from behavior, the Phase 9-derived fold-breadth values, deterministic comparison direction/operators, best-score rule, and three non-exclusive ordered slots; recursively reject promotion/release/final-holdout/WC2026 decision fields or values and keep `evaluate_promotion()` unreachable. Recompute the protocol SHA from canonical JSON with the hash field excluded, and recompute every ablation row/settings hash from canonical non-hash fields. Implement `measure_challenger_partition_storage()` and `validate_challenger_storage_preflight()` so the deterministic production-schema pilot has exactly 630 fixtures x two tracks = 1,260 distributions and 1,260 x 41 x 41 = 2,118,060 rows. Let measured compressed bytes be `B`; recompute `score_projection = 7 * B`, `one_bundle_projection = score_projection + max(1 GiB, ceiling(0.25 * score_projection))`, and `minimum_free_bytes = ceiling(3 * one_bundle_projection * 1.10)`. Recompute cardinalities, bytes, schema/generator hashes, worker ceiling 2, available bytes, and pass state; do not accept stored booleans as proof.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'source("R/benchmark/registry.R"); source("R/benchmark/challenger_protocol.R"); testthat::test_file("tests/testthat/test_statistical_storage_preflight.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); x &lt;- load_and_validate_challenger_protocol("data/benchmark/phase10"); stopifnot(isTRUE(x$valid), identical(x$shortlist_slots, c("best_proper_score", "simplest_non_inferior", "dependence_representative")), identical(x$thresholds, c(dependence_rps_gain = -0.001, practical_tie = 0.0005, simpler_noninferiority = 0.001, brier_relative = 0.01, log_relative = 0.01, calibration = 0.01, maximum_fold_regression = 0.015, fold_wins = 8, world_cup_wins = 2, euro_wins = 2)), isTRUE(validate_challenger_storage_preflight(x$storage)))'</automated>
  </verify>
  <acceptance_criteria>
    - The complete exact ablation graph, thresholds, operators, shortlist order, canonical hashes, and no-promotion boundary validate and reject every missing/extra/mutated case.
    - Deeper ablations remain auditable non-activated nodes, never silently omitted or fitted.
    - The pilot is deterministic, format-identical, exact-cardinality, independent of fitted candidates, and the recomputed projection/headroom formula fails closed.
    - Task 2 tests do not require candidate fitting, historical benchmark execution, or Task 1 test reruns beyond loading its validated artifacts.
  </acceptance_criteria>
  <done>D-11 through D-16 selection, hierarchy, no-promotion, and resource protocols are immutable canonical inputs.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `R/benchmark/challenger_protocol.R` | Canonical constants/builders/validators, `load_and_validate_challenger_protocol()`, pre-2002 diagnostic, and storage measurement/validation |
| `data/benchmark/phase10/model_registry.csv` | Exact ordered seven registrations and exact Phase 9 parents |
| `data/benchmark/phase10/feature_contract.csv` | Inherited schema plus cold-start/dynamic/dependence/inactive fields with canonical row hashes |
| `data/benchmark/phase10/tuning_editions.csv` | Exact ordered 114-row outer-to-prior-inner relation and eligible-ID hashes |
| `data/benchmark/phase10/tuning_grid.csv` | Exact ordered 33-row ridge/lasso/pseudo-exposure/dependence specification |
| `data/benchmark/phase10/ablation_registry.csv` | Complete exact hierarchical graph and activation reasons |
| `data/benchmark/phase10/selection_protocol.json` | Exact thresholds, fold-breadth, shortlist, tie/veto, no-promotion, and canonical hash rules |
| `data/benchmark/phase10/storage_preflight.csv` | Recomputed partition footprint and locked free-space threshold |
| `tests/testthat/test_statistical_registry_protocol.R` | Task 1 canonical parent/hash/grid/chronology/diagnostic mutation suite |
| `tests/testthat/test_statistical_storage_preflight.R` | Task 2 ablation/selection/no-promotion/storage mutation suite |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 9 bundle → Phase 10 registry | A stale or tampered parent would invalidate every comparison. |
| CSV/JSON registry → adapter dispatch | Registry text must remain canonical validated data, never executable R. |
| Historical outcomes → chronology diagnostic | Assessment results must not alter grids or eligible prior-edition identities. |
| Research protocol → Phase 12 | Threshold or vocabulary drift could create an unauthorized promotion decision. |
| Storage pilot → staged publication | Underestimated storage can leave partial normal/reversed bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | Phase 9 parent and Phase 10 registries | high | mitigate | Exact durable SHA constants, canonical content recomputation, full mutation tests, hard-coded order, and foreign-key closure. |
| T-10-06 | Information Disclosure | tuning relation/diagnostic | high | mitigate | Exact 114-row prior relation, eligible-ID hashes, four-edition pre-2002 execution, assessment-row denial, and before/after grid hashes. |
| T-10-07 | Elevation of Privilege | research selection protocol | critical | mitigate | Exact thresholds/slots, complete ablation graph, recursive forbidden-field scan, no evaluator path, and fail-closed canonical JSON validation. |
| T-10-08 | Denial of Service | G=40 publication | medium | mitigate | Recomputed exact-cardinality projection, two-worker ceiling, free-space hard gate, and explicit normal/reversed/rollback headroom. |
</threat_model>

<verification>
Run both task-scoped mutation suites, execute/assert the pre-2002 diagnostic, load every registry through the canonical validator, and recompute the storage pilot formula. Do not install packages, fit a 2002-2024 candidate fold, or run the historical benchmark.
</verification>

<success_criteria>
- Exact parent SHAs, canonical hashes, seven ordered candidates, 33 ordered tuning/dependence rows, and 114 complete chronology relations validate.
- The pre-2002 diagnostic executes against only the four exact pre-2002 editions and leaves frozen hashes unchanged.
- Numerical selection thresholds, complete ablation graph, exact shortlist, no-promotion boundary, and storage projection fail closed on missing/extra/mutated inputs.
- Phase 9 remains immutable and every later adapter/runner consumes this plan's validators instead of defining registry/storage rules elsewhere.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-09-SUMMARY.md` when done.
</output>
