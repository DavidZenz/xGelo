---
phase: 10-statistical-goal-model-challengers
plan: "04"
type: execute
wave: 2
depends_on: [10-01, 10-10, 10-09]
files_modified:
  - R/forecast/dynamic_goal_ability.R
  - tests/testthat/test_statistical_dynamic_state.R
  - tests/testthat/test_statistical_dynamic_tuning.R
autonomous: true
requirements: [STAT-02]

must_haves:
  truths:
    - "Standalone and Elo-augmented dynamic attack/defence variants use one deterministic pre-date state for every fixture in a matchday batch."
    - "Updates consume only observed goals from completed earlier batches, preserve all eligible history, and continuously revert inactive teams toward the global log-scale mean."
    - "Dynamic settings and Elo coefficients are tuned from prior tournaments only and frozen for both outer tracks, with historical xG/form explicitly inactive."
  artifacts:
    - path: "R/forecast/dynamic_goal_ability.R"
      provides: "Dynamic sufficient-state replay, prior tuning, variants, prediction, and evidence"
      exports: ["initialize_dynamic_goal_state", "decay_dynamic_goal_state", "predict_dynamic_goal_batch", "update_dynamic_goal_batch", "replay_dynamic_goal_states", "select_dynamic_goal_hyperparameters", "fit_dynamic_elo_coefficient", "dynamic_goal_manifest"]
  key_links:
    - from: "replay_dynamic_goal_states"
      to: "data/benchmark/phase09/boundaries.csv"
      via: "date-complete boundary batches"
      pattern: "evidence_cutoff_exclusive|boundary_id"
    - from: "select_dynamic_goal_hyperparameters"
      to: "data/benchmark/phase10/tuning_editions.csv"
      via: "prior completed tournament objective"
      pattern: "inner_edition_id"
---

<objective>
Implement and test two chronology-safe dynamic attack/defence candidates.

Purpose: Satisfy STAT-02 while isolating whether dynamic goal state replaces Elo or benefits from it.
Output: One dynamic forecast module and a GREEN focused replay suite.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-09-SUMMARY.md

<interfaces>
- `compute_goal_ability_features()` is the prior-only evidence analogue but not the candidate state implementation.
- `benchmark_importance_weight()` supplies the frozen open-data importance treatment.
- Phase 9 boundary rows provide one `evidence_cutoff_exclusive` per full date batch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic matchday-batch state and inactivity reversion</name>
  <read_first>
    <file>R/forecast/dynamic_goal_ability.R</file>
    <file>tests/testthat/test_statistical_dynamic_state.R</file>
    <file>tests/testthat/helper_statistical_challengers.R</file>
    <file>R/forecast/goal_ability.R</file>
    <file>R/benchmark/cutoffs.R</file>
    <file>R/benchmark/weights.R</file>
    <file>data/benchmark/phase09/boundaries.csv</file>
    <file>data/benchmark/phase10/feature_contract.csv</file>
  </read_first>
  <files>R/forecast/dynamic_goal_ability.R, tests/testthat/test_statistical_dynamic_state.R</files>
  <behavior>
    - Every date batch is predicted from one immutable pre-date snapshot and updated once after all completed outcomes in the batch are aggregated.
    - Shuffling rows within a date yields byte-identical states, means, predictions, and hashes.
    - With no new evidence, attack and defence effects converge continuously toward zero on the log scale.
    - No fixed recent window or cycle/tournament reset is present.
  </behavior>
  <action>Implement team sufficient statistics `GF`, `GA`, and `W` keyed by stable team ID. `initialize_dynamic_goal_state()` establishes a prior global goal rate and fixed pseudo-exposure. `decay_dynamic_goal_state()` applies `exp(-log(2) * days / 730)` to observed evidence while leaving the global pseudo-exposure fixed. `predict_dynamic_goal_batch()` computes attack/defence effects from decayed sufficient statistics and predicts every fixture before mutation. `update_dynamic_goal_batch()` applies frozen importance weights to observed scored/conceded regulation goals only after aggregating the complete date. `replay_dynamic_goal_states()` sorts by date and stable keys, snapshots each Phase 9 boundary once, and preserves all history per D-06 through D-08. Emit state age, observed exposure, shrinkage weight, cold-start, and source dates; mark historical xG/form source/value coverage inactive rather than treating zero as observed per D-07.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_dynamic_state.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Same-date permutation tests are byte-identical.
    - Tests prove a result from date d affects only a later boundary.
    - Long inactivity approaches the global mean without deleting old history or resetting a cycle, and this gate does not invoke Task 2 tuning/Elo APIs.
  </acceptance_criteria>
  <done>The dynamic state machine is leakage-safe, order-invariant, and continuously mean-reverting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the nested Elo variant and prior-only dynamic tuning</name>
  <read_first>
    <file>R/forecast/dynamic_goal_ability.R</file>
    <file>tests/testthat/test_statistical_dynamic_tuning.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>data/benchmark/phase10/tuning_editions.csv</file>
    <file>data/benchmark/phase10/tuning_grid.csv</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/processed/goal_training_features_hybrid.csv</file>
    <file>data/benchmark/phase10/feature_contract.csv</file>
  </read_first>
  <files>R/forecast/dynamic_goal_ability.R, tests/testthat/test_statistical_dynamic_tuning.R</files>
  <behavior>
    - Prior inner tournaments select pseudo-exposure using equal-tournament updating RPS and stronger-shrinkage tie-breaking.
    - A signed point-in-time Elo coefficient is estimated only from adapter-supplied `elo_diff` and its canonical provenance companions, and adds to rather than replaces the same dynamic means.
    - Selected pseudo-exposure and Elo coefficient are identical across frozen/updating outer tracks and invariant to poisoned outer labels.
  </behavior>
  <action>Implement `select_dynamic_goal_hyperparameters()` over the frozen pseudo-exposure grid, using only registered inner editions, common G=40 distributions/scoring, equal-inner-tournament updating RPS, and a larger-pseudo-exposure tie-break. Implement `fit_dynamic_elo_coefficient()` on the adapter-prepared prior training frame sourced from `data/processed/goal_training_features_hybrid.csv`, requiring `elo_diff`, `elo_diff__value_present`, `elo_diff__source_present`, `elo_diff__source_date`, `elo_diff__imputed`, and `elo_diff__imputation_reason` and validating each source date against the exclusive prediction cutoff and the Phase 10 feature contract. Use the standalone dynamic log mean as offset and one signed point-in-time Elo term; fail on missing/invalid provenance, invalid chronology, or nonfinite fit instead of substituting another model. Direct reads of `data/processed/elo_ratings.csv`, joins to raw rating histories, and any model-layer reconstruction or nearest-date lookup of Elo are prohibited: the adapter-supplied canonical point-in-time value and companions are the sole Elo input. Register `dynamic_goal_ability` and `dynamic_goal_ability_elo` as a controlled nested pair per D-05, freeze one settings row per outer fold for both tracks, and implement `dynamic_goal_manifest()` with state/tuning/eligible-ID hashes, coefficient status, Elo value/provenance source maxima and hashes, xG inactivity reason, package versions, and convergence.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_dynamic_tuning.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_cutoffs.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Both D-05 variants use identical dynamic parent means before the Elo term.
    - Outer labels cannot change settings, coefficients, or frozen predictions.
    - Tests reject absent/stale Elo provenance and any direct raw-rating join path; only canonical adapter-supplied point-in-time columns are accepted.
    - Manifests expose inactive xG/form provenance and every dynamic state/tuning parent.
  </acceptance_criteria>
  <done>Both dynamic candidates are prior-tuned, track-frozen, and adapter-ready.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `R/forecast/dynamic_goal_ability.R` | `initialize_dynamic_goal_state()`, `decay_dynamic_goal_state()`, `predict_dynamic_goal_batch()`, `update_dynamic_goal_batch()`, `replay_dynamic_goal_states()`, `select_dynamic_goal_hyperparameters()`, `fit_dynamic_elo_coefficient()`, `dynamic_goal_manifest()` |
| `tests/testthat/test_statistical_dynamic_state.R` | GREEN Task 1 date-batch, reversion, provenance, and replay tests |
| `tests/testthat/test_statistical_dynamic_tuning.R` | GREEN Task 2 prior-only tuning, poisoning, and nested-Elo tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Completed results → mutable state | Same-day mutation can leak results into peer fixtures. |
| Inactivity transform → forecasts | Decaying numerator/denominator without a fixed prior can fail to revert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Tampering | matchday updater | high | mitigate | Predict-before-update batches, stable aggregation keys, shuffle tests, and strict boundary dates. |
| T-10-12 | Tampering | mean reversion | medium | mitigate | Fixed global pseudo-exposure, limiting-case tests, and explicit state-age/effective-weight evidence. |
| T-10-13 | Repudiation | dynamic fit | medium | mitigate | State, tuning, eligible-ID, Elo-coefficient, and source hashes in manifests. |
</threat_model>

<verification>
Run the dynamic suite and inherited cutoff regressions; keep historical outer benchmark execution for Plan 10-08.
</verification>

<success_criteria>
- D-05 through D-08 are implemented and tested.
- Date batches are deterministic and leakage-safe.
- Both dynamic candidates remain complete for inactive and unseen teams.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-04-SUMMARY.md` when done.
</output>
