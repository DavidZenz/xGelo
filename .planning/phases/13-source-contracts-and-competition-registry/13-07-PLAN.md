---
phase: 13-source-contracts-and-competition-registry
plan: "07"
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - scripts/acquire_uefa_snapshot.R
  - .gitignore
  - tests/testthat/test_phase13_source_contracts.R
  - data/competition/registries/source_bundles.csv
  - data/competition/registries/source_artifacts.csv
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
gap_closure: true
must_haves:
  truths:
    - "D-01/D-02/D-03: The bounded capture entrypoint assembles fixtures, groups, standings, results, and status as one edition-scoped candidate and fails closed before later accepted publication when any class or schema is invalid."
    - "D-01/D-05: Live capture requires explicit HTTPS structured URLs for fixtures, groups, standings, and results; status accepts an explicit HTTPS structured URL when available or uses the validated derived-status contract when no separate status URL exists."
    - "D-04/D-05/D-08: Exact raw bytes are retained under the ignored edition-scoped local store, while source_bundles.csv and source_artifacts.csv retain URL lineage, retrieval time, byte count, raw SHA-256, parser Git SHA, fallback status, review state, and stable hashes."
    - "D-06/D-07: A reviewed manual fallback is a complete edition-wide bundle with visible review metadata and no official/fallback mixing."
  artifacts:
    - "scripts/acquire_uefa_snapshot.R"
    - ".gitignore"
    - "data/competition/registries/source_bundles.csv"
    - "data/competition/registries/source_artifacts.csv"
    - "tests/testthat/test_phase13_source_contracts.R"
  key_links:
    - "Four mandatory HTTPS structured inputs plus optional status URL -> phase13_capture_structured_bundle() -> five-class source registries."
    - "No status URL -> status-bearing fields in accepted official resources -> derived status artifact -> sorted source_artifact_id links in the source registry."
    - "Candidate raw bytes -> ignored local_raw/<edition>/<bundle> store -> exact-byte verification against source_artifacts.csv."
    - "Reviewed fallback metadata -> edition-wide source bundle row -> accepted publication plans without mixed provenance."
  prohibitions:
    - "The live branch must not require a fifth independent status URL when the structured inputs provide the status-bearing fields needed for derivation."
    - "The script must not scrape rendered pages, parse PDFs, or infer status from prose."
    - "A failed candidate must not write a partial source registry or raw response into Git."
---

<objective>
Implement the bounded capture entrypoint and compact source registries on top of the Phase 13 source-contract foundation.

Purpose: Make official structured capture, optional status derivation, exact-byte local retention, and reviewed fallback metadata executable without coupling the capture path to accepted-directory publication or later competition logic.
Output: `scripts/acquire_uefa_snapshot.R`, the ignored raw-store rule, compact source bundle/artifact registries, and source-focused replay/fallback regressions.
</objective>

<execution_context>
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-source-contracts-and-competition-registry/13-02-PLAN.md
@R/competition/source_contracts.R
@scripts/update_worldcup_dashboard.R
@scripts/auto_update_worldcup_dashboard.sh
@tests/testthat/test_phase13_source_contracts.R
@tests/fixtures/phase13/uefa_nations_league_sample.json
@tests/fixtures/phase13/euro2028_predraw_sample.json
@tests/fixtures/phase13/reviewed_fallback_bundle.json
@.gitignore
</execution_context>

## Artifacts This Phase Produces

- `scripts/acquire_uefa_snapshot.R`, with deterministic fixture replay and bounded live capture using four mandatory HTTPS structured URLs plus an optional status URL.
- `.gitignore` coverage for `data/competition/local_raw/`, preserving exact response bodies outside Git.
- `data/competition/registries/source_bundles.csv` and `source_artifacts.csv`, including explicit or derived status provenance and complete fallback metadata.
- `tests/testthat/test_phase13_source_contracts.R` regressions for capture, optional status derivation, exact-byte retention, fallback approval, and no-mixing behavior.

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Analysts can capture authoritative UEFA snapshots and register both editions under one auditable contract. | 13-07, 13-09, 13-10, 13-03, 13-04, 13-05, 13-06, 13-08 | COVERED | This plan supplies bounded capture and provenance registries; later plans publish and consume accepted outputs. |
| REQ | DATA-01 | Capture official UEFA fixtures, groups, standings, results, and status snapshots. | 13-02, 13-07, 13-09, 13-10 | COVERED | The five classes are mandatory; only the status URL is optional because status can be derived from accepted structured fields. |
| REQ | DATA-02 | Record source URL, retrieval time, raw-byte hash, parser identity, and fallback status. | 13-02, 13-07 | COVERED | Derived status carries URL lineage and source_artifact_id links. |
| REQ | DATA-03 | Normalize UEFA/open results to stable team and edition IDs. | 13-03, 13-04, 13-08 | COVERED | Capture preserves source-shaped values for the normalization seam. |
| REQ | DATA-04 | Support audited manual fallback snapshots with visible metadata. | 13-07 | COVERED | The fallback remains a reviewed edition-wide variant. |
| REQ | COMP-01 | Register lifecycle, ruleset, source bundle, model release, and output bundle. | 13-03, 13-05, 13-06 | COVERED | Capture creates the source-bundle side of the registry link. |
| RESEARCH | R-01/R-02 | Bounded structured capture, compact manifests, and local-only raw bytes. | 13-07, 13-09, 13-10 | COVERED | No broad scraper or raw-body publication is planned. |
| RESEARCH | R-03/R-04 | Truthful EURO pre_draw and reuse of trusted-root/hash/staged-write patterns. | 13-07, 13-10, 13-05 | COVERED | EURO output is published later from the same contract. |
| CONTEXT | D-01..D-16 | Locked source, provenance, identity, lifecycle, fallback, blocked, and release decisions. | 13-02, 13-07, 13-09, 13-10, 13-03, 13-04, 13-05, 13-06, 13-08 | COVERED | D-01 through D-08 are explicit here; later plans implement D-09 through D-16. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 13-07-01: Trace bounded structured capture with optional status derivation</name>
  <precondition>Plan 13-02-01 and 13-02-02 source-contract APIs pass their focused tests.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-02-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
    R/competition/source_contracts.R
    scripts/update_worldcup_dashboard.R
    tests/testthat/test_phase13_source_contracts.R
    tests/fixtures/phase13/uefa_nations_league_sample.json
    tests/fixtures/phase13/euro2028_predraw_sample.json
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_source_contracts.R</files>
  <behavior>
    - Fixture replay supplies all five resource classes without network access and remains deterministic.
    - Live capture requires explicit HTTPS structured URLs for fixtures, groups, standings, and results; `status_url` is accepted when available but is not required when derivation succeeds.
    - The no-status-URL branch creates a status artifact only from status-bearing fields in the four validated official structured payloads and records all contributing source_artifact_id links.
    - Missing status-bearing fields, an explicit non-HTTPS status URL, an HTML/PDF response, a missing mandatory URL, or a schema-drifted payload fails before candidate acceptance.
  </behavior>
  <action>Create `scripts/acquire_uefa_snapshot.R` using the existing small helper and `main()` pattern. Add separate live-input handling for the four mandatory structured HTTPS URLs and the optional status URL, then call the Plan 13-02 explicit/derived status contract so the candidate still contains exactly five resource classes. Keep the structured-only gate on every response, retain exact raw bytes and source URL lineage, and make failure messages identify whether the missing status source was an unavailable optional URL or absent status-bearing fields. Preserve deterministic fixture replay and keep this entrypoint bounded to one edition candidate; do not add broad scraping, rendered-page parsing, standings calculations, forecast fields, competition rules, or accepted-directory publication in this task.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
  </verify>
  <acceptance_criteria>Fixture replay and the optional-status/derived-status branches exercise the same five-class candidate contract; four mandatory HTTPS URLs are enforced, a separate status URL is optional, and malformed, non-structured, incomplete, or status-untraceable inputs fail closed before publication.</acceptance_criteria>
  <done>The capture entrypoint reaches a valid five-class structured candidate without requiring five separate network URLs and exposes deterministic status provenance for the later publication plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-07-02: Persist compact source registries and reviewed fallback provenance</name>
  <precondition>Task 13-07-01 can produce a complete official or reviewed-fallback candidate in a temporary workspace.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-02-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    R/competition/source_contracts.R
    scripts/acquire_uefa_snapshot.R
    tests/testthat/test_phase13_source_contracts.R
    tests/fixtures/phase13/reviewed_fallback_bundle.json
    .gitignore
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R, .gitignore, data/competition/registries/source_bundles.csv, data/competition/registries/source_artifacts.csv, tests/testthat/test_phase13_source_contracts.R</files>
  <behavior>
    - Exact raw bytes are written to the ignored edition/bundle local store and re-read to verify byte counts and raw SHA-256 values.
    - Source registries contain one accepted five-class bundle and five artifact rows, including derived status source-artifact lineage, parser Git SHA, fallback/review state, and stable hashes.
    - Reviewed fallback requires source, retrieval date, reason, operator note, checksum, approved review state, and edition-wide reviewed_fallback status per D-06/D-07.
    - Candidate registry writes are staged and no source registry or raw response is exposed as a partial Git artifact after a failed candidate.
  </behavior>
  <action>Complete the registry and local-store portion of `scripts/acquire_uefa_snapshot.R`. Add the exact-byte `data/competition/local_raw/<edition_id>/<bundle_id>/` retention rule to `.gitignore`, serialize the validated source bundle and artifact rows to `source_bundles.csv` and `source_artifacts.csv`, and preserve the derived status artifact's source_artifact_id link set and contributing URL lineage. Keep official and reviewed fallback statuses edition-wide, require the complete D-06 review record for fallback, and stage registry writes through the existing Phase 13 CSV helpers. Do not write accepted fixtures, groups, standings, results, or status directories here; those outputs are owned by Plans 13-09 and 13-10.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
    <automated>test -z "$(git ls-files data/competition/local_raw)"</automated>
  </verify>
  <acceptance_criteria>The compact source registries validate against complete official and reviewed-fallback candidates, exact raw bytes remain locally replayable but untracked, derived status provenance links are preserved, and failed candidates cannot leave partial registry rows.</acceptance_criteria>
  <done>DATA-01, DATA-02, and DATA-04 capture metadata is durable and ready for accepted-edition publication without a separate status endpoint requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator-supplied URL -> capture script | URL, response bytes, size, and JSON shape are untrusted. |
| Official contributing artifacts -> derived status registry row | Derived status must preserve exact source-artifact lineage and cannot invent state. |
| Local raw store -> committed source registries | Exact bytes remain ignored while compact hashes and metadata become durable. |
| Reviewed fallback input -> accepted provenance | Human review metadata controls whether fallback artifacts can enter the bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-07-01 | Tampering | `scripts/acquire_uefa_snapshot.R` URL/input path | high | mitigate | Require bounded HTTPS structured inputs, validate all five resource classes, and reject HTML/PDF/schema drift before registry writes. |
| T-13-07-02 | Repudiation | source_bundles.csv/source_artifacts.csv | high | mitigate | Persist URL lineage, retrieval time, raw SHA-256, parser Git SHA, fallback/review state, canonical hashes, and derived source_artifact_id links. |
| T-13-07-03 | Information Disclosure | `data/competition/local_raw` | medium | mitigate | Keep exact response bodies below an explicit ignored root and assert no raw-store path is tracked. |
| T-13-07-04 | Tampering | reviewed fallback branch | high | mitigate | Require complete D-06 metadata, approved review state, edition-wide status, and no mixed official/fallback artifacts. |
| T-13-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install tasks exist; existing R dependencies are reused. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
<automated>test -z "$(git ls-files data/competition/local_raw)"</automated>
<human-check>For the final live-source backstop, inspect one official capture where status is supplied by a separate structured URL and one where status is derived from status-bearing fields; in both cases verify the five resource classes, URL/hash/parser/fallback metadata, and source_artifact_id lineage.</human-check>
</verification>

<success_criteria>
Plan 13-07 succeeds when bounded capture produces compact, hash-backed source registries for all five resource classes, accepts four mandatory HTTPS URLs plus optional status sourcing, retains exact raw bytes only locally, and preserves complete reviewed-fallback provenance.
</success_criteria>

<output>
Create `.planning/phases/13-source-contracts-and-competition-registry/13-07-SUMMARY.md` when done.
</output>
