---
phase: 13-source-contracts-and-competition-registry
plan: "11"
type: execute
wave: 8
depends_on:
  - 13-04
files_modified:
  - R/competition/publication_hashes.R
  - R/competition/publication_manifests.R
  - tests/testthat/test_phase13_publication_hashes.R
  - tests/testthat/test_phase13_publication_manifests.R
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
gap_closure: true
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05/D-08: Every normalized accepted resource table has refreshed row hashes and a canonical whole-file content hash computed from the staged CSV bytes, while raw-byte hashes, source URLs, parser Git SHA, fallback state, and review metadata remain unchanged."
    - "D-05/D-08/D-16: The canonical table/hash helper refreshes the matching source_artifacts.csv rows without publishing any partial registry or accepted-directory state."
    - "D-02/D-03/D-05/D-07/D-08: The manifest helper regenerates both accepted manifests, source-bundle/artifact-manifest derived hashes, and manifest self-hashes from one consistent staged graph, rejecting missing, duplicate, forged, or cross-edition links."
    - "D-14: The EURO pre_draw empty fixtures, groups, standings, and results tables retain complete schemas, status provenance, and refreshed hashes without fabricated rows."
  artifacts:
    - "R/competition/publication_hashes.R"
    - "R/competition/publication_manifests.R"
    - "tests/testthat/test_phase13_publication_hashes.R"
    - "tests/testthat/test_phase13_publication_manifests.R"
  key_links:
    - "Normalized staged table bytes -> publication_hashes.R -> source_artifacts.csv canonical_content_sha256 rows."
    - "Refreshed source_artifacts.csv rows -> publication_manifests.R -> source_bundles.csv, both accepted manifests, artifact-manifest hashes, bundle hashes, and manifest self-hashes."
    - "Plan 13-09/13-10 source-shaped handoff seeds -> deterministic hash regeneration -> Plan 13-12 transaction input; no helper owns locks, backups, promotion, or rollback."
  prohibitions:
    - "The hash helpers must not replace durable targets independently of the Plan 13-12 transaction."
    - "The canonical hash helper must not recompute or replace raw_sha256 values from local raw bytes."
    - "The manifest helper must not accept a mixed official/reviewed-fallback graph or cross-edition artifact links."
---

<objective>
Separate deterministic canonical table/registry regeneration from the publication transaction so the complete hash graph can be tested independently and then invoked atomically by Plan 13-12.

Purpose: Ensure normalized content cannot leave stale `canonical_content_sha256`, `source_artifacts.csv`, `source_bundles.csv`, accepted manifests, or derived manifest hashes, while keeping lock/snapshot/promotion/rollback ownership in one bounded transaction plan.
Output: Pure staged-root hash and manifest regeneration helpers with focused tests for ordering, provenance, pre_draw, stale-link, and fallback invariants.
</objective>

<execution_context>
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
@R/competition/source_contracts.R
@R/competition/team_identity.R
@R/release/release_bundle.R
</execution_context>

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Analysts can capture authoritative snapshots and register both editions under one auditable contract. | 13-11, 13-12 | COVERED | This plan freezes the deterministic hash graph consumed by the atomic publication plan. |
| REQ | DATA-01 | Accepted five-class snapshots remain complete and linked. | 13-04, 13-09, 13-10, 13-11, 13-12 | COVERED | All ten accepted resource tables participate in the staged hash graph. |
| REQ | DATA-02 | Provenance and raw-byte identity remain auditable after normalization. | 13-11 | COVERED | Canonical table hashes are refreshed without changing raw-byte provenance. |
| REQ | DATA-03 | Stable normalized identity is retained in accepted table content. | 13-04, 13-11, 13-12 | COVERED | Hashes bind the normalized identity-bearing fixture/result bytes. |
| REQ | DATA-04 | Reviewed fallback remains edition-wide and visible. | 13-07, 13-11, 13-12 | COVERED | Manifest regeneration preserves fallback/review state and rejects mixed graphs. |
| REQ | COMP-01 | Edition registry links remain durable around the accepted graph. | 13-03, 13-05, 13-06, 13-12 | COVERED | The transaction passes the regenerated graph to the loader and refresh boundaries. |
| RESEARCH | R-01/R-04 | Compact CSVs, SHA-256, staged writes, and fail-closed manifests. | 13-11 | COVERED | Helpers reuse existing source/release hash conventions without new dependencies. |
| CONTEXT | D-01..D-16 | Locked source, provenance, fallback, identity, lifecycle, blocked, and release decisions. | 13-11, 13-12 | COVERED | D-01/D-02/D-03/D-04/D-05/D-07/D-08/D-14 are enforced here; transaction and blocked state remain in dependent plans. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 13-11-01: Regenerate canonical table hashes and artifact registry projections</name>
  <precondition>Plan 13-04 exposes normalized fixture/result projections and Plans 13-09/13-10 provide source-shaped handoff seeds for both editions.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    R/competition/source_contracts.R
    R/competition/team_identity.R
    R/release/release_bundle.R
    tests/testthat/test_phase13_source_contracts.R
  </read_first>
  <files>R/competition/publication_hashes.R, tests/testthat/test_phase13_publication_hashes.R</files>
  <behavior>
    - A declared target vector covers fixtures.csv, groups.csv, standings.csv, results.csv, and status.csv for both editions and rejects missing, duplicate, unsafe, or cross-edition paths.
    - Every staged table is serialized with refreshed row_sha256 values, then receives canonical_content_sha256 from complete CSV bytes including headers; harmless input-row ordering does not change row identity hashes, while permitted content changes change the content hash.
    - The helper updates only the matching source_artifacts.csv canonical table/hash fields and preserves raw_sha256, source_url, retrieved_at_utc, parser_commit_sha, fallback_status, review_state, and artifact identity fields byte-for-byte.
    - Schema-valid empty EURO pre_draw tables receive the same complete hash treatment as non-empty Nations League tables.
  </behavior>
  <action>Create `R/competition/publication_hashes.R` with a deterministic staged-root API that accepts the explicit two-edition resource target vector and source_artifacts.csv data, validates trusted paths and five-class schemas, refreshes row hashes before computing each complete CSV-content hash, and returns the updated staged tables plus source-artifact registry rows without promoting any durable target. Keep raw_sha256 tied to exact local bytes and never recalculate it from normalized CSV content. Preserve source URL, retrieval time, parser Git SHA, fallback/review state, source_artifact_id, source_bundle_id, and edition_id per D-01/D-02/D-03/D-04/D-05/D-07/D-08/D-14. Use the hash and staged-file conventions in `R/release/release_bundle.R` and the analog paths `scripts/release_bundle.R`, `scripts/worldcup_dashboard.R`, `scripts/elo/preprocess.R`, and `scripts/update_worldcup_dashboard.R` named by 13-PATTERNS.md, without taking ownership of locks, backups, target promotion, or rollback. Write failing tests first in `tests/testthat/test_phase13_publication_hashes.R` for target coverage, stale row/content hashes, ordering, raw-hash preservation, EURO empties, unsafe paths, and malformed source-artifact links.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_hashes.R")'</automated>
  </verify>
  <acceptance_criteria>The helper deterministically refreshes all ten accepted resource-table row/content hashes and the corresponding source_artifacts.csv canonical fields in a temporary staged root, preserves raw/provenance fields, rejects unsafe or incomplete target graphs, and leaves no durable target changed during focused tests.</acceptance_criteria>
  <done>Canonical table and artifact-registry regeneration is independently executable and ready for the manifest and transaction helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-11-02: Regenerate accepted manifests and derived bundle hashes</name>
  <precondition>Task 13-11-01 passes and returns refreshed staged source-artifact rows for both editions.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-11-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    R/competition/source_contracts.R
    R/competition/publication_hashes.R
    R/release/release_bundle.R
    tests/testthat/test_phase13_publication_hashes.R
    data/competition/accepted/uefa_nations_league_2026_27/source_bundle_manifest.csv
    data/competition/accepted/uefa_euro_2028_qualifying/source_bundle_manifest.csv
    data/competition/registries/source_bundles.csv
    data/competition/registries/source_artifacts.csv
  </read_first>
  <files>R/competition/publication_manifests.R, tests/testthat/test_phase13_publication_manifests.R</files>
  <behavior>
    - Both accepted source_bundle_manifest.csv files are regenerated from the refreshed artifact rows with exactly five resource artifact links per edition, matching edition_id, bundle_id, source_artifact_id, raw SHA-256, parser SHA, fallback/review state, and canonical table hashes.
    - source_bundles.csv receives deterministic source_bundle_sha256 and artifact_manifest_sha256 values from the canonical source-artifact projection, and each accepted manifest receives a matching manifest self-hash.
    - Missing, duplicate, unknown, cross-edition, forged, stale, or mixed fallback links fail closed before the helper returns.
    - The derived hash outputs remain stable under harmless registry-row ordering and preserve explicit EURO pre_draw status lineage.
  </behavior>
  <action>Create `R/competition/publication_manifests.R` with a pure staged-root API that consumes the output of `phase13_refresh_canonical_table_hashes()`, rebuilds both accepted manifests, updates source_bundles.csv derived bundle/artifact-manifest fields, and recomputes each manifest self-hash only after all component rows agree. Require the five resource classes, sorted deterministic artifact IDs, exact edition/bundle foreign keys, and uniform official or reviewed-fallback status per D-01/D-02/D-03/D-05/D-06/D-07/D-08/D-14. Use the manifest and self-hash patterns in `R/release/release_bundle.R`, together with the named analog paths `scripts/release_bundle.R`, `scripts/worldcup_dashboard.R`, `scripts/elo/preprocess.R`, and `scripts/update_worldcup_dashboard.R` from 13-PATTERNS.md; keep lock/snapshot/promotion/rollback outside this helper. Add focused temporary-root tests for stale canonical values, forged manifest links, duplicate artifacts, mixed fallback, derived-status lineage, order-stable derived hashes, and truthful EURO pre_draw empties.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_manifests.R")'</automated>
  </verify>
  <acceptance_criteria>The helper regenerates both accepted manifests, source_bundles.csv derived hashes, artifact-manifest hashes, bundle hashes, and manifest self-hashes from one validated staged graph; it rejects stale, forged, duplicate, cross-edition, mixed-provenance, and incomplete inputs without durable publication.</acceptance_criteria>
  <done>The canonical/hash and accepted-manifest/derived-manifest slices are separate, deterministic, and consumable by the Plan 13-12 transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Staged normalized CSVs -> canonical hashes | Normalized content becomes durable integrity metadata. |
| Source-artifact rows -> accepted manifests | Registry projections claim artifact identity and provenance. |
| Accepted manifests -> source-bundle derived hashes | Cross-file lineage must remain consistent before transaction promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-11-01 | Tampering | canonical table/content hash regeneration | high | mitigate | Recompute row hashes and complete CSV-content hashes from staged bytes, preserving raw-byte hashes and provenance fields. |
| T-13-11-02 | Tampering | accepted manifests and source-bundle derived hashes | high | mitigate | Require exact five-artifact, edition, bundle, fallback, lineage, and self-hash agreement before returning derived projections. |
| T-13-11-03 | Repudiation | parser/source/fallback metadata | medium | mitigate | Preserve source URL, retrieval time, parser Git SHA, raw SHA-256, review state, and artifact IDs through both helpers. |
| T-13-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is planned. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_hashes.R"); testthat::test_file("tests/testthat/test_phase13_publication_manifests.R")'</automated>
</verification>

<success_criteria>
Plans 13-11-01 and 13-11-02 independently prove canonical normalized table hashes, source-artifact registry projections, accepted manifests, source-bundle derived hashes, artifact-manifest hashes, and manifest self-hashes without owning durable promotion or rollback.
</success_criteria>

<output>
Create `.planning/phases/13-source-contracts-and-competition-registry/13-11-SUMMARY.md` when done.
</output>
