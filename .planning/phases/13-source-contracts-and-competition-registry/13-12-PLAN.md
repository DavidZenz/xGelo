---
phase: 13-source-contracts-and-competition-registry
plan: "12"
type: execute
wave: 9
depends_on:
  - 13-04
  - 13-11
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
gap_closure: true
files_modified:
  - R/competition/publication_transaction.R
  - scripts/acquire_uefa_snapshot.R
  - tests/testthat/test_phase13_publication_transaction.R
  - tests/testthat/test_phase13_publication_integration.R
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05/D-07/D-08/D-14/D-16: One explicit normalized publication transaction locks the publication root, snapshots every declared target, stages both editions and both shared registries, validates the complete graph, promotes all targets, or restores every prior target byte-for-byte."
    - "The transaction target set is explicit and bounded: source_artifacts.csv, source_bundles.csv, both accepted source_bundle_manifest.csv files, and fixtures/groups/standings/results/status for both editions; registry-side refresh_batches records remain outside its replacement scope."
    - "Plan 13-11 canonical table/hash/registry and accepted-manifest/derived-manifest helpers run inside the staged transaction, so canonical_content_sha256, source registries, accepted manifests, bundle hashes, artifact-manifest hashes, and manifest self-hashes cannot become stale or partially refreshed."
    - "A lock collision, staging failure, cross-file validation failure, or injected failure after any target promotion fails closed and restores the complete prior target snapshot while removing only transaction staging/backups."
    - "The source-shaped handoff seeds from Plans 13-09/13-10 are normalized before promotion; normalized stable IDs remain in the accepted/publication path and the result/fixture identity/provenance regressions remain intact."
  artifacts:
    - "R/competition/publication_transaction.R"
    - "scripts/acquire_uefa_snapshot.R"
    - "tests/testthat/test_phase13_publication_transaction.R"
    - "tests/testthat/test_phase13_publication_integration.R"
  key_links:
    - "Explicit target vector -> publication lock and byte/hash snapshot -> sibling staging root."
    - "Both source-shaped edition seeds -> normalized fixtures/results -> Plan 13-11 hash/manifest helpers -> staged registries/manifests/derived hashes."
    - "Staged complete graph -> accepted snapshot/source-bundle validation -> ordered promotion -> byte-for-byte rollback on any failure."
    - "Successful transaction -> Plan 13-05 post-normalization loader success; failed transaction leaves the last accepted output and registry provenance untouched."
  prohibitions:
    - "The transaction must not publish one edition, registry, or manifest independently of the other declared targets."
    - "The transaction must not include, delete, rename, or rewrite registry-side refresh_batches blocked records; Plan 13-06 owns that separate durable boundary."
    - "The transaction must not recompute competition standings, form windows, forecasts, simulations, or competition rules."
---

<objective>
Make normalized accepted publication an executable all-or-nothing transaction after the canonical/hash and manifest slices are independently proven.

Purpose: Close the accepted DATA-03 publication gap without allowing stale hash chains, partial edition promotion, or rollback that restores only a subset of the durable graph.
Output: A reusable lock/snapshot/rollback envelope, production acquisition integration, and focused success/failure regressions over the complete normalized publication target set.
</objective>

<execution_context>
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-11-PLAN.md
@R/competition/source_contracts.R
@R/competition/team_identity.R
@R/competition/publication_hashes.R
@R/competition/publication_manifests.R
@R/release/release_bundle.R
@scripts/acquire_uefa_snapshot.R
</execution_context>

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Analysts can capture authoritative snapshots and register both editions under one auditable contract. | 13-12, 13-05, 13-06 | COVERED | This plan is the normalized publication boundary consumed by loader and refresh plans. |
| REQ | DATA-01 | Complete five-class accepted snapshots are promoted together. | 13-09, 13-10, 13-12 | COVERED | Both editions and all ten resource tables are in the target vector. |
| REQ | DATA-02 | URL, retrieval, raw hash, parser, fallback, and derived hashes remain linked. | 13-11, 13-12 | COVERED | Staged helpers and transaction validation reject stale or partial lineage. |
| REQ | DATA-03 | Stable IDs and source display values reach accepted production files. | 13-04, 13-12, 13-05 | COVERED | Normalized fixture/result output is promoted only with the complete graph. |
| REQ | DATA-04 | Reviewed fallback remains edition-wide and atomic. | 13-07, 13-09, 13-10, 13-12, 13-06 | COVERED | The transaction preserves uniform fallback/review status and prior output on failure. |
| REQ | COMP-01 | Registry links remain coherent with accepted output. | 13-03, 13-05, 13-06, 13-12 | COVERED | Edition registry itself is not partially replaced by this transaction. |
| RESEARCH | R-01/R-04 | Trusted roots, SHA-256, staged writes, and rollback patterns. | 13-11, 13-12 | COVERED | Existing release patterns are adapted to the bounded target vector. |
| CONTEXT | D-01..D-16 | Locked source, provenance, identity, lifecycle, fallback, blocked, and release decisions. | 13-11, 13-12 | COVERED | D-01/D-02/D-03/D-04/D-05/D-07/D-08/D-14/D-16 are enforced at the transaction boundary; Plan 13-06 owns blocked sidecars. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 13-12-01: Build the publication lock, target snapshot, and rollback envelope</name>
  <precondition>Plan 13-11 canonical/hash and manifest helpers pass their focused temporary-root tests.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-11-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    R/competition/publication_hashes.R
    R/competition/publication_manifests.R
    R/release/release_bundle.R
    tests/testthat/test_phase12_release.R
  </read_first>
  <files>R/competition/publication_transaction.R, tests/testthat/test_phase13_publication_transaction.R</files>
  <behavior>
    - `phase13_normalized_publication_targets()` returns exactly the two shared registries, two accepted manifests, and ten edition resource-table paths under trusted roots, excluding registry-side refresh_batches.
    - `phase13_with_publication_lock()` creates one exclusive lock for the transaction root, rejects an existing live lock, and always removes only its own lock/staging artifacts on exit.
    - `phase13_snapshot_publication_targets()` records existence, bytes, and SHA-256 for every target; `phase13_restore_publication_snapshot()` restores missing, changed, and existing targets exactly after an injected failure.
    - Failure injection after each ordered target promotion restores every declared target and removes temporary staging/backups without touching unrelated paths or refresh-batch records.
  </behavior>
  <action>Create `R/competition/publication_transaction.R` with explicit APIs for the normalized target vector, trusted-root resolution, exclusive lock acquisition, complete target byte/hash snapshot, sibling staging directory creation, ordered promotion, and rollback from the snapshot. The transaction envelope must accept callbacks for staged validation and promotion-failure injection, record which target was being promoted, restore every prior target including files that did not exist before the attempt, and clean only its own lock/staging/backup paths. Do not calculate row hashes, canonical table hashes, source registry projections, accepted manifests, or derived bundle hashes here; call those concerns through the Plan 13-11 interfaces. Keep registry-side `data/competition/registries/refresh_batches/` outside the target vector and outside cleanup. Use the atomic/staged-write patterns in `R/release/release_bundle.R` plus the named analog paths `scripts/release_bundle.R`, `scripts/worldcup_dashboard.R`, `scripts/elo/preprocess.R`, and `scripts/update_worldcup_dashboard.R` from 13-PATTERNS.md for path guards and bounded orchestration. Write failing tests first in `tests/testthat/test_phase13_publication_transaction.R` for lock collision, exact snapshot capture, injected failure at every promotion index, complete restoration, and cleanup boundaries.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_transaction.R")'</automated>
  </verify>
  <acceptance_criteria>The transaction envelope exposes the exact 14-file target vector, rejects lock collisions and unsafe targets, snapshots every prior byte/hash, restores every target after any injected promotion failure, cleans only owned temporary paths, and leaves registry-side refresh-batch records untouched.</acceptance_criteria>
  <done>Lock, snapshot, ordered-promotion, and complete-rollback orchestration is independently tested and ready for acquisition integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-12-02: Integrate normalized hash-graph publication into acquisition</name>
  <precondition>Task 13-12-01 and both Plan 13-11 helper tasks pass; Plans 13-09 and 13-10 provide validated source-shaped handoff directories.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-11-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-12-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    R/competition/source_contracts.R
    R/competition/team_identity.R
    R/competition/publication_hashes.R
    R/competition/publication_manifests.R
    R/competition/publication_transaction.R
    R/release/release_bundle.R
    scripts/acquire_uefa_snapshot.R
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_publication_integration.R</files>
  <behavior>
    - Production normalized publication reads both source-shaped handoff directories, normalizes each edition's fixtures/results, carries groups/standings/status unchanged through staging, and invokes both Plan 13-11 hash/manifest helpers before validation.
    - The staged target vector contains source_artifacts.csv, source_bundles.csv, both accepted manifests, and all ten accepted resource tables; every target is validated for schema, edition, source-artifact, source-bundle, fallback/review, canonical hash, derived hash, and EURO pre_draw invariants before promotion.
    - Successful publication leaves normalized stable IDs in accepted fixtures/results, preserves UEFA source IDs/display names/status/goals and raw provenance, and exposes a complete loader-ready graph for the dependent Plan 13-05 loader check.
    - Appending/reordering input or changing only a later score preserves baseline identity/edition assignments, while identity, edition, forged-link, stale-hash, or any injected promotion failure aborts and restores every declared target byte-for-byte.
  </behavior>
  <action>Replace the source-shaped-only sealing call in `scripts/acquire_uefa_snapshot.R` with `phase13_publish_normalized_editions()`: resolve both edition seed roots and their source registries under trusted paths, acquire the exclusive transaction lock, snapshot the exact 14-file target vector, create a sibling staging root, normalize fixtures with `phase13_normalize_fixture_rows()` and results with `phase13_normalize_accepted_result_rows()`, carry groups/standings/status through unchanged, call `phase13_refresh_canonical_table_hashes()` followed by `phase13_refresh_accepted_manifest_hashes()`, and run the full cross-file validator before ordered promotion. Make the validation compare every canonical_content_sha256, source_artifacts.csv row, source_bundles.csv derived hash, accepted-manifest artifact/bundle/self hash, raw_sha256/provenance field, source_bundle_id, edition_id, and D-14 empty-table rule; then promote all targets through `phase13_with_publication_lock()` or restore the pre-transaction snapshot on any failure. Keep Plan 13-09/13-10 source-shaped handoff hashes as inputs only, leave `data/competition/registries/refresh_batches/` untouched for Plan 13-06, preserve D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-08/D-14/D-16, and do not add downstream competition logic. Add temporary-root integration tests in `tests/testthat/test_phase13_publication_integration.R` for successful normalized publication, complete 14-target coverage, post-helper stale-hash rejection, forged manifest/source-bundle links, append/reorder/score-only identity stability, and injected failure after each target promotion with byte/hash equality for every prior target and zero changes under the refresh-batches root.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_integration.R")'</automated>
    <automated>Rscript --vanilla -e 'source("R/competition/publication_transaction.R"); stopifnot(length(phase13_normalized_publication_targets("data/competition/accepted", "data/competition/registries")) == 14L)'</automated>
  </verify>
  <acceptance_criteria>Acquisition publishes both normalized editions only after canonical table/hash/registry and accepted-manifest/derived-manifest regeneration succeeds; all 14 targets promote together under one lock, and every injected failure restores all target bytes/hashes while preserving source-shaped handoff and refresh-batch boundaries.</acceptance_criteria>
  <done>The accepted/publication path contains stable IDs and a no-stale-hash-chain all-or-nothing transaction; Plan 13-05 can now prove post-normalization loader success and Plan 13-06 can retain durable blocked refresh state separately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source-shaped accepted seeds -> normalized staging | Source values and identity mappings become normalized durable candidates. |
| Staged target graph -> durable accepted publication | A complete hash/provenance graph crosses the publication boundary. |
| Transaction lock/snapshot -> rollback | Promotion failures must not leave partial or stale state. |
| Accepted publication -> registry-side refresh history | Refresh-batch records remain outside this transaction's replacement scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-12-01 | Tampering | normalized target vector | critical | mitigate | Resolve an explicit 14-file target vector under trusted roots and validate every schema, foreign key, content hash, and derived hash before promotion. |
| T-13-12-02 | Denial of Service | lock/stage/promotion boundary | high | mitigate | Reject lock collisions, use sibling staging, inject and test failures at every promotion index, and restore the complete pre-transaction snapshot. |
| T-13-12-03 | Tampering | canonical/manifest hash chain | high | mitigate | Invoke Plan 13-11 helpers in order and reject any stale or mismatched canonical, artifact-manifest, bundle, or manifest-self hash. |
| T-13-12-04 | Information Integrity | normalized identity-bearing fixtures/results | high | mitigate | Normalize through the stable identity resolver, preserve source/display/provenance values, and fail closed on identity/edition/link mutations. |
| T-13-12-05 | Tampering | registry-side refresh_batches | high | mitigate | Exclude the refresh-batch root from target, backup, deletion, and rename scopes; Plan 13-06 owns its durable blocked/recovery transaction. |
| T-13-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is planned. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_publication_transaction.R"); testthat::test_file("tests/testthat/test_phase13_publication_integration.R")'</automated>
<automated>Rscript --vanilla -e 'source("R/competition/publication_transaction.R"); stopifnot(length(phase13_normalized_publication_targets("data/competition/accepted", "data/competition/registries")) == 14L)'</automated>
</verification>

<success_criteria>
The normalized accepted publication succeeds only as a locked, fully validated 14-target transaction that regenerates every canonical and derived hash, preserves stable identity/source provenance and truthful EURO pre_draw rows, rolls every target back on any failure, and leaves registry-side blocked refresh records untouched for Plan 13-06.
</success_criteria>

<output>
Create `.planning/phases/13-source-contracts-and-competition-registry/13-12-SUMMARY.md` when done.
</output>
