---
phase: 13-source-contracts-and-competition-registry
plan: "04"
type: execute
wave: 7
depends_on:
  - 13-03
  - 13-09
  - 13-10
files_modified:
  - R/competition/team_identity.R
  - scripts/acquire_uefa_snapshot.R
  - tests/testthat/test_phase13_source_contracts.R
  - tests/testthat/test_phase13_competition_registry.R
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
gap_closure: true
must_haves:
  truths:
    - "G-13-01 / DATA-03: Accepted fixture publication contains stable home_team_id and away_team_id values while retaining UEFA source team IDs, source display names, source_artifact_id, and edition_id."
    - "DATA-03: Accepted Nations League result rows inherit stable identity from the normalized fixture contract, preserve result status and valid score fields, and retain result-artifact and fixture-artifact provenance."
    - "DATA-03: Accepted-table normalization is stable when a later-dated fixture/result is appended, source rows are reordered, or the future result score changes; valid score-only changes preserve stable IDs, while identity or edition mutations fail closed."
    - "D-01/D-02/D-03/D-09/D-10/D-11/D-12: Acquisition uses the deterministic identity resolver for every non-empty accepted fixture table and fails closed on unresolved or ambiguous identity."
    - "D-04/D-05/D-08: Accepted fixture/result publication remains compact and provenance-linked; exact raw bytes stay in the ignored local store and parser identity remains the Git commit SHA."
    - "D-14: EURO qualifying keeps schema-complete normalized empty fixtures/results tables while lifecycle is pre_draw; no fixture or result rows are fabricated."
    - "D-01/D-02/D-03/D-04/D-05/D-08/D-16: This plan supplies the normalized table projections; Plan 13-11 regenerates the canonical/derived hash graph and Plan 13-12 owns the lock, target snapshot, atomic promotion, and complete rollback."
    - "The normalized projection is not publishable until Plan 13-11 and Plan 13-12 validate the complete target set; Plan 13-05 then proves post-normalization loader success."
  artifacts:
    - "R/competition/team_identity.R"
    - "scripts/acquire_uefa_snapshot.R"
    - "tests/testthat/test_phase13_source_contracts.R"
    - "tests/testthat/test_phase13_competition_registry.R"
  key_links:
    - "Accepted source fixtures -> phase13_normalize_fixture_rows() -> stable team_id/display/provenance fields in accepted fixtures.csv."
    - "Accepted source results + normalized fixtures -> exact source_fixture_id join -> normalized results.csv with stable team IDs, edition_id, source_artifact_id, and fixture_source_artifact_id."
    - "Normalized fixture/result outputs -> Plan 13-11 deterministic canonical/hash helpers -> Plan 13-12 all-or-nothing publication transaction."
    - "The source-shaped handoff seeds from Plans 13-09/13-10 remain inputs until Plan 13-12 promotes the normalized target set."
  prohibitions:
    - "Accepted fixtures and results must not remain source-shaped without stable identity and edition fields."
    - "The publication path must not replace UEFA source IDs or display names with canonical values."
    - "EURO pre_draw normalized fixtures/results must not gain invented rows or structures."
    - "This plan must not compute standings, form windows, forecast fields, or competition rules."
    - "A source-shaped seed hash from Plans 13-09/13-10 must not remain as the final canonical hash after normalized publication."
    - "The normalized publication must not update one accepted table or registry view without updating every linked canonical and derived hash in the same transaction."
---

<objective>
Close the G-13-01 and accepted-result DATA-03 gaps by wiring the existing identity resolver into accepted fixture and result publication, then sealing the complete normalized provenance graph.

Purpose: Make accepted production records consumable by later phases by wiring stable identity into fixture/result projection while preserving source values, provenance, and truthful EURO pre_draw rows. Deterministic hash regeneration and the atomic publication boundary are deliberately owned by Plans 13-11 and 13-12 so each implementation slice has bounded file ownership.
Output: Normalized fixture/result APIs, production acquisition wiring, and focused identity/provenance regressions handed to the canonical/hash and transaction plans.
</objective>

<execution_context>
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-source-contracts-and-competition-registry/13-03-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
@scripts/acquire_uefa_snapshot.R
@R/competition/source_contracts.R
@R/competition/team_identity.R
@tests/testthat/test_phase13_source_contracts.R
@tests/testthat/test_phase13_competition_registry.R
</execution_context>

## Artifacts This Phase Produces

- `R/competition/team_identity.R` with accepted fixture/result schemas and normalizers tied to the stable identity registry.
- `scripts/acquire_uefa_snapshot.R` with identity resolution wired into accepted fixture/result publication.
- Normalized Nations League and EURO fixture/result CSVs with stable IDs, preserved source values, edition provenance, and row hashes.
- Focused source/registry tests proving direct-ID mapping, visible fallback, ambiguity rejection, accepted-result joins, and truthful EURO pre_draw empties.
- Normalized fixture/result projections handed to Plan 13-11 for canonical table/hash and registry regeneration, then to Plan 13-12 for complete staged publication and rollback.

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Capture authoritative UEFA snapshots and register both editions under one auditable contract. | 13-04, 13-05, 13-06, 13-08 | COVERED | This plan closes the accepted identity publication seam. |
| REQ | DATA-01 | Five structured resource classes remain captured as one edition-scoped candidate. | 13-07, 13-09, 13-10, 13-04 | COVERED | Fixture/result projections stay inside the accepted five-class bundle. |
| REQ | DATA-02 | URL, retrieval time, raw-byte hash, parser identity, fallback status, and table lineage remain linked. | 13-02, 13-07, 13-09, 13-04, 13-05 | COVERED | Accepted normalized rows retain source artifact IDs; raw bytes stay local. |
| REQ | DATA-03 | Stable team and edition IDs are published while source names remain visible. | 13-04 | COVERED | This is the dedicated accepted UEFA fixture/result normalization plan. |
| REQ | DATA-04 | Reviewed fallback remains explicit and edition-wide. | 13-07, 13-09, 13-10, 13-06 | COVERED | Normalization never creates a second fallback path. |
| REQ | COMP-01 | Edition, bundle, release, and output links remain durable. | 13-03, 13-05, 13-06 | COVERED | Loader and refresh plans validate the registry links around these files. |
| RESEARCH | R-01/R-02/R-03/R-04 | Compact accepted tables, bounded capture, truthful pre_draw, hashes, staged writes, and existing identity patterns. | 13-04, 13-05, 13-10 | COVERED | The normalized transaction owns the complete accepted-table/source-registry/manifest hash graph and rollback boundary without adding competition logic. |
| CONTEXT | D-01..D-16 | Locked source, provenance, identity, lifecycle, fallback, blocked, and release decisions. | 13-02, 13-07, 13-09, 13-10, 13-03, 13-04, 13-05, 13-06, 13-08 | COVERED | This plan explicitly cites D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11/D-12/D-14/D-16 for normalized publication and rollback; refresh-batch and historical safeguards remain dedicated later tasks. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer">
  <name>Task 13-04-01: Wire normalized fixture publication through acquisition</name>
  <precondition>Plan 13-03 identity and edition registries plus the Plan 13-09/13-10 source-shaped Nations League and EURO handoff directories exist and validate.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-03-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-09-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-10-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    data/raw/worldcup_2026_groups.csv
    scripts/acquire_uefa_snapshot.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    tests/testthat/test_phase13_source_contracts.R
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_source_contracts.R</files>
  <behavior>
    - Production accepted publication loads the selected edition identity map and lifecycle state from the registry paths rather than injecting an unrelated map.
    - Non-empty fixture rows pass through phase13_normalize_fixture_rows() before accepted write and expose stable IDs, preserved UEFA values, edition_id, artifact provenance, mapping methods, warnings, and row hashes.
    - EURO pre_draw empty fixtures use the schema-complete normalized path and remain zero rows.
    - Unresolved or ambiguous identity aborts candidate publication before replacing the accepted directory.
  </behavior>
  <action>Source `R/competition/team_identity.R` beside the source-contract module at acquisition startup. Adapt the source/display-value separation and compact lookup-input convention in `data/raw/worldcup_2026_groups.csv` to accepted fixture rows: use its display-team distinction as an input-shape reference, then resolve through the Phase 13 identity registry while preserving UEFA source IDs and display names rather than importing World Cup group or dashboard semantics. Add acquisition seams that load the selected edition's team_identity.csv and lifecycle state, then route the fixtures resource through `phase13_source_resource_table()` and `phase13_normalize_fixture_rows()` before the accepted writer. Emit `phase13_normalized_fixture_schema()` with stable home/away team IDs, UEFA source IDs, source display names, edition_id, source_artifact_id, mapping methods/warnings, and row hashes. Keep the existing five-class candidate validation, optional/derived status provenance, exact raw-byte store, parser Git SHA, and compact publication per D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11/D-12/D-14; do not add fuzzy matching or competition logic.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
  </verify>
  <acceptance_criteria>Fixture-backed acquisition writes normalized fixtures with the exact Phase 13 schema, stable IDs, preserved source values, provenance, and valid hashes for Nations League while emitting a schema-complete empty EURO pre_draw table; unresolved or ambiguous identity fails closed.</acceptance_criteria>
  <done>One production fixture path reaches an accepted normalized fixtures.csv through the durable identity registry.</done>
</task>

<task type="auto">
  <name>Task 13-04-02: Normalize accepted competition results from fixture identity</name>
  <precondition>Task 13-04-01 publishes a normalized fixture table through the production acquisition path.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
    .planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
    scripts/acquire_uefa_snapshot.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    tests/testthat/test_phase13_source_contracts.R
    tests/testthat/test_phase13_competition_registry.R
    data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
    data/competition/accepted/uefa_nations_league_2026_27/results.csv
  </read_first>
  <files>R/competition/team_identity.R, scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_source_contracts.R, tests/testthat/test_phase13_competition_registry.R</files>
  <behavior>
    - phase13_normalized_result_schema() is the exact accepted result contract and includes stable teams, edition_id, source names, status, valid goal fields, result source_artifact_id, fixture_source_artifact_id, mapping metadata, and row_sha256.
    - phase13_normalize_accepted_result_rows() joins each result to exactly one normalized fixture by source_fixture_id, inherits identity/display/mapping fields, preserves status and valid scores, and rejects unknown or duplicate fixture keys.
    - Empty EURO pre_draw results return the exact normalized result header with zero rows.
    - A score-only change updates valid score fields while stable fixture identity and edition_id remain unchanged; changing a team identity or edition mapping is rejected by the explicit join/registry contract.
    - Accepted-table fixtures/results retain every baseline identity and edition assignment after appending a later-dated row, reordering source rows, or perturbing the later row's scores.
  </behavior>
  <action>Add the accepted-result schema and normalizer to `R/competition/team_identity.R`, then route `phase13_acquire_publish_accepted()` through it after normalized fixtures are available. Join only on the source fixture key, copy stable home/away team IDs, UEFA source IDs, source display names, edition_id, mapping methods/warnings, and fixture_source_artifact_id from the normalized fixture, preserve result status and valid home/away goal values, attach the result source_artifact_id, and recompute row hashes. Use the input-shape and required-column guards from `R/elo/preprocess.R`, the canonical/display separation checks from `R/visualization/worldcup_dashboard.R`, the staged CSV/hash discipline from `R/release/release_bundle.R`, and the bounded file-path/main-entrypoint conventions from `scripts/update_worldcup_dashboard.R` as the named PATTERNS.md analogs. Keep EURO pre_draw results schema-complete and empty. Add focused tests for exact joins, duplicate/unknown fixtures, source-value preservation, score-only updates, and identity/edition mismatches failing closed. Preserve D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-14/D-15/D-16 by keeping this projection inside the accepted edition bundle and out of lifecycle, model-release, and refresh-batch mutation.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R"); testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
  </verify>
  <acceptance_criteria>Nations League results.csv has the exact normalized result schema, stable fixture-derived identity and edition fields, preserved source status/goals/names, both artifact links, and valid hashes; EURO pre_draw results.csv has the same header and zero rows; invalid fixture identity/edition joins fail closed.</acceptance_criteria>
  <done>Accepted UEFA result rows are normalized through the stable fixture identity contract without mixing in historical, standings, rules, or refresh behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Structured source rows -> identity resolver | Source IDs and display names select durable internal identity. |
| Normalized fixtures -> normalized results | Result rows inherit identity only through an exact source fixture key. |
| Accepted normalized tables -> source manifests/registries | Compact tables claim artifact and bundle provenance. |
| EURO pre_draw metadata -> empty normalized tables | Lifecycle state controls whether zero-row output is truthful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-04-01 | Spoofing | `phase13_normalize_fixture_rows()` | high | mitigate | Resolve source IDs first, expose normalized-name warnings, and reject unresolved or ambiguous identity before promotion. |
| T-13-04-02 | Tampering | `phase13_normalize_accepted_result_rows()` | high | mitigate | Require one exact fixture foreign key, copy identity/provenance from the normalized fixture, preserve source status/goals, and recompute hashes. |
| T-13-04-03 | Information Disclosure | accepted CSV publication | medium | mitigate | Commit compact normalized tables only and keep exact raw bytes under the ignored local root. |
| T-13-04-04 | Denial of Service | EURO pre_draw normalization | medium | mitigate | Require exact empty schemas and reject fabricated rows or schema drift. |
| T-13-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is planned. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R"); testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
<automated>test -z "$(git ls-files data/competition/local_raw)"</automated>
</verification>

<success_criteria>
G-13-01 is closed when fixture-backed acquisition publishes stable home/away xGelo IDs in accepted fixtures.csv and results.csv for Nations League, preserves UEFA source IDs/display names/status/valid scores and edition/artifact provenance, emits normalized empty EURO pre_draw tables, and passes the focused identity/provenance gates without tracking raw bytes; Plans 13-11 and 13-12 then own canonical/derived hash refresh, all-target promotion, and rollback.
</success_criteria>

<output>
Create `.planning/phases/13-source-contracts-and-competition-registry/13-04-SUMMARY.md` when done.
</output>
