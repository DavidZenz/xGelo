---
phase: 13-source-contracts-and-competition-registry
plan: "04"
type: execute
wave: 4
depends_on:
  - "13-02"
  - "13-03"
files_modified:
  - R/competition/team_identity.R
  - scripts/acquire_uefa_snapshot.R
  - tests/testthat/test_phase13_source_contracts.R
  - tests/testthat/test_phase13_competition_registry.R
  - tests/fixtures/phase13/martj42_history_sample.csv
  - tests/fixtures/phase13/martj42_history_edition_map.csv
  - data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
  - data/competition/accepted/uefa_nations_league_2026_27/results.csv
  - data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv
  - data/competition/accepted/uefa_euro_2028_qualifying/results.csv
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
gap_closure: true
must_haves:
  truths:
    - "G-13-01 / DATA-03: Fixture-backed accepted publication contains stable home_team_id and away_team_id values while retaining UEFA source team IDs, source display names, source_artifact_id, and edition_id."
    - "DATA-03: Accepted Nations League result rows are normalized through the fixture identity contract to stable home_team_id, away_team_id, and edition_id values while retaining result status, scores, UEFA source IDs, source display names, result-artifact provenance, and mapping metadata."
    - "DATA-03: Open historical rows loaded by preprocess_martj42() pass through phase13_normalize_historical_result_rows() with an explicit edition lookup, preserving source names, dates, scores, and tournament fields while rejecting unresolved identity or edition mappings without using future outcomes for identity assignment."
    - "D-01/D-02/D-03/D-09/D-10/D-11/D-12: The acquisition path uses the existing deterministic identity resolver for every accepted fixture table and fails closed on unresolved or ambiguous identity."
    - "D-04/D-05/D-08: Accepted fixture publication remains compact and provenance-linked; exact raw bytes stay in the ignored local store and parser identity remains the Git commit SHA."
    - "D-14: EURO qualifying keeps a schema-complete normalized empty fixtures table while lifecycle is pre_draw; no fixture rows are fabricated."
  artifacts:
    - "R/competition/team_identity.R"
    - "scripts/acquire_uefa_snapshot.R"
    - "data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv"
    - "data/competition/accepted/uefa_nations_league_2026_27/results.csv"
    - "data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv"
    - "data/competition/accepted/uefa_euro_2028_qualifying/results.csv"
    - "tests/fixtures/phase13/martj42_history_sample.csv"
    - "tests/fixtures/phase13/martj42_history_edition_map.csv"
    - "tests/testthat/test_phase13_source_contracts.R"
    - "tests/testthat/test_phase13_competition_registry.R"
  key_links:
    - "phase13_source_resource_table(fixtures) -> phase13_normalize_fixture_rows() -> accepted/<edition_id>/fixtures.csv"
    - "phase13_source_resource_table(results) + normalized fixtures -> phase13_normalize_accepted_result_rows() -> accepted/<edition_id>/results.csv"
    - "preprocess_martj42() -> phase13_normalize_historical_result_rows() -> stable historical result rows with explicit edition mapping"
    - "Accepted fixture source_artifact_id -> source_artifacts.csv fixture artifact -> edition_id and source bundle lineage"
    - "Fixture/result publication -> phase13_normalized_fixture_schema(), phase13_normalized_result_schema(), and phase13_row_sha256()"
  prohibitions:
    - "The accepted fixtures artifact must not remain source-shaped without stable home and away team IDs."
    - "The accepted results artifact must not remain source-shaped without stable home_team_id, away_team_id, and edition_id values."
    - "The publication path must not replace UEFA source IDs or display names with canonical values."
    - "The historical normalizer must not infer identity or edition IDs from match scores, future rows, or forecast state, and must not change preprocess_martj42()'s existing Elo output contract."
    - "The pre_draw EURO fixture table must not gain invented rows or structures."
---

<objective>
Close the G-13-01 and DATA-03 verification gaps: the acquisition script currently writes source-shaped fixtures/results and never invokes the identity contract for accepted result or open historical rows.

Purpose: Make the accepted production fixture artifact satisfy DATA-03 and the Phase 13 identity contract at the exact publication seam, while preserving the existing five-class structured capture and compact-output boundary.
Output: A production acquisition path that publishes normalized fixture and result rows for both editions, a bounded identity seam for open historical results, updated compact accepted fixture/result CSVs, and regression assertions for both record families.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VERIFICATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-02-SUMMARY.md
@.planning/phases/13-source-contracts-and-competition-registry/13-03-SUMMARY.md
@scripts/acquire_uefa_snapshot.R
@R/competition/source_contracts.R
@R/competition/team_identity.R
@R/elo/preprocess.R
@_targets.R
@tests/testthat/test_phase13_source_contracts.R
@tests/testthat/test_phase13_competition_registry.R
</context>

## Artifacts This Phase Produces

- `R/competition/team_identity.R` with accepted-result and open-historical-result schemas/normalizers tied to the Phase 13 identity resolver.
- `scripts/acquire_uefa_snapshot.R` with the identity resolver wired into accepted fixture and result publication.
- `data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv` and `results.csv` with normalized stable IDs and preserved UEFA values.
- `data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv` and `results.csv` with normalized empty pre_draw schemas.
- `tests/fixtures/phase13/martj42_history_sample.csv` and `martj42_history_edition_map.csv` as compact open-history identity/edition fixtures.
- `tests/testthat/test_phase13_source_contracts.R` and `test_phase13_competition_registry.R` with fixture-backed accepted-publication and result/history regressions.

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Capture authoritative UEFA snapshots and register both editions under one auditable contract | 13-04, 13-05, 13-06 | COVERED | This plan closes the accepted fixture/result identity break and adds the bounded open-history identity seam without changing the phase boundary. |
| REQ | DATA-01 | Five structured resource classes remain captured as one edition-scoped candidate | 13-04, 13-05 | COVERED | The candidate still captures all five source classes; accepted fixtures/results receive identity-bearing projections while groups, standings, and status retain source-shaped contracts. |
| REQ | DATA-02 | URL, retrieval time, raw-byte hash, parser identity, and fallback status remain linked | 13-04, 13-05, 13-06 | COVERED | Fixture and result rows retain source_artifact_id/fixture_source_artifact_id and accepted manifests remain the provenance authority. |
| REQ | DATA-03 | Stable team and edition IDs are published while source names are preserved for accepted UEFA fixtures/results and open historical results | 13-04 | COVERED | The fixture seam, accepted-result seam, and preprocess_martj42()-fed historical normalizer close the complete result/history publication path. |
| REQ | DATA-04 | Reviewed fallback remains explicit and edition-wide | 13-06 | COVERED | The failure path retains the existing reviewed-fallback gate and never promotes an unreviewed candidate. |
| REQ | COMP-01 | Edition, bundle, release, and output links remain durable | 13-05, 13-06 | COVERED | Loader and refresh plans validate the registry links around this artifact. |
| RESEARCH | R-01/R-04 | Compact accepted tables, ignored raw bytes, staged writes, SHA-256, and testthat conventions | 13-04, 13-05, 13-06 | COVERED | Existing Phase 13 helpers are reused; no package or runtime is added. |
| RESEARCH | R-02/R-03 | Bounded structured capture, historical-result compatibility, and truthful EURO pre_draw behavior | 13-04 | COVERED | Fixture/result replay remains deterministic; the existing preprocess_martj42() output is consumed through an explicit identity/edition seam; live official capture is a separate human backstop. |
| CONTEXT | D-01 through D-16 | Locked source, provenance, identity, lifecycle, fallback, and release decisions | 13-04, 13-05, 13-06 | COVERED | The fixture/result/history tasks cite D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11/D-12/D-14; later gap plans preserve the remaining D-IDs and blocked/recovery decisions. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer">
  <name>Task 13-04-01: Wire normalized fixture publication through acquisition</name>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-VERIFICATION.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    scripts/acquire_uefa_snapshot.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    tests/testthat/test_phase13_source_contracts.R
  </read_first>
  <files>scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_source_contracts.R</files>
  <action>
    Trace G-13-01 from 13-VERIFICATION.md: this is an execution wiring miss because phase13_normalize_fixture_rows() already resolves and hashes rows in the registry test but the production writer bypasses it. Source R/competition/team_identity.R beside source_contracts.R at script startup and add a small acquisition seam named phase13_acquire_load_identity_map() that reads the edition registry's team_identity.csv through the existing identity loader before accepted publication. Add a phase13_acquire_read_lifecycle_state() seam that obtains the selected edition lifecycle state from competition_editions.csv so the normalizer can enforce pre_draw empty-input rules.

    Change phase13_acquire_publish_accepted() and its resource-table writer so the fixtures artifact is flattened with phase13_source_resource_table(), then passed to phase13_normalize_fixture_rows() with the candidate fixture artifact ID, the candidate edition_id, the loaded identity map, and the selected lifecycle state. Keep groups, standings, and status on their existing source-shaped writer path; Task 13-04-02 owns the result branch after the normalized fixture table is available. The normalized writer must emit phase13_normalized_fixture_schema(), including home_team_id, away_team_id, home_uefa_source_team_id, away_uefa_source_team_id, home_display_name, away_display_name, edition_id, source_artifact_id, mapping methods, warning fields, and row_sha256. For the EURO pre_draw empty resource, call the existing schema-complete empty normalized-table path and emit zero rows. Preserve structured-only validation, candidate-before-acceptance, exact local raw bytes, parser Git SHA, and compact CSV publication per D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11/D-12/D-14. Do not add fuzzy identity matching or a second fallback publication path.

    Extend the fixture-backed acquisition test setup to copy the compact team_identity.csv and competition_editions.csv inputs into its temporary registry root, so the test exercises the same production lookup path rather than injecting an in-memory identity map.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
  </verify>
  <acceptance_criteria>In the fixture-backed CLI acceptance test, the published fixtures.csv is produced by phase13_normalize_fixture_rows(), has the full phase13_normalized_fixture_schema(), and contains stable IDs plus preserved source values; an unresolved or ambiguous identity still aborts candidate publication.</acceptance_criteria>
  <done>One real fixture-backed acquisition path reaches an accepted normalized fixtures.csv through the existing identity resolver, with no change to the five-class source contract or raw-byte publication boundary.</done>
</task>

<task type="auto">
  <name>Task 13-04-02: Normalize accepted competition results and open historical result rows</name>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-VERIFICATION.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    .planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
    scripts/acquire_uefa_snapshot.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    R/elo/preprocess.R
    _targets.R
    tests/testthat/test_phase13_source_contracts.R
    tests/testthat/test_phase13_competition_registry.R
    data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
    data/competition/accepted/uefa_nations_league_2026_27/results.csv
    data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv
    data/competition/accepted/uefa_euro_2028_qualifying/results.csv
    data/raw/martj42/results.csv
  </read_first>
  <files>R/competition/team_identity.R, scripts/acquire_uefa_snapshot.R, tests/testthat/test_phase13_source_contracts.R, tests/testthat/test_phase13_competition_registry.R, tests/fixtures/phase13/martj42_history_sample.csv, tests/fixtures/phase13/martj42_history_edition_map.csv</files>
  <behavior>
    - `phase13_normalized_result_schema()` is the exact accepted result contract: `schema_version`, `edition_id`, `fixture_id`, `source_fixture_id`, `home_team_id`, `away_team_id`, `home_uefa_source_team_id`, `away_uefa_source_team_id`, `home_display_name`, `away_display_name`, `status`, `home_goals`, `away_goals`, `source_artifact_id`, `fixture_source_artifact_id`, `home_mapping_method`, `away_mapping_method`, `home_mapping_warning`, `away_mapping_warning`, and `row_sha256`.
    - `phase13_normalize_accepted_result_rows(source_results, normalized_fixtures, edition_id, result_source_artifact_id)` joins each source result to exactly one normalized fixture by `source_fixture_id`, carries the fixture's stable team IDs, UEFA source IDs, source display names, mapping metadata, and edition ID, retains source result status and goal values, records both result-artifact and fixture-artifact provenance, and rejects unknown or duplicate fixture keys.
    - `phase13_normalized_historical_result_schema()` is the exact open-history contract: `schema_version`, `source_dataset`, `source_result_id`, `edition_id`, `result_date`, `home_team_id`, `away_team_id`, `home_display_name`, `away_display_name`, `home_canonical_name`, `away_canonical_name`, `home_fifa_code`, `away_fifa_code`, `home_goals`, `away_goals`, `tournament`, `neutral`, `source_artifact_id`, `home_mapping_method`, `away_mapping_method`, `home_mapping_warning`, `away_mapping_warning`, and `row_sha256`.
    - `phase13_normalize_historical_result_rows(results, identity_map, edition_lookup, source_dataset, source_artifact_id)` consumes the data-frame returned by the established `preprocess_martj42()` loader plus a required one-to-one `edition_lookup` keyed by its stable `match_id`; it resolves both source display names through `phase13_resolve_team_identity()`, preserves source names/date/scores/tournament/neutral fields, and rejects missing or ambiguous identity/edition mappings.
    - Historical normalization is row-local and point-in-time safe: team identity and edition joins use source IDs/names and the explicit lookup only, never scores, future rows, forecast state, or a guessed edition; the existing `preprocess_martj42()` and `_targets.R` Elo output contract is unchanged.
  </behavior>
  <action>
    Add `phase13_normalized_result_schema()`, `phase13_normalized_historical_result_schema()`, `phase13_normalize_accepted_result_rows(source_results, normalized_fixtures, edition_id, result_source_artifact_id)`, and `phase13_normalize_historical_result_rows(results, identity_map, edition_lookup, source_dataset, source_artifact_id)` to `R/competition/team_identity.R`. The accepted-result normalizer must consume the source-shaped table from `phase13_source_resource_table(results, ...)` and the in-memory output of `phase13_normalize_fixture_rows()`, join only on `source_fixture_id`, require one matching fixture per result, carry `fixture_id`, `home_team_id`, `away_team_id`, `home_uefa_source_team_id`, `away_uefa_source_team_id`, `home_display_name`, `away_display_name`, mapping methods/warnings, and `edition_id`, preserve `status`, `home_goals`, and `away_goals` exactly, set the result `source_artifact_id` from the explicit `result_source_artifact_id` argument plus `fixture_source_artifact_id`, and recompute `row_sha256` over the exact normalized schema. An empty EURO `pre_draw` result input must return the exact normalized result header with zero rows.

    Use the existing `preprocess_martj42()` return value as the open historical input seam rather than changing that Elo loader or `_targets.R`: its current output has `date`, `home_team`, `away_team`, `home_score`, `away_score`, `tournament`, `neutral`, and `match_id`, but no Phase 13 edition identity. Make `phase13_normalize_historical_result_rows()` require the explicit `source_dataset` and `source_artifact_id` provenance arguments plus an `edition_lookup` with exactly `match_id` and `edition_id`, unique keys, and complete coverage of the loaded history. Map `source_result_id` from the stable input `match_id`, `source_dataset`/`source_artifact_id` from those explicit arguments, `edition_id` only from the lookup, source display names/date/scores/tournament/neutral from the input rows, and canonical names/FIFA codes from the resolved identity rows. Resolve each `home_team` and `away_team` with `phase13_resolve_team_identity()` using missing source IDs and the preserved source display names, emit the exact historical schema above, keep normalized-name fallback warnings visible, and fail closed on unresolved or ambiguous aliases. The function must not use scores or future rows to establish identity or edition membership, must not filter completed outcomes into forecast inputs, and must not overwrite `data/processed/elo_matches.csv`.

    Extend `scripts/acquire_uefa_snapshot.R` so `phase13_acquire_publish_accepted()` retains the normalized fixture table, passes the source results resource and its artifact ID to `phase13_normalize_accepted_result_rows()`, and writes normalized `results.csv` alongside normalized `fixtures.csv`; groups, standings, and status remain source-shaped contract tables. Add compact `martj42_history_sample.csv` and `martj42_history_edition_map.csv` fixtures, and add focused tests that first feed a compact `preprocess_martj42()`-shaped result frame through the historical normalizer, verify source-name/score/date preservation and explicit edition IDs, then cover missing/ambiguous edition keys, unresolved/ambiguous aliases, and score mutation without identity drift. Cite D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11/D-12/D-14 and preserve D-06/D-07/D-13/D-15/D-16 by keeping result normalization inside the same accepted edition bundle, retaining review/provenance metadata, rejecting partial candidates, and avoiding any lifecycle/model-release behavior in this seam. Do not add standings calculations, form windows, forecast fields, or competition rules.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R"); testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
    <automated>Rscript --vanilla -e 'source("R/competition/source_contracts.R"); source("R/competition/team_identity.R"); nl <- read.csv("data/competition/accepted/uefa_nations_league_2026_27/results.csv", stringsAsFactors = FALSE, check.names = FALSE); eu <- read.csv("data/competition/accepted/uefa_euro_2028_qualifying/results.csv", stringsAsFactors = FALSE, check.names = FALSE); stopifnot(identical(names(nl), phase13_normalized_result_schema()), identical(names(eu), phase13_normalized_result_schema()), nrow(eu) == 0L, all(nl$edition_id == "uefa_nations_league_2026_27"), all(nl$row_sha256 == phase13_row_sha256(nl)))'</automated>
  </verify>
  <acceptance_criteria>Accepted Nations League results.csv has the exact normalized result schema, stable home/away team IDs and edition_id copied from the normalized fixture identity contract, preserved UEFA source IDs/display names/status/goals, both artifact provenance links, and valid row hashes; EURO pre_draw results.csv has the same header and zero rows; a compact data frame returned by preprocess_martj42() can be normalized only with an explicit edition lookup and retains source names/date/scores/tournament/neutral values; missing or ambiguous identity/edition mappings and score-only identity changes fail closed.</acceptance_criteria>
  <done>DATA-03 covers both accepted UEFA result records and open historical result rows through explicit Phase 13 identity/edition seams without changing the established Elo loader output or introducing data leakage.</done>
</task>

<task type="auto">
  <name>Task 13-04-03: Seal normalized accepted fixture and result artifacts and schema regressions</name>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-VERIFICATION.md
    .planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    scripts/acquire_uefa_snapshot.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    tests/testthat/test_phase13_source_contracts.R
    tests/testthat/test_phase13_competition_registry.R
    data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv
    data/competition/accepted/uefa_nations_league_2026_27/results.csv
    data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv
    data/competition/accepted/uefa_euro_2028_qualifying/results.csv
  </read_first>
  <files>tests/testthat/test_phase13_source_contracts.R, data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv, data/competition/accepted/uefa_nations_league_2026_27/results.csv, data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv, data/competition/accepted/uefa_euro_2028_qualifying/results.csv</files>
  <action>
    Trace G-13-01 and the DATA-03 result seam from 13-VERIFICATION.md with independent fixture- and result-backed assertions. Extend the existing acquisition regression to read the accepted fixtures.csv and results.csv and recompute both row-hash columns with phase13_row_sha256(). Assert the Nations League sample exposes home_team_id = team_aut and away_team_id = team_deu, source team IDs 100 and 101, display names Austria and Germany, the requested edition_id, the candidate fixtures source_artifact_id, and source-ID mapping methods with no warning token. Assert the fixture output column set is exactly phase13_normalized_fixture_schema(), and assert the result output column set is exactly phase13_normalized_result_schema(), with the same stable team IDs/display names plus the result source_artifact_id and fixture_source_artifact_id.

    Add companion assertions for uefa_euro_2028_qualifying that its pre_draw fixtures.csv and results.csv have their respective normalized column sets and zero rows, while the source-shaped groups, standings, and status contracts remain schema-valid. Regenerate only the four compact committed fixtures.csv/results.csv artifacts from deterministic fixture replay after the tests pass; retain the existing source_bundle_manifest.csv and source registry metadata, keep exact raw response bodies in data/competition/local_raw only, and verify no raw/local file becomes tracked per D-01/D-04/D-09/D-11/D-12/D-14. Keep live official URL capture as a human backstop rather than treating fixture replay as evidence that current UEFA endpoints are available.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R")'</automated>
    <automated>Rscript --vanilla -e 'source("R/competition/source_contracts.R"); source("R/competition/team_identity.R"); nl_f <- read.csv("data/competition/accepted/uefa_nations_league_2026_27/fixtures.csv", stringsAsFactors = FALSE, check.names = FALSE); nl_r <- read.csv("data/competition/accepted/uefa_nations_league_2026_27/results.csv", stringsAsFactors = FALSE, check.names = FALSE); eu_f <- read.csv("data/competition/accepted/uefa_euro_2028_qualifying/fixtures.csv", stringsAsFactors = FALSE, check.names = FALSE); eu_r <- read.csv("data/competition/accepted/uefa_euro_2028_qualifying/results.csv", stringsAsFactors = FALSE, check.names = FALSE); stopifnot(identical(names(nl_f), phase13_normalized_fixture_schema()), identical(names(nl_r), phase13_normalized_result_schema()), identical(names(eu_f), phase13_normalized_fixture_schema()), identical(names(eu_r), phase13_normalized_result_schema()), nrow(eu_f) == 0L, nrow(eu_r) == 0L, all(nl_f$row_sha256 == phase13_row_sha256(nl_f)), all(nl_r$row_sha256 == phase13_row_sha256(nl_r)))'</automated>
    <automated>test "$(git ls-files data/competition/local_raw | wc -l | tr -d " ")" = "0"</automated>
  </verify>
  <acceptance_criteria>The focused suite proves accepted fixtures.csv and results.csv preserve source IDs/display names/status/goal values while adding stable IDs, edition provenance, mapping metadata, and valid hashes; both EURO pre_draw identity-bearing artifacts are empty but schema-valid; source-shaped groups/standings/status remain valid; and the ignored raw store has no tracked files.</acceptance_criteria>
  <done>G-13-01 and the accepted-result portion of DATA-03 are closed in production artifacts and regression coverage, with compact Git outputs and truthful pre_draw handling preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Structured UEFA payload or compact fixture -> acquisition script | Source rows and source metadata are untrusted input to schema and identity resolution. |
| Team identity registry -> normalized fixture publication | Stable internal IDs are selected from a durable mapping contract. |
| Normalized fixture identity -> accepted result publication | Result rows inherit stable teams/display names only through an exact source_fixture_id join. |
| preprocess_martj42() history -> Phase 13 historical result contract | Open historical names and edition assignments cross an explicit identity/edition-map boundary. |
| Ignored raw store -> compact accepted CSVs | Exact bytes remain local while hashes and normalized rows cross into committed artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-04-01 | Tampering | scripts/acquire_uefa_snapshot.R fixture writer | high | mitigate | Validate the five structured resources and invoke phase13_normalize_fixture_rows() before accepted fixture publication; recompute row hashes. |
| T-13-04-02 | Spoofing | R/competition/team_identity.R resolver | high | mitigate | Use source-ID-first resolution, visible normalized-name warnings, and ambiguity errors for every non-empty fixture row. |
| T-13-04-03 | Information Disclosure | data/competition/local_raw | medium | mitigate | Keep exact response bodies under the ignored local root and commit only compact accepted tables and manifests. |
| T-13-04-04 | Denial of Service | EURO pre_draw empty input | medium | mitigate | Require the normalized empty schema and reject fabricated rows or schema drift before publication. |
| T-13-04-05 | Tampering | `phase13_normalize_accepted_result_rows()` | high | mitigate | Require one exact fixture foreign key per result, copy identity/display/provenance fields from the normalized fixture, preserve source status/goals, and recompute result row hashes. |
| T-13-04-06 | Spoofing / Information Integrity | `phase13_normalize_historical_result_rows()` | high | mitigate | Consume only the established `preprocess_martj42()` output plus an explicit unique edition lookup, resolve source names through the checked identity map, preserve source fields, and reject missing/ambiguous mappings without score- or future-row joins. |
| T-13-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is planned; the existing R runtime and packages are reused. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_source_contracts.R"); testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
<automated>Rscript --vanilla -e 'testthat::test_dir("tests/testthat")'</automated>
<human-check>As a Phase 13 backstop, an operator supplies five current official HTTPS structured URLs to scripts/acquire_uefa_snapshot.R for the Nations League and inspects the accepted manifest plus normalized fixtures/results. The check must confirm structured JSON, five resource classes, URL/retrieval/hash/parser/fallback metadata, stable team/edition IDs, preserved source display names/status/goals, and prior-output retention on failure. This live check is not replaced by or inferred from deterministic fixture replay; EURO remains an explicit pre_draw check until the official draw snapshot exists.</human-check>
</verification>

<success_criteria>
G-13-01 and the result/history DATA-03 gap are closed when fixture-backed acquisition publishes stable home and away xGelo IDs in accepted fixtures.csv and results.csv for the Nations League, preserves UEFA source IDs/display names/status/goals and edition provenance, emits normalized empty schemas for EURO pre_draw, proves the preprocess_martj42()-fed historical seam with explicit edition mapping, and the focused plus full test gates pass without tracking raw bytes.
</success_criteria>

<output>
Create .planning/phases/13-source-contracts-and-competition-registry/13-04-SUMMARY.md when done.
</output>
