---
phase: 13-source-contracts-and-competition-registry
plan: "08"
type: execute
wave: 11
depends_on:
  - 13-05
  - 13-12
files_modified:
  - R/competition/team_identity.R
  - _targets.R
  - tests/testthat/test_phase13_competition_registry.R
  - tests/fixtures/phase13/martj42_history_sample.csv
  - tests/fixtures/phase13/martj42_history_edition_map.csv
  - tests/fixtures/phase13/martj42_identity_map_cases.csv
  - data/competition/registries/martj42_identity_map.csv
  - data/competition/registries/martj42_edition_lookup.csv
  - data/processed/martj42_historical_normalized.csv
autonomous: true
requirements:
  - DATA-03
gap_closure: true
must_haves:
  truths:
    - "DATA-03/D-09/D-10/D-11/D-12: preprocess_martj42()-shaped historical rows normalize through stable team identity and an explicit one-to-one edition lookup while preserving source names, dates, scores, tournament, neutral, and provenance fields."
    - "DATA-03/D-09/D-11/D-12: The production martj42 path consumes the existing preprocess_martj42() output through a named targets seam and persists data/processed/martj42_historical_normalized.csv with source IDs when present, source display names, stable team_id values, and edition_id values."
    - "DATA-03/D-09/D-10/D-11/D-12: A production martj42 identity map is generated from the complete preprocessed historical input with explicit map version, source version, source-input SHA-256, source artifact provenance, deterministic row hashes, and a map hash before historical normalization consumes it."
    - "The full historical input has exactly one identity-map row for every unique home/away source identity; unresolved, ambiguous, duplicate, malformed, or conflicting mappings fail closed before any normalized artifact replacement."
    - "Historical normalization is future-row safe: appending a later-dated row, reordering rows, or perturbing a future row's scores does not change existing rows' team_id or edition_id assignments or source identity fields."
    - "A valid score-only mutation changes only the historical score fields; stable home_team_id, away_team_id, and edition_id remain unchanged."
    - "Changing a source identity or the explicit edition mapping fails closed rather than silently reassigning a historical row."
  artifacts:
    - "R/competition/team_identity.R"
    - "_targets.R"
    - "tests/fixtures/phase13/martj42_history_sample.csv"
    - "tests/fixtures/phase13/martj42_history_edition_map.csv"
    - "tests/fixtures/phase13/martj42_identity_map_cases.csv"
    - "data/competition/registries/martj42_identity_map.csv"
    - "data/competition/registries/martj42_edition_lookup.csv"
    - "data/processed/martj42_historical_normalized.csv"
    - "tests/testthat/test_phase13_competition_registry.R"
  key_links:
    - "preprocess_martj42() output shape -> phase13_generate_martj42_identity_map() -> phase13_validate_martj42_identity_coverage() -> phase13_normalize_historical_result_rows() -> stable historical result schema."
    - "elo_matches target -> phase13_load_martj42_historical_results() -> data/processed/martj42_historical_normalized.csv; the existing preprocess_martj42() boundary remains unchanged."
    - "Stable match_id -> explicit edition_lookup -> edition_id; no score, row order, or future-row value participates in the mapping."
    - "Source team name/ID -> phase13_resolve_team_identity() -> stable home/away team_id with visible fallback warning or fail-closed ambiguity."
    - "Complete historical input -> martj42_identity_map.csv source/version/hash/provenance -> one-to-one coverage check over every source identity token."
  prohibitions:
    - "Historical identity or edition assignment must not use match scores, future rows, forecast state, or guessed edition membership."
    - "A score-only change must not alter team_id or edition_id, and identity/edition changes must not be accepted as score edits."
    - "The existing preprocess_martj42() and `_targets.R` Elo output contract must not be rewritten by this plan."
    - "A partial identity map, sample-only map, or guessed historical coverage must not be accepted as the production map."
---

<objective>
Close the historical-normalization portion of DATA-03 with explicit edition mapping and regression coverage for future-row and score-only safety.

Purpose: Make open historical results deterministic and point-in-time safe before Phase 14 consumes them, while keeping the existing martj42 preprocessing and Elo output interfaces unchanged.
Output: Historical result schema/normalizer, compact martj42-shaped fixtures, a checked production edition lookup, a targets-integrated loader seam, the durable `data/processed/martj42_historical_normalized.csv` artifact, and focused regressions for append, reorder, future perturbation, score-only edits, and fail-closed identity/edition changes.
</objective>

<execution_context>
@.planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
@.planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
@.planning/phases/13-source-contracts-and-competition-registry/13-VALIDATION.md
@.planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
@.planning/phases/13-source-contracts-and-competition-registry/13-05-PLAN.md
@R/competition/team_identity.R
@R/elo/preprocess.R
@_targets.R
@data/raw/martj42/results.csv
@data/raw/team_name_map.csv
@data/competition/registries/team_identity.csv
@tests/testthat/test_pipeline.R
@R/release/release_bundle.R
@tests/testthat/test_phase13_competition_registry.R
</execution_context>

## Artifacts This Phase Produces

- `R/competition/team_identity.R` historical result schema, deterministic martj42 identity-map generator, full-coverage validator, and explicit identity/edition normalizer.
- `tests/fixtures/phase13/martj42_history_sample.csv`, `martj42_history_edition_map.csv`, and `martj42_identity_map_cases.csv` compact source/history and malformed-map fixtures.
- `data/competition/registries/martj42_identity_map.csv` complete production source-identity map with source/version/hash/provenance metadata.
- `data/competition/registries/martj42_edition_lookup.csv` checked one-to-one production match-to-edition mapping.
- `_targets.R` and `phase13_load_martj42_historical_results()` production seam downstream of the unchanged `preprocess_martj42()` target.
- `data/processed/martj42_historical_normalized.csv` durable normalized historical artifact with source/provenance, display, stable-team, edition, score, and row-hash fields.
- `tests/testthat/test_phase13_competition_registry.R` future-row safety, score-only stability, fail-closed identity/edition regressions, and production artifact/target-seam coverage.

## Source Coverage Audit

| SOURCE | ID | Feature or requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 13 | Capture authoritative UEFA snapshots and register both editions under one auditable contract. | 13-08, 13-03, 13-04, 13-05, 13-06 | COVERED | Historical normalization completes the open-results side of the source contract. |
| REQ | DATA-01 | Official five-class UEFA snapshots remain captured and accepted. | 13-07, 13-09, 13-10, 13-05 | COVERED | This plan does not alter official capture. |
| REQ | DATA-02 | Historical rows retain source artifact and parser provenance. | 13-02, 13-07, 13-08 | COVERED | Explicit source_dataset/source_artifact_id arguments are required. |
| REQ | DATA-03 | UEFA and open historical results normalize to stable team/edition IDs while preserving source values. | 13-04, 13-08 | COVERED | Plan 13-04 covers accepted UEFA results; this plan covers open history. |
| REQ | DATA-04 | Reviewed fallback remains edition-wide and visible. | 13-07, 13-09, 13-10, 13-06 | COVERED | Historical normalization does not create fallback provenance. |
| REQ | COMP-01 | Edition registry remains the authority for explicit competition IDs. | 13-03, 13-05, 13-06 | COVERED | Historical rows use explicit edition lookup, not inferred lifecycle. |
| RESEARCH | R-01/R-04 | Reuse file-based schemas, aliases, hashes, and source-style testthat. | 13-08 | COVERED | No new runtime or package is introduced. |
| CONTEXT | D-09/D-10/D-11/D-12 | Stable canonical team identity, visible fallback, preserved names, explicit edition IDs. | 13-03, 13-04, 13-08 | COVERED | Every regression cites the locked identity/edition behavior. |

Deferred Phase 14-17 ideas remain excluded.

<tasks>

<task type="tracer">
  <name>Task 13-08-01: Add the explicit historical result normalization seam</name>
  <precondition>Plans 13-05 and 13-12 have established the Phase 13 identity resolver, registry schemas, accepted-source validation, and normalized UEFA publication conventions.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-04-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-05-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
    R/competition/team_identity.R
    R/elo/preprocess.R
    _targets.R
    data/raw/martj42/results.csv
    data/raw/team_name_map.csv
    data/competition/registries/team_identity.csv
    tests/testthat/test_phase13_competition_registry.R
  </read_first>
  <files>R/competition/team_identity.R, tests/fixtures/phase13/martj42_history_sample.csv, tests/fixtures/phase13/martj42_history_edition_map.csv, tests/fixtures/phase13/martj42_identity_map_cases.csv</files>
  <behavior>
    - `phase13_generate_martj42_identity_map()` emits one deterministic mapping row per source identity token with `identity_map_version`, `source_dataset`, explicit `source_version`, exact `source_input_sha256`, `source_artifact_id`, mapping method/warning/review fields, stable `team_id`, row hashes, and a canonical map hash.
    - Map generation and validation reject missing provenance, malformed hash/version fields, duplicate source identity keys, duplicate map rows, conflicting team_id assignments, unresolved identities, and ambiguous normalized aliases.
    - `phase13_validate_martj42_identity_coverage()` requires every unique home/away identity in the supplied historical input to resolve to exactly one map row before normalization can proceed.
  </behavior>
  <action>First create `tests/fixtures/phase13/martj42_history_sample.csv`, `tests/fixtures/phase13/martj42_history_edition_map.csv`, and `tests/fixtures/phase13/martj42_identity_map_cases.csv`; these fixture outputs are created by this task and must exist before the focused test is run, so they are intentionally not read_first prerequisites. Then add `phase13_generate_martj42_identity_map()`, `phase13_validate_martj42_identity_coverage()`, `phase13_normalized_historical_result_schema()`, and `phase13_normalize_historical_result_rows()` to `R/competition/team_identity.R`. Generate the map from the complete supplied martj42-shaped history, enumerate every unique home/away source identity token, resolve it through the stable registry, and persist deterministic `identity_map_version`, `source_version`, exact `source_input_sha256`, `source_artifact_id`, row hashes, and canonical map hash; reject missing provenance, malformed metadata, duplicates, conflicts, unresolved tokens, and ambiguous aliases. Require the data-frame shape returned by `preprocess_martj42()` plus explicit `source_dataset`, `source_artifact_id`, and a one-to-one `edition_lookup` keyed by stable `match_id`; run the full coverage validator before resolving rows, preserve `home_team`/`away_team` as display names, copy optional source IDs unchanged, derive edition_id only from the lookup, attach visible mapping warnings, and recompute row hashes. Make the fixtures cover known rows, a later-dated row, accented names, optional source IDs, and malformed/duplicate/conflicting/ambiguous map cases. Use the analog paths `scripts/release_bundle.R`, `scripts/worldcup_dashboard.R`, `scripts/elo/preprocess.R`, and `scripts/update_worldcup_dashboard.R` named in 13-PATTERNS.md, following the mapped `R/release/`, `R/visualization/`, and `R/elo/` implementations in this checkout. Keep the existing `preprocess_martj42()` function and its Elo output boundary unchanged; the production target wiring is a separate task. Keep scores, dates, row order, and future rows out of identity/edition assignment and implement D-09/D-10/D-11/D-12 and DATA-03 exactly through the explicit mapping seam.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
  </verify>
  <acceptance_criteria>The task creates all three compact fixtures before focused verification; generation from a complete history produces a deterministic source/version/hash/provenance identity map, full identity coverage is required, malformed/duplicate/conflicting/unresolved/ambiguous map cases fail closed, and the historical frame normalizes only with a complete explicit edition lookup while preserving source IDs, display names, source values, warnings, and provenance.</acceptance_criteria>
  <done>The historical result contract, deterministic identity-map generator/coverage seam, and three source-shaped fixtures exist as a row-local identity/edition projection independent of scores and future rows.</done>
</task>

<task type="auto">
  <name>Task 13-08-02: Prove future-row and score-only normalization invariants</name>
  <precondition>Task 13-08-01 provides a passing historical normalizer and compact history fixtures.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-08-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-RESEARCH.md
    R/competition/team_identity.R
    tests/fixtures/phase13/martj42_history_sample.csv
    tests/fixtures/phase13/martj42_history_edition_map.csv
    tests/testthat/test_phase13_competition_registry.R
  </read_first>
  <files>tests/testthat/test_phase13_competition_registry.R</files>
  <behavior>
    - Baseline normalization produces stable source_result_id, home_team_id, away_team_id, and edition_id assignments for every fixture row.
    - The identity-map fixture cases prove missing source version/hash/provenance, duplicate source identity keys, conflicting mappings for one source token, unresolved names, and ambiguous normalized aliases fail closed before any historical row is emitted.
    - Appending a future-dated row, reordering all rows, and perturbing the future row's scores leave the baseline rows' team IDs, edition IDs, source names, and source_result_ids identical.
    - Changing only a valid baseline row's home_goals/away_goals changes those score fields while its home_team_id, away_team_id, and edition_id remain identical.
    - Changing a baseline row's source team identity or its edition_lookup value raises a fail-closed error; it is not treated as a score-only update.
  </behavior>
  <action>Extend `tests/testthat/test_phase13_competition_registry.R` with a baseline normalized historical result and fixture-backed malformed/duplicate/conflicting identity-map cases, then create three independent regression variants: append a later-dated row, reorder the combined input, and perturb the later row's scores. Compare the normalized baseline subset by stable source_result_id and assert unchanged team IDs, edition IDs, source names, and mapping metadata. Add a score-only variant with valid numeric goal changes and assert that only the score fields change. Add separate identity-name/source-ID and edition-lookup mutations and require explicit errors. Use the named analog paths `scripts/release_bundle.R`, `scripts/worldcup_dashboard.R`, `scripts/elo/preprocess.R`, and `scripts/update_worldcup_dashboard.R` from `13-PATTERNS.md` when structuring deterministic fixture inputs, source/display assertions, input guards, and bounded test entrypoints; follow mapped `R/release/`, `R/visualization/`, and `R/elo/` files where this checkout differs. The tests must prove full identity-map coverage, fail-closed unresolved/ambiguous/duplicate/conflicting cases, and identity/edition assignment independent of row order, future rows, and score values while preserving D-09 through D-12.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
  </verify>
  <acceptance_criteria>Future-row append, reorder, and future-score perturbation regressions preserve all baseline identity/edition assignments; valid score-only edits preserve team_id and edition_id while changing score fields; identity or edition changes fail closed.</acceptance_criteria>
  <done>DATA-03 historical normalization is demonstrably future-row safe and distinguishes permitted score changes from identity/edition contract violations.</done>
</task>

<task type="auto">
  <name>Task 13-08-03: Wire martj42 normalization into the production target and durable artifact</name>
  <precondition>Tasks 13-08-01 and 13-08-02 provide the identity-map generator, full-coverage validator, passing historical normalizer, optional source-ID preservation, and malformed/duplicate/conflicting mapping regressions.</precondition>
  <read_first>
    .planning/phases/13-source-contracts-and-competition-registry/13-08-PLAN.md
    .planning/phases/13-source-contracts-and-competition-registry/13-CONTEXT.md
    .planning/phases/13-source-contracts-and-competition-registry/13-PATTERNS.md
    R/elo/preprocess.R
    R/competition/source_contracts.R
    R/competition/team_identity.R
    _targets.R
    data/raw/martj42/results.csv
    data/raw/team_name_map.csv
    data/competition/registries/team_identity.csv
    data/competition/registries/martj42_identity_map.csv
    tests/testthat/test_pipeline.R
    tests/testthat/test_phase13_competition_registry.R
    R/release/release_bundle.R
  </read_first>
  <files>R/competition/team_identity.R, _targets.R, data/competition/registries/martj42_identity_map.csv, data/competition/registries/martj42_edition_lookup.csv, data/processed/martj42_historical_normalized.csv, tests/testthat/test_phase13_competition_registry.R</files>
  <behavior>
    - `phase13_load_martj42_historical_results()` reads the file produced by the existing `preprocess_martj42()` boundary, deterministically generates or re-materializes `martj42_identity_map.csv` from the complete input, verifies its source/version/hash/provenance metadata, runs full identity coverage, loads the complete one-to-one edition lookup, and writes the exact normalized historical schema atomically.
    - The normalized artifact preserves `home_source_team_id`/`away_source_team_id` when present, preserves martj42 source display names in dedicated display columns, resolves stable `home_team_id`/`away_team_id`, records explicit `edition_id`, carries source_dataset/source_artifact_id, and recomputes row hashes.
    - `_targets.R` sources the Phase 13 modules and declares `martj42_historical_normalized_file` downstream of `elo_matches`, returning `data/processed/martj42_historical_normalized.csv` with `format = "file"`; the existing `preprocess_martj42()` and Elo output target remain unchanged.
    - Missing, duplicate, or incomplete production identity-map/edition mappings, missing map provenance, unresolved or ambiguous identity, conflicting mappings, or a changed source identity fails before the durable artifact is replaced.
  </behavior>
  <action>Create the durable `data/competition/registries/martj42_identity_map.csv` first by running `phase13_generate_martj42_identity_map()` over the complete production output of `preprocess_martj42()`, using an explicit `source_version`, exact `source_input_sha256`, `source_artifact_id`, `identity_map_version`, deterministic row hashes, and a canonical map hash; validate that every unique home/away source identity appears exactly once and fail closed on unresolved, ambiguous, duplicate, malformed, or conflicting mappings. Then create `data/competition/registries/martj42_edition_lookup.csv` with a checked one-to-one row for every production preprocessed `match_id`, explicit `edition_id`, `source_dataset`, `source_artifact_id`, schema version, and row hash; reject duplicate or unknown match IDs rather than deriving edition membership from scores, dates, row order, or tournament text. Add `phase13_load_martj42_historical_results(results_path = "data/processed/elo_matches.csv", identity_registry_path = "data/competition/registries/team_identity.csv", identity_map_path = "data/competition/registries/martj42_identity_map.csv", edition_lookup_path = "data/competition/registries/martj42_edition_lookup.csv", source_dataset = "martj42", source_artifact_id = "martj42-results", output_path = "data/processed/martj42_historical_normalized.csv")` to `R/competition/team_identity.R`; it must regenerate or deterministically re-materialize the identity map in a temporary location from the full input, compare source/version/hash/provenance to the committed map, run `phase13_validate_martj42_identity_coverage()` over every historical row, call `phase13_normalize_historical_result_rows()`, validate the explicit edition map and identity provenance, write the compact normalized artifact through the existing staged CSV helper from `R/competition/source_contracts.R`, and return the output path for targets. Source `R/competition/source_contracts.R` and `R/competition/team_identity.R` in `_targets.R`, then add `tar_target(martj42_historical_normalized_file, { elo_matches; phase13_load_martj42_historical_results(...) }, format = "file")` immediately downstream of `elo_matches` so the production path is reachable without changing `preprocess_martj42()` or the existing `elo_matches`/Elo outputs. Extend the Phase 13 registry tests with temporary full-input coverage, optional source IDs, a temporary checked identity/map/edition registry, loader invocation, malformed/duplicate/conflicting/unresolved/ambiguous cases, read-back schema/hash assertions, and a static target declaration assertion. Follow the staged compact-write and manifest/hash discipline from `R/release/release_bundle.R`, `scripts/release_bundle.R`, the source/display-value separation from `R/visualization/worldcup_dashboard.R` and `scripts/worldcup_dashboard.R`, the input-shape guards from `R/elo/preprocess.R` and `scripts/elo/preprocess.R`, and the bounded path/main-entrypoint conventions from `scripts/update_worldcup_dashboard.R`; these four named analog paths are the required references from 13-PATTERNS.md. Implement D-09/D-10/D-11/D-12 and DATA-03 exactly; keep this seam limited to historical source normalization and durable artifact production, with no standings, form, forecast, simulation, or competition-rule behavior.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
    <automated>test -f data/processed/martj42_historical_normalized.csv && rg -n 'martj42_historical_normalized_file|phase13_load_martj42_historical_results' _targets.R</automated>
  </verify>
  <acceptance_criteria>The production target graph consumes the unchanged `preprocess_martj42()` output only after generating and validating the complete `martj42_identity_map.csv` with deterministic source/version/hash/provenance, proving one-to-one coverage for every historical source identity, and rejecting malformed/duplicate/conflicting/unresolved/ambiguous mappings; it then writes `data/processed/martj42_historical_normalized.csv` with source IDs when present, source display names, stable team IDs, explicit edition IDs, provenance, and valid row hashes before replacement.</acceptance_criteria>
  <done>DATA-03 has a reachable martj42 ingestion/targets seam, an explicit full-coverage production identity-map artifact, and a durable normalized historical artifact; the existing preprocessing and Elo output contracts remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| martj42-shaped history -> identity resolver | Open source names and optional IDs cross into canonical xGelo identity. |
| match_id -> edition lookup | Explicit historical edition assignment crosses a caller-provided mapping boundary. |
| Historical scores/future rows -> normalized identity | Untrusted outcomes must not influence stable identity or edition membership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-08-01 | Spoofing | historical team identity resolution | high | mitigate | Use source-ID-first resolution, deterministic normalized aliases with visible warnings, and ambiguity rejection. |
| T-13-08-02 | Tampering | explicit edition lookup | high | mitigate | Require unique complete match_id coverage and reject changed/unknown edition mappings. |
| T-13-08-03 | Information Integrity | future-row/score invariants | high | mitigate | Regression tests compare baseline IDs/editions under append, reorder, future perturbation, and score-only edits. |
| T-13-08-04 | Tampering | durable normalized historical artifact and targets seam | high | mitigate | Validate the unchanged preprocess output, identity/edition provenance, schema, and row hashes before staged replacement; expose the artifact only through the downstream `format = "file"` target. |
| T-13-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install tasks exist. |
</threat_model>

<verification>
<automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase13_competition_registry.R")'</automated>
<automated>test -f data/processed/martj42_historical_normalized.csv && rg -n 'martj42_historical_normalized_file|phase13_load_martj42_historical_results' _targets.R</automated>
</verification>

<success_criteria>
Plan 13-08 succeeds when open historical rows normalize through explicit edition and identity contracts, future-row append/reorder/perturbation tests pass, score-only edits preserve team_id/edition_id while changing valid scores, and identity/edition mutations fail closed without changing the established Elo preprocessing contract.
</success_criteria>

<output>
Create `.planning/phases/13-source-contracts-and-competition-registry/13-08-SUMMARY.md` when done.
</output>
