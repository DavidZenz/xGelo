---
phase: 09-rolling-tournament-benchmark-harness
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/testthat/helper_benchmark.R
  - tests/testthat/test_benchmark_registry.R
  - tests/testthat/test_benchmark_cutoffs.R
  - tests/testthat/test_benchmark_seal.R
  - data/benchmark/phase09/tournaments.csv
  - data/benchmark/phase09/fixtures.csv
  - data/benchmark/phase09/teams.csv
  - data/benchmark/phase09/formats.csv
  - data/benchmark/phase09/route_rules.csv
  - data/benchmark/phase09/corrections.csv
  - data/benchmark/phase09/boundaries.csv
  - data/benchmark/phase09/SOURCES.md
  - R/benchmark/registry.R
  - R/benchmark/cutoffs.R
autonomous: true
requirements: [BENCH-01, BENCH-02, BENCH-03]

must_haves:
  truths:
    - "The development registry contains exactly the fixed 12-tournament World Cup/Euro panel and 630 unique official fixtures per D-01 and D-05."
    - "Frozen and pre-match-updating tracks use deterministic date-complete boundaries, strict prior-only evidence, and no future tournament information per D-02, D-03, and D-10."
    - "Any WC2026 outcome-bearing row is rejected before fitting, feature selection, tuning, calibration, or adapter invocation."
    - "Every fixture uses a stable team_id backed by a checked FIFA code, with source-attributed regulation/status corrections and no display-name joins."
    - "Every historical stage/status/regulation correction is a validated, authoritative, source-attributed local input before Plan 09-02 starts; unresolved provenance stops sealing and triggers a blocking approval checkpoint."
  artifacts:
    - path: "data/benchmark/phase09/tournaments.csv"
      provides: "The sealed 12-edition fold inventory"
      contains: "wc2002"
    - path: "data/benchmark/phase09/fixtures.csv"
      provides: "The 630-fixture assessment denominator with regulation outcomes and provenance"
      contains: "actual_completion_date"
    - path: "data/benchmark/phase09/boundaries.csv"
      provides: "Frozen and date-batch updating cutoffs"
      contains: "evidence_cutoff_exclusive"
    - path: "data/benchmark/phase09/corrections.csv"
      provides: "Machine-readable authoritative local correction inputs and provenance hashes"
      contains: "source_artifact_sha256"
    - path: "R/benchmark/registry.R"
      provides: "Fail-fast registry loading, validation, path safety, and canonical SHA-256"
      exports: ["load_benchmark_registries", "validate_benchmark_registries", "canonical_benchmark_sha256"]
    - path: "R/benchmark/cutoffs.R"
      provides: "Rolling state construction and the WC2026 purpose gate"
      exports: ["make_benchmark_boundaries", "build_benchmark_track_states", "guard_benchmark_purpose"]
  key_links:
    - from: "R/benchmark/registry.R"
      to: "data/benchmark/phase09/fixtures.csv"
      via: "validated registry_dir loaders and foreign-key checks"
      pattern: "load_benchmark_registries|validate_benchmark_registries"
    - from: "R/benchmark/cutoffs.R"
      to: "data/benchmark/phase09/boundaries.csv"
      via: "date-complete boundary generation and checksum reconciliation"
      pattern: "make_benchmark_boundaries|evidence_cutoff_exclusive"
    - from: "guard_benchmark_purpose"
      to: "every later model adapter"
      via: "purpose-gated outcome access before adapter invocation"
      pattern: "development|baseline_reproduction|candidate_selection"
---

<objective>
Freeze the historical tournament registry and implement the leakage boundary that every later baseline and challenger must obey.

Purpose: BENCH-01 and BENCH-02 are meaningless unless fixture identity, regulation outcomes, tournament formats, date-complete state transitions, and the WC2026 seal are authoritative before model code runs. This plan implements D-01, D-02, D-03, D-05, and D-10, and establishes the stable identity portion of D-15.
Output: Checked registry CSVs and provenance notes, reusable synthetic fixtures, registry validators, cutoff state services, and adversarial seal tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VALIDATION.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-03-SUMMARY.md

<interfaces>
Existing strict-cutoff and immutable-ledger interfaces to reuse:

- `R/forecast/features.R`: `latest_team_value_before()`, `assert_no_feature_leakage()` use strict source date `<` lookup/cutoff semantics.
- `R/evaluation/worldcup_ledger.R`: `validate_fixture_registry()`, `forecast_revision_hash()`, `write_checksum_manifest()` provide fail-fast cardinality, deterministic ordering, provenance, and artifact-manifest patterns.
- `data/processed/elo_matches.csv`: local historical result source; target inventory is 630 assessment fixtures but lacks the curated stage/status/regulation contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 benchmark fixtures and executable contract tests</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VALIDATION.md</file>
    <file>tests/testthat/test_worldcup_scoring.R</file>
    <file>tests/testthat/test_worldcup_retrospective.R</file>
    <file>tests/testthat/test_transfermarkt_benchmark.R</file>
    <file>R/evaluation/proper_scores.R</file>
    <file>R/evaluation/worldcup_ledger.R</file>
    <file>R/forecast/features.R</file>
  </read_first>
  <files>tests/testthat/helper_benchmark.R, tests/testthat/test_benchmark_registry.R, tests/testthat/test_benchmark_cutoffs.R, tests/testthat/test_benchmark_seal.R</files>
  <action>Create `helper_benchmark.R` with `synthetic_benchmark_registries()`, `synthetic_benchmark_history()`, `synthetic_format_fixtures()`, `fixed_benchmark_score_grid()`, and `recording_benchmark_adapter()`; helpers return in-memory data and write only to test temporary directories. Add registry tests for 12 editions, 630 unique fixtures, expected per-edition counts, Euro 2020's edition/played-year distinction, all three format families, complete team/FIFA identities, regulation/final-score separation, and canonical hash stability. Add cutoff tests proving all same-date fixtures share one pre-date state, date order permutation cannot change state hashes, frozen state is pre-opener, and prior-boundary linkage is complete per D-02, D-03, and D-10. Add seal tests that inject WC2026 labels into fit, feature-selection, tuning, and calibration roles and prove the recording adapter is never called, implementing BENCH-02. Tests may be red until Tasks 2-3, but all files must parse and helpers must source now.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'files &lt;- c("tests/testthat/helper_benchmark.R", "tests/testthat/test_benchmark_registry.R", "tests/testthat/test_benchmark_cutoffs.R", "tests/testthat/test_benchmark_seal.R"); invisible(lapply(files, parse)); source(files[[1]]); stopifnot(all(vapply(c("synthetic_benchmark_registries", "synthetic_benchmark_history", "synthetic_format_fixtures", "fixed_benchmark_score_grid", "recording_benchmark_adapter"), exists, logical(1), mode = "function")))'</automated>
  </verify>
  <acceptance_criteria>
    - Every Wave 0 test file referenced by 09-VALIDATION exists and parses.
    - Synthetic fixtures cover all three format families, same-date ordering, future-row injection, and WC2026 adapter non-invocation.
    - Tests assert exact registered denominators rather than discovering scope from available predictions.
  </acceptance_criteria>
  <done>The benchmark registry, cutoff, and seal expectations are executable before production services are added.</done>
</task>

<task type="auto">
  <name>Task 2: Curate and freeze the canonical historical registries</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VALIDATION.md</file>
    <file>data/processed/elo_matches.csv</file>
    <file>data/raw/martj42/results.csv</file>
    <file>data/raw/team_name_map.csv</file>
    <file>data/raw/martj42/shootouts.csv</file>
    <file>data/raw/verified_score_fallbacks.csv</file>
    <file>data/processed/verified_score_fallback_audit.csv</file>
    <file>R/elo/preprocess.R</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
  </read_first>
  <files>data/benchmark/phase09/tournaments.csv, data/benchmark/phase09/fixtures.csv, data/benchmark/phase09/teams.csv, data/benchmark/phase09/formats.csv, data/benchmark/phase09/route_rules.csv, data/benchmark/phase09/corrections.csv, data/benchmark/phase09/boundaries.csv, data/benchmark/phase09/SOURCES.md</files>
  <action>Build checked-in, cache-only registries for the exact World Cups 2002/2006/2010/2014/2018/2022 and Euros 2004/2008/2012/2016/2020/2024 per D-01 and D-05. `fixtures.csv` must contain all 630 rows and the full schema researched for identity, stage/group/round, scheduled and actual completion dates, boundary, venue, regulation goals, final goals, extra time, penalties, winner, status, fit/score eligibility, exclusion reason, source/license, and row SHA-256. Resolve Republic of Ireland, China, Scotland, and every other participant once in `teams.csv` with stable `team_id`, checked FIFA code, canonical display name, aliases, and historical-entity lineage; later code must not join display names. Encode the three format families and routing rules as data, including Euro best-third routes. Materialize 12 frozen and 272 updating date-complete boundaries. Create `corrections.csv` as the machine-readable local input for every curated stage/status/regulation change, with authoritative source title/URL, access date, license, affected fixture ID, original and corrected values, rationale, reviewer, source-artifact SHA-256, and row SHA-256; mirror its human-readable evidence in `SOURCES.md`. Establish authoritative local provenance autonomously and validate every correction before registry sealing. If any correction cannot be backed by authoritative local provenance, stop before Task 3 and create a blocking human-approval checkpoint for the supplied evidence; Plan 09-02 must not start until approval and corrected checksums exist. The benchmark runner must consume these local files only; do not add a downloader or any runtime network dependency. Copa America and AFCON remain excluded. Keep these interdependent registries in one curation task because their foreign keys, correction lineage, and canonical hashes form one referentially closed source-of-truth.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 't &lt;- read.csv("data/benchmark/phase09/tournaments.csv"); f &lt;- read.csv("data/benchmark/phase09/fixtures.csv"); tm &lt;- read.csv("data/benchmark/phase09/teams.csv"); b &lt;- read.csv("data/benchmark/phase09/boundaries.csv"); c &lt;- read.csv("data/benchmark/phase09/corrections.csv"); req &lt;- c("fixture_id", "field", "original_value", "corrected_value", "source_title", "source_url", "access_date", "license", "rationale", "reviewer", "source_artifact_sha256", "row_sha256"); stopifnot(nrow(t) == 12L, nrow(f) == 630L, anyDuplicated(f$fixture_id) == 0L, all(f$home_team_id %in% tm$team_id), all(f$away_team_id %in% tm$team_id), all(nzchar(tm$fifa_code)), sum(b$track == "frozen") == 12L, sum(b$track == "updating") == 272L, all(!is.na(f$regulation_home_goals[f$score_eligible])), all(!is.na(f$regulation_away_goals[f$score_eligible])), all(req %in% names(c)), all(c$fixture_id %in% f$fixture_id), all(nzchar(c$source_title)), all(nzchar(c$source_url)), all(nzchar(c$license)), all(grepl("^[0-9a-f]{64}$", c$source_artifact_sha256)), all(grepl("^[0-9a-f]{64}$", c$row_sha256)))'</automated>
  </verify>
  <acceptance_criteria>
    - Tournament counts are 64 for each World Cup, 31 for Euros 2004-2012, and 51 for Euros 2016-2024.
    - Open-core scope is all 630 fixtures; excluded or abnormal matches remain explicit rows with stable reasons.
    - No assessment row conflates regulation goals, final-after-extra-time goals, or shootout winner.
    - Registry curation is reproducible from checked local data plus the source-attributed correction ledger; benchmark execution needs no network.
  </acceptance_criteria>
  <done>The historical denominator, format routing, identities, outcomes, and boundaries are frozen as auditable local artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement registry validation, deterministic boundaries, and the WC2026 seal</name>
  <read_first>
    <file>R/evaluation/worldcup_ledger.R</file>
    <file>R/forecast/features.R</file>
    <file>R/forecast/goal_ability.R</file>
    <file>tests/testthat/helper_benchmark.R</file>
    <file>tests/testthat/test_benchmark_registry.R</file>
    <file>tests/testthat/test_benchmark_cutoffs.R</file>
    <file>tests/testthat/test_benchmark_seal.R</file>
    <file>data/benchmark/phase09/tournaments.csv</file>
    <file>data/benchmark/phase09/fixtures.csv</file>
    <file>data/benchmark/phase09/teams.csv</file>
    <file>data/benchmark/phase09/formats.csv</file>
    <file>data/benchmark/phase09/route_rules.csv</file>
    <file>data/benchmark/phase09/corrections.csv</file>
    <file>data/benchmark/phase09/boundaries.csv</file>
    <file>data/benchmark/phase09/SOURCES.md</file>
  </read_first>
  <files>R/benchmark/registry.R, R/benchmark/cutoffs.R, tests/testthat/test_benchmark_registry.R, tests/testthat/test_benchmark_cutoffs.R, tests/testthat/test_benchmark_seal.R</files>
  <behavior>
    - Registry load: exact 12/630 counts, foreign keys, allowed statuses, verified score fields, format routes, and hashes pass; any silent row loss or identity gap fails.
    - Cutoff state: frozen evidence is before opener; updating evidence is strictly before the complete assessment date; same-date row ordering yields identical states.
    - Seal: outcome-bearing `wc2026` rows fail for `development`, `baseline_reproduction`, and `candidate_selection` before any supplied adapter callback runs.
    - Integrity: canonical sorted serialization yields the same SHA-256 after harmless input row permutation.
  </behavior>
  <action>Implement `benchmark_registry_paths()`, `load_benchmark_registries()`, `validate_benchmark_registries()`, `validate_benchmark_registry_paths()`, `canonical_benchmark_sha256()`, and `benchmark_registry_manifest()` in `registry.R`. Restrict path inputs to normalized project-relative registered roots, reject path traversal/absolute external paths, validate required/allowed columns and enums, exact cardinalities, all cross-table keys, score/status consistency, source/license fields, correction-to-fixture/source foreign keys, authoritative local correction provenance, and canonical sorted hashes. Refuse to produce the Plan 09-01 completion manifest while any correction is unverified or awaiting human approval, making Plan 09-02's dependency gate executable. Implement `make_benchmark_boundaries()`, `eligible_benchmark_history()`, `assert_benchmark_cutoffs()`, `build_benchmark_track_states()`, and `guard_benchmark_purpose()` in `cutoffs.R`. Use `actual_completion_date` as the date-batch key, strict `< evidence_cutoff_exclusive`, one state per full date batch, one frozen pre-opener state, and result unlock only at the next boundary per D-02, D-03, and D-10. The purpose guard must redact or reject WC2026 labels for every development role per BENCH-02 and D-20 while allowing label-free sealed fixture identities. Do not fabricate kickoff precision. Make all tests from Task 1 green.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_registry.R"); testthat::test_file("tests/testthat/test_benchmark_cutoffs.R"); testthat::test_file("tests/testthat/test_benchmark_seal.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Validators fail before downstream use on malformed rows, unknown keys/enums, noncanonical hashes, unverified score-eligible regulation outcomes, or paths outside approved roots.
    - Every same-date fixture shares the same result/feature/Elo cutoff and state hash.
    - The WC2026 seal is enforced at data access, not by a caller convention, and adversarial tests prove adapters never receive forbidden labels.
    - Registry SHA-256 values are order-stable and include source provenance.
  </acceptance_criteria>
  <done>BENCH-01/02 prerequisites are enforced by reusable, tested services rather than by model-specific filtering.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `tests/testthat/helper_benchmark.R` | `synthetic_benchmark_registries()`, `synthetic_benchmark_history()`, `synthetic_format_fixtures()`, `fixed_benchmark_score_grid()`, `recording_benchmark_adapter()` |
| `tests/testthat/test_benchmark_registry.R` | Registry cardinality, identity, format, provenance, and hash tests |
| `tests/testthat/test_benchmark_cutoffs.R` | Frozen/updating, same-date, strict-before, and permutation tests |
| `tests/testthat/test_benchmark_seal.R` | WC2026 purpose-gate adversarial tests |
| `data/benchmark/phase09/tournaments.csv` | 12-edition fold registry |
| `data/benchmark/phase09/fixtures.csv` | 630-fixture assessment registry |
| `data/benchmark/phase09/teams.csv` | Stable team/FIFA identity registry |
| `data/benchmark/phase09/formats.csv` | Three tournament-format families and stage definitions |
| `data/benchmark/phase09/route_rules.csv` | Data-driven knockout and best-third routes |
| `data/benchmark/phase09/corrections.csv` | Validated machine-readable local correction inputs with authoritative source and row hashes |
| `data/benchmark/phase09/boundaries.csv` | 12 frozen plus 272 updating boundary rows |
| `data/benchmark/phase09/SOURCES.md` | Human-readable correction/source ledger |
| `R/benchmark/registry.R` | `benchmark_registry_paths()`, `load_benchmark_registries()`, `validate_benchmark_registries()`, `validate_benchmark_registry_paths()`, `canonical_benchmark_sha256()`, `benchmark_registry_manifest()` |
| `R/benchmark/cutoffs.R` | `make_benchmark_boundaries()`, `eligible_benchmark_history()`, `assert_benchmark_cutoffs()`, `build_benchmark_track_states()`, `guard_benchmark_purpose()` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cached historical files → registry validators | Local CSV rows and curated corrections are untrusted until schema, provenance, cardinality, and hashes pass. |
| Registry identity → model/scoring paths | Display names must not spoof or remap stable team/FIFA identities. |
| Sealed WC2026 data → development loaders | Label-bearing final-holdout data must never cross into fit/selection/tuning/calibration roles. |
| Configured path → filesystem | Registry paths must stay under approved project-relative roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | fixture/boundary registries | high | mitigate | Exact 12/630 cardinality, strict schemas, canonical SHA-256, source ledger, and verify-before-use in `validate_benchmark_registries()`. |
| T-09-02 | Spoofing | team identity joins | high | mitigate | One `teams.csv` with unique stable team/FIFA keys; reject missing/duplicate codes and prohibit display-name joins in tests. |
| T-09-03 | Tampering | WC2026 sealed outcomes | critical | mitigate | Purpose-gated label access in `guard_benchmark_purpose()` plus adversarial adapter non-invocation tests. |
| T-09-04 | Repudiation | registry freeze | medium | mitigate | Canonical row hashes, artifact manifest, source Git SHA, and source/correction provenance retained with every frozen registry. |
| T-09-05 | Information Disclosure | path parameters | medium | mitigate | Normalize and allowlist project-relative roots; reject traversal and absolute external paths before reads/writes. |
</threat_model>

<verification>
Run the Wave 0 focused suite:

`rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_registry.R"); testthat::test_file("tests/testthat/test_benchmark_cutoffs.R"); testthat::test_file("tests/testthat/test_benchmark_seal.R")'`

Then review every `SOURCES.md` correction against its cited record before accepting the registry checksums. This governance review does not add runtime network access to the benchmark.
</verification>

<success_criteria>
- Exactly 12 editions, 630 unique fixtures, 12 frozen boundaries, and 272 updating date batches validate.
- Stable team/FIFA keys cover every assessment fixture and all format routes reconcile.
- Same-date ordering cannot change evidence, fit, feature, or Elo state.
- WC2026 labels cannot enter any development-purpose path.
- Registry and boundary hashes are stable across reruns and harmless input ordering changes.
- All correction provenance is validated and approved where required before Plan 09-02 can begin.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-01-SUMMARY.md` when done.
</output>
