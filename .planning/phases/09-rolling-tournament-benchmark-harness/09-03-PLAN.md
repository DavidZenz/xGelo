---
phase: 09-rolling-tournament-benchmark-harness
plan: "03"
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - R/evaluation/benchmark_scores.R
  - tests/testthat/test_benchmark_scoring.R
  - R/evaluation/promotion.R
  - tests/testthat/test_benchmark_promotion.R
  - data/benchmark/phase09/promotion_protocol.json
autonomous: true
requirements: [BENCH-05]

must_haves:
  truths:
    - "Headline RPS, Brier, log loss, and calibration average each registered tournament first and then weight all 12 tournaments equally per D-04."
    - "Comparisons pair identical fixture IDs, retain all 12 fold deltas, use 10,000 deterministic tournament-level bootstrap replicates, and report breadth, maximum regression, and leave-one-tournament-out stability."
    - "Fixed one-vs-rest probability bins are shared across models and weighted so each tournament contributes equally."
    - "The promotion gate implements the exact D-16 through D-20 thresholds, ordered vetoes, optional-panel rule, and retain-incumbent default without rounded inputs."
    - "D-19 optional-data promotion requires rich-panel RPS delta <= -0.003 and paired 95% CI upper < 0 versus production_hybrid_nb, plus a complete open companion with open-core RPS delta <= 0.000, paired CI upper < 0.003, maximum fold regression <= 0.015, and every common veto passing."
    - "Rich-panel metadata declares membership, provenance, the frozen per-edition floor, and required outputs; final promotion eligibility is derived from post-adapter `output_coverage_complete` plus those declarations, never predeclared as an observed fact."
    - "Protocol, panels, candidates, seeds, tie-break order, model registration/settings hashes, and the normalized score-support-audit hash are frozen before WC2026 labels can be opened."
  artifacts:
    - path: "R/evaluation/benchmark_scores.R"
      provides: "Fixture scoring, tournament-first aggregation, fixed-bin calibration, paired deltas, uncertainty, breadth, and regression diagnostics"
      exports: ["score_benchmark_fixtures", "aggregate_benchmark_scores", "fixed_benchmark_calibration", "make_paired_fold_comparisons", "paired_tournament_bootstrap"]
    - path: "R/evaluation/promotion.R"
      provides: "Pure promotion/veto and deterministic candidate selection"
      exports: ["load_promotion_protocol", "validate_promotion_protocol", "evaluate_promotion", "select_promoted_candidate"]
    - path: "data/benchmark/phase09/promotion_protocol.json"
      provides: "Canonical checksum-backed thresholds, incumbents, panels, seeds, score-support-audit parent hash, vetoes, and tie-breaks"
      contains: "-0.003"
  key_links:
    - from: "R/evaluation/benchmark_scores.R"
      to: "R/evaluation/proper_scores.R"
      via: "shared validated RPS/Brier/log/distribution formulas"
      pattern: "ranked_probability_score|multiclass_brier|log_score"
    - from: "make_paired_fold_comparisons"
      to: "data/benchmark/phase09/panel_fixtures.csv"
      via: "exact predeclared paired fixture set asserted before deltas"
      pattern: "validate_panel_prediction_coverage|fixture_id"
    - from: "R/evaluation/promotion.R"
      to: "data/benchmark/phase09/promotion_protocol.json"
      via: "pure gate consumes validated frozen policy and checks its SHA-256"
      pattern: "load_promotion_protocol|validate_promotion_protocol"
    - from: "data/benchmark/phase09/score_support_audit.csv"
      to: "data/benchmark/phase09/promotion_protocol.json"
      via: "protocol parent hash binds the complete normalized support audit and selected G"
      pattern: "score_support_audit_sha256|selected_g"
---

<objective>
Implement the one shared scorer, paired tournament uncertainty, and frozen promotion protocol used by every baseline and later challenger.

Purpose: BENCH-05 requires a stable estimand and governance rule, not just metric calculations. This plan implements D-04 and D-16 through D-20 exactly, including optional-panel checks and deterministic tie-breaking.
Output: Tournament-first score tables, fixed-bin calibration, paired fold comparisons and uncertainty, regression diagnostics, a pure ordered promotion gate, and the checksummed policy JSON.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VALIDATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-02-SUMMARY.md

<interfaces>
Inputs produced by Plan 09-02 and existing scoring contracts:

- `validate_benchmark_predictions()`, `validate_benchmark_score_distributions()`, `validate_panel_prediction_coverage()`, and `validate_model_manifests()` fail closed before metric computation.
- `R/evaluation/proper_scores.R`: `ranked_probability_score()`, `multiclass_brier()`, `log_score()`, `binary_brier()`, `discrete_rps()`, and `score_scoreline_distribution()` define the locked score scales.
- `R/evaluation/worldcup_retrospective.R`: `metric_row()`, `bootstrap_mean()`, `aggregate_worldcup_scores()`, and `make_calibration_bins()` provide analog mechanics; Phase 09 replaces fixture bootstrap and model-specific quantile bins with tournament-paired bootstrap and shared fixed bins.
- `data/benchmark/phase09/panels.csv`, `panel_fixtures.csv`, `model_registry.csv`, `score_support_audit.csv`, and `seed_registry.csv` define declared denominators/requirements, incumbents, selected support with audit lineage, candidate metadata, and deterministic seeds. `panel_fixtures.csv` does not contain observed output completeness or final promotion eligibility.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score fixtures and compute equal-tournament paired diagnostics</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/evaluation/proper_scores.R</file>
    <file>R/evaluation/worldcup_retrospective.R</file>
    <file>tests/testthat/test_worldcup_scoring.R</file>
    <file>data/benchmark/phase09/tournaments.csv</file>
    <file>data/benchmark/phase09/fixtures.csv</file>
    <file>data/benchmark/phase09/panels.csv</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/seed_registry.csv</file>
    <file>tests/testthat/test_benchmark_contracts.R</file>
  </read_first>
  <files>R/evaluation/benchmark_scores.R, tests/testthat/test_benchmark_scoring.R</files>
  <behavior>
    - Hand-calculated 1X2 RPS/Brier/log, scoreline log, goal marginal RPS, totals/BTTS, and fixed-bin calibration cases match exact expected values.
    - Unequal tournament sizes yield a headline equal to the unweighted mean of 12 tournament means and a separately labelled fixture-weighted pooled estimate.
    - Candidate-minus-incumbent rows pair exact registered fixtures, and any missing/extra row fails instead of shrinking the sample.
    - Paired uncertainty resamples 12 tournament deltas 10,000 times with the registered seed; repeated calls are identical.
    - Outputs include 12 fold deltas, World Cup/Euro breadth, maximum regression, and leave-one-tournament-out estimates.
  </behavior>
  <action>Implement `benchmark_metric_row()`, `score_benchmark_fixtures()`, `aggregate_benchmark_scores()`, `fixed_benchmark_calibration()`, `make_paired_fold_comparisons()`, `paired_tournament_bootstrap()`, and `leave_one_tournament_out_deltas()` in `benchmark_scores.R`. Score each fixture once with existing proper-score formulas and explicit coverage; aggregate within edition, then average the 12 edition estimates equally for the headline per D-04. Keep fixture-weighted pooling secondary and labelled. For calibration, use shared one-vs-rest bins `[0,.1), ... [.9,1]`, weight each class observation by `1/(3*n_t)` within its tournament, and emit counts/sparse flags without changing bins by model. Define every lower-is-better delta as challenger minus incumbent. Assert the predeclared fixture set before pairing, retain each edition delta, compute win counts by competition, maximum positive regression, and leave-one-edition-out stability. Bootstrap the 12 paired edition deltas with 10,000 replacement draws, registered seed, and type-8 2.5/97.5 percent quantiles; never resample unpaired fixtures. Keep winner-pick accuracy descriptive only.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_scoring.R"); testthat::test_file("tests/testthat/test_worldcup_scoring.R")'</automated>
  </verify>
  <acceptance_criteria>
    - RPS uses class order home/draw/away and the existing normalized scale; Brier and log loss remain unchanged.
    - Headline and pooled values differ on the unequal-size fixture and are correctly labelled.
    - Paired comparison is invariant to row order and fails on incomplete coverage.
    - Calibration, bootstrap, fold breadth, maximum regression, and leave-one-out values are deterministic and inspectable.
  </acceptance_criteria>
  <done>The benchmark reports every required proper score and diagnostic on the locked equal-tournament estimand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze and enforce the promotion, veto, and tie-break protocol</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/evaluation/worldcup_retrospective.R</file>
    <file>data/benchmark/phase09/tournaments.csv</file>
    <file>data/benchmark/phase09/panels.csv</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/score_support_audit.csv</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/seed_registry.csv</file>
    <file>tests/testthat/test_benchmark_scoring.R</file>
  </read_first>
  <files>R/evaluation/promotion.R, tests/testthat/test_benchmark_promotion.R, data/benchmark/phase09/promotion_protocol.json</files>
  <behavior>
    - A clean candidate passes only when core RPS delta is at most `-0.003`, paired CI upper bound is strictly below zero, wins are at least 8/12 with at least two World Cups and two Euros, and maximum fold regression is at most `0.015`.
    - Relative Brier/log-loss worsening above 1 percent, calibration worsening above `0.01`, or any probability/distribution/fixture/coverage/provenance/license/seed/checksum/reproducibility failure vetoes promotion.
    - D-19 rich gate: optional-data candidate minus `production_hybrid_nb` RPS is at most `-0.003`, paired 95% CI upper is strictly below zero, breadth/regression gates pass, panel coverage is promotion-eligible, and every common veto passes.
    - Rich-panel promotion eligibility is computed from observed post-adapter `output_coverage_complete`, predeclared point-in-time provenance, and the frozen per-edition coverage floor; declarations alone cannot pass the gate.
    - D-19 open companion gate: all 630 open-core fixtures are covered; companion minus `open_nb_incumbent` RPS is at most `0.000`, paired CI upper is strictly below `0.003`, maximum fold regression is at most `0.015`, default-open mode is preserved, and every common veto passes.
    - Exact fold ties do not count as wins; exact promotion-threshold boundaries obey the declared inclusive/strict operators; a candidate self-comparison yields zero deltas and retains the incumbent.
    - Among multiple fully eligible passers, deterministic tie-breaks are lowest core headline RPS, then log loss, Brier, calibration error, registered complexity rank, then model_id; an exact tie retains the incumbent.
  </behavior>
  <action>Create canonical `promotion_protocol.json` with schema/protocol versions, incumbent IDs, primary updating/open-core panel, frozen secondary track, all 12 edition IDs, fixed bins, 10,000 bootstrap repetitions and seed ID, candidate/panel/model registration/settings hashes, the exact `score_support_audit_sha256` parent hash and selected G, D-16's `rps_delta &lt;= -0.003` and CI-upper `&lt; 0` checks, D-17's 8/12 plus 2 World Cup plus 2 Euro breadth and `max_fold_regression &lt;= 0.015`, D-18's 1% relative Brier/log-loss and `0.01` calibration vetoes, all hard contract vetoes, and D-20's code/features/settings/panels/seeds/checksum freeze and final WC2026 conditions. Validate that the support-audit hash covers every normalized candidate row and parent hash and that `model_registry.csv` remains registration metadata rather than an embedded audit. Freeze D-19 numerically: an optional-data rich variant must compare on the exact declared paired panel against `production_hybrid_nb`; after adapter generation, compute per-edition `output_coverage_complete` from required prediction/distribution rows and derive final promotion eligibility from that observed coverage, predeclared point-in-time provenance, and the frozen 80% floor. Only then may the rich variant achieve tournament-weighted RPS delta `&lt;= -0.003`, paired 95% CI upper `&lt; 0`, at least 8/12 fold wins including two World Cups and two Euros, maximum fold regression `&lt;= 0.015`, and every D-18/common contract veto. Its registered open companion must cover all 630 fixtures against `open_nb_incumbent`, achieve RPS delta `&lt;= 0.000`, paired CI upper `&lt; 0.003`, maximum fold regression `&lt;= 0.015`, preserve default-open operation, and pass the same Brier/log-loss/calibration plus probability/distribution/fixture/coverage/point-in-time-provenance/license/seed/checksum/reproducibility vetoes. Incomplete observed output coverage, a descriptive-only rich result, or a missing open companion is promotion-ineligible while open-core evaluation continues. Freeze candidate selection tie-break order as lowest core headline RPS, log loss, Brier, ECE, then lower registered complexity rank and lexical model_id; exact equality with the applicable incumbent retains the incumbent. Implement `canonicalize_promotion_protocol()`, `load_promotion_protocol()`, `validate_promotion_protocol()`, `promotion_veto_reasons()`, `evaluate_promotion()`, and `select_promoted_candidate()` as pure functions. Gate order is hard contracts/coverage, core effect and CI, breadth/regression, supporting metrics/calibration, optional panel, then reproducibility/seal. Emit all gate values/booleans and ordered reason codes; contract failure returns `veto`, clean statistical failure returns `retain_incumbent`, and only every-pass returns `eligible_for_final_holdout`. Add exact-boundary tests for rich delta `-0.003`, rich CI upper `0`, open delta `0.000`, open CI upper `0.003`, open maximum fold regression `0.015`, and one-step values on both sides; add cases proving declarations cannot substitute for observed `output_coverage_complete` and a support-audit parent-hash mismatch vetoes. Do not round gate inputs. This plan freezes the Phase 12 opening contract but must not read or score WC2026 labels.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_promotion.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Every threshold is tested immediately below, exactly at, and immediately above its boundary with no rounding.
    - D-19 rich/open companion thresholds have dedicated exact-boundary tests, including strict rejection at rich CI upper `0` and open CI upper `0.003`.
    - One missing prediction, malformed distribution, provenance/license failure, hash mismatch, or nondeterministic rerun produces a veto reason.
    - Final rich-panel eligibility is false until post-adapter output coverage is complete for every required row and every edition meets the frozen threshold with point-in-time provenance.
    - Promotion-protocol validation fails when the normalized score-support-audit hash, selected G, or any parent hash does not match the frozen audit artifact.
    - Self-comparison has exact zero deltas/interval and retains the incumbent; weaker controls cannot pass.
    - Protocol canonical serialization and SHA-256 are stable across JSON key/input ordering.
    - The protocol contains no WC2026 result value and cannot open the holdout.
  </acceptance_criteria>
  <done>The practical effect, uncertainty, breadth, regression, supporting-score, coverage, optional-panel, reproducibility, and tie-break decisions are frozen and executable.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/evaluation/benchmark_scores.R` | `benchmark_metric_row()`, `score_benchmark_fixtures()`, `aggregate_benchmark_scores()`, `fixed_benchmark_calibration()`, `make_paired_fold_comparisons()`, `paired_tournament_bootstrap()`, `leave_one_tournament_out_deltas()` |
| `tests/testthat/test_benchmark_scoring.R` | Hand-calculated scoring, fixed-bin calibration, equal-tournament, paired-bootstrap, breadth, regression, and leave-one-out tests |
| `R/evaluation/promotion.R` | `canonicalize_promotion_protocol()`, `load_promotion_protocol()`, `validate_promotion_protocol()`, `promotion_veto_reasons()`, `evaluate_promotion()`, `select_promoted_candidate()` |
| `tests/testthat/test_benchmark_promotion.R` | Boundary-exact gates, ordered vetoes, self-comparison, weaker-control, optional-panel, hash, and tie-break tests |
| `data/benchmark/phase09/promotion_protocol.json` | Frozen incumbents, panels, tracks, candidates, thresholds, vetoes, seeds, normalized score-support-audit parent hash/selected G, final-holdout conditions, tie-break order, and SHA-256 metadata |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated prediction rows → metric aggregation | Row filtering, incorrect weights, or altered score scales can change the promotion estimand. |
| Paired model outputs → uncertainty | Unpaired/missing fixtures or fixture-level resampling can create false confidence. |
| Frozen protocol → promotion decision | Post-result threshold/panel/candidate changes would invalidate governance. |
| Optional panel → default-open promotion | A rich-data win must not hide regression or missing open-mode behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Tampering | headline aggregation/calibration | high | mitigate | Tournament-first tables, explicit aggregation labels, fixed shared bins, exact scale regression tests, and no rounded gate inputs. |
| T-09-13 | Tampering | paired uncertainty | high | mitigate | Assert exact panel fixture IDs, aggregate pairwise within each edition, resample 12 edition deltas with a registered seed. |
| T-09-14 | Repudiation | promotion protocol changes | high | mitigate | Canonical JSON, registry parent hashes, Git SHA, verify-before-decision, and all gate values/reasons in output. |
| T-09-15 | Information Disclosure | optional-data comparison | medium | mitigate | License/coverage flags are hard vetoes; only aggregate rich features/results are emitted and open-core companion remains mandatory. |
| T-09-16 | Elevation of Privilege | WC2026 holdout opening | critical | mitigate | Phase 09 protocol records final conditions but exposes no label-opening path; Plan 09-01 purpose guard remains mandatory. |
</threat_model>

<verification>
Run scoring and promotion suites together:

`rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_scoring.R"); testthat::test_file("tests/testthat/test_benchmark_promotion.R"); testthat::test_file("tests/testthat/test_worldcup_scoring.R")'`

Inspect the canonical protocol JSON to confirm the registered checksums, thresholds, panel IDs, incumbent IDs, seeds, and tie-break order are frozen before any WC2026 final-evaluation operation.
</verification>

<success_criteria>
- RPS, Brier, log loss, fixed-bin calibration, scoreline/goal diagnostics, paired fold deltas, breadth, maximum regression, leave-one-out stability, and tournament uncertainty are emitted.
- Equal-tournament headline weighting and fixture-pooled secondary weighting are both explicit and tested.
- Every D-16 through D-20 threshold and veto has boundary-exact tests and an ordered machine-readable reason code.
- Protocol canonical hash is stable and contains no WC2026 outcomes.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-03-SUMMARY.md` when done.
</output>
