---
phase: 09-rolling-tournament-benchmark-harness
plan: "05"
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - R/benchmark/contracts.R
  - R/benchmark/baselines.R
  - R/benchmark/runner.R
  - tests/testthat/test_benchmark_contracts.R
  - tests/testthat/test_benchmark_pipeline.R
autonomous: true
gap_closure: true
requirements: [BENCH-03]

must_haves:
  truths:
    - "Every one of the 6,300 canonical prediction rows resolves its feature_coverage_id to runtime evidence for every feature registered to that model's panel per D-10 and D-15."
    - "Feature evidence records model, panel, edition, track, boundary, fixture, feature, source date, strict-cutoff status, imputation reason, active-in-fit status, coverage status, and license/provenance fields."
    - "Feature coverage is validated during adapter construction, bundle writing, and bundle validation; feature_coverage_valid is derived from those checks and is never assigned as an unconditional success fact per D-18."
    - "Coverage construction remains cache-only, uses no network access, and does not expose restricted raw optional-source rows."
  artifacts:
    - path: "R/benchmark/baselines.R"
      provides: "Per-prediction feature evidence generated from registered runtime inputs and fit state"
      exports: ["benchmark_feature_coverage_id", "build_registered_feature_coverage", "run_registered_baseline_adapter"]
    - path: "R/benchmark/contracts.R"
      provides: "Exact feature-key, cutoff, imputation, license, and prediction-reference validation"
      exports: ["validate_feature_coverage", "validate_prediction_feature_coverage_links"]
    - path: "R/benchmark/runner.R"
      provides: "Feature-level coverage assembly and derived bundle/run-manifest validity"
      exports: ["benchmark_runner_feature_coverage", "validate_rolling_benchmark_bundle"]
    - path: "tests/testthat/test_benchmark_contracts.R"
      provides: "Synthetic feature-evidence and dangling-reference failures"
    - path: "tests/testthat/test_benchmark_pipeline.R"
      provides: "Bundle-level feature coverage construction and validation regressions"
  key_links:
    - from: "R/benchmark/baselines.R"
      to: "data/benchmark/phase09/feature_contract.csv"
      via: "model-panel feature expansion and runtime source/imputation evidence"
      pattern: "build_registered_feature_coverage|feature_contract"
    - from: "fixture_predictions.csv"
      to: "feature_coverage.csv"
      via: "feature_coverage_id group foreign key with one row per registered feature"
      pattern: "validate_prediction_feature_coverage_links|feature_coverage_id"
    - from: "R/benchmark/runner.R"
      to: "R/benchmark/contracts.R"
      via: "construction-time and read-back validate_feature_coverage calls"
      pattern: "validate_feature_coverage"
---

<objective>
Replace the aggregate coverage summary with complete runtime feature-level provenance and make prediction-to-coverage linkage executable.

Purpose: Close the BENCH-03 blocker where the canonical bundle has 6,300 dangling feature_coverage_id values and a hard-coded success flag. This repairs D-10, D-15, D-18, and D-20 evidence without changing the registered models, panels, G=40 support, or sealed holdout.
Output: Runtime feature evidence, strict foreign-key validation, derived run-manifest validity, and focused failure-propagating tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-04-SUMMARY.md

<interfaces>
Existing contracts to extend:

- `validate_feature_coverage(coverage, expected_keys)` already requires keys `model_id`, `boundary_id`, `fixture_id`, `feature_id` plus `source_date`, `evidence_cutoff_exclusive`, `cutoff_valid`, imputation, active-fit, coverage, and license fields.
- `run_registered_baseline_adapter(registration, history, fixtures, seed_registry, support_max, run_id, frozen_registry)` currently returns predictions, distributions, and manifests; its prediction rows already carry `feature_coverage_id` but return no evidence rows.
- `benchmark_runner_fixture_features()` carries fixture predictors and `feature_source_date`; `benchmark_runner_prepare_history()` must retain whether a value/source existed before any registered imputation is applied.
- `benchmark_default_execution_engine()` currently replaces feature evidence with `benchmark_output_coverage()` aggregates and assigns `feature_coverage_valid = TRUE`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit and validate runtime feature-level coverage groups</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/runner.R</file>
    <file>R/forecast/features.R</file>
    <file>tests/testthat/test_benchmark_contracts.R</file>
    <file>tests/testthat/test_benchmark_baselines.R</file>
  </read_first>
  <files>R/benchmark/contracts.R, R/benchmark/baselines.R, tests/testthat/test_benchmark_contracts.R</files>
  <behavior>
    - A prediction coverage ID is deterministic from run, model, track, boundary, and fixture and resolves to a non-empty group of exactly the features registered for its panel.
    - Coverage keys are unique by model/boundary/fixture/feature; missing or extra registered feature rows fail.
    - Source-present rows require a real source date strictly before evidence_cutoff_exclusive; source-absent rows retain an explicit imputation/missingness reason and cannot masquerade as source-observed values.
    - active_in_fit is derived from the boundary manifest's active predictor set; inactive and zero-variance features remain visible with a machine-readable status.
    - Required license_class and source/provenance identity come from the frozen feature contract and cannot be replaced by adapter defaults.
  </behavior>
  <action>First add RED tests covering a two-track prediction set, multiple registered panel features, a dangling prediction feature_coverage_id, one missing feature row, one extra feature row, a same-day/future source date, missing imputation reason, inactive predictors, and license drift. Implement `benchmark_feature_coverage_id()` so track/boundary identity prevents frozen and updating predictions from sharing an ambiguous group. Implement `build_registered_feature_coverage()` in `baselines.R`; expand each prediction fixture over the exact `feature_contract.csv` rows for the registration's panel and emit `schema_version`, `feature_coverage_id`, `run_id`, `model_id`, `panel_id`, `edition_id`, `track_id`, `boundary_id`, `fixture_id`, `feature_id`, `source_id`, `source_artifact_sha256`, `value_present`, `source_present`, `source_date`, `evidence_cutoff_exclusive`, `cutoff_valid`, `imputed`, `imputation_reason`, `active_in_fit`, `coverage_status`, `license_class`, and the feature-contract row hash. Preserve per-feature source-presence/source-date companions while preparing fixture features; apply the registered imputation rule only after recording absence, and never infer a source date for unavailable data. Return `feature_coverage` from every adapter result and set each prediction's ID from the same helper. Extend `validate_feature_coverage()` and add `validate_prediction_feature_coverage_links()` to enforce exact keys, exact panel feature groups, valid strict cutoffs, registered provenance/license equality, and set equality between prediction coverage IDs and evidence groups. Invoke both validators before adapter output returns per D-10 and D-15.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_baselines.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic frozen/updating predictions receive distinct deterministic coverage IDs and every ID resolves.
    - Coverage contains every required schema and provenance column accepted by `validate_feature_coverage()`.
    - Missing, extra, future-dated, falsely source-present, unreasoned-imputation, and license-drift rows fail with non-zero test exits.
    - No Phase 10/11 model or external data fetch is introduced.
  </acceptance_criteria>
  <done>Each adapter prediction carries a validated foreign key into complete point-in-time feature evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire feature evidence through bundle construction and validation</name>
  <read_first>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/runner.R</file>
    <file>tests/testthat/test_benchmark_contracts.R</file>
    <file>tests/testthat/test_benchmark_pipeline.R</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/feature_coverage.csv</file>
  </read_first>
  <files>R/benchmark/runner.R, tests/testthat/test_benchmark_pipeline.R</files>
  <behavior>
    - Synthetic bundle construction writes feature-level rows rather than model/edition aggregate coverage rows.
    - Bundle validation rebuilds expected feature keys from predictions, model registrations, and the frozen feature contract, then invokes both coverage validators.
    - A dangling prediction ID, an orphan evidence group, or a malformed feature row causes bundle validation to fail.
    - run_manifest.feature_coverage_valid is derived from successful construction/read-back validation and cannot remain true when either validator fails.
  </behavior>
  <action>Replace `benchmark_runner_feature_coverage()` aggregate construction with binding of adapter-emitted feature rows, canonical sorting, expected-key expansion from predictions plus model/panel feature registrations, and immediate `validate_feature_coverage()`/`validate_prediction_feature_coverage_links()` calls. Keep post-prediction panel output coverage as a separate in-memory result used by manifests and promotion inputs; it must no longer be serialized under the feature coverage name. Extend `benchmark_runner_validate_bundle_data()` and default-path validation to load the frozen feature contract, validate every feature row and all 6,300 prediction references, and reject aggregate-only schemas. Derive `feature_coverage_valid` from the actual validator result when creating the run manifest and recheck it on bundle load; remove the unconditional assignment. Update synthetic pipeline fixtures to create real feature-level rows and add corruption cases for dangling IDs and missing feature keys. Keep the checksum key specification at model/panel/edition/track/boundary/fixture/feature grain so parent/content hashes change when any evidence row changes.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_pipeline.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - The synthetic bundle contains feature-level evidence and validates on write and read-back.
    - Tests prove aggregate-only coverage, dangling prediction references, orphan groups, and malformed cutoff/license evidence fail.
    - Run-manifest feature validity is produced from successful validators and fails closed on tampering.
    - Bundle construction remains local-only and preserves the WC2026 seal.
  </acceptance_criteria>
  <done>The canonical bundle path cannot publish or accept hollow feature coverage evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/benchmark/contracts.R` | Extended `validate_feature_coverage()`; new `validate_prediction_feature_coverage_links()` |
| `R/benchmark/baselines.R` | `benchmark_feature_coverage_id()`, `build_registered_feature_coverage()`, and adapter `feature_coverage` output |
| `R/benchmark/runner.R` | Feature-level `benchmark_runner_feature_coverage()`, expected-key construction, validator calls, and derived `feature_coverage_valid` |
| `tests/testthat/test_benchmark_contracts.R` | Feature-key, cutoff, imputation, provenance, license, and prediction-link tests |
| `tests/testthat/test_benchmark_pipeline.R` | Feature-level synthetic bundle and dangling/orphan/tamper regressions |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime feature inputs → evidence rows | Missing or imputed values can be misrepresented as observed pre-cutoff evidence. |
| Prediction IDs → feature coverage artifact | Dangling or ambiguous foreign keys can make provenance unverifiable. |
| Restricted local aggregates → public bundle | Coverage may disclose only registered aggregate evidence and license metadata, never raw snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-G05-01 | Tampering | feature evidence construction | high | mitigate | Exact registered key expansion, strict source-date checks, explicit imputation reasons, and construction-time validation. |
| T-09-G05-02 | Repudiation | prediction-to-coverage linkage | high | mitigate | Deterministic group IDs, set-equal foreign-key validation, and bundle read-back checks. |
| T-09-G05-03 | Information Disclosure | optional-source coverage | medium | mitigate | Emit contract IDs, aggregate values/status, license, and hashes only; no raw restricted rows enter outputs. |
</threat_model>

<verification>
Run both focused suites with failure and warning propagation. Confirm the source scan still finds no HTTP/download/refresh call in benchmark modules. Bundle regeneration is intentionally deferred to 09-08 after panel and promotion wiring are green.
</verification>

<success_criteria>
- Runtime feature evidence replaces the 60-row aggregate artifact.
- Every prediction coverage reference resolves to exact registered feature rows.
- Construction and bundle validation both invoke the feature validators.
- The run manifest derives feature coverage validity from evidence.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-05-SUMMARY.md` when done.
</output>
