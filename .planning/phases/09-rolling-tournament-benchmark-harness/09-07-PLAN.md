---
phase: 09-rolling-tournament-benchmark-harness
plan: "07"
type: execute
wave: 6
depends_on: [09-06]
files_modified:
  - R/evaluation/promotion.R
  - R/benchmark/runner.R
  - tests/testthat/test_benchmark_promotion.R
  - tests/testthat/test_benchmark_pipeline.R
autonomous: true
gap_closure: true
requirements: [BENCH-03, BENCH-05]

must_haves:
  truths:
    - "Every canonical baseline decision is the serialized result of evaluate_promotion() under the checksum-validated D-16 through D-20 protocol; runner.R has no alternate decision path."
    - "Candidate inputs are derived from proper scores, fixed-bin calibration, paired uncertainty, fold/competition breadth, maximum regression, exact panel coverage, feature provenance/license, seeds, checksums, reproducibility, code/features/settings/panels/seeds freezes, and the WC2026 seal."
    - "Every failed statistical or contract gate persists a non-empty ordered reason code plus its full-precision value and boolean; a clean all-pass result preserves commit 5f4c013's character() empty-reason semantics."
    - "The frozen operators and thresholds remain exact: D-16 effect/CI, D-17 breadth/regression, D-18 supporting vetoes, D-19 rich/open companion gates, and D-20 freeze/seal requirements."
    - "No canonical decision can be finalized as reproducible until the independent reversed-order pass matches; no WC2026 label is opened."
  artifacts:
    - path: "R/evaluation/promotion.R"
      provides: "Complete gate values and booleans from the frozen pure evaluator"
      exports: ["promotion_gate_values", "promotion_gate_passes", "evaluate_promotion"]
    - path: "R/benchmark/runner.R"
      provides: "Complete candidate input assembly, mandatory evaluator invocation, and post-reproducibility finalization"
      exports: ["benchmark_runner_promotion_candidate", "benchmark_runner_decisions", "finalize_benchmark_promotion_decisions"]
    - path: "tests/testthat/test_benchmark_promotion.R"
      provides: "Complete-value/boolean serialization and empty-reason regressions"
    - path: "tests/testthat/test_benchmark_pipeline.R"
      provides: "Runner bypass, missing-reason, evidence-tamper, and reproducibility-order failures"
  key_links:
    - from: "benchmark_summaries.csv"
      to: "benchmark_runner_promotion_candidate"
      via: "full-precision RPS, Brier, log-loss, and calibration values"
      pattern: "benchmark_runner_promotion_candidate|calibration_error"
    - from: "paired_comparisons.csv"
      to: "evaluate_promotion"
      via: "panel-exact delta, CI, fold wins, competition wins, and maximum regression"
      pattern: "benchmark_runner_decisions|evaluate_promotion"
    - from: "two-pass content comparison"
      to: "promotion_decisions.csv"
      via: "reproducibility fact finalized only after reversed-order equality"
      pattern: "finalize_benchmark_promotion_decisions|reproducible"
---

<objective>
Make the checksum-frozen promotion evaluator the sole canonical decision path and persist complete gate evidence.

Purpose: Close the BENCH-05 blocker where the pure D-16 through D-20 gate exists but runner.R hard-codes retain_incumbent and loses statistical failure reasons. This plan keeps the frozen rule intact and wires real canonical evidence into it.
Output: Complete pure-gate diagnostics, candidate-input builders, mandatory runner invocation, post-two-pass finalization, and bypass/reason tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-03-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-06-SUMMARY.md

<interfaces>
Frozen governance and current bypass:

- `evaluate_promotion(candidate, protocol)` returns candidate/incumbent IDs, decision, ordered reason_codes, partial gate_values, and aggregate gate_passes.
- `promotion_veto_reasons()` already preserves reason order and `promotion_contract_reasons()` returns `character()` when all contract flags pass, including the 5f4c013 correction.
- `benchmark_runner_decisions(comparisons, coverage, model_registry)` currently assigns `decision = "retain_incumbent"` and never calls the evaluator.
- `benchmark_runner_score_outputs()` produces proper-score/calibration summaries; `benchmark_runner_comparisons()` produces fold, headline, bootstrap, breadth, regression, and leave-one-out rows on the exact panels repaired by 09-06.
- `run_rolling_tournament_benchmark()` executes normal and reversed model orders and compares canonical hashes before marking the run reproducible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose every frozen promotion gate value and boolean</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md</file>
    <file>R/evaluation/promotion.R</file>
    <file>tests/testthat/test_benchmark_promotion.R</file>
    <file>data/benchmark/phase09/promotion_protocol.json</file>
    <file>data/benchmark/phase09/panels.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/seed_registry.csv</file>
  </read_first>
  <files>R/evaluation/promotion.R, tests/testthat/test_benchmark_promotion.R</files>
  <behavior>
    - evaluate_promotion returns full-precision values and pass booleans for every core, supporting, rich, open-companion, common-contract, freeze, reproducibility, and seal gate.
    - Gate booleans use the protocol's inclusive/strict operators exactly and are consistent with ordered reason codes.
    - A failed gate always contributes its frozen reason code; every passing gate contributes no reason.
    - An all-pass case returns character() reasons, not NA or a synthetic success reason.
  </behavior>
  <action>Add RED tests that enumerate the complete D-16 through D-20 value/boolean names and assert one-to-one consistency between a false gate and its ordered reason. Implement `promotion_gate_values(candidate, protocol)` and `promotion_gate_passes(candidate, protocol)` as pure helpers and make `evaluate_promotion()` return their complete named results. Include core RPS delta/CI, fold/World Cup/Euro wins, maximum regression, Brier relative change, log-loss relative change, calibration change; rich equivalents and per-edition coverage/provenance; open-companion RPS/CI/max regression/supporting changes, fixture count and default-open mode; all common contract flags; code/features/settings/panels/seeds/checksum freeze facts; reproducibility; and WC2026 seal status. Keep all arithmetic at source precision and preserve every threshold/operator in the existing checksum-validated protocol. Preserve `promotion_contract_reasons()` returning `character()` when all fields pass and add serialization-facing tests for clean empty reasons versus non-empty failed reasons. Do not add a second policy implementation or alter threshold values.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_promotion.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Every D-16 through D-20 gate has a named value where applicable and a named boolean.
    - Immediate-below/exact/immediate-above boundary tests remain green.
    - False booleans and ordered reasons agree one-to-one.
    - The all-pass reason vector remains length zero per commit 5f4c013 semantics.
  </acceptance_criteria>
  <done>The pure evaluator emits complete machine-serializable evidence without changing the frozen governance rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build canonical candidate inputs and require evaluate_promotion in the runner</name>
  <read_first>
    <file>R/evaluation/promotion.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/runner.R</file>
    <file>tests/testthat/test_benchmark_promotion.R</file>
    <file>tests/testthat/test_benchmark_pipeline.R</file>
    <file>data/benchmark/phase09/promotion_protocol.json</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/scores/benchmark_summaries.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/comparisons/paired_comparisons.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/run_manifest.csv</file>
  </read_first>
  <files>R/benchmark/runner.R, tests/testthat/test_benchmark_pipeline.R</files>
  <behavior>
    - The runner constructs one complete candidate object per canonical model from persisted-source tables and validated contract facts.
    - evaluate_promotion is called exactly once per canonical decision; replacing or bypassing it causes a test failure.
    - A candidate that misses the practical effect or CI gate serializes a non-empty ordered reason, its exact gate values, and false gate booleans.
    - Promotion decisions cannot claim reproducibility before normal/reversed execution artifacts match.
    - Bundle validation recomputes evaluator results from source artifacts and rejects changed decisions, reasons, values, booleans, or order.
  </behavior>
  <action>Implement `benchmark_runner_promotion_candidate()` to derive candidate/core/rich/open-companion inputs from panel-exact score summaries, calibration summaries, paired comparison folds/headlines/bootstrap intervals, output coverage, feature coverage validation, model manifests, registration/license/seed/checksum parents, run Git cleanliness and freeze facts, and the WC2026 purpose guard. Compute Brier/log-loss relative changes and calibration changes from full-precision equal-tournament headline values; use the 12 RPS fold rows for breadth and maximum regression. For production_hybrid_nb's canonical self-decision, build its rich self-comparison and the complete 630-fixture open incumbent companion facts so the optional-data shape is complete; later challengers must provide their own registered open companion. Rewrite `benchmark_runner_decisions()` to call `evaluate_promotion()` once for every model and flatten ordered reasons, all gate values, and all gate booleans into stable columns with `gate_order`; remove the hard-coded decision assignment. Implement `finalize_benchmark_promotion_decisions()` so normal and reversed executions first reconcile all non-decision canonical inputs, then both receive `reproducible = TRUE`, rerun the pure evaluator, and compare final decision hashes before publication. Failed pre-reproducibility or hash checks remain hard veto evidence. Extend bundle validation to reconstruct and compare canonical evaluator output. Add behavioral tests that temporarily wrap the evaluator and count calls, a static runner-body assertion against bypass, a statistically ineligible synthetic candidate that must have reasons, and tampering cases for one reason/value/boolean. Preserve blank serialization only for a genuinely all-pass reason vector; failed rows must never serialize blank or NA reasons.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_pipeline.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_promotion.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove every canonical model calls `evaluate_promotion()` and runner.R has no alternate hard-coded decision path.
    - A failed effect/CI candidate persists ordered non-empty reasons, exact values, and false booleans.
    - Candidate contract flags come from validators, hashes, freezes, seed/license/provenance evidence, two-pass identity, and the WC2026 seal.
    - Bundle validation rejects tampered decisions and independently reproduced evaluator output differs nowhere.
  </acceptance_criteria>
  <done>Canonical promotion_decisions.csv becomes a faithful, reproducible serialization of the frozen pure evaluator.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/evaluation/promotion.R` | `promotion_gate_values()`, `promotion_gate_passes()`, complete `evaluate_promotion()` result while preserving `promotion_contract_reasons()` empty semantics |
| `R/benchmark/runner.R` | `benchmark_runner_promotion_candidate()`, evaluator-backed `benchmark_runner_decisions()`, `finalize_benchmark_promotion_decisions()`, and decision read-back reconciliation |
| `tests/testthat/test_benchmark_promotion.R` | Complete gate value/boolean/reason and empty-reason tests |
| `tests/testthat/test_benchmark_pipeline.R` | Evaluator-call, bypass, statistically-ineligible reason, reproducibility-order, and decision-tamper tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Score/coverage/hash evidence → candidate object | Missing or defaulted facts can make an ineligible model appear eligible. |
| Frozen evaluator → canonical CSV | A runner-side decision path can bypass registered governance. |
| First/second execution → reproducibility gate | Reproducibility must be proven before it becomes a passing decision input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-G07-01 | Tampering | candidate input assembly | high | mitigate | Derive every value/flag from validated artifacts and add missing/tampered evidence tests. |
| T-09-G07-02 | Elevation of Privilege | runner decision path | critical | mitigate | Mandatory evaluator call, behavioral call-count test, static bypass guard, and bundle recomputation. |
| T-09-G07-03 | Repudiation | reasons and gate diagnostics | high | mitigate | Stable ordered reason codes, full-precision values, booleans, and read-back equality. |
| T-09-G07-04 | Tampering | reproducibility fact | high | mitigate | Finalize decisions only after reversed-order non-decision inputs match, then compare decision hashes. |
</threat_model>

<verification>
Run promotion and pipeline suites with failure propagation. Confirm the protocol still validates at its canonical checksum and contains the exact D-16 through D-20 operators, thresholds, G=40, seed 920001, 10,000 replicates, and no WC2026 result fields.
</verification>

<success_criteria>
- Every canonical decision invokes evaluate_promotion.
- Complete candidate evidence reaches the frozen gate.
- Failed gates have ordered non-empty reasons, values, and booleans.
- Reproducibility and sealing are derived facts, not assumptions.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-07-SUMMARY.md` when done.
</output>
