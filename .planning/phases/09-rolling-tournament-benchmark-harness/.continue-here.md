---
context: phase
phase: 09-rolling-tournament-benchmark-harness
plan: 8
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-07-21T14:09:44.354Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These constraints were discovered through failed canonical acceptance attempts. Explain each in your own words and name its prevention mechanism before proceeding.

- [ ] CONSTRAINT: Preserve the accepted seal - Never remove or overwrite `phase09-baselines-frozen` before the staged candidate passes validation; use staged pre-validation plus atomic install and rollback.
- [ ] CONSTRAINT: Evaluate durable numbers - Promotion decisions must be derived from the CSV-persisted numeric representation, not higher-precision in-memory values that can drift on read-back.
- [ ] CONSTRAINT: Validate in isolation - The acceptance validator must work in a fresh R process after sourcing its declared dependencies; ambient runner-session state is not evidence.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Install then trust | An early reconciliation path could replace the prior seal before semantic read-back validation completed. | blocking | Generate to a unique staged root, validate before install, retain a backup through post-install validation, and restore it on any error via `benchmark_runner_install_staged_bundle()`. |
| In-memory promotion evidence | Promotion gates built from full-precision in-memory values disagreed with durable CSV reconstruction. | blocking | Pass comparisons, coverage, summaries, and run manifest through `benchmark_runner_persisted_view()` before `evaluate_promotion()` and compare durable decision hashes. |
| Ambient-state validation | The independent acceptance command exposed a validator dependency that only existed in the producer's R session. | blocking | Source all validator dependencies explicitly and run the exact Task 2 acceptance block in a fresh `Rscript` process. |

<current_state>
Phase 09 gap execution is paused in Plan 09-08, Task 1. Plans 09-05 through 09-07 are complete. The Plan 09-08 executor was cleanly stopped on user request. The prior `phase09-baselines-frozen` bundle is restored and git-clean. There is no `09-08-SUMMARY.md`, no temporary `phase09-repro-*` directory, and no async job to resume.
</current_state>

<completed_work>

Completed Plans:
- 09-05: Pre-imputation feature provenance and deterministic prediction-to-coverage links.
- 09-06: Durable evidence routing plus exact 630 open / 609 rich panel enforcement.
- 09-07: Evaluator-backed canonical promotion decisions with complete gate evidence.

Plan 09-08 corrective commits retained:
- `a845df6` - Preserve the sealed bundle during reconciliation and test rollback.
- `58ff288` - Evaluate promotion from durable numeric evidence and prove hash stability after CSV reconstruction.
- `03b3a1e` - Make bundle validation standalone in a fresh process.

Two expensive regeneration/reconciliation attempts were run. Both correctly refused completion after exposing the issues above. Their temporary bundles were removed and the prior seal was restored.
</completed_work>

<remaining_work>

- Task 1: From commit `03b3a1e`, perform a fresh complete two-pass regeneration into a unique temporary root; independently reconcile it; atomically install only after validation succeeds.
- Task 2: Run the exact fail-fast block from `09-08-PLAN.md`, including external SHA-256/byte checks, streamed row counts, self-hash, exact sorted parent graph, score-support parents, target edges/ancestry, panel linkage, promotion reasons, and focused regressions.
- Create and commit `09-08-SUMMARY.md`, then update STATE/ROADMAP tracking.
- Run phase-level code review/regression gates and a fresh `gsd-verifier`; do not edit `09-VERIFICATION.md` from the executor.
</remaining_work>

<decisions_made>

- Keep all frozen scientific settings unchanged: D-01 through D-20, G=40, temporal cutoffs, seeds, two-worker ceiling, no-network behavior, and the WC2026 seal.
- Retain the three acceptance-derived fixes as the baseline for the next run; do not revert them when handling the safe-resume PLAN-without-SUMMARY gate.
- Treat the old sealed bundle as the only accepted output until the complete independent acceptance block passes.
</decisions_made>

<blockers>
- No external or human-action blocker. The remaining blocker is runtime: another full canonical regeneration is required and historically takes roughly 60-75 minutes, followed by acceptance and verification.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` - machine-readable recovery state.
2. `.planning/phases/09-rolling-tournament-benchmark-harness/09-08-PLAN.md` - exact execution and acceptance contract.
3. `.planning/phases/09-rolling-tournament-benchmark-harness/09-VALIDATION.md` - matching Nyquist commands.
4. `.planning/phases/09-rolling-tournament-benchmark-harness/09-07-SUMMARY.md` - dependency state entering the reseal.
5. Commits `a845df6`, `58ff288`, and `03b3a1e` - failures already discovered and structural fixes already made.
6. `R/benchmark/runner.R` and `tests/testthat/test_benchmark_pipeline.R` - corrected install, durable decision, and standalone validation paths.

## Critical Anti-Patterns (do NOT repeat these)

- Do not regenerate directly into `phase09-baselines-frozen`; stage and validate first.
- Do not compare evaluator outputs from full-precision memory against CSV-persisted decisions.
- Do not run acceptance in a session that already sourced undeclared helpers.
- Do not mark 09-08 complete merely because corrective commits exist; it requires a summary, accepted artifacts, exact Task 2 checks, and fresh goal verification.

## Infrastructure State

- Background executor: stopped.
- R/targets processes: none should be assumed running.
- Accepted bundle: `outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/`, restored and git-clean.
- Temporary repro/staged bundles: none remain.
- Async manifests: none.
- Unrelated dirty files remain user-owned: `.planning/config.json`, `.planning/research/.cache/`, `outputs/design_audit/`, and `outputs/reports/xgelo_elo_decision/`.

<context>
The long Plan 09-08 run did useful work despite not completing: independent acceptance found three real integrity bugs and each has a focused regression commit. The next run should begin from those commits and preserve the old seal throughout. Monitor long fits at six-minute intervals; sparse output is expected. A visible `phase09-repro-*` directory indicates staged progress, not accepted publication.
</context>

<next_action>
Start with: `$gsd-resume-work`. After the blocking-constraint understanding check, resume only Plan 09-08 from commit `03b3a1e`; preserve the corrective commits and run a fresh staged two-pass regeneration.
</next_action>
