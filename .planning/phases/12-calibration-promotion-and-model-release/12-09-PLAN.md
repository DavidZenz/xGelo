---
phase: 12-calibration-promotion-and-model-release
plan: 09
type: execute
wave: 9
depends_on: [12-07, 12-08]
files_modified:
  - R/visualization/worldcup_dashboard.R
  - tests/testthat/test_worldcup_dashboard.R
autonomous: true
gap_closure: true
requirements: [PROMO-03]
must_haves:
  truths:
    - "The exported build_worldcup_dashboard_data() and build_worldcup_dashboard() entry points resolve exactly one approved or incumbent-retained Phase 12 release before any model object or forecast is loaded."
    - "Missing, ambiguous, unapproved, invalid, hash-mismatched, wrong-candidate, and wrong-G release state fails closed before model loading and before forecast generation."
    - "A valid incumbent-retained release still produces the existing dashboard/export payload, release metadata, semantic tabs/search/bracket controls, and fitted scoreline behavior without exposing WC2026 labels."
    - "No caller-supplied raw model path or baseline-selection argument can become production dashboard model authority; the only model authority is the validated release manifest and model contract per D-15/D-16."
  artifacts:
    - "R/visualization/worldcup_dashboard.R with strict dashboard_resolve_approved_release(), dashboard_reject_raw_model_paths(), build_worldcup_dashboard_data(), and build_worldcup_dashboard() release-only wiring"
    - "tests/testthat/test_worldcup_dashboard.R with a valid temporary release-backed fixture and fail-closed boundary regressions"
  key_links:
    - "build_worldcup_dashboard() -> build_worldcup_dashboard_data() -> dashboard_resolve_approved_release() -> resolve_phase12_approved_release() -> validated release model objects"
    - "dashboard_reject_raw_model_paths() -> forecast_dashboard_matches()/make_knockout_route_estimator() so path-based model loading is unreachable from exported builders"
    - "Release validation failure -> no readRDS() of an untrusted model artifact and no forecast_dashboard_matches() invocation"
---

<objective>
Close the PROMO-03 verification gap by making the exported World Cup dashboard builders exclusively consume one validated Phase 12 approved or incumbent-retained release, while preserving the current dashboard presentation and forecast behavior for a valid release.

Purpose: D-15 requires manifest-and-contract-only model loading and D-16 requires an incumbent-retained release to remain usable. The current exported builders can bypass that boundary with raw model paths when release_root is NULL, so the release gate must be enforced at the public builder boundary and proven with pre-load regressions.

Output: A strict release-only dashboard/export API and regression coverage for valid, missing, ambiguous, unapproved, invalid, mismatched, wrong-candidate, and wrong-G release states.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@.planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VERIFICATION.md
@.planning/phases/12-calibration-promotion-and-model-release/12-07-SUMMARY.md
@.planning/phases/12-calibration-promotion-and-model-release/12-08-SUMMARY.md
@R/visualization/worldcup_dashboard.R
@R/release/release_contract.R
@R/release/release_bundle.R
@R/release/release_install.R
@tests/testthat/test_worldcup_dashboard.R
@tests/testthat/test_phase12_release.R
@_targets.R
@scripts/update_worldcup_dashboard.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Enforce the approved-release boundary in both exported dashboard builders</name>
  <read_first>R/visualization/worldcup_dashboard.R, R/release/release_contract.R, R/release/release_bundle.R, R/release/release_install.R, _targets.R, scripts/update_worldcup_dashboard.R, .planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md, .planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md</read_first>
  <files>R/visualization/worldcup_dashboard.R, tests/testthat/test_worldcup_dashboard.R</files>
  <action>Change `dashboard_resolve_approved_release()` so `release_root = NULL` is an immediate error, the resolver always calls `resolve_phase12_approved_release(release_root)` to select exactly one trusted manifest, and an optional `approved_release` argument is only an identity-consistency assertion rather than an alternate model authority. Add `dashboard_reject_raw_model_paths(extra_args)` and call it from `build_worldcup_dashboard_data()` before forecast setup; reject `home_model_path`, `away_model_path`, and `baseline_comparison` supplied to the exported path, then pass only `dashboard_release_model_pair()` model objects to `forecast_dashboard_matches()` and `make_knockout_route_estimator()`. Remove the default raw model-path fallback from the production call chain while preserving the existing payload metadata, release panel, fitted scoreline label, five tabs, search/filter/bracket hooks, semantic HTML, CSS, and vanilla JavaScript required by the UI-SPEC. Keep `_targets.R` and `scripts/update_worldcup_dashboard.R` behavior unchanged; they already supply the trusted release root and resolved identity per D-15 and D-16.</action>
  <verify>
    <automated>Rscript --vanilla -e 'source("R/visualization/worldcup_dashboard.R"); stopifnot(exists("dashboard_resolve_approved_release"), exists("dashboard_reject_raw_model_paths"), exists("build_worldcup_dashboard_data"), exists("build_worldcup_dashboard")); stopifnot(grepl("release_root", paste(deparse(body(dashboard_resolve_approved_release)), collapse=" "), fixed=TRUE)); cat("dashboard release boundary loaded\n")'</automated>
  </verify>
  <acceptance_criteria>`dashboard_resolve_approved_release()` cannot return NULL; both exported builders require one resolver-validated release; raw model paths and baseline-selection arguments are rejected before forecast setup; valid incumbent-retained and approved releases still supply model objects and release metadata to every forecast path; and no `_targets.R`, update-script, release publisher, installer, or generated-output file is modified.</acceptance_criteria>
  <done>The production dashboard/export entry points have one manifest-only model authority and a runnable valid-release path that reaches the existing dashboard payload and renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Convert dashboard fixtures to release-backed inputs and prove every fail-closed boundary</name>
  <read_first>R/visualization/worldcup_dashboard.R, R/release/release_contract.R, R/release/release_bundle.R, tests/testthat/test_worldcup_dashboard.R, tests/testthat/test_phase12_release.R, .planning/phases/12-calibration-promotion-and-model-release/12-VERIFICATION.md, .planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md</read_first>
  <files>tests/testthat/test_worldcup_dashboard.R</files>
  <action>Replace the current raw temporary `home_model_path`/`away_model_path` dashboard fixture with a clearly named test-only `dashboard_test_release_fixture()` that creates a temporary copy of a validated Phase 12 release, installs deterministic constant home/away model objects under the release model artifact, and recomputes the copied artifact and self-manifest SHA-256 values before calling `resolve_phase12_approved_release()`. Add a focused failure harness that records whether `forecast_dashboard_matches()` was entered and uses deliberately invalid model bytes to demonstrate that resolver failures occur before `readRDS()` can load an untrusted model. Add exact regressions for: `release_root = NULL` and a missing trusted root; two release manifests under one trusted root; an unapproved status; malformed or identity-mismatched `model_contract.json`; model artifact SHA mismatch; contract artifact SHA mismatch; wrong selected candidate; wrong `score_support_g`; and a path-escaping or missing required artifact. Each case must assert the documented release-validation error family, assert the forecast sentinel was not invoked, and for hash-invalid model/contract fixtures assert the failure is a hash/identity error rather than an RDS read error. Add a valid `incumbent retained` build assertion for the existing 72-match/48-group/bracket/export fixture, checking exact retained status, one primary probability view, release provenance, `role="status"`/`aria-live`, preserved tabs/search/bracket hooks, `rel="noopener"`, and absence of WC2026 labels from JSON/HTML/CSV. Keep all fixture mutations in temporary directories and do not add production helpers, package installation, network access, or generated-output edits.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_dashboard.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The existing dashboard regression runs only through a valid temporary release-backed fixture; missing, ambiguous, unapproved, invalid, mismatched, wrong-candidate, wrong-G, path-escaping, and missing-artifact states fail before model loading and forecast generation; approved and exact `incumbent retained` fixtures remain usable; and the existing semantic dashboard surface plus UI-SPEC release metadata/accessibility assertions remain green.</acceptance_criteria>
  <done>PROMO-03’s approved-consumer boundary is regression-tested at the exported API, including the previously accepted raw-model bypass, and the verifier’s single blocking gap is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller input -> release resolver | `release_root`, optional release identity, manifest discovery, and relative artifact paths are untrusted control inputs. |
| Validated release -> model loader/forecast engine | Manifest status, contract identity, G=40, and SHA-256 checks determine which model objects may reach forecasting. |
| Forecast payload -> dashboard/export presentation | Release metadata and forecast rows cross into JSON, CSV, HTML, and client-side rendering; WC2026 labels must remain outside this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-09-01 | Tampering | `dashboard_resolve_approved_release()` and release artifacts | critical | mitigate | Resolve exactly one manifest and run the existing complete-bundle/contract hash validation before model objects reach either forecast path. |
| T-12-09-02 | Spoofing | Trusted release-root candidate discovery | high | mitigate | Reject missing, ambiguous, unapproved, path-escaping, wrong-candidate, and wrong-G release fixtures before model loading. |
| T-12-09-03 | Elevation of privilege | `build_worldcup_dashboard_data()` variadic model arguments | high | mitigate | `dashboard_reject_raw_model_paths()` rejects caller model paths and baseline-selection authority; only resolver-returned model objects are forwarded. |
| T-12-09-04 | Information disclosure | Dashboard JSON/CSV/HTML exports | high | mitigate | Preserve release metadata and semantic UI hooks while asserting WC2026 labels are absent from consumer-facing payloads and exports. |
| T-12-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; no dependency changes are permitted in this gap closure. |
</threat_model>

<verification>
Run the focused dashboard and Phase 12 release contract suites with warnings treated as failures. Confirm the production diff is limited to `R/visualization/worldcup_dashboard.R`, the test diff is limited to `tests/testthat/test_worldcup_dashboard.R`, and `_targets.R`, `scripts/update_worldcup_dashboard.R`, `R/release/release_contract.R`, `R/release/release_bundle.R`, `R/release/release_install.R`, and generated outputs are unchanged. Re-run the verifier’s PROMO-03 dashboard/export check after execution.
</verification>

<success_criteria>
PROMO-03 is complete when both exported dashboard builders require one resolver-validated approved or incumbent-retained release, no raw model-path or baseline bypass reaches model loading, all required invalid-release cases fail before model loading/forecast generation, and valid release-backed dashboard behavior and UI-SPEC regressions pass without WC2026 label exposure.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Phase 12 calibration/promotion/release goal | 12-01..12-09 | COVERED | Existing plans cover calibration, freeze, one-shot evaluation, promotion, and release; 12-09 closes the consumer-boundary failure. |
| REQ | CAL-01 | Inner-OOF calibration without outer-fold leakage | 12-02 | COVERED | Existing plan; not changed by this gap. |
| REQ | CAL-02 | Shared raw/calibrated proper-score comparison | 12-03 | COVERED | Existing plan; not changed by this gap. |
| REQ | PROMO-01 | Freeze candidates/settings/features/thresholds before holdout | 12-01 | COVERED | Existing plan; not changed by this gap. |
| REQ | PROMO-02 | One-shot comparison with incumbent fallback | 12-05 | COVERED | Existing plan; not changed by this gap. |
| REQ | PROMO-03 | Versioned approved artifact, reports, and dashboard regression tests | 12-06, 12-07, 12-08, 12-09 | COVERED | 12-09 adds the missing exported-builder boundary and regressions. |
| RESEARCH | Pattern 6 | Manifest-only consumer resolution before model loading | 12-07, 12-09 | COVERED | 12-09 removes the remaining NULL/raw-path branch. |
| CONTEXT | D-01..D-14 | Calibration, freeze, holdout, and release decisions | 12-01..12-06, 12-08 | COVERED | Existing executed plans remain authoritative. |
| CONTEXT | D-15 | Consumers load only through the approved manifest/contract and fail closed | 12-07, 12-09 | COVERED | 12-09 enforces this at both exported builder entry points. |
| CONTEXT | D-16 | Exact `incumbent retained` release remains usable and challengers stay audit-only | 12-06..12-09 | COVERED | 12-09 preserves retained-release behavior while removing raw authority. |
</source_audit>

<artifacts>
## Artifacts this phase produces

- Modified production file: `R/visualization/worldcup_dashboard.R`.
- Modified regression file: `tests/testthat/test_worldcup_dashboard.R`.
- Modified existing symbol: `dashboard_resolve_approved_release()`.
- New production boundary symbol: `dashboard_reject_raw_model_paths()`.
- Modified exported symbols: `build_worldcup_dashboard_data()` and `build_worldcup_dashboard()`.
- New test-only fixture symbol: `dashboard_test_release_fixture()`.
- Temporary-only release fixture copies with rehashed `release_manifest.csv`, `model_contract.json`, and model artifacts; no durable generated output is produced.
</artifacts>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-09-SUMMARY.md` when done.
</output>
