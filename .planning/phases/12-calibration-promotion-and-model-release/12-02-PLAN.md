---
phase: 12-calibration-promotion-and-model-release
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - R/release/freeze_manifest.R
  - tests/testthat/test_phase12_freeze.R
autonomous: true
requirements:
  - PROMO-01
must_haves:
  truths:
    - "One aggregate freeze manifest contains all nine Phase 11 registry rows, including inactive xG/context/structural candidates as explicit no-score rows with their inactive reasons (D-05, D-08)."
    - "Candidate code, features, settings, panels, seeds, calibration recipe, thresholds, parent graph, selected G=40, code identity, and WC2026 sealed status are hashed before any production calibration fit or label-bearing read (D-06, D-07, D-08, D-09)."
    - "Freeze validation fails closed on candidate activation, candidate/order drift, hash drift, changed recipe/support/seed/thresholds, missing parents, dirty code status, or an already-consumed WC2026 state."
    - "The manifest is canonically ordered, self-hashed, and independently readable without relying on an in-memory producer object."
    - "FLAGGED ASSUMPTION [edge probe PROMO-01, adjacency/unresolved]: duplicate candidate IDs or colliding identity hashes are a freeze error; equal values in distinct registered rows remain distinct and protocol boundary operators decide threshold equality."
    - "FLAGGED ASSUMPTION [edge probe PROMO-01, empty/unresolved]: the production freeze requires exactly nine registry rows; empty and partial registries are rejected, while one-row inputs exist only in synthetic tests."
    - "FLAGGED ASSUMPTION [edge probe PROMO-01, ordering/unresolved]: canonical identity sort is the stable output order even when metric values tie, and no sort by score or file discovery order is permitted."
  artifacts:
    - path: R/release/freeze_manifest.R
      provides: "Phase 12 aggregate freeze construction, canonical parent graph, preflight, and validation"
    - path: tests/testthat/test_phase12_freeze.R
      provides: "PROMO-01 nine-row, inactive-row, hash-drift, ordering, and unopened-holdout tests"
    - path: data/benchmark/phase12/freeze_manifest.csv
      provides: "Authoritative aggregate freeze identity consumed by calibration and final evaluation"
  key_links:
    - from: data/benchmark/phase11/model_registry.csv
      to: R/release/freeze_manifest.R::build_phase12_freeze_manifest
      via: "All nine registered candidate rows, statuses, and reasons become immutable freeze inputs"
    - from: data/benchmark/phase12/calibration_recipe.json
      to: data/benchmark/phase12/freeze_manifest.csv
      via: "Recipe bytes and SHA-256 are bound before calibrator fitting"
    - from: data/benchmark/phase12/freeze_manifest.csv
      to: R/release/final_evaluation.R::phase12_preflight_final_evaluation
      via: "Final evaluation can proceed only from a validated unopened freeze identity"
---

<objective>
Create the single authoritative Phase 12 freeze manifest and fail-closed preflight required before calibration fitting or final-label access.

Purpose: PROMO-01 makes the candidate set and every decision-relevant identity immutable. The freeze must preserve inactive candidates, parent lineage, and protocol values without introducing a second promotion policy.
Output: `R/release/freeze_manifest.R`, the aggregate `data/benchmark/phase12/freeze_manifest.csv` contract, and focused freeze tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase09/promotion_protocol.json
@data/benchmark/phase11/model_registry.csv
@data/benchmark/phase11/run_manifest.csv
@R/benchmark/runner.R
@R/benchmark/challenger_runner.R
@R/evaluation/promotion.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the nine-row freeze from registry inputs to a validated manifest</name>
  <read_first>data/benchmark/phase11/model_registry.csv, data/benchmark/phase11/run_manifest.csv, data/benchmark/phase09/promotion_protocol.json, R/benchmark/runner.R, R/benchmark/challenger_runner.R, R/evaluation/promotion.R, data/benchmark/phase12/calibration_recipe.json, tests/testthat/test_phase12_calibration_release.R</read_first>
  <files>R/release/freeze_manifest.R, tests/testthat/test_phase12_freeze.R</files>
  <action>Implement `build_phase12_freeze_manifest(candidate_registry, parent_manifests, code_identity, feature_hashes, settings_hashes, panel_hashes, seed_hashes, calibration_recipe_path, promotion_protocol_path, selected_g = 40L)` and `validate_phase12_freeze_manifest(manifest, expected_registry, expected_parents, promotion_protocol, require_unopened_labels = TRUE)`. Normalize and sort the exact nine Phase 11 rows by `candidate_id`, preserve `active_status`, `score_status`, `inactive_reason`, `research_only`, and `wc2026_sealed`, and reject omissions, duplicates, activation changes, or fabricated scores for inactive rows. Add named hashes for candidate registration, code commit/clean-worktree status, features, settings, panels, seeds, calibration recipe, promotion protocol, Phase 9/10/11 parent bundles, and G=40. Reuse the Phase 9/11 canonical SHA-256/table identity conventions, add one parent graph hash and a checksum self-row, and set `phase12_decision_authority = TRUE` only on the validated freeze artifact; do not mutate Phase 11 manifests. This tracer implements D-05, D-06, D-07, and D-08, satisfies validation-map entry 12-03-01, and must not read WC2026 labels.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_freeze.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>A synthetic nine-row registry produces one canonical manifest with eight explicit inactive/no-score rows where supplied, all required hashes, G=40, parent graph, self-hash, and sealed-before-label status; a registry mutation fails before any downstream calibration input is accepted.</acceptance_criteria>
  <done>The aggregate freeze path is executable and proves the candidate registry cannot change silently between Phase 11 and Phase 12.</done>
  <reversibility rating="costly">A freeze identity controls all downstream calibration and final-evaluation eligibility; replacing it requires a new reviewed freeze and must never overwrite the accepted manifest in place.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add drift, parent, and unopened-holdout validation coverage</name>
  <read_first>R/release/freeze_manifest.R, data/benchmark/phase09/promotion_protocol.json, data/benchmark/phase11/model_registry.csv, tests/testthat/test_phase12_freeze.R, R/benchmark/cutoffs.R</read_first>
  <files>R/release/freeze_manifest.R, tests/testthat/test_phase12_freeze.R</files>
  <action>Complete `phase12_freeze_parent_graph()`, `phase12_freeze_self_hash()`, `phase12_assert_unopened_holdout()`, and `phase12_freeze_candidate_rows()` with canonical field names and ordered reason codes. Validate every required parent artifact from persisted paths, require `thresholds_frozen = TRUE`, `sealed_before_final_labels = TRUE`, `network_free = TRUE`, and the inherited Phase 9 common veto identities, and reject absolute/escaping parent paths. Add tests for empty/partial/single synthetic registries, duplicate IDs, equal-value stable ordering, inactive-row disappearance, registration/settings/panel/seed/recipe/threshold drift, parent checksum drift, dirty code identity, wrong G, and an existing consumed-label marker. Assert no test fixture uses or opens the sealed WC2026 label source. Cite D-05 through D-09 in the focused assertions and retain the Phase 9 evaluator as a downstream-only authority; satisfy validation-map entry 12-03-02.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_freeze.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The freeze test file covers every PROMO-01 validation-map entry; all declared drift and pre-label failures stop with ordered reason codes; inactive rows remain visible; and inherited Phase 9 promotion tests remain green.</acceptance_criteria>
  <done>A durable, independently validated nine-candidate freeze manifest is ready to gate the calibration and one-shot evaluation chains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 11 registry/parent bundles → Phase 12 freeze | Existing durable artifacts may be stale, incomplete, or tampered with. |
| Code/settings/recipe identities → freeze authority | A changed implementation or threshold could alter the meaning of later evidence. |
| Freeze state → sealed label opener | The manifest is the last label-free gate before a one-way operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-02-01 | Tampering | Nine-row candidate registry and inactive rows | high | mitigate | Require exact registry membership, stable identity order, status/reason preservation, and no-score rows for inactive candidates. |
| T-12-02-02 | Tampering | Parent graph and freeze self-hash | high | mitigate | Recompute persisted parent hashes and canonical self-hash; reject missing, changed, absolute, or escaping parent paths. |
| T-12-02-03 | Elevation of privilege | Candidate/threshold/recipe freeze | critical | mitigate | Bind code, feature, settings, panel, seed, recipe, protocol, and threshold hashes before calibration and expose decision authority only on validated freeze output. |
| T-12-02-04 | Information disclosure | Pre-label holdout boundary | critical | mitigate | Require unopened state and sealed flags; never pass label-bearing rows into freeze construction or tests. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires the package legitimacy gate and blocking human review. |
</threat_model>

<verification>
Run the focused freeze suite after each task, then run inherited Phase 9 promotion and complete testthat checks. Validate the persisted freeze from a fresh R process and confirm the test fixtures contain no WC2026 label-bearing input.
</verification>

<success_criteria>
PROMO-01 is satisfied by one canonical, self-hashed, parent-linked manifest containing all nine Phase 11 candidates and every required frozen identity, with fail-closed drift and unopened-holdout tests.
</success_criteria>

### Artifacts this phase produces

- Functions: `build_phase12_freeze_manifest()`, `validate_phase12_freeze_manifest()`, `phase12_freeze_parent_graph()`, `phase12_freeze_self_hash()`, `phase12_assert_unopened_holdout()`, and `phase12_freeze_candidate_rows()`.
- Files: `R/release/freeze_manifest.R` and `tests/testthat/test_phase12_freeze.R`.
- Manifest: `data/benchmark/phase12/freeze_manifest.csv` with nine candidate rows, inactive/no-score states, parent graph, self-hash, G=40, code/status, and all recipe/threshold identities.
- Contract: pre-label freeze/preflight contract consumed by calibration, final evaluation, promotion, and release.
- CLI/test surface: `Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_freeze.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'`.

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-02-SUMMARY.md` when done.
</output>
