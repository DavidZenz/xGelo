---
phase: 12-calibration-promotion-and-model-release
plan: 02
type: execute
wave: 2
depends_on:
  - 12-00
  - 12-01
files_modified:
  - R/calibration/inner_oof.R
  - R/calibration/probability_calibration.R
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/inner_oof_predictions.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibrators.rds
  - tests/testthat/test_phase12_calibration.R
autonomous: true
requirements:
  - CAL-01
must_haves:
  truths:
    - "Calibration cannot fit or produce a calibrator object until the validated Phase 12 freeze and frozen recipe/checksum are present (D-06, D-08)."
    - "Each candidate/track calibrator consumes only its own expanding prior-tournament inner-OOF rows, rejects future/non-strict cutoffs and WC2026 outcome-bearing rows, and records source hashes and support (D-02, D-03, D-10)."
    - "The deterministic base-R one-parameter temperature scaling recipe uses fixed `stats::optim` starting values/bounds and the frozen seed/support rule; insufficient history/class support produces explicit raw fallback (D-06)."
    - "Calibration changes only derived 1X2 probabilities; the fitted goal distribution and fixed G=40 scoreline rows remain byte/content unchanged (D-01)."
  artifacts:
    - path: R/calibration/inner_oof.R
      provides: "Chronology-safe inner-OOF assembly and validation after freeze"
    - path: R/calibration/probability_calibration.R
      provides: "Deterministic 1X2 fit/apply/validation over frozen recipe"
    - path: outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/inner_oof_predictions.csv
      provides: "Durable candidate/track inner-OOF rows and provenance"
    - path: outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibrators.rds
      provides: "Durable candidate/track calibrator objects and explicit raw-fallback states"
    - path: tests/testthat/test_phase12_calibration.R
      provides: "CAL-01 chronology, isolation, recipe, support, simplex, and holdout tests"
  key_links:
    - from: data/benchmark/phase12/freeze_manifest.csv
      to: R/calibration/inner_oof.R
      via: "Calibration asserts the exact validated freeze identity before assembling rows"
    - from: R/benchmark/cutoffs.R::guard_benchmark_purpose
      to: R/calibration/inner_oof.R::assemble_phase12_inner_oof
      via: "Development calibration rejects sealed outcomes before any fit callback"
    - from: R/calibration/inner_oof.R
      to: R/calibration/probability_calibration.R
      via: "Only strictly prior candidate/track inner-OOF rows feed the frozen optimizer"
---

<objective>
Implement chronology-safe candidate/track calibration after the validated freeze.

Purpose: CAL-01 turns the frozen recipe into auditable calibrator objects without outer-fold or WC2026 leakage while preserving the joint goal distribution.
Output: inner-OOF and calibrator services, durable inner-OOF/calibrator artifacts, and the exact `test_phase12_calibration.R` suite.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase12/calibration_recipe.json
@data/benchmark/phase12/freeze_manifest.csv
@R/benchmark/cutoffs.R
@R/benchmark/hybrid_runner.R
@R/evaluation/proper_scores.R
@R/evaluation/benchmark_scores.R
@tests/testthat/test_phase12_calibration.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one frozen candidate/track from prior inner OOF to calibrated 1X2</name>
  <read_first>data/benchmark/phase12/freeze_manifest.csv, data/benchmark/phase12/calibration_recipe.json, R/benchmark/cutoffs.R, R/benchmark/hybrid_runner.R, R/evaluation/proper_scores.R, R/evaluation/benchmark_scores.R, tests/testthat/test_phase12_calibration.R</read_first>
  <files>R/calibration/inner_oof.R, R/calibration/probability_calibration.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/inner_oof_predictions.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibrators.rds, tests/testthat/test_phase12_calibration.R</files>
  <action>Require `validate_phase12_freeze_manifest()` and the frozen recipe SHA before any fit. Implement `assemble_phase12_inner_oof(predictions, fixtures, boundaries, candidate_id, track_id, outer_edition_id, freeze_manifest)` using the chronology/cutoff guard from `R/benchmark/cutoffs.R` and candidate/track routing/provenance from `R/benchmark/hybrid_runner.R`; retain fixture, boundary, exclusive cutoff, observed class, raw 1X2, source prediction hash, and maximum evidence date. Implement `phase12_calibration_recipe()` as a read-back of the frozen JSON, then `fit_phase12_1x2_calibrator()` using the single positive temperature parameter over log-derived 1X2 probabilities with `stats::optim(method = "L-BFGS-B")`, initial value 1.0, bounds [0.25, 4.0], and the frozen seed; return raw fallback when the frozen 60-row/10-per-class support rule is not met. Implement `apply_phase12_1x2_calibrator()` with simplex validation from `R/evaluation/proper_scores.R`, preserving all score-distribution columns and G=40 rows. Persist the durable inner-OOF CSV and calibrators RDS with canonical file/row/hash read-back conventions from `R/benchmark/runner.R`. The tracer test uses only synthetic schema-faithful rows and a recording guard, never the real label artifact. Implement D-01, D-02, D-03, D-06, and D-10 and satisfy validation-map entry 12-02-01.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_calibration.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The focused tracer passes only after a validated freeze; inner rows are strictly prior and candidate/track-specific; the optimizer is deterministic; insufficient support is explicit raw fallback; derived probabilities remain simplex-valid; G=40 scoreline content is unchanged; and WC2026-bearing input fails before fit.</acceptance_criteria>
  <done>One frozen candidate/track can produce an auditable calibrator or explicit raw fallback from prior inner OOF without holdout access.</done>
</task>

<task type="auto">
  <name>Task 2: Complete reusable inner-OOF and calibrator provenance validation</name>
  <read_first>R/calibration/inner_oof.R, R/calibration/probability_calibration.R, data/benchmark/phase12/freeze_manifest.csv, data/benchmark/phase12/calibration_recipe.json, R/benchmark/cutoffs.R, R/benchmark/hybrid_runner.R, R/evaluation/proper_scores.R, tests/testthat/test_phase12_calibration.R</read_first>
  <files>R/calibration/inner_oof.R, R/calibration/probability_calibration.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/inner_oof_predictions.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibrators.rds, tests/testthat/test_phase12_calibration.R</files>
  <action>Complete `validate_phase12_inner_oof_chronology()`, `phase12_calibration_manifest_row()`, and `validate_phase12_calibrator()` so every durable fit records candidate/track/outer edition, sorted prior editions, row/class counts, recipe SHA, seed, source prediction SHA, max evidence date, fit status, and fallback reason. Reject mixed identities, duplicate fixture rows, future editions, same-date/non-strict cutoffs, non-simplex probabilities, invalid optimizer state, and holdout-bearing rows. Keep raw, calibrated, and fitted-distribution views separate. Reuse `R/evaluation/proper_scores.R` validation and document the deviation that the new temperature optimizer is not copied from the legacy draw-only helper. Add deterministic repeated-fit, empty/single/insufficient-support, isolation, cutoff, and unchanged-scoreline tests; cite D-01 through D-04 and D-06. No labels are read or created.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_calibration.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>CAL-01 test coverage is complete, repeated fits and persisted bytes are deterministic, invalid chronology/support/identity/holdout inputs fail closed, and durable artifacts reconcile to the frozen recipe and freeze hashes.</acceptance_criteria>
  <done>The calibrated inner-OOF services are reusable by Plan 03 and cannot run against a pre-freeze or label-bearing input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Freeze/recipe → calibration optimizer | A stale or altered contract could change fitted probabilities. |
| Predictions/fixtures → inner-OOF service | Untrusted dates, identities, labels, and probabilities cross into fitting. |
| Calibrator → scoring/release | Derived probabilities must not mutate the fitted distribution or expose holdout outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-02-01 | Tampering | Freeze-gated recipe and optimizer | critical | mitigate | Validate freeze/recipe SHA, fixed optimizer start/bounds/seed, support floors, and persisted recipe identity before fit. |
| T-12-02-02 | Information disclosure | WC2026 rows entering calibration | critical | mitigate | Call the cutoff purpose guard before any fitting callback and test a recording callback that remains uncalled on rejection. |
| T-12-02-03 | Tampering | Candidate/track chronology | high | mitigate | Require exact identity, strict prior editions, exclusive evidence dates, duplicate rejection, and source hashes. |
| T-12-02-04 | Denial of service | Malformed or sparse inputs | medium | mitigate | Validate simplex/finite values and return explicit raw fallback rather than fitting malformed or unsupported data. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires the package legitimacy gate and blocking human verification. |
</threat_model>

<verification>
Run the focused calibration suite after each task, then inherited cutoff/scoring/seal tests and fresh-process read-back of inner-OOF/calibrator artifacts. Confirm Plan 02 cannot execute without the validated Plan 01 freeze and never reads the operator-supplied label path.
</verification>

<success_criteria>
CAL-01 is satisfied by deterministic, freeze-gated candidate/track calibration with strict chronology, cutoff, support, seed, source-hash, simplex, raw-fallback, and WC2026-rejection evidence while preserving G=40 distributions.
</success_criteria>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-02-SUMMARY.md` when done.
</output>
