---
phase: 12-calibration-promotion-and-model-release
plan: 04
type: execute
wave: 4
depends_on:
  - 12-00
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - R/release/final_fit.R
  - R/release/final_evaluation.R
  - R/release/promotion_report.R
  - _targets.R
  - data/benchmark/phase12/wc2026_labels.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/predictions.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/scores.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/final_fit/final_fit_manifest.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/manifests/final_evaluation_manifest.csv
  - tests/testthat/test_phase12_final_evaluation.R
  - tests/testthat/test_phase12_promotion.R
autonomous: false
requirements:
  - PROMO-02
user_setup:
  - service: "Operator-supplied WC2026 label artifact"
    why: "Provide the reviewed execution-time data/benchmark/phase12/wc2026_labels.csv and its source SHA-256 only after the label-free preflight and blocking approval checkpoint"
must_haves:
  truths:
    - "A label-free preflight validates the freeze, calibration gate, candidate admissibility, contracts, and unopened state before the sole WC2026 label-opening function can run (D-09)."
    - "The final evaluation copies labels once into an immutable scoring/reporting artifact, never passes those labels to fitting/calibration/selection, and appends one manifest linking freeze, label, prediction, score, coverage, and decision hashes (D-10, D-12)."
    - "Every frozen candidate that passes admissibility is evaluated; inactive candidates remain explicit no-score rows in the final registry and report (D-05, D-11)."
    - "The final decision delegates policy to `evaluate_promotion()` exactly once per registered candidate, uses `select_promoted_candidate()` only for deterministic selection among evaluator outputs, and publishes exact `incumbent retained` when no challenger clears every inherited gate (D-13, D-16)."
    - "The approved final model object is produced only through the frozen active registry identity `phase11_rf_dynamic_elo_open` and its existing ranger adapter/provenance contracts; inactive candidates remain no-score rows (D-06, D-11)."
    - "The allowlisted execution-time provider is exactly data/benchmark/phase12/wc2026_labels.csv, exposed only by phase12_open_final_labels(label_path, expected_source_sha256, approval_state) and the targets file target phase12_final_labels; it validates wc2026 identity and the operator-reviewed source hash, runs only after freeze/development gates and the blocking checkpoint, and rejects a second open or pre-checkpoint read (D-09, D-10)."
    - "FLAGGED ASSUMPTION [resolved edge contract PROMO-02]: the opener is a monotonic unopened-to-consumed transition with an append-only manifest; preflight failure leaves the state unopened and a second open is rejected."
    - "FLAGGED ASSUMPTION [edge probe PROMO-01, adjacency/unresolved]: equality at promotion thresholds is resolved only by the operators stored in `promotion_protocol.json`; no Phase 12 comparison silently changes `<` to `<=`."
    - "FLAGGED ASSUMPTION [edge probe PROMO-01, ordering/unresolved]: final candidate rows and reason codes are sorted by registered identity, while candidate selection delegates tie order to `select_promoted_candidate()`."
    - "FLAGGED ASSUMPTION [execution fallback]: if 12-UI-SPEC.md is unavailable at execution, retain the existing semantic HTML/CSS/vanilla-JavaScript dashboard structure and apply the same release-state, metadata, and fail-closed contracts; do not invent a component-library surface."
  artifacts:
    - path: R/release/final_fit.R
      provides: "Allowlisted label-free final model fit/rehydration and model-object identity"
    - path: R/release/final_evaluation.R
      provides: "Label-free preflight, one-shot label opener, immutable final scoring, and append-only manifest"
    - path: R/release/promotion_report.R
      provides: "Phase 9 evaluator-backed final candidate assembly, decision serialization, and incumbent fallback report"
    - path: _targets.R
      provides: "Approval-gated file target phase12_final_labels with the sole execution-time label-opening seam"
    - path: data/benchmark/phase12/wc2026_labels.csv
      provides: "Operator-supplied execution-time input only; never created or read during planning/development"
    - path: tests/testthat/test_phase12_final_evaluation.R
      provides: "Preflight, one-shot, immutable artifact, hash, coverage, and inactive-row tests"
    - path: tests/testthat/test_phase12_promotion.R
      provides: "Exactly-once evaluator delegation and incumbent-retained/promotion decision tests"
  key_links:
    - from: data/benchmark/phase12/freeze_manifest.csv
      to: R/release/final_evaluation.R::phase12_preflight_final_evaluation
      via: "The final opener receives only a validated, sealed, unopened freeze identity"
    - from: R/release/final_evaluation.R::phase12_open_final_labels
      to: R/release/final_evaluation.R::run_phase12_final_evaluation_once
      via: "Copied immutable labels are exposed only to scoring/reporting after the one-way transition"
    - from: R/release/promotion_report.R::evaluate_phase12_candidates
      to: R/evaluation/promotion.R::evaluate_promotion
      via: "The inherited Phase 9 evaluator is the only promotion policy authority"
    - from: R/release/promotion_report.R::write_phase12_promotion_report
      to: "incumbent retained"
      via: "Failed challenger gates produce a usable fallback release status and audit-only alternatives"
---

<objective>
Execute the guarded final comparison once and produce the evaluator-backed incumbent-or-challenger decision artifact.

Purpose: PROMO-02 is the irreversible governance boundary. The plan separates label-free final-fit/preflight work from the one label-opening operation, preserves all nine registry rows, and makes the Phase 9 evaluator the sole policy authority.
Output: final-fit, one-shot final-evaluation, and promotion-report services with focused negative-path and delegation tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase09/promotion_protocol.json
@R/benchmark/cutoffs.R
@R/benchmark/runner.R
@R/benchmark/hybrid_runner.R
@R/evaluation/proper_scores.R
@R/evaluation/benchmark_scores.R
@R/evaluation/promotion.R
@R/benchmark/runner.R
@R/release/freeze_manifest.R
@R/calibration/calibration_selection.R
@tests/testthat/test_benchmark_promotion.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace label-free final fit, preflight, synthetic opening, and decision serialization</name>
  <read_first>R/benchmark/cutoffs.R, R/benchmark/runner.R, R/benchmark/hybrid_runner.R, R/evaluation/proper_scores.R, R/evaluation/benchmark_scores.R, R/evaluation/promotion.R, R/release/freeze_manifest.R, R/calibration/calibration_selection.R, data/benchmark/phase09/promotion_protocol.json, tests/testthat/test_benchmark_seal.R, tests/testthat/test_benchmark_promotion.R</read_first>
  <files>R/release/final_fit.R, R/release/final_evaluation.R, R/release/promotion_report.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/final_fit/final_fit_manifest.csv, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</files>
  <action>Implement a synthetic-provider tracer that never reads the real sealed label source. Add `phase12_final_fit_allowlist(freeze_manifest)` and `fit_phase12_release_candidate(candidate_id, frozen_inputs, calibration_gate)` that admit only the frozen active registry identity `phase11_rf_dynamic_elo_open` through its existing `ranger` adapter/provenance contracts, use only frozen pre-2026 inputs, retain inactive candidates as no-score records, and write model/calibrator hashes to the final-fit manifest. Add `phase12_preflight_final_evaluation(freeze_manifest, calibration_gate, final_state, protocol)` that validates freeze/self-hash, calibration gate, all contract flags, exact G=40, unopened state, and candidate admissibility without accessing labels. Define `phase12_open_final_labels(label_path, expected_source_sha256, approval_state)` as the only label-opening function: production accepts exactly `data/benchmark/phase12/wc2026_labels.csv`, validates the wc2026 identity and the operator-reviewed expected source hash, enforces the approval/preflight state, persists consumed state, and rejects a second open; tests use only an injected synthetic provider through this same boundary and never inspect the operator path. Add `run_phase12_final_evaluation_once()` and `write_phase12_final_evaluation_manifest()` for copied labels, predictions, shared scores, coverage, timestamps, hashes, and promotion linkage. Add `evaluate_phase12_candidates()` and `write_phase12_promotion_report()` that construct the exact candidate shape required by `evaluate_promotion()` and call it exactly once per registered candidate; do not implement thresholds locally. Reuse the seal-before-callback pattern from `R/benchmark/cutoffs.R`, persisted manifest/self-hash patterns from `R/benchmark/runner.R`, inactive adapter/provenance patterns from `R/benchmark/hybrid_runner.R`, score validation from `R/evaluation/proper_scores.R`, and evaluator authority from `R/evaluation/promotion.R`; use Phase 9 staged publication/seal patterns for immutable state. Implement D-09 through D-13 and D-16 and satisfy validation-map entry 12-04-01.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The synthetic tracer proves preflight failure prevents provider invocation, one opening creates an immutable label copy, a second opening fails, all nine final rows remain visible, evaluator call count equals registered candidate count, and a failing challenger produces exact `incumbent retained` with audit-only failures.</acceptance_criteria>
  <done>The final-evaluation and decision services are wired end-to-end under synthetic authority without touching the real sealed labels.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the one-way WC2026 label-opening operation</name>
  <decision>May the executor invoke the sole real one-shot WC2026 label-opening boundary after the label-free preflight has passed?</decision>
  <context>The operation is irreversible. Approval is allowed only after the freeze, calibration gate, final-fit hashes, contract flags, and unopened state have been independently checked.</context>
  <options>
    <option id="approve-final-evaluation">
      <name>Approve opening</name>
      <pros>Permits the required one-shot final comparison and release decision.</pros>
      <cons>Consumes the sealed holdout and cannot be undone.</cons>
    </option>
    <option id="repair-before-opening">
      <name>Keep labels sealed</name>
      <pros>Preserves the holdout while a failed precondition is repaired through a new reviewed freeze.</pros>
      <cons>Blocks final evaluation and release until the freeze is replaced.</cons>
    </option>
  </options>
  <read_first>.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md, data/benchmark/phase09/promotion_protocol.json, data/benchmark/phase12/freeze_manifest.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</read_first>
  <files>data/benchmark/phase12/freeze_manifest.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv</files>
  <action>Pause after the synthetic tracer and require human confirmation that the persisted freeze, calibration gate, final-fit hashes, contract flags, and unopened state have been independently validated. Do not open, inspect, or copy the real WC2026 labels during this checkpoint. Resume only with an explicit approval signal for the one-shot final-evaluation operation required by D-09 through D-12.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The human has confirmed the exact freeze hash, calibration gate hash, final-fit/model hashes, protocol identity, unopened state, and one-shot scope; no label-bearing artifact was opened before approval.</acceptance_criteria>
  <done>Approval is recorded as the only authorization to invoke the real one-shot final-evaluation boundary.</done>
  <reversibility rating="one-way">Opening the sealed labels changes the project state and cannot be undone; the checkpoint precedes the sole label-bearing call.</reversibility>
  <resume-signal>Type `approved` to authorize the one-shot final evaluation, or describe the failed precondition and keep labels unopened.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Open the final holdout once and publish the evaluator-backed decision</name>
  <read_first>R/release/final_fit.R, R/release/final_evaluation.R, R/release/promotion_report.R, _targets.R, data/benchmark/phase12/freeze_manifest.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, data/benchmark/phase09/promotion_protocol.json, tests/testthat/test_benchmark_seal.R, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</read_first>
  <files>R/release/final_fit.R, R/release/final_evaluation.R, R/release/promotion_report.R, _targets.R, data/benchmark/phase12/wc2026_labels.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/predictions.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/scores.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/manifests/final_evaluation_manifest.csv, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</files>
  <action>After the blocking approval, invoke `phase12_open_final_labels(label_path = "data/benchmark/phase12/wc2026_labels.csv", expected_source_sha256 = reviewed_source_sha256, approval_state = approved)` exactly once through the targets file target `phase12_final_labels` with `format = "file"`. The production target may read only the reviewed operator-supplied input; validate its wc2026 identity and expected source hash, copy it once to `outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv`, and expose that immutable copy only to scoring/reporting. Do not read or create the input path during planning/development. Run every frozen admissible candidate, retain inactive/no-score rows, score raw and selected calibrated 1X2 through the inherited scorer on fixed fixtures, and write `predictions.csv`, `scores.csv`, and `manifests/final_evaluation_manifest.csv` with freeze/label/run/candidate/prediction/score/coverage/decision hashes. Build promotion candidates from persisted numeric views, invoke `evaluate_promotion()` once per registered candidate, serialize `value__*`, `pass__*`, and ordered reason codes, then call `select_promoted_candidate()` only on evaluator outputs. Publish exact `incumbent retained` when no challenger is eligible; alternatives and failed gates remain audit-only. Reject second opener calls and mutation of copied labels/predictions/scores/coverage/decision. Follow the Phase 9 seal and staged publication patterns; implement D-10 through D-16 without changing protocol values or using WC2026 labels for fitting, calibration, or selection, satisfying validation-map entries 12-04-02 and 12-04-03.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The real final evaluation is recorded exactly once; immutable final artifacts and append-only manifest hashes reconcile; all nine rows are reported; promotion policy is delegated to the inherited evaluator; incumbent fallback is exact and usable; and tampering/second-open tests fail closed.</acceptance_criteria>
  <done>PROMO-02 is satisfied with a one-shot final comparison and durable incumbent-or-challenger decision evidence.</done>
  <reversibility rating="one-way">The label-opening and final-evaluation artifacts are append-only evidence; repair requires a new reviewed freeze and cannot rewrite the consumed-label state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Freeze/calibration artifacts → final preflight | Stale or tampered development artifacts could authorize an invalid opening. |
| Final preflight → WC2026 label provider | This is the one-way information-disclosure boundary. |
| Final scores → promotion evaluator/release | Final labels must influence reporting only, while policy remains delegated and durable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-04-01 | Information disclosure | `phase12_open_final_labels()` | critical | mitigate | Keep the real provider behind the approved checkpoint and preflight; validate the allowlisted path and reviewed source hash, expose copied labels only to scoring/reporting, and reject second opens. |
| T-12-04-02 | Tampering | Final labels/predictions/scores/coverage | critical | mitigate | Write immutable artifacts, hash every file, append one manifest, and reject read-back hash drift. |
| T-12-04-03 | Elevation of privilege | Promotion decision assembly | critical | mitigate | Call `evaluate_promotion()` exactly once per registered candidate and prohibit local threshold logic or shortlist authority. |
| T-12-04-04 | Denial of service | Final-evaluation state and rollback | high | mitigate | Persist consumed state before exposing results, validate append-only lineage, and fail closed on incomplete artifacts without replacing prior accepted release. |
| T-12-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires legitimacy review and blocking human confirmation. |
</threat_model>

<verification>
Run the synthetic final-evaluation and promotion suites before the checkpoint. After approval, run the real one-shot path, then validate all persisted final artifacts from a fresh R process, run inherited promotion/seal/scoring suites, and run the complete testthat suite. Do not rerun the opener after a successful consumption.
</verification>

<success_criteria>
PROMO-02 is satisfied when a preflight-gated, exactly-once final comparison produces immutable linked artifacts and a sole-evaluator decision that either selects a fully eligible challenger or publishes exact `incumbent retained` with audit-only alternatives.
</success_criteria>

### Artifacts this phase produces

- Functions: `phase12_final_fit_allowlist()`, `fit_phase12_release_candidate()`, `phase12_preflight_final_evaluation()`, `phase12_open_final_labels()`, `run_phase12_final_evaluation_once()`, `write_phase12_final_evaluation_manifest()`, `evaluate_phase12_candidates()`, and `write_phase12_promotion_report()`.
- Files: `R/release/final_fit.R`, `R/release/final_evaluation.R`, `R/release/promotion_report.R`, `_targets.R`, `tests/testthat/test_phase12_final_evaluation.R`, and `tests/testthat/test_phase12_promotion.R`.
- Manifests/reports: `outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv`, `predictions.csv`, `scores.csv`, `final_fit/final_fit_manifest.csv`, and `manifests/final_evaluation_manifest.csv`, plus the operator-supplied input `data/benchmark/phase12/wc2026_labels.csv` at execution time only.
- Contract: append-only one-shot final-evaluation state and Phase 9 evaluator decision with exact fallback status `incumbent retained`.
- CLI/target surface: final-evaluation controller invoked only after the blocking approval; focused final-evaluation and promotion test commands.

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-04-SUMMARY.md` when done.
</output>
