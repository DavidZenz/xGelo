---
phase: 12-calibration-promotion-and-model-release
plan: 01
type: execute
wave: 1
depends_on:
  - 12-00
files_modified:
  - R/release/freeze_manifest.R
  - data/benchmark/phase12/calibration_recipe.json
  - data/benchmark/phase12/freeze_manifest.csv
  - tests/testthat/test_phase12_freeze.R
autonomous: true
requirements:
  - PROMO-01
must_haves:
  truths:
    - "The first executable production slice writes and independently validates one aggregate freeze before any calibrator fit, calibrator object, calibration-fit test, final-fit model, or label-bearing read (D-06, D-07, D-08, D-09)."
    - "The freeze contains exactly all nine Phase 11 registry identities, preserves inactive xG/context/structural candidates as explicit no-score rows, and binds candidate/code/features/settings/panels/seeds/recipe/threshold/parent identities (D-05, D-06, D-08)."
    - "The base-R temperature-scaling recipe and minimum-history/class-support rule are serialized and hashed in the freeze before Plan 02 can fit it; no package is added (D-06)."
    - "Freeze validation is fail-closed for activation, order, hash, parent, dirty-code, G=40, threshold, and already-consumed-holdout drift."
  artifacts:
    - path: R/release/freeze_manifest.R
      provides: "Phase 12 aggregate freeze construction, canonical parent graph, recipe binding, preflight, and validation"
    - path: data/benchmark/phase12/calibration_recipe.json
      provides: "Frozen base-R one-parameter temperature recipe, fixed start/bounds, support floors, seed, epsilon, and G=40"
    - path: data/benchmark/phase12/freeze_manifest.csv
      provides: "Authoritative self-hashed nine-candidate freeze consumed by all later plans"
    - path: tests/testthat/test_phase12_freeze.R
      provides: "PROMO-01 freeze/checksum/preflight contract tests"
  key_links:
    - from: data/benchmark/phase11/model_registry.csv
      to: R/release/freeze_manifest.R::build_phase12_freeze_manifest
      via: "The exact nine registered candidate rows become immutable freeze inputs"
    - from: data/benchmark/phase12/calibration_recipe.json
      to: data/benchmark/phase12/freeze_manifest.csv
      via: "Recipe bytes and SHA-256 are bound before calibration fitting"
    - from: data/benchmark/phase12/freeze_manifest.csv
      to: Plan 02 calibration services
      via: "Every fit asserts the validated freeze identity first"
---

<objective>
Create the single authoritative Phase 12 freeze and frozen calibration recipe before any fitting.

Purpose: PROMO-01 is the first production boundary. It makes the candidate set, parent lineage, protocol, G=40 support, and calibration recipe immutable before later plans can produce a calibrator or final-fit object.
Output: `R/release/freeze_manifest.R`, `data/benchmark/phase12/calibration_recipe.json`, the aggregate `data/benchmark/phase12/freeze_manifest.csv`, and focused freeze tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase09/promotion_protocol.json
@data/benchmark/phase11/model_registry.csv
@data/benchmark/phase11/run_manifest.csv
@R/benchmark/cutoffs.R
@R/benchmark/runner.R
@R/benchmark/hybrid_runner.R
@R/benchmark/challenger_runner.R
@R/evaluation/promotion.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace registry, frozen recipe, parent graph, and checksum into one validated freeze</name>
  <read_first>data/benchmark/phase11/model_registry.csv, data/benchmark/phase11/run_manifest.csv, data/benchmark/phase09/promotion_protocol.json, R/benchmark/cutoffs.R, R/benchmark/runner.R, R/benchmark/hybrid_runner.R, R/benchmark/challenger_runner.R, R/evaluation/promotion.R, tests/testthat/test_phase12_freeze.R</read_first>
  <files>R/release/freeze_manifest.R, data/benchmark/phase12/calibration_recipe.json, data/benchmark/phase12/freeze_manifest.csv, tests/testthat/test_phase12_freeze.R</files>
  <action>Implement the first production vertical path with `build_phase12_freeze_manifest()` and `validate_phase12_freeze_manifest()`. Before any calibrator API is sourced or called, write canonical `data/benchmark/phase12/calibration_recipe.json` with `recipe_id = "phase12_multiclass_temperature"`, deterministic base-R one-parameter temperature scaling over log-derived 1X2 probabilities, `optimizer = "stats::optim-L-BFGS-B"`, `initial_temperature = 1.0`, `temperature_bounds = c(0.25, 4.0)`, `epsilon = 1e-15`, `minimum_history_rows = 60L`, `minimum_class_count = 10L`, `seed = 920012L`, and `score_support = 40L`; bind its SHA-256 into the freeze. Normalize and sort exactly nine Phase 11 rows by candidate identity, preserve active/no-score/research/sealed fields, and hash candidate registration, code/clean-worktree, features, settings, panels, seeds, recipe, promotion protocol, Phase 9/10/11 parents, G=40, and the parent graph. Reuse the manifest row/self-hash conventions from `R/benchmark/runner.R`, parent path and absent-hash rules from `R/benchmark/hybrid_runner.R`, parent admissibility from `R/benchmark/challenger_runner.R`, and the Phase 9 staged publication/seal pattern; use `R/benchmark/cutoffs.R` for the unopened holdout guard and `R/evaluation/promotion.R` only as a downstream authority. Do not fit, instantiate, or test a calibrator here, do not read WC2026 labels, and do not mutate Phase 11 manifests. Implement D-05, D-06, D-07, and D-08 and satisfy validation-map entry 12-01-01.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_freeze.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>A synthetic nine-row registry produces the canonical recipe and freeze with required hashes, parent graph, G=40, self-hash, inactive/no-score rows, and sealed-before-label status; no calibrator fit or label read occurs; registry mutation fails before downstream calibration can accept the freeze.</acceptance_criteria>
  <done>The first production slice is a durable, independently validated freeze and recipe contract that authorizes later fitting only by exact identity.</done>
  <reversibility rating="costly">The freeze controls every downstream fit and one-shot evaluation; replacement requires a new reviewed freeze and never overwrites the accepted artifact in place.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Complete freeze drift, parent, and unopened-holdout validation</name>
  <read_first>R/release/freeze_manifest.R, data/benchmark/phase09/promotion_protocol.json, data/benchmark/phase11/model_registry.csv, tests/testthat/test_phase12_freeze.R, R/benchmark/cutoffs.R, R/benchmark/runner.R, R/benchmark/hybrid_runner.R</read_first>
  <files>R/release/freeze_manifest.R, data/benchmark/phase12/freeze_manifest.csv, tests/testthat/test_phase12_freeze.R</files>
  <action>Complete `phase12_freeze_parent_graph()`, `phase12_freeze_self_hash()`, `phase12_assert_unopened_holdout()`, and `phase12_freeze_candidate_rows()` with canonical names and ordered reason codes. Recompute persisted parent hashes, require `thresholds_frozen = TRUE`, `sealed_before_final_labels = TRUE`, `network_free = TRUE`, inherited Phase 9 common veto identities, and exact G=40; reject absolute/escaping parents, dirty code, duplicate or missing candidates, activation/order drift, recipe/support/seed/threshold drift, parent checksum drift, and consumed-label markers. Extend the contract tests for empty/partial/single synthetic registries and equal-value identity ordering. Keep the tests synthetic and label-free, cite D-05 through D-09, and verify the freeze is complete before any dependent plan can fit.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_freeze.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Every PROMO-01 validation-map case fails closed with ordered reason codes, inactive rows remain visible, persisted hashes reconcile from a fresh process, and no WC2026 label source is accessed.</acceptance_criteria>
  <done>The authoritative freeze is ready to gate calibration, final fit, one-shot final evaluation, promotion, and release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 11 registry/parents → Phase 12 freeze | Durable inputs may be stale, incomplete, or tampered with. |
| Frozen recipe/protocol → later optimizer | Recipe bytes and support floors control the meaning of calibration. |
| Freeze → sealed-label opener | The freeze is the final label-free authority before the one-way checkpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01-01 | Tampering | Nine-row registry and inactive rows | high | mitigate | Require exact registry membership, stable identity ordering, status preservation, and explicit no-score rows. |
| T-12-01-02 | Tampering | Parent graph and freeze self-hash | high | mitigate | Recompute persisted parent hashes, canonical graph, and self-hash; reject missing or escaping paths. |
| T-12-01-03 | Elevation of privilege | Recipe/protocol/threshold freeze | critical | mitigate | Persist and hash the exact recipe before fit, bind all decision identities, and expose authority only on validated output. |
| T-12-01-04 | Information disclosure | Pre-label holdout boundary | critical | mitigate | Require unopened state and seal flags; never pass label-bearing rows into freeze construction or tests. |
| T-12-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires the package legitimacy gate and blocking human verification. |
</threat_model>

<verification>
Run the focused freeze suite after each task, then inherited Phase 9 promotion/seal tests and fresh-process freeze read-back. Confirm the freeze and recipe exist before any Plan 02 calibration command and that no label-bearing source is read.
</verification>

<success_criteria>
PROMO-01 is satisfied by one canonical, self-hashed, parent-linked nine-candidate freeze with a pre-fit calibration recipe and fail-closed drift/unopened validation.
</success_criteria>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-01-SUMMARY.md` when done.
</output>
