---
phase: 12-calibration-promotion-and-model-release
plan: 05
type: execute
wave: 5
depends_on:
  - 12-04
files_modified:
  - R/release/final_evaluation.R
  - R/release/promotion_report.R
  - _targets.R
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/predictions.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/scores.csv
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/manifests/final_evaluation_manifest.csv
  - tests/testthat/test_phase12_final_evaluation.R
  - tests/testthat/test_phase12_promotion.R
autonomous: false
requirements:
  - PROMO-02
user_setup:
  - service: "Operator-supplied WC2026 label artifact"
    why: "Provide the reviewed execution-time data/benchmark/phase12/wc2026_labels.csv and its source SHA-256 only after the label-free preflight and blocking approval checkpoint"
must_haves:
  truths:
    - "The sole WC2026 opening occurs only after the blocking approval, copies the allowlisted labels once into an immutable scoring/reporting artifact, and never passes them to fitting, calibration, or selection (D-09, D-10)."
    - "The final-evaluation manifest is append-only and links freeze, label, prediction, score, coverage, run, and promotion-decision hashes; a second open or any mutation fails closed (D-10, D-12)."
    - "Every admissible frozen candidate is evaluated while inactive candidates remain explicit no-score rows in the final registry and report (D-05, D-11)."
    - "The final decision calls `evaluate_promotion()` exactly once per registered candidate and delegates selection to `select_promoted_candidate()`; failed challenger gates publish exact `incumbent retained` with audit-only alternatives (D-13, D-16)."
    - "The target seam remains exactly `phase12_open_final_labels(label_path, expected_source_sha256, approval_state)` and target `phase12_final_labels` with `format = \"file\"`; no label read occurs before the checkpoint (D-09, D-10)."
  artifacts:
    - path: R/release/final_evaluation.R
      provides: "One-shot copied-label scoring/reporting controller and immutable final-evaluation state"
    - path: R/release/promotion_report.R
      provides: "Exactly-once inherited-evaluator delegation and incumbent-retained decision report"
    - path: _targets.R
      provides: "Approval-gated file target `phase12_final_labels` and final-evaluation DAG edge"
    - path: outputs/benchmarks/rolling_tournaments/phase12-calibration-release/manifests/final_evaluation_manifest.csv
      provides: "Append-only linked final-evaluation and promotion evidence"
    - path: tests/testthat/test_phase12_final_evaluation.R
      provides: "One-shot, immutability, hash, coverage, and checkpoint-boundary tests"
    - path: tests/testthat/test_phase12_promotion.R
      provides: "Exactly-once evaluator and incumbent-retained decision tests"
  key_links:
    - from: R/release/final_evaluation.R::phase12_preflight_final_evaluation
      to: R/release/final_evaluation.R::phase12_open_final_labels
      via: "The real opener is reachable only after label-free preflight and the blocking approval"
    - from: R/release/final_evaluation.R::phase12_open_final_labels
      to: outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv
      via: "The sole allowlisted source is copied once to an immutable reporting/scoring artifact"
    - from: R/release/promotion_report.R::evaluate_phase12_candidates
      to: R/evaluation/promotion.R::evaluate_promotion
      via: "The locked Phase 9 evaluator is called once per registered candidate and remains policy authority"
---

<objective>
Run the approval-gated one-shot final evaluation and produce the evaluator-backed incumbent-or-challenger decision.

Purpose: This is the irreversible governance boundary. Label-free implementation and preflight are complete in Plan 04; this plan owns synthetic wiring, the blocking decision, the single real opening, immutable final artifacts, and policy-delegated promotion.
Output: one-shot final-evaluation artifacts, append-only manifest, promotion report, and exact target/checkpoint wiring.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase09/promotion_protocol.json
@R/benchmark/cutoffs.R
@R/benchmark/runner.R
@R/evaluation/benchmark_scores.R
@R/evaluation/promotion.R
@R/release/final_fit.R
@R/release/final_evaluation.R
@R/release/freeze_manifest.R
@R/calibration/calibration_selection.R
@tests/testthat/test_benchmark_seal.R
@tests/testthat/test_benchmark_promotion.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace synthetic one-shot scoring into evaluator-backed promotion</name>
  <read_first>R/release/final_fit.R, R/release/final_evaluation.R, R/release/freeze_manifest.R, R/calibration/calibration_selection.R, R/benchmark/cutoffs.R, R/benchmark/runner.R, R/evaluation/benchmark_scores.R, R/evaluation/promotion.R, R/evaluation/challenger_selection.R, R/visualization/worldcup_retrospective.R, data/benchmark/phase09/promotion_protocol.json, tests/testthat/test_benchmark_promotion.R, tests/testthat/test_benchmark_seal.R, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</read_first>
  <files>R/release/final_evaluation.R, R/release/promotion_report.R, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</files>
  <action>Using only an injected synthetic provider, implement `run_phase12_final_evaluation_once()` and `write_phase12_final_evaluation_manifest()` so one copied-label payload is exposed only to scoring/reporting, while fitting, calibration, and selection receive no label-bearing rows. Implement `evaluate_phase12_candidates()` and `write_phase12_promotion_report()` to retain all nine registry identities, construct the exact candidate shape required by `evaluate_promotion()`, call it exactly once per registered candidate, serialize `value__*`, `pass__*`, and ordered reason codes, and call `select_promoted_candidate()` only on evaluator outputs. Map `R/release/promotion_report.R` to `R/evaluation/promotion.R`, `R/evaluation/challenger_selection.R`, and `R/visualization/worldcup_retrospective.R`; map the focused promotion test to `tests/testthat/test_benchmark_promotion.R` and `tests/testthat/test_benchmark_seal.R`. Preserve exact `incumbent retained` fallback and audit-only challenger failures. Use the inherited scorer and Phase 9 persisted manifest/seal patterns; do not implement local thresholds or invoke the real path. Implement D-05, D-10, D-11, D-12, D-13, and D-16 and satisfy validation-map entries 12-05-01 and 12-05-02.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Synthetic execution produces one immutable copied-label state, linked hashes and coverage, nine visible candidate rows, exactly one evaluator call per registry row, and exact incumbent fallback when no challenger clears the inherited gates.</acceptance_criteria>
  <done>The approval-gated controller and policy-delegated promotion path are proven without touching the operator label source.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the one-way WC2026 label-opening operation</name>
  <decision>May the executor invoke the sole real one-shot WC2026 label-opening boundary after the label-free preflight has passed?</decision>
  <context>The operation is irreversible. Approval is allowed only after the freeze, calibration gate, final-fit hashes, contract flags, and unopened state have been independently checked.</context>
  <options>
    <option id="approve-final-evaluation">
      <name>Approve opening</name>
      <pros>Permits the required one-shot final comparison and release decision.</pros>
      <cons>Consumes the sealed holdout and cannot be undone.</cons>
    </option>
    <option id="repair-before-opening">
      <name>Keep labels sealed</name>
      <pros>Preserves the holdout while a failed precondition is repaired through a new reviewed freeze.</pros>
      <cons>Blocks final evaluation and release until the freeze is replaced.</cons>
    </option>
  </options>
  <read_first>.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md, data/benchmark/phase09/promotion_protocol.json, data/benchmark/phase12/freeze_manifest.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/final_fit/final_fit_manifest.csv, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</read_first>
  <action>Pause after the synthetic tracer and require human confirmation that the persisted freeze, calibration gate, final-fit hashes, contract flags, and unopened state have been independently validated. Do not open, inspect, or copy the operator-supplied label artifact during this checkpoint. Resume only with an explicit approval signal for the one-shot final-evaluation operation required by D-09 through D-12.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The human has confirmed the exact freeze hash, calibration gate hash, final-fit/model hashes, protocol identity, unopened state, and one-shot scope; no label-bearing artifact was opened before approval.</acceptance_criteria>
  <done>Approval is recorded as the only authorization to invoke the real one-shot final-evaluation boundary.</done>
  <reversibility rating="one-way">Opening the sealed labels changes project state and cannot be undone; this checkpoint precedes the sole label-bearing call.</reversibility>
  <resume-signal>Type `approved` to authorize the one-shot final evaluation, or describe the failed precondition and keep labels unopened.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Open the final holdout once and publish the decision manifest</name>
  <read_first>R/release/final_fit.R, R/release/final_evaluation.R, R/release/promotion_report.R, _targets.R, data/benchmark/phase12/freeze_manifest.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/final_fit/final_fit_manifest.csv, data/benchmark/phase09/promotion_protocol.json, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</read_first>
  <files>R/release/final_evaluation.R, R/release/promotion_report.R, _targets.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/predictions.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/scores.csv, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/manifests/final_evaluation_manifest.csv, tests/testthat/test_phase12_final_evaluation.R, tests/testthat/test_phase12_promotion.R</files>
  <action>After explicit approval, invoke `phase12_open_final_labels(label_path = "data/benchmark/phase12/wc2026_labels.csv", expected_source_sha256 = reviewed_source_sha256, approval_state = approved)` exactly once through the targets file target `phase12_final_labels` with `format = "file"`. The production target may read only the reviewed operator-supplied input, validate its wc2026 identity and expected source hash, copy it once to `outputs/benchmarks/rolling_tournaments/phase12-calibration-release/final_evaluation/labels.csv`, and expose that immutable copy only to scoring/reporting. Do not read or create the input path during planning/development. Run every frozen admissible candidate, retain inactive/no-score rows, score raw and selected calibrated 1X2 through the inherited scorer on fixed fixtures, and write predictions, scores, and the append-only final manifest with freeze/label/run/candidate/prediction/score/coverage/decision hashes. Invoke `evaluate_promotion()` once per registered candidate, serialize numeric gate evidence and ordered reasons, then call `select_promoted_candidate()` only on evaluator outputs. Publish exact `incumbent retained` when no challenger is eligible; alternatives and failed gates remain audit-only. Reject second opener calls and mutation of copied labels/predictions/scores/coverage/decision. Implement D-09 through D-16 without changing protocol values or using WC2026 labels for fitting, calibration, or selection.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_final_evaluation.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_promotion.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The approved execution records exactly one real opening; immutable final artifacts and append-only hashes reconcile; all nine rows are reported; promotion is delegated to the inherited evaluator; exact incumbent fallback is usable; and second-open/tamper tests fail closed.</acceptance_criteria>
  <done>PROMO-02 is satisfied with one-shot final comparison evidence and a durable incumbent-or-challenger decision.</done>
  <reversibility rating="one-way">The label-opening and final-evaluation artifacts are append-only evidence; repair requires a new reviewed freeze and cannot rewrite consumed state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Final preflight → WC2026 provider | The sole one-way information-disclosure boundary is protected by the checkpoint and exact opener contract. |
| Copied labels → scoring/reporting | Labels may influence only final scores, coverage, and audit artifacts. |
| Final evidence → promotion evaluator | Numeric evidence must not bypass the inherited policy authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-05-01 | Information disclosure | `phase12_open_final_labels()` | critical | mitigate | Require passed preflight, explicit blocking approval, exact source identity/hash, one consumed-state transition, and second-open rejection. |
| T-12-05-02 | Tampering | Final labels/predictions/scores/coverage | critical | mitigate | Copy once, hash every durable artifact, append one manifest, and reject read-back drift. |
| T-12-05-03 | Elevation of privilege | Promotion assembly | critical | mitigate | Call `evaluate_promotion()` exactly once per registered candidate and prohibit local threshold logic or shortlist authority. |
| T-12-05-04 | Denial of service | Consumed-label state | high | mitigate | Persist consumed state before exposing results and fail closed on incomplete or inconsistent artifacts. |
| T-12-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires legitimacy review and blocking human verification. |
</threat_model>

<verification>
Run synthetic final-evaluation/promotion tests before the checkpoint. Only after approval run the real one-shot path, then validate all persisted artifacts from a fresh R process, inherited promotion/seal/scoring suites, and the complete testthat suite. Never rerun the opener after successful consumption.
</verification>

<success_criteria>
PROMO-02 is satisfied when a preflight- and approval-gated exactly-once final comparison produces immutable linked artifacts and a sole-evaluator decision that either selects a fully eligible challenger or publishes exact `incumbent retained` with audit-only alternatives.
</success_criteria>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-05-SUMMARY.md` when done.
</output>
