---
phase: 12-calibration-promotion-and-model-release
plan: 07
type: execute
wave: 8
depends_on:
  - 12-05
  - 12-08
files_modified:
  - R/release/release_contract.R
  - R/visualization/worldcup_dashboard.R
  - _targets.R
  - tests/testthat/test_worldcup_dashboard.R
autonomous: true
requirements:
  - PROMO-03
must_haves:
  truths:
    - "Dashboard and export consumers resolve exactly one approved or exact `incumbent retained` release through a trusted-root-relative manifest/model contract and fail closed before model loading or forecast generation on missing, ambiguous, unapproved, unsafe, stale, wrong-identity, wrong-G, or hash-mismatched artifacts (D-13, D-15, D-16)."
    - "Every dashboard view and export carries one release identity and one explicit `calibrated_1x2` or `raw_1x2` primary view while fitted G=40 score distributions remain unchanged (D-01, D-04, D-15)."
    - "The target DAG keeps `phase12_open_final_labels(label_path, expected_source_sha256, approval_state)` and target `phase12_final_labels` with `format = \"file\"` downstream of the blocking approval; no label-bearing source is an ancestor of freeze or calibration (D-09, D-10)."
    - "The existing Groups, Matches, Bracket, Teams, and Elo Ratings information architecture and interactions remain present, with UI-SPEC loading, empty, error, populated, partial, overflow, zero-one-many, and long-text states covered by regression tests."
    - "The UI-SPEC fallback remains the existing semantic HTML/CSS/vanilla-JavaScript surface when no component-library specification exists; no new frontend dependency or bypass path is introduced."
    - "Seven spec-less/fallback assumptions remain explicit: no raw-path authority, one approved resolver, both approved and retained statuses usable, unchanged scoreline simulation, exact primary-view metadata, no label payload, and existing dashboard interaction preservation."
  backstops:
    - statement: "Long release IDs, candidate/track IDs, hashes, report limitations, and team names wrap or ellipsize inside the release panel/table/card, while full values remain readable in provenance details and no page-level horizontal overflow occurs."
      verification: backstop
  artifacts:
    - path: R/release/release_contract.R
      provides: "Fresh-process approved-release resolver, contract validator, and release metadata projection"
    - path: R/visualization/worldcup_dashboard.R
      provides: "Manifest-only dashboard/export consumer with release states and fail-closed rendering"
    - path: _targets.R
      provides: "Explicit file-oriented Phase 12 ancestry from final decision through release resolver/dashboard"
    - path: tests/testthat/test_worldcup_dashboard.R
      provides: "Existing dashboard shape/interactions plus release and UI-SPEC regression states"
  key_links:
    - from: outputs/releases/<release_id>/release_manifest.csv
      to: R/release/release_contract.R::resolve_phase12_approved_release
      via: "Only a staged, validated release root can be resolved"
    - from: R/release/release_contract.R::resolve_phase12_approved_release
      to: R/visualization/worldcup_dashboard.R::build_worldcup_dashboard_data
      via: "Dashboard receives loaded artifacts only after manifest, contract, path, hash, G=40, and status validation"
    - from: R/release/release_contract.R::phase12_release_metadata
      to: dashboard JSON and CSV exports
      via: "Release version/status, candidate/track, primary view, model/manifest hashes, and generated timestamp remain consistent"
---

<objective>
Move every dashboard/export consumer behind the approved-release resolver and wire the durable target DAG with full UI regression coverage.

Purpose: PROMO-03 is complete only when a valid release is the sole consumer authority and the existing dashboard experience remains safe across all documented UI states. This plan runs after Plans 06 and 08 have produced and installed the complete release; it has a disjoint write set from both release plans and does not alter the release publisher or durable bundle fixture files.
Output: approved-release resolver, dashboard/export metadata and states, target ancestry, and regression coverage.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@.planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md
@R/release/release_bundle.R
@R/release/final_evaluation.R
@R/release/promotion_report.R
@R/evaluation/benchmark_scores.R
@R/visualization/worldcup_dashboard.R
@_targets.R
@tests/testthat/test_phase12_release.R
@tests/testthat/test_worldcup_dashboard.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace approved-manifest resolution into one dashboard/export path</name>
  <read_first>R/release/release_bundle.R, R/release/release_install.R, R/release/final_fit.R, R/release/final_evaluation.R, R/release/promotion_report.R, R/benchmark/runner.R, R/benchmark/hybrid_runner.R, R/evaluation/benchmark_scores.R, R/evaluation/proper_scores.R, R/evaluation/promotion.R, R/evaluation/challenger_selection.R, R/visualization/worldcup_retrospective.R, R/visualization/worldcup_dashboard.R, tests/testthat/test_benchmark_promotion.R, tests/testthat/test_benchmark_seal.R, tests/testthat/test_phase12_release.R, tests/testthat/test_worldcup_dashboard.R, .planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md</read_first>
  <files>R/release/release_contract.R, R/visualization/worldcup_dashboard.R, tests/testthat/test_worldcup_dashboard.R</files>
  <action>Implement `resolve_phase12_approved_release(trusted_root, release_manifest_path = NULL)`, `validate_phase12_release_contract(release_root, release_manifest, model_contract)`, and `phase12_release_metadata()` using fresh-process-safe SHA-256 hashes and trusted-root-relative paths. Read the completed bundle and installer seam from Plan 08, and use the resolved analog mappings for `R/release/final_fit.R` to `R/benchmark/hybrid_runner.R`/`R/benchmark/runner.R` and `R/release/promotion_report.R` to `R/evaluation/promotion.R`/`R/evaluation/challenger_selection.R`/`R/visualization/worldcup_retrospective.R`; retain the focused promotion/seal test mappings in the regression contract. Require exactly one manifest with status `approved` or `incumbent retained`, candidate/track identity, score support 40, primary view `calibrated_1x2` or `raw_1x2`, required model/calibrator/contract/freeze/final-manifest/report/model-card/provenance/limitations/reproducibility artifacts, and matching hashes before `readRDS()` or forecast generation. Reject missing/ambiguous/stale/unapproved/path-escaping/wrong-G/wrong-panel/wrong-candidate/wrong-track artifacts. Replace existence/mtime/raw-path selection in the dashboard and export path with the resolver, pass one explicit primary probability view plus fallback metadata, preserve fitted score-distribution simulation, and render the UI-SPEC release panel with exact approved/retained copy, `Open approved release`, full hash/root in `Release provenance`, `role=\"status\"`, `role=\"alert\"`, and `aria-live=\"polite\"`. Preserve all five tabs/interactions and implement D-01, D-13, D-14, D-15, and D-16.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_dashboard.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Complete synthetic approved and incumbent-retained releases resolve before model loading; invalid/missing/unsafe/hash-mismatched releases stop before forecast payload generation; dashboard and exports carry one release identity and primary view; and the release panel/error copy is present.</acceptance_criteria>
  <done>The production consumer boundary has one approved-manifest resolver and no raw model-path authority.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the label-safe target DAG and complete UI-SPEC regressions</name>
  <read_first>_targets.R, R/release/release_contract.R, R/release/release_bundle.R, R/release/release_install.R, R/release/final_evaluation.R, R/release/promotion_report.R, R/visualization/worldcup_dashboard.R, tests/testthat/test_phase12_release.R, tests/testthat/test_worldcup_dashboard.R, .planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md, .planning/phases/12-calibration-promotion-and-model-release/12-UI-SPEC.md</read_first>
  <files>_targets.R, R/visualization/worldcup_dashboard.R, tests/testthat/test_worldcup_dashboard.R</files>
  <action>Source Phase 12 scripts in `_targets.R` after inherited benchmark modules and add file-oriented targets named `phase12_calibration_recipe_file`, `phase12_freeze_manifest_file`, `phase12_calibration_gate_files`, `phase12_final_evaluation_manifest_file`, `phase12_promotion_report_file`, `phase12_release_bundle_files`, and `phase12_approved_release`. Preserve the sole label target `phase12_final_labels` with `format = \"file\"`, keep its dependency downstream of the blocking approval, and make ancestry explicit as Phase 9/10/11 parents → freeze/preflight → calibration → raw/calibrated gate → final pre-label gate → one-shot final evaluation → evaluator-backed promotion → staged release → completed atomic install → approved resolver → dashboard/export; no label-bearing source may be an ancestor of freeze or calibration. Extend dashboard regression fixtures to assert 72 fixtures, 48 group rows, bracket routes, deterministic seeds, preserved tabs/toggles/search/filter/inspector hooks, exact release metadata, approved/retained status, raw fallback metadata, and no WC2026 label payload. Add loading, empty, error, populated, partial, zero/one/many, overflow, and long-text backstop states with exact UI-SPEC copy, roles, `aria-live`, focusable controls, `rel=\"noopener\"`, wrapping/ellipsis, full provenance values, and no page-level horizontal clipping. If 12-UI-SPEC.md is unavailable, retain the documented semantic HTML/CSS/vanilla-JavaScript fallback contract. Run all five exact Phase 12 validation files, inherited dashboard/scoring/promotion/seal suites, and the complete testthat suite; satisfy validation-map entries 12-07-01 and 12-07-02.</action>
  <verify>
    <automated>Rscript --vanilla -e 'files <- c("tests/testthat/test_phase12_calibration.R", "tests/testthat/test_phase12_freeze.R", "tests/testthat/test_phase12_final_evaluation.R", "tests/testthat/test_phase12_promotion.R", "tests/testthat/test_phase12_release.R"); for (path in files) testthat::test_file(path, stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_worldcup_dashboard.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_dir("tests/testthat", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The target-DAG test proves label-free ancestry and the exact opener/target seam; dashboard/export consumers use only the resolver; all UI-SPEC states and long-text backstop pass; existing interactions remain present; and the complete testthat suite passes with warnings treated as failures.</acceptance_criteria>
  <done>PROMO-03 is satisfied with approved-manifest-only consumers, explicit target ancestry, and regression-tested dashboard/export behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Release manifest/contract → model loader | Manifest paths, hashes, status, identities, and G=40 control which model is loaded. |
| Resolver → dashboard/export payload | Presentation must not emit forecasts before validation or expose WC2026 labels. |
| Targets DAG → durable outputs | Incorrect ancestry could make labels available to freeze or calibration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-07-01 | Tampering | Release contract and model hashes | critical | mitigate | Validate required files, hashes, paths, status, identity, panel, and G before `readRDS()` or forecast generation. |
| T-12-07-02 | Spoofing | Trusted release root | high | mitigate | Normalize root-relative paths and reject absolute, escaping, ambiguous, stale, and mtime/latest-based resolution. |
| T-12-07-03 | Information disclosure | Dashboard/export payloads | high | mitigate | Keep labels outside consumer-facing files and assert absence from HTML/JSON/CSV while exposing only release metadata. |
| T-12-07-04 | Elevation of privilege | `_targets.R` label ancestry | critical | mitigate | Preserve `phase12_final_labels` as a downstream `format = \"file\"` target and test that freeze/calibration have no label-bearing ancestor. |
| T-12-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted install requires legitimacy review and blocking human verification. |
</threat_model>

<verification>
Run focused resolver/dashboard and target-DAG regressions after each task, then all five exact Phase 12 validation files, inherited suites, and the complete testthat suite. Confirm all UI-SPEC state coverage, seven fallback assumptions, no label payload, and preserved dashboard interactions.
</verification>

<success_criteria>
PROMO-03 is complete when every consumer resolves one validated approved/retained release, the label-safe target DAG is explicit, the existing dashboard experience is preserved, and all release/UI regression checks pass.
</success_criteria>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-07-SUMMARY.md` when done.
</output>
