---
phase: 08-forecast-ledger-and-wc-2026-retrospective
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/evaluation/worldcup_ledger.R
  - scripts/reconstruct_worldcup_2026_ledger.R
  - tests/testthat/test_worldcup_ledger.R
autonomous: true
requirements: [LEDGER-01, LEDGER-02, LEDGER-03]
user_setup: []
must_haves:
  truths:
    - "D-01: Strict records require pre-kickoff generation, source cutoffs, and committer time in UTC with no grace period."
    - "D-02 and D-03: Every committed occurrence is retained and deterministic first_valid/latest_valid views are derived."
    - "D-04: verified and documented evidence tiers are explicit, with only verified records strict-eligible."
    - "D-05 and D-06: post-kickoff records are audit-only and missing fixtures remain visible without imputation."
    - "D-07 and D-08: each rejected row has a primary reason plus all flags, and strict/exploratory samples never blend."
  artifacts:
    - path: R/evaluation/worldcup_ledger.R
      provides: Git reconstruction, provenance classification, and view selection
    - path: scripts/reconstruct_worldcup_2026_ledger.R
      provides: Read-only reproducible ledger entry point
    - path: tests/testthat/test_worldcup_ledger.R
      provides: Unit, contract, and bounded repository tests
  key_links:
    - from: R/evaluation/worldcup_ledger.R
      to: outputs/dashboard/worldcup_prematch_forecasts.csv
      via: historical Git blob extraction
    - from: R/evaluation/worldcup_ledger.R
      to: data/raw/espn
      via: canonical UTC kickoff and result reconciliation
---

<objective>
Reconstruct an immutable, point-in-time ledger for every official 2026 World Cup
fixture and classify each forecast occurrence by evidence quality.

Purpose: Establish trustworthy evaluation input before any tournament score is
computed.
Output: Ledger module, command-line runner, provenance artifacts, and tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-RESEARCH.md
@R/visualization/worldcup_dashboard.R
@scripts/update_worldcup_dashboard.R
@tests/testthat/test_worldcup_dashboard.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical fixture and result registry</name>
  <files>R/evaluation/worldcup_ledger.R, tests/testthat/test_worldcup_ledger.R</files>
  <read_first>R/visualization/worldcup_dashboard.R, data/raw/worldcup_2026_group_fixtures.csv, tests/testthat/test_worldcup_dashboard.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md</read_first>
  <action>Create `load_worldcup_2026_result_events()`, `build_worldcup_2026_fixture_registry()`, and `validate_fixture_registry()`. Parse cached `data/raw/espn/scoreboard_*.json` event timestamps as UTC, regulation scores, status, and winners; reconcile all 103 IDs using normalized team/date/bracket identity; retain ESPN event ID and source path; reject ambiguous, duplicate, or missing mappings. Add synthetic tests for timezone parsing, reversed ESPN home/away labels, knockout advancement, duplicate mappings, and the exact 103-row registry contract.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_ledger.R")'</verify>
  <acceptance_criteria>
    - `R/evaluation/worldcup_ledger.R` contains `build_worldcup_2026_fixture_registry` and `validate_fixture_registry`.
    - The registry has 103 unique `match_id` values and non-missing `kickoff_utc`, regulation goals, stage, and result provenance.
    - Ambiguous and duplicate result mappings raise deterministic errors instead of choosing a row.
  </acceptance_criteria>
  <done>Canonical fixture registry tests pass for all official fixtures and edge fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Extract every committed forecast occurrence and artifact identity</name>
  <files>R/evaluation/worldcup_ledger.R, tests/testthat/test_worldcup_ledger.R</files>
  <read_first>R/evaluation/worldcup_ledger.R, R/visualization/worldcup_dashboard.R, scripts/update_worldcup_dashboard.R, outputs/dashboard/worldcup_prematch_forecasts.csv, outputs/dashboard/worldcup_bracket_prematch_forecasts.csv</read_first>
  <action>Add `list_forecast_archive_commits()`, `read_git_blob_text()`, `git_blob_id()`, `extract_forecast_occurrences()`, and `extract_selected_distributions()`. Default to commits reachable from configurable `source_ref = "HEAD"`; record full commit SHA, author/committer ISO times, parent, archive path/blob, dashboard JSON blob, model RDS blobs, feature-table blob, generated time, declared cutoffs, probabilities, and a normalized `forecast_revision_id`. Parse compact CSV archives for every occurrence, then parse dashboard JSON/stage files only for selected commit IDs. Commands must be read-only and use argument vectors rather than interpolated shell pipelines.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_ledger.R")'</verify>
  <acceptance_criteria>
    - Every extracted occurrence contains full commit and blob identifiers plus source path.
    - Unchanged forecast rows across commits remain separate occurrences but share a stable `forecast_revision_id`.
    - Selected full scoreline probabilities sum to one within `1e-6` and retain their source commit.
    - No reconstruction function writes to or changes Git history.
  </acceptance_criteria>
  <done>Bounded real-repository and synthetic extraction tests are deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Classify evidence, derive views, and write the ledger bundle</name>
  <files>R/evaluation/worldcup_ledger.R, scripts/reconstruct_worldcup_2026_ledger.R, tests/testthat/test_worldcup_ledger.R</files>
  <read_first>R/evaluation/worldcup_ledger.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md, R/pipeline/validation.R</read_first>
  <action>Implement `classify_forecast_evidence()`, `derive_forecast_views()`, `validate_forecast_ledger()`, and `write_forecast_ledger_bundle()`. Use stable flags including `post_kickoff_generation`, `post_kickoff_commit`, `source_cutoff_not_proven`, `artifact_missing`, `probability_invalid`, and `fixture_identity_mismatch`; retain all flags and select one documented precedence for `primary_reason`. Set `strict_eligible` only for verified rows and `exploratory_eligible` only for documented pre-kickoff rows. Add explicit missing rows for official fixtures with no eligible forecast. Derive first/latest with commit time, generation time, and full SHA tie-breaks. Write CSV/RDS ledger, selected distributions, fixture results, rejection summary, and checksum manifest under `outputs/evaluation/wc2026/`; expose a read-only CLI with `--source-ref` and `--output-dir` arguments.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_ledger.R")'</verify>
  <acceptance_criteria>
    - All 103 official fixtures appear in coverage output as valid, documented, rejected, or missing.
    - No row with generation or committer time at/after kickoff is strict- or exploratory-eligible.
    - Every rejected row has non-empty `primary_reason` and serialized `validation_flags`.
    - Strict and exploratory view labels are mutually exclusive and no probability is imputed.
    - Running the bounded reconstruction twice yields identical content hashes after excluding bundle generation time.
  </acceptance_criteria>
  <done>The ledger bundle is complete, deterministic, and passes all provenance tests.</done>
</task>

</tasks>

<verification>
- [ ] `Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_ledger.R")'` passes.
- [ ] Bounded Git reconstruction performs no worktree or history mutation.
- [ ] Coverage includes exactly 103 official fixtures.
- [ ] Strict, exploratory, rejected, and missing counts reconcile.
</verification>

<success_criteria>
- All tasks completed and ledger tests pass.
- LEDGER-01, LEDGER-02, and LEDGER-03 are observable in generated schemas.
- No retrospective probability is synthesized or mislabelled as pre-match.
</success_criteria>

## Artifacts This Phase Produces

- `R/evaluation/worldcup_ledger.R`: `load_worldcup_2026_result_events()`,
  `build_worldcup_2026_fixture_registry()`, `list_forecast_archive_commits()`,
  `extract_forecast_occurrences()`, `extract_selected_distributions()`,
  `classify_forecast_evidence()`, `derive_forecast_views()`,
  `validate_forecast_ledger()`, and `write_forecast_ledger_bundle()`.
- `scripts/reconstruct_worldcup_2026_ledger.R`: `--source-ref` and `--output-dir` CLI.
- `tests/testthat/test_worldcup_ledger.R`.
- `outputs/evaluation/wc2026/{forecast_ledger.csv,forecast_ledger.rds,fixture_results.csv,selected_forecasts.csv,selected_distributions.rds,rejection_summary.csv,bundle_manifest.csv}`.

<output>
After completion, create `.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-01-SUMMARY.md`.
</output>
