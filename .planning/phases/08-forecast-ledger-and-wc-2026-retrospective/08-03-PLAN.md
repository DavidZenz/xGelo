---
phase: 08-forecast-ledger-and-wc-2026-retrospective
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - R/visualization/worldcup_retrospective.R
  - notebooks/worldcup_2026_retrospective.Rmd
  - scripts/run_worldcup_2026_retrospective.R
  - tests/testthat/test_worldcup_retrospective.R
  - _targets.R
autonomous: true
requirements: [LEDGER-01, LEDGER-02, LEDGER-03, EVAL-01, EVAL-02, EVAL-03]
user_setup: []
must_haves:
  truths:
    - "D-13: The canonical artifact is a reproducible HTML report backed by immutable CSV/RDS contracts."
    - "D-14: The report opens with strict coverage, headline RPS, uncertainty, and caveats."
    - "D-15: Every official fixture is inspectable with first/latest probabilities, validity, scores, and commit provenance."
    - "D-16: The main report contains coverage flow, cumulative RPS, calibration, forecast evolution, and goal-distribution diagnostics; dense cuts live in appendices."
  artifacts:
    - path: notebooks/worldcup_2026_retrospective.Rmd
      provides: Canonical reproducible retrospective
    - path: R/visualization/worldcup_retrospective.R
      provides: Five deterministic core visual families
    - path: scripts/run_worldcup_2026_retrospective.R
      provides: End-to-end artifact and report runner
    - path: tests/testthat/test_worldcup_retrospective.R
      provides: Bundle, render, and pipeline regression tests
  key_links:
    - from: notebooks/worldcup_2026_retrospective.Rmd
      to: outputs/evaluation/wc2026/aggregate_scores.csv
      via: read-only report inputs
    - from: _targets.R
      to: scripts/run_worldcup_2026_retrospective.R
      via: ledger, score, figure, and report targets
---

<objective>
Publish the audited tournament retrospective and integrate its immutable data,
figures, and report into the reproducible pipeline.

Purpose: Make the Phase 8 evidence inspectable and rerunnable before model
challenger work begins.
Output: Core figures, canonical HTML report, pipeline targets, and regression tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-RESEARCH.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-02-SUMMARY.md
@notebooks/model_performance.Rmd
@R/visualization/calibration.R
@_targets.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic retrospective figures and report</name>
  <files>R/visualization/worldcup_retrospective.R, notebooks/worldcup_2026_retrospective.Rmd, tests/testthat/test_worldcup_retrospective.R</files>
  <read_first>R/visualization/calibration.R, notebooks/model_performance.Rmd, R/evaluation/worldcup_retrospective.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md</read_first>
  <action>Create `plot_forecast_coverage()`, `plot_cumulative_rps()`, `plot_outcome_calibration()`, `plot_forecast_revision_changes()`, and `plot_goal_distribution_diagnostics()` with explicit sample/view labels and stable dimensions. Build `notebooks/worldcup_2026_retrospective.Rmd` to read only bundle artifacts. Open with a strict scorecard containing coverage, latest RPS, 95 percent interval, first/latest delta, and caveats; then present provenance, strict/exploratory comparisons, match/goal/advancement/stage scores, calibration, and a complete 103-fixture table with commit identity and rejection evidence. Put detailed stage, outcome-class, confidence, and team-balanced tables/figures in appendices.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_retrospective.R")'</verify>
  <acceptance_criteria>
    - The report source contains sections for scorecard, provenance, match outcomes, goals, advancement/stage reach, calibration, fixture ledger, and appendices.
    - Every headline score displays strict coverage and a 95 percent interval.
    - All five required figure files are generated with non-zero dimensions.
    - The fixture table contains 103 unique match IDs and commit/rejection fields.
  </acceptance_criteria>
  <done>Report fixtures and figure contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add an end-to-end runner and targets pipeline contracts</name>
  <files>scripts/run_worldcup_2026_retrospective.R, _targets.R, tests/testthat/test_worldcup_retrospective.R</files>
  <read_first>_targets.R, scripts/update_worldcup_dashboard.R, R/evaluation/worldcup_ledger.R, R/evaluation/worldcup_retrospective.R, R/visualization/worldcup_retrospective.R</read_first>
  <action>Add source declarations for the three Phase 8 R modules. Create targets for fixture registry, forecast ledger bundle, score bundle, retrospective figures, and rendered HTML, using file formats and dependencies so report rendering cannot precede scoring. Implement `scripts/run_worldcup_2026_retrospective.R` with `--source-ref`, `--output-dir`, `--bootstrap-reps`, and `--seed` arguments; default to `HEAD`, `outputs/evaluation/wc2026`, `2000`, and `20260720`. The runner must perform no live network calls or model fitting and must write a final manifest with source HEAD SHA and checksums.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_retrospective.R")'</verify>
  <acceptance_criteria>
    - `_targets.R` sources all Phase 8 modules and declares ordered ledger, score, figure, and report targets.
    - The runner exposes all four documented arguments and defaults.
    - Static tests prove there are no HTTP calls, downloads, model-training calls, or 2026 fitting paths in the runner/report.
    - Manifest source SHA equals the requested reconstruction ref resolution.
  </acceptance_criteria>
  <done>Pipeline and runner contract tests pass without changing existing dashboard targets.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the full retrospective and lock regression evidence</name>
  <files>tests/testthat/test_worldcup_retrospective.R, outputs/evaluation/wc2026</files>
  <read_first>tests/testthat/test_worldcup_retrospective.R, scripts/run_worldcup_2026_retrospective.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-VALIDATION.md</read_first>
  <action>Run the end-to-end reconstruction, scoring, plotting, and report render against the recorded source ref. Add regression assertions that all 103 fixtures are represented; strict/exploratory counts reconcile; probability rows normalize; report and five core figures exist; bundle checksums verify; repeated runs with the same ref/seed reproduce analytical files; and no post-kickoff record appears in scored samples. Run Phase 8 tests followed by the full existing testthat suite. Record actual strict and exploratory coverage in the plan summary, including any metric-specific missing distribution coverage.</action>
  <verify>Rscript --vanilla scripts/run_worldcup_2026_retrospective.R --source-ref HEAD --output-dir outputs/evaluation/wc2026 --bootstrap-reps 2000 --seed 20260720 && Rscript --vanilla -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - `outputs/evaluation/wc2026/worldcup_2026_retrospective.html` exists and is non-empty.
    - Bundle and report represent exactly 103 official fixtures with no imputation.
    - Repeated analytical outputs match checksums for the same source ref and seed.
    - Phase 8 and full testthat suites exit successfully.
    - Summary reports actual coverage and any unavailable metric families rather than hiding them.
  </acceptance_criteria>
  <done>The canonical report and immutable data bundle reproduce end to end with all regression tests green.</done>
</task>

</tasks>

<verification>
- [ ] Phase 8 quick suite passes.
- [ ] Full testthat suite passes.
- [ ] End-to-end runner writes and verifies the immutable bundle.
- [ ] HTML report and all five core figures exist and are non-empty.
- [ ] Exactly 103 fixtures appear, and scored samples contain no post-kickoff rows.
</verification>

<success_criteria>
- All Phase 8 requirements and D-13..D-16 are visible in published artifacts.
- The report is fully reproducible from the recorded Git ref and cached sources.
- Dashboard behavior remains unchanged; the retrospective consumes separate approved contracts.
</success_criteria>

## Artifacts This Phase Produces

- `R/visualization/worldcup_retrospective.R`: `plot_forecast_coverage()`,
  `plot_cumulative_rps()`, `plot_outcome_calibration()`,
  `plot_forecast_revision_changes()`, and
  `plot_goal_distribution_diagnostics()`.
- `notebooks/worldcup_2026_retrospective.Rmd`.
- `scripts/run_worldcup_2026_retrospective.R`: `--source-ref`, `--output-dir`,
  `--bootstrap-reps`, and `--seed` CLI flags.
- `tests/testthat/test_worldcup_retrospective.R`.
- `_targets.R` targets for ledger, scores, figures, and report.
- `outputs/evaluation/wc2026/worldcup_2026_retrospective.html` and
  `outputs/evaluation/wc2026/figures/*.png`.

<output>
After completion, create `.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-03-SUMMARY.md`.
</output>
