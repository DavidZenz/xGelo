---
phase: 09-rolling-tournament-benchmark-harness
plan: "05"
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - R/forecast/features.R
  - R/forecast/goal_ability.R
  - R/benchmark/contracts.R
  - R/benchmark/baselines.R
  - R/benchmark/runner.R
  - _targets.R
  - data/processed/goal_training_features_hybrid.csv
  - tests/testthat/test_transfermarkt_benchmark.R
  - tests/testthat/test_benchmark_baselines.R
autonomous: true
gap_closure: true
requirements: [BENCH-03]

must_haves:
  truths:
    - "The canonical `goal_training_features_hybrid.csv` preserves per-feature source presence, the actual selected source-row date, value presence, and pre-imputation status before any registered zero/default is written."
    - "An observed numeric zero remains source-present and non-imputed, while a missing source that becomes numeric zero remains source-absent and explicitly imputed."
    - "Every adapter prediction receives a deterministic feature_coverage_id and leaves the adapter with one runtime evidence row for every feature registered to that model's panel per D-10 and D-15."
    - "Feature evidence records model, panel, edition, track, boundary, fixture, feature, source date, strict-cutoff status, imputation reason, active-in-fit status, coverage status, and license/provenance fields."
    - "Feature coverage is validated before adapter return, giving 09-06 an exact, executable bundle handoff rather than reconstructed post-imputation evidence per D-18."
    - "Coverage construction remains cache-only, uses no network access, and does not expose restricted raw optional-source rows."
  artifacts:
    - path: "R/forecast/features.R"
      provides: "Evidence-returning strict latest-before lookups and canonical wide feature/evidence companions"
      exports: ["make_latest_team_evidence_lookup", "build_forecast_feature_table", "validate_forecast_feature_evidence"]
    - path: "R/forecast/goal_ability.R"
      provides: "Actual latest prior-result dates and presence flags for goal-ability differences"
      exports: ["compute_goal_ability_features"]
    - path: "_targets.R"
      provides: "Validated persistence of feature values and their pre-imputation evidence in the canonical benchmark input"
      contains: "hybrid_goal_training_features_file"
    - path: "data/processed/goal_training_features_hybrid.csv"
      provides: "Canonical match-keyed feature values plus producer-captured per-feature evidence companions"
      contains: "match_id"
    - path: "R/benchmark/baselines.R"
      provides: "Per-prediction feature evidence generated from registered runtime inputs and fit state"
      exports: ["benchmark_feature_coverage_id", "build_registered_feature_coverage", "run_registered_baseline_adapter"]
    - path: "R/benchmark/contracts.R"
      provides: "Exact feature-key, cutoff, imputation, license, and prediction-reference validation"
      exports: ["validate_feature_coverage", "validate_prediction_feature_coverage_links"]
    - path: "R/benchmark/runner.R"
      provides: "Pre-imputation history and fixture-feature preparation that preserves producer evidence companions"
      exports: ["benchmark_runner_prepare_history", "benchmark_runner_fixture_features"]
    - path: "tests/testthat/test_benchmark_baselines.R"
      provides: "Adapter-level exact feature-group, dangling-reference, cutoff, provenance, and license failures"
  key_links:
    - from: "R/forecast/features.R"
      to: "data/processed/goal_training_features_hybrid.csv"
      via: "hybrid_goal_training_features_file writes each feature value with same-row evidence companions before benchmark ingestion"
      pattern: "build_forecast_feature_table|validate_forecast_feature_evidence|hybrid_goal_training_features_file"
    - from: "data/processed/goal_training_features_hybrid.csv"
      to: "benchmark_runner_fixture_features"
      via: "match_id/date/home/away identity carries untouched per-feature evidence through fixture matching"
      pattern: "source_present|source_date|imputation_reason"
    - from: "R/benchmark/baselines.R"
      to: "data/benchmark/phase09/feature_contract.csv"
      via: "model-panel feature expansion and runtime source/imputation evidence"
      pattern: "build_registered_feature_coverage|feature_contract"
    - from: "run_registered_baseline_adapter"
      to: "validate_prediction_feature_coverage_links"
      via: "adapter output is rejected unless every prediction ID resolves to its exact registered feature group"
      pattern: "feature_coverage_id|validate_prediction_feature_coverage_links"
---

<objective>
Capture complete pre-imputation feature provenance and make every adapter return an exact validated prediction-to-coverage handoff.

Purpose: Close the upstream BENCH-03 evidence gap that made observed zero indistinguishable from missing-then-zero and left adapter prediction references without runtime feature groups. This repairs D-10, D-15, D-18, and D-20 evidence without changing the registered models, panels, G=40 support, or sealed holdout.
Output: Producer-captured evidence companions, adapter-emitted feature groups, strict adapter-return validation, and focused failure-propagating tests ready for 09-06 bundle wiring.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-04-SUMMARY.md

<interfaces>
Existing contracts to extend:

- `validate_feature_coverage(coverage, expected_keys)` already requires keys `model_id`, `boundary_id`, `fixture_id`, `feature_id` plus `source_date`, `evidence_cutoff_exclusive`, `cutoff_valid`, imputation, active-fit, coverage, and license fields.
- `run_registered_baseline_adapter(registration, history, fixtures, seed_registry, support_max, run_id, frozen_registry)` currently returns predictions, distributions, and manifests; its prediction rows already carry `feature_coverage_id` but return no evidence rows.
- `benchmark_runner_fixture_features()` carries fixture predictors and `feature_source_date`; `benchmark_runner_prepare_history()` must retain whether a value/source existed before any registered imputation is applied.
- `_targets.R::hybrid_goal_training_features_file` is the responsible canonical producer: it calls `compute_goal_ability_features()` and `build_forecast_feature_table()`, then writes `data/processed/goal_training_features_hybrid.csv`.
- `R/forecast/features.R::make_latest_team_lookup()` currently returns only a value and defaults Elo/form/squad lookups to 1500/0, discarding the selected row date and making observed zero indistinguishable from missing-then-zero.
- `R/forecast/goal_ability.R::compute_goal_ability_features()` currently emits neutral fallback differences and `lookup_date - 1`; it must retain whether both teams had prior result evidence and the actual latest prior result date.
- `benchmark_runner_prepare_history()` creates absent formula columns as zero and `benchmark_runner_fixture_features()` replaces non-finite numeric values with zero. Both must preserve producer evidence companions and must never reconstruct evidence from those post-imputation values.
- The adapter handoff for 09-06 is `list(predictions, distributions, manifests, feature_coverage)`, validated before return; 09-06 owns binding that handoff into the durable bundle and deriving `feature_coverage_valid`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture producer evidence before registered imputation</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>R/forecast/features.R</file>
    <file>R/forecast/goal_ability.R</file>
    <file>R/integration/rolling_form.R</file>
    <file>R/transfermarkt/squad_strength.R</file>
    <file>_targets.R</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/evaluation/worldcup_ledger.R</file>
    <file>R/benchmark/euro2024.R</file>
    <file>tests/testthat/test_transfermarkt_benchmark.R</file>
  </read_first>
  <files>R/forecast/features.R, R/forecast/goal_ability.R, _targets.R, data/processed/goal_training_features_hybrid.csv, tests/testthat/test_transfermarkt_benchmark.R</files>
  <behavior>
    - Every source-backed registered feature reaches the canonical CSV with same-row value-present, source-present, actual source-date, imputed, and imputation-reason companions.
    - A difference of zero from two observed equal inputs remains source-present and non-imputed; a zero introduced for an absent input remains source-absent and imputed with a reason.
    - Every source date is the actual latest prior evidence date and remains strictly before the deterministic cutoff; no cutoff-minus-one evidence is synthesized.
  </behavior>
  <action>Start with RED tests in `test_transfermarkt_benchmark.R` for the real producer path. Add `make_latest_team_evidence_lookup()` in `R/forecast/features.R` so each strict latest-before lookup returns the selected numeric value, `source_present`, the actual selected row date, `value_present`, `imputed`, and `imputation_reason`; retain `make_latest_team_lookup()` as the value-only compatibility wrapper. Make `build_forecast_feature_table()` retain `match_id` and write deterministic companions for every source-backed registered feature using the exact names `{feature_id}__value_present`, `{feature_id}__source_present`, `{feature_id}__source_date`, `{feature_id}__imputed`, and `{feature_id}__imputation_reason`. For home-away differences, `source_present` is true only when both selected source rows exist and `source_date` is their actual maximum; an observed 0 from two present equal values is non-imputed, while a 0 produced because either lookup was absent is source-absent and imputed. Modify `compute_goal_ability_features()` to emit the same facts for attack/defense differences using the actual maximum prior result date, not a synthesized cutoff-minus-one date, and distinguish observed equal abilities from neutral fallbacks. In `_targets.R::hybrid_goal_training_features_file`, call `validate_forecast_feature_evidence()` before writing `data/processed/goal_training_features_hybrid.csv`; require every source-backed feature companion plus declared mappings for adapter-derived `elo_difference_for_team` and `venue_advantage_for_team`, unique `match_id`, strict source dates, and the genuine-zero/missing-zero invariant. Preserve the `euro2024.R` shared feature/prediction-row shape while replacing its legacy pre-evidence zero-fill behavior, and preserve the `worldcup_ledger.R` build -> validate -> write -> hash ordering mapped in `09-PATTERNS.md` per D-03, D-10, D-14, and D-15.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_transfermarkt_benchmark.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove observed `home_value == away_value` yields value 0 with `source_present=TRUE`/`imputed=FALSE`, while absent source input yields value 0 with `source_present=FALSE`/`imputed=TRUE` and a reason.
    - The canonical CSV has unique `match_id` rows and complete companions for every source-backed registered feature.
    - No Phase 10/11 model or external data fetch is introduced.
  </acceptance_criteria>
  <done>The canonical producer and runner handoff preserve actual point-in-time evidence before any imputation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit and validate adapter-level feature coverage groups</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/runner.R</file>
    <file>tests/testthat/test_benchmark_baselines.R</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/processed/goal_training_features_hybrid.csv</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/evaluation/proper_scores.R</file>
  </read_first>
  <files>R/benchmark/contracts.R, R/benchmark/baselines.R, R/benchmark/runner.R, tests/testthat/test_benchmark_baselines.R</files>
  <behavior>
    - A prediction coverage ID is deterministic from run, model, track, boundary, and fixture and resolves to exactly the features registered for its panel.
    - Source-present rows require real strict source dates; source-absent rows preserve explicit missingness/imputation reasons and cannot masquerade as observations.
    - active_in_fit is derived from the boundary manifest, while inactive and zero-variance registered features remain audit-visible with machine-readable status.
    - Registered source, license, provenance hash, and feature-contract hash values cannot be replaced by adapter defaults.
    - Runner history and fixture preparation copies producer companions unchanged before any registered default and never infers evidence from an imputed value.
  </behavior>
  <action>Start with RED adapter tests in `test_benchmark_baselines.R` covering both tracks, every registered panel feature, a dangling prediction coverage ID, missing and extra feature rows, future source dates, absent-source date fabrication, observed-zero/missing-zero inversion, missing imputation reasons, inactive predictors, provenance/hash drift, and license drift. Update `benchmark_runner_prepare_history()` and `benchmark_runner_fixture_features()` only at the producer-to-adapter boundary so companions are copied unchanged before any registered default is applied and presence/date is never inferred from an already-imputed value or aggregate `feature_source_date`. Implement `benchmark_feature_coverage_id()` and `build_registered_feature_coverage()` in `baselines.R`; expand each prediction fixture over the exact `feature_contract.csv` rows for its panel and emit `schema_version`, `feature_coverage_id`, `run_id`, `model_id`, `panel_id`, `edition_id`, `track_id`, `boundary_id`, `fixture_id`, `feature_id`, `source_id`, `source_artifact_sha256`, `value_present`, `source_present`, `source_date`, `evidence_cutoff_exclusive`, `cutoff_valid`, `imputed`, `imputation_reason`, `active_in_fit`, `coverage_status`, `license_class`, and feature-contract row hash. Map `elo_difference_for_team` to the producer-captured `elo_diff` facts and derive `venue_advantage_for_team` from checked fixture/venue evidence without fabricating a source date. Extend `validate_feature_coverage()` and add `validate_prediction_feature_coverage_links()` with `proper_scores.R`-style fail-fast schema, unique-key, finite/range, exact registered-key, cutoff, zero/imputation, source/license/hash, and prediction-group checks. Return `feature_coverage` from every adapter result, assign prediction IDs with the same helper, and invoke both validators before adapter return per D-03, D-10, D-14, D-15, and D-18. Leave durable binding, parent wiring, run-manifest derivation, and read-back validation to the dependent 09-06 plan.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_baselines.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic frozen/updating predictions receive distinct deterministic coverage IDs and every ID resolves before adapter return.
    - Coverage contains every required schema and provenance column accepted by `validate_feature_coverage()`.
    - Missing, extra, dangling, future-dated, falsely source-present, unreasoned-imputation, provenance/hash, and license-drift rows fail with non-zero test exits.
    - Runner preparation preserves producer companions before applying registered defaults.
    - Adapter execution remains local-only and preserves G=40, frozen settings, common seeds, temporal cutoffs, and the WC2026 seal.
  </acceptance_criteria>
  <done>Every adapter prediction leaves 09-05 with a validated foreign key into complete producer-backed point-in-time evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/benchmark/contracts.R` | Extended `validate_feature_coverage()`; new `validate_prediction_feature_coverage_links()` |
| `R/forecast/features.R` | Evidence-returning latest-before lookup; value + per-feature evidence companions retained in the canonical feature table |
| `R/forecast/goal_ability.R` | Actual prior-result source presence/date for attack and defense ability differences |
| `R/benchmark/baselines.R` | `benchmark_feature_coverage_id()`, `build_registered_feature_coverage()`, and adapter `feature_coverage` output |
| `R/benchmark/runner.R` | Pre-imputation history/fixture preparation that preserves producer evidence companions |
| `_targets.R` | `hybrid_goal_training_features_file` evidence validation and canonical persistence |
| `data/processed/goal_training_features_hybrid.csv` | Canonical values plus pre-imputation evidence companions keyed by `match_id` |
| `tests/testthat/test_transfermarkt_benchmark.R` | Genuine-zero versus missing-zero producer regressions across strict latest-before lookups |
| `tests/testthat/test_benchmark_baselines.R` | Adapter feature-key, cutoff, imputation, provenance, license, and prediction-link tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime feature inputs → evidence rows | Missing or imputed values can be misrepresented as observed pre-cutoff evidence. |
| Prediction IDs → feature coverage artifact | Dangling or ambiguous foreign keys can make provenance unverifiable. |
| Restricted local producer evidence → adapter output | Coverage may disclose only registered evidence fields and license metadata, never raw optional-source snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-G05-01 | Tampering | feature evidence construction | high | mitigate | Exact registered key expansion, strict source-date checks, explicit imputation reasons, and construction-time validation. |
| T-09-G05-02 | Repudiation | prediction-to-coverage linkage | high | mitigate | Deterministic group IDs, set-equal foreign-key validation, and adapter-return checks before the 09-06 handoff. |
| T-09-G05-03 | Information Disclosure | optional-source coverage | medium | mitigate | Emit contract IDs, aggregate values/status, license, and hashes only; no raw restricted rows enter outputs. |
</threat_model>

<verification>
Run both focused suites with failure and warning propagation. Confirm the source scan still finds no HTTP/download/refresh call in benchmark modules. Durable bundle binding and read-back validation proceed in dependent Plan 09-06; regeneration remains in 09-08 after panel and promotion wiring are green.
</verification>

<success_criteria>
- The canonical feature producer preserves actual pre-imputation evidence.
- Every adapter prediction coverage reference resolves to exact registered feature rows before return.
- Genuine observed zeros and missing-then-zero defaults remain distinguishable.
- Plan 09-06 receives a validated feature-coverage handoff without reconstructing evidence.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-05-SUMMARY.md` when done.
</output>
