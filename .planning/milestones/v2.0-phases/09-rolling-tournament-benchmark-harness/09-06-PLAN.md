---
phase: 09-rolling-tournament-benchmark-harness
plan: "06"
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - R/benchmark/contracts.R
  - R/evaluation/benchmark_scores.R
  - R/benchmark/runner.R
  - _targets.R
  - tests/testthat/test_benchmark_contracts.R
  - tests/testthat/test_benchmark_scoring.R
  - tests/testthat/test_benchmark_pipeline.R
autonomous: true
gap_closure: true
requirements: [BENCH-03, BENCH-04, BENCH-05]

must_haves:
  truths:
    - "All 6,300 audit-visible prediction feature_coverage_id values resolve in the durable bundle to the exact adapter-emitted registered feature groups produced by 09-05."
    - "Bundle construction and read-back independently validate feature evidence and links, while run_manifest.feature_coverage_valid is derived from those checks per D-18 and D-20."
    - "The frozen open_core denominator remains exactly 630 fixtures and feature_rich remains exactly the 609 predeclared eligible fixtures per D-05 and D-13."
    - "Panel-specific prediction validation rejects missing required fixtures, duplicate fixtures, and extra out-of-panel fixtures per D-15."
    - "Scoring, equal-tournament and pooled summaries, fixed-bin calibration, paired comparisons, uncertainty, fold breadth, maximum regression, and promotion inputs consume the exact fixture IDs declared for that panel per D-04 and D-16 through D-19."
    - "production_hybrid_nb may remain audit-visible in the full prediction artifact, but its 21 rich-ineligible fixtures contribute to no rich-panel score, calibration row, comparison, diagnostic, or promotion input."
    - "No panel projection changes G=40, model settings, common seeds, cutoff boundaries, or the WC2026 seal."
  artifacts:
    - path: "R/benchmark/contracts.R"
      provides: "Strict feature-evidence/link and exact-set panel validation"
      exports: ["validate_feature_coverage", "validate_prediction_feature_coverage_links", "benchmark_panel_fixture_ids", "select_benchmark_panel_predictions", "validate_panel_prediction_coverage"]
    - path: "R/evaluation/benchmark_scores.R"
      provides: "Panel-aware fixture scoring, calibration, aggregation, and exact pairing"
      exports: ["score_benchmark_fixtures", "fixed_benchmark_calibration", "make_paired_fold_comparisons"]
    - path: "R/benchmark/runner.R"
      provides: "Adapter feature-evidence binding plus frozen panel routing and independent bundle reconciliation"
      exports: ["benchmark_runner_feature_coverage", "benchmark_runner_score_outputs", "benchmark_runner_comparisons", "validate_rolling_benchmark_bundle"]
    - path: "_targets.R"
      provides: "Explicit canonical feature-input dependency and checked parent wiring for benchmark predictions"
      contains: "hybrid_goal_training_features_file"
    - path: "tests/testthat/test_benchmark_pipeline.R"
      provides: "Exact 630/609 end-to-end denominator regressions"
  key_links:
    - from: "run_registered_baseline_adapter"
      to: "feature_coverage.csv"
      via: "runner binds adapter-emitted rows, validates exact prediction links, and keeps post-output panel coverage separately named"
      pattern: "benchmark_runner_feature_coverage|validate_prediction_feature_coverage_links"
    - from: "hybrid_goal_training_features_file"
      to: "benchmark_phase09_predictions"
      via: "explicit targets dependency and goal_training_features_hybrid input-parent hash"
      pattern: "goal_training_features_hybrid|benchmark_phase09_predictions"
    - from: "data/benchmark/phase09/panel_fixtures.csv"
      to: "R/evaluation/benchmark_scores.R"
      via: "exact eligible fixture IDs supplied before scoring/calibration"
      pattern: "benchmark_panel_fixture_ids|expected_fixture_ids"
    - from: "R/benchmark/runner.R"
      to: "R/evaluation/benchmark_scores.R"
      via: "model registration panel_id selects one frozen panel projection"
      pattern: "select_benchmark_panel_predictions|score_benchmark_fixtures"
    - from: "paired_comparisons.csv"
      to: "promotion_decisions.csv"
      via: "panel-exact fold counts and uncertainty"
      pattern: "paired_fixture_count|panel_id"
---

<objective>
Bind the 09-05 feature-evidence handoff into the durable bundle and enforce the frozen 630/609 panel contract through every evaluation consumer.

Purpose: Close the downstream BENCH-03 evidence/link blocker and the D-05/D-13 denominator blocker where production_hybrid_nb currently contributes 21 ineligible fixtures to rich scores and comparisons. Validated adapter evidence must survive publication, and the panel registry must govern the estimand rather than output availability.
Output: Feature-level bundle construction/read-back validation, derived manifest validity, strict panel selectors, panel-aware scorers and diagnostics, checked targets parent wiring, and exact-denominator tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-RESEARCH.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-05-SUMMARY.md

<interfaces>
Current denominator-sensitive interfaces:

- Plan 09-05 makes every adapter return validated `feature_coverage` beside predictions, distributions, and manifests; `validate_feature_coverage()` and `validate_prediction_feature_coverage_links()` are available for construction and read-back checks.
- `benchmark_default_execution_engine()` still replaces adapter evidence with `benchmark_output_coverage()` aggregates and assigns `feature_coverage_valid = TRUE`; `_targets.R::benchmark_phase09_predictions` reads the canonical CSV by path without an explicit target dependency or input-parent entry.
- `validate_panel_prediction_coverage(predictions, panel_fixtures, model_id)` computes required eligible IDs and rejects missing rows, but currently permits extras.
- `score_benchmark_fixtures(predictions, fixtures, distributions)` currently requires every model to equal the global score-eligible fixture set.
- `fixed_benchmark_calibration(predictions, fixtures)` has the same global-set assumption.
- `make_paired_fold_comparisons(..., expected_fixture_ids, ...)` already rejects any candidate/incumbent mismatch and should receive panel IDs rather than the global registry.
- `benchmark_runner_score_outputs()` and `benchmark_runner_comparisons()` currently pass all 630 score-eligible fixture IDs to every model.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make panel selection, validation, scoring, and calibration exact-set operations</name>
  <read_first>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-CONTEXT.md</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md</file>
    <file>data/benchmark/phase09/panels.csv</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/evaluation/worldcup_ledger.R</file>
    <file>R/evaluation/proper_scores.R</file>
    <file>R/evaluation/worldcup_retrospective.R</file>
    <file>tests/testthat/test_benchmark_contracts.R</file>
    <file>tests/testthat/test_benchmark_scoring.R</file>
  </read_first>
  <files>R/benchmark/contracts.R, R/evaluation/benchmark_scores.R, tests/testthat/test_benchmark_contracts.R, tests/testthat/test_benchmark_scoring.R</files>
  <behavior>
    - open_core selects exactly 630 eligible fixture IDs; feature_rich selects exactly 609, across all 12 editions.
    - Strict panel validation fails on one missing ID, one duplicate, one extra ineligible ID, a wrong panel ID, or a non-ok required row.
    - Scoring and calibration accept an explicit exact fixture set and reject both missing and extra rows before computing any value.
    - Aggregation still requires all 12 tournaments and keeps equal-tournament headline weighting; paired uncertainty still uses 12 fold deltas, seed 920001, 10,000 replicates, and type-8 quantiles.
  </behavior>
  <action>Add RED contract tests for missing, duplicate, wrong-panel, and extra out-of-panel rows, including a 630-row full prediction audit projected to a 609-row rich comparison. Implement `benchmark_panel_fixture_ids(panel_fixtures, panel_id)` to return the sorted eligible/output-required fixture IDs and fail on duplicate declarations. Implement `select_benchmark_panel_predictions(predictions, panel_fixtures, model_id, panel_id)` to create the explicit evaluation projection, preserve an auditable count of excluded rows, and call strict `validate_panel_prediction_coverage()` on the projection. Strengthen that validator to require set equality, uniqueness, one model/panel identity, and ok status. Preserve the `worldcup_ledger.R` immutable-registry pattern from `09-PATTERNS.md`: begin with the complete sorted declared inventory, left-match outputs, and fail rather than silently reducing scope. Preserve `proper_scores.R` fail-fast exact probability/distribution contract behavior.

Add `expected_fixture_ids` to `score_benchmark_fixtures()` and `fixed_benchmark_calibration()`; subset the fixture registry to that exact set, reject any prediction outside it, and retain panel identity on every score/calibration output. Preserve `worldcup_retrospective.R`'s long metric-row and identical-fixture paired-merge patterns: score fixtures once, aggregate each tournament second, average all 12 tournaments equally for the headline, and derive paired deltas only after exact fixture-ID matching. Keep `make_paired_fold_comparisons()` exact-set behavior and add rich-panel tests proving its 12 fold counts sum to 609 while open-core sums to 630 per track. Preserve D-04 equal-tournament aggregation and all D-16/D-17 bootstrap/breadth/regression semantics.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_scoring.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Exact-set tests cover missing, duplicate, extra, wrong-panel, and incomplete output rows.
    - Open and rich scoring/calibration tests prove 630 and 609 unique fixtures respectively.
    - Rich paired fold counts sum to 609 and retain all 12 editions; open paired counts sum to 630.
    - Bootstrap seed, repetitions, quantile type, and G=40 score semantics are unchanged.
  </acceptance_criteria>
  <done>Every evaluation primitive requires an explicit frozen panel fixture set and fails on denominator drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route feature evidence and frozen panels through the bundle</name>
  <read_first>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/benchmark/runner.R</file>
    <file>_targets.R</file>
    <file>.planning/phases/09-rolling-tournament-benchmark-harness/09-PATTERNS.md</file>
    <file>R/evaluation/worldcup_ledger.R</file>
    <file>R/evaluation/proper_scores.R</file>
    <file>R/evaluation/worldcup_retrospective.R</file>
    <file>tests/testthat/test_benchmark_contracts.R</file>
    <file>tests/testthat/test_benchmark_scoring.R</file>
    <file>tests/testthat/test_benchmark_pipeline.R</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase09/panels.csv</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/processed/goal_training_features_hybrid.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/predictions/fixture_predictions.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/feature_coverage.csv</file>
  </read_first>
  <files>R/benchmark/contracts.R, R/benchmark/runner.R, _targets.R, tests/testthat/test_benchmark_contracts.R, tests/testthat/test_benchmark_pipeline.R</files>
  <behavior>
    - Runner construction binds adapter-emitted feature rows, validates exact registered keys and every prediction reference, and serializes no aggregate-only substitute.
    - `feature_coverage_valid` is derived from successful construction/read-back validation and fails closed on any malformed row, dangling/orphan group, or canonical-input parent drift.
    - Runner scoring selects each model's registered panel before score/distribution lookup.
    - Runner calibration, summaries, paired diagnostics, breadth, regression, and promotion-source tables all use the same selected fixture IDs.
    - production_hybrid_nb has no score or comparison row for any of the 21 rich-ineligible fixture IDs.
    - Bundle validation independently reconstructs panel IDs and rejects a rich score/comparison/count contaminated by an ineligible fixture.
  </behavior>
  <action>First consume the exact 09-05 adapter handoff: extend bundle-level contract tests for aggregate-only coverage, dangling prediction IDs, orphan groups, missing/extra registered feature keys, observed-zero/missing-zero inversion, fabricated source dates, provenance/license/hash drift, and canonical-input parent drift. Replace `benchmark_runner_feature_coverage()` aggregate construction with canonical binding of adapter-emitted feature rows and immediate `validate_feature_coverage()` plus `validate_prediction_feature_coverage_links()` calls against expected keys rebuilt from predictions, model registrations, and `feature_contract.csv`. Keep post-prediction panel output coverage as a separately named object for manifests and promotion inputs. In `_targets.R::benchmark_phase09_predictions`, make `hybrid_goal_training_features_file` an explicit dependency, assert producer companions and declared adapter-derived mappings before engine invocation, and pass its canonical content hash into the parent graph. Extend `benchmark_runner_additional_input_specs()` so `goal_training_features_hybrid` is a checked local parent. Extend construction and read-back validation to reject aggregate-only or reconstructed evidence across all 6,300 prediction references, derive `feature_coverage_valid` from validator success, and remove the unconditional success assignment per D-10, D-15, D-18, and D-20.

Then change `benchmark_runner_score_outputs()` to accept `panel_fixtures` and `model_registry`, derive each model's exact panel IDs, project predictions before selecting distributions, and pass the same expected IDs to scoring and calibration. Change `benchmark_runner_comparisons()` to accept `panel_fixtures`, derive expected IDs per candidate panel, and persist exact panel denominators on fold/headline/leave-one-out rows. Keep all 6,300 prediction rows audit-visible and linked to feature evidence, but exclude the 21 rich-ineligible rows before any score or calibration computation. Follow `worldcup_ledger.R`'s build -> validate -> write ordering and `proper_scores.R`'s fail-fast rejection; do not repair or drop evidence or denominator mismatches during serialization. Preserve `worldcup_retrospective.R`'s one-score-per-fixture long rows, tournament-first summaries, and exact paired merge as mapped in `09-PATTERNS.md`.

Extend `benchmark_runner_validate_bundle_data()` to independently reconstruct exact panels and verify fixture-score IDs, summary `n_fixtures`/coverage denominators, calibration counts, comparison fold sums, and paired counts: 630 for open_core and 609 for feature_rich on each track. Add synthetic bundle corruption tests that insert one rich-ineligible score/comparison and that alter a denominator; each must fail. Ensure output coverage and later promotion inputs use the exact panel projection rather than all available predictions.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_pipeline.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic bundle construction and read-back contain exact feature-level evidence for every prediction ID and reject aggregate-only, dangling/orphan, malformed, or parent-drift cases.
    - Run-manifest feature validity is derived from validator results and becomes false or fails validation on tampering.
    - Synthetic runner output proves open_core=630 and feature_rich=609 in scores, calibration, comparisons, and diagnostics.
    - The 21 rich-ineligible fixture IDs appear in no production_hybrid_nb evaluation artifact.
    - Bundle validation catches inserted ineligible rows and changed denominators.
    - The full prediction audit and its feature coverage links remain complete.
  </acceptance_criteria>
  <done>The durable bundle preserves exact feature evidence and the frozen panel registry governs every model-comparison denominator end to end.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/benchmark/contracts.R` | Bundle-level feature/link validators plus `benchmark_panel_fixture_ids()`, `select_benchmark_panel_predictions()`, and strict `validate_panel_prediction_coverage()` |
| `R/evaluation/benchmark_scores.R` | Panel-exact `score_benchmark_fixtures()` and `fixed_benchmark_calibration()`; retained exact `make_paired_fold_comparisons()` |
| `R/benchmark/runner.R` | Adapter-evidence binding, derived `feature_coverage_valid`, panel-routed scoring/comparisons, and independent bundle reconciliation |
| `_targets.R` | Explicit canonical feature dependency, evidence preflight, and checked input-parent wiring |
| `tests/testthat/test_benchmark_contracts.R` | Feature-link corruption plus missing/duplicate/extra/wrong-panel prediction tests |
| `tests/testthat/test_benchmark_scoring.R` | Exact 630/609 score, calibration, fold, bootstrap, breadth, and regression tests |
| `tests/testthat/test_benchmark_pipeline.R` | Rich-ineligible contamination and denominator-tamper bundle tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen panel registry → runtime outputs | Available predictions must not redefine a declared comparison denominator. |
| Adapter evidence → durable feature coverage | Validated producer evidence must not be replaced by aggregates or reconstructed from imputed values. |
| Full prediction audit → scoring projection | Audit-visible out-of-panel rows must not leak into metrics or promotion inputs. |
| Score tables → paired uncertainty | Candidate/incumbent rows must be paired on one exact panel fixture set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-G06-01 | Tampering | panel prediction validation | high | mitigate | Set-equality, uniqueness, identity, and status checks on every panel projection. |
| T-09-G06-02 | Tampering | scoring/calibration routing | high | mitigate | One frozen fixture-ID vector is passed to scoring, calibration, summaries, and comparisons. |
| T-09-G06-03 | Repudiation | persisted denominators | high | mitigate | Persist exact counts and independently reconcile 630/609 and fold totals during bundle validation. |
| T-09-G06-04 | Tampering | bundle feature evidence and parent graph | high | mitigate | Exact prediction/group validation, canonical feature-input parent, derived manifest validity, and read-back corruption tests. |
</threat_model>

<verification>
Run the contract, scoring, and pipeline suites with failure propagation. Confirm every adapter feature group survives bundle construction/read-back, no rich-ineligible fixture reaches production_hybrid_nb evaluation outputs, and all 12 editions remain represented.
</verification>

<success_criteria>
- Every one of the 6,300 prediction coverage IDs resolves to exact registered feature evidence in the bundle.
- Feature coverage validity and the canonical feature-input parent are derived and independently rechecked.
- Open-core evaluation remains exactly 630 fixtures.
- Feature-rich evaluation remains exactly 609 predeclared eligible fixtures.
- Missing and extra panel rows fail validation.
- Every evaluation and later promotion input uses the exact panel set.
</success_criteria>

<output>
Create `.planning/phases/09-rolling-tournament-benchmark-harness/09-06-SUMMARY.md` when done.
</output>
