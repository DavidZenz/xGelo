---
context: phase
phase: 10-statistical-goal-model-challengers
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-07-23T04:55:57.956Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> This constraint was discovered through an actual five-hour benchmark failure.
> Acknowledge it explicitly before proceeding.

- [ ] CONSTRAINT: Preflight canonical fixture identity and shared seeds before expensive fitting. Run all seven candidates on a bounded one-edition slice through the complete adapter contract before launching another 168-job canonical order.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Deferred identity validation | `benchmark_runner_fixture_features()` copied history `fixture_id` values over canonical registry IDs. All 168 jobs fitted before the seed join rejected them, and parallel errors collapsed into `subscript out of bounds`. | blocking | Keep identity/track/boundary/cutoff columns out of predictor joins, run `.phase10_runner_validate_fixture_seeds()` before fitting, expose fork errors directly, and pass a bounded all-candidate adapter preflight. |

<current_state>
Phase 10 Plan 10-08 Task 1 is paused. Plans 10-05 through 10-07 are committed and verified. Plan 10-08 integration changes are uncommitted. Both expensive tuning families completed and are cached in `/tmp`, but the first normal-order canonical pass failed after about 5.6 hours and wrote no stage. The reversed pass, reconciliation, publication, Task 2 smoke, final suite, coverage, and verifier have not started.
</current_state>

<completed_work>

Completed this session:
- Plans 10-05, 10-06, and 10-07 executed, tested, summarized, and committed.
- Plan 10-08 production integration wiring implemented across runner, selection, goal-model, targets, and tests.
- Penalized tuning cache completed: `/tmp/phase10_penalized_all.rds`.
- Dynamic tuning cache completed: `/tmp/phase10_dynamic_all.rds`.
- Failed normal run diagnosed: history `fixture_id` overwrote Phase 9 canonical fixture IDs, so no fixtures matched shared seeds.
- `benchmark_runner_fixture_features()` now protects canonical identity, track, boundary, and cutoff columns.
- `.phase10_default_execution_engine()` now validates fixture-to-seed coverage before fitting and reports child job errors directly.
- Focused tests pass: `test_benchmark_baselines.R` and `test_statistical_bundle.R`.
- Full one-candidate adapter check passes for `poisson_team_ridge`, frozen EURO 2004: 31 predictions, 52,111 distribution rows, one manifest, 465 coverage rows.
</completed_work>

<remaining_work>

- Run all seven candidates on one edition, preferably both tracks, through `run_registered_challenger_adapter()` and inspect every returned contract.
- If the bounded preflight passes, rerun the normal fresh process using `/tmp/xgelo_phase10_canonical_run.R` and the existing tuning caches.
- Run the reversed fresh process, reconcile canonical hashes, and atomically publish all 12 artifacts.
- Run Plan 10-08 Task 2 focused smoke under 30 seconds.
- Run deep bundle validation, complete testthat suite, Phase 10 coverage gate, code-review hook if enabled, and `gsd-verifier`.
- Create `10-08-SUMMARY.md`, update phase state/roadmap/requirements, and commit completed work.
</remaining_work>

<decisions_made>

- Do not rerun the full benchmark in this session; observed runtime is about 5.5-6 hours per order and about 12-13 hours through final verification.
- Reuse completed tuning caches when available; their work completed before canonical candidate execution.
- Treat canonical fixture identity as immutable registry data, never a history-derived predictor.
- Keep accepted Phase 9, dashboard, and WC2026 evaluation paths untouched.
</decisions_made>

<blockers>
- No durable normal stage exists. The implementation defect is fixed and focused checks pass, but the mandatory all-candidate bounded preflight and expensive rerun remain.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` - machine-readable paths, status, and next action.
2. `.planning/phases/10-statistical-goal-model-challengers/10-08-PLAN.md` - locked execution and acceptance contract.
3. `R/benchmark/runner.R` - protected fixture feature join.
4. `R/benchmark/challenger_runner.R` - pre-fit seed guard, job execution, staging, and validation.
5. `.planning/phases/10-statistical-goal-model-challengers/10-VALIDATION.md` - final suite and coverage gate.

## Critical Anti-Patterns (do NOT repeat these)

- Launching all 168 jobs after only synthetic tests -> first run the real adapter contract for all seven candidates on a bounded historical slice.
- Trusting `mclapply()` warnings -> inspect and surface each `try-error` before indexing result fields.
- Letting feature history replace registry identities -> preserve canonical fixture, edition, team, track, boundary, sequence, venue, and cutoff columns.

## Infrastructure State

- Background processes: none.
- Git HEAD: `1f8f57b` (`docs(state): start phase 10 final benchmark`).
- Normal stage: `outputs/benchmarks/rolling_tournaments/.phase10-normal-stage-1008` is absent.
- Accepted Phase 10 output: absent and unchanged.
- Tuning caches: `/tmp/phase10_penalized_all.rds`, `/tmp/phase10_dynamic_all.rds`.
- Drivers: `/tmp/xgelo_phase10_canonical_run.R`, `/tmp/xgelo_phase10_reconcile_publish.R`.
- Free disk after failure: about 13.3 GB; required preflight floor is 4,170,989,111 bytes.
- User-owned changes to preserve: `.planning/config.json`, `.planning/research/.cache/`, `outputs/design_audit/`, and `outputs/reports/xgelo_elo_decision/`.

<context>
The expensive run itself was healthy and used two workers as required. It completed model fitting but failed at result assembly because fixture identity had been overwritten upstream. The repaired single-candidate path now completes end to end. The next session should prove the repair across every candidate on a small real slice before paying the full canonical runtime again.
</context>

<next_action>
Start with: acknowledge the blocking constraint, verify the two `/tmp` tuning caches, and run all seven candidates on one real edition through their complete adapters. Only after that passes should the normal canonical driver be restarted.
</next_action>
