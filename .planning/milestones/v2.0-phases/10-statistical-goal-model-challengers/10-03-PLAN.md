---
phase: 10-statistical-goal-model-challengers
plan: "03"
type: execute
wave: 2
depends_on: [10-01, 10-09]
files_modified:
  - R/forecast/penalized_poisson.R
  - tests/testthat/test_statistical_penalized_poisson_design.R
  - tests/testthat/test_statistical_penalized_poisson_tuning.R
autonomous: true
requirements: [STAT-01]

must_haves:
  truths:
    - "The minimal and Elo-augmented penalized-Poisson models are nested and retain ridge-shrunk attack/defence identity for sparse teams."
    - "Each outer tournament selects penalties only from completed earlier tournaments, then reuses them unchanged for frozen and updating tracks."
    - "Unseen teams receive the global log-scale effect without fixture loss, and every fallback/shrinkage fact is retained in model and feature evidence."
    - "The augmented model consumes Elo only from the adapter-supplied canonical value/provenance columns and rejects raw rating histories, direct rating-file access, and nearest-date reconstruction."
  artifacts:
    - path: "R/forecast/penalized_poisson.R"
      provides: "Sparse design, nested fit, prior-only tuning, prediction, and evidence"
      exports: ["build_penalized_poisson_design", "fit_penalized_team_means", "fit_penalized_elo_offset", "select_penalized_poisson_hyperparameters", "predict_penalized_poisson_means", "penalized_poisson_manifest"]
  key_links:
    - from: "select_penalized_poisson_hyperparameters"
      to: "data/benchmark/phase10/tuning_editions.csv"
      via: "strict outer-to-prior-inner edition registry"
      pattern: "outer_edition_id|inner_edition_id"
    - from: "predict_penalized_poisson_means"
      to: "data/benchmark/phase10/feature_contract.csv"
      via: "cold-start, prior-count, and shrinkage evidence"
      pattern: "cold_start|shrinkage_weight|prior_count"
    - from: "fit_penalized_elo_offset"
      to: "data/processed/goal_training_features_hybrid.csv"
      via: "adapter-supplied elo_diff and its five canonical provenance companions"
      pattern: "elo_diff(__value_present|__source_present|__source_date|__imputed|__imputation_reason)?"
---

<objective>
Implement and test the nested team-specific penalized-Poisson pair.

Purpose: Satisfy STAT-01 while making team effects, Elo increment, chronology, and cold starts separately attributable.
Output: One forecast-layer module and a GREEN focused test suite.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-09-SUMMARY.md

<interfaces>
- `benchmark_observation_weights()` supplies the frozen Phase 9 recency/importance likelihood weights.
- `benchmark_row_sha256()` and Phase 10 tuning registries supply immutable settings and eligible-ID hashes.
- The downstream adapter expects per-fixture `mu_home`, `mu_away`, fit/evidence metadata, and no model-specific score or market calculations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the sparse identified design and global cold-start path</name>
  <read_first>
    <file>R/forecast/penalized_poisson.R</file>
    <file>tests/testthat/test_statistical_penalized_poisson_design.R</file>
    <file>tests/testthat/helper_statistical_challengers.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/weights.R</file>
    <file>R/forecast/poisson.R</file>
    <file>data/benchmark/phase10/feature_contract.csv</file>
    <file>data/benchmark/phase10/tuning_grid.csv</file>
  </read_first>
  <files>R/forecast/penalized_poisson.R, tests/testthat/test_statistical_penalized_poisson_design.R</files>
  <behavior>
    - Two goal-perspective rows per match yield fixed full attack and defence indicator blocks, unpenalized intercept/venue, and signed point-in-time Elo.
    - After fitting, both team coefficient blocks are sum-to-zero centered and the intercept is shifted so every fitted linear predictor is invariant.
    - Team columns remain ridge-penalized and assessment identities absent from training resolve to zero centered attack/defence effects plus the global intercept.
    - Neutral team-order reversal swaps means without changing their unordered pair.
  </behavior>
  <action>Start from the Wave 0 tests. Implement `build_penalized_poisson_design()` with `Matrix::sparseMatrix`, stable FIFA/team IDs, two rows per match, and fixed full indicator columns for every registered attack and defence level; do not drop a reference column, infer levels from prediction rows, or join on display names. Define the goal-row predictor as `eta = intercept + attack[scoring_team] + defence[opponent] + venue_term`. Implement `fit_penalized_team_means()` with `glmnet::glmnet(family = "poisson", alpha = 0)`, unpenalized intercept/venue, ridge-only attack/defence columns, Phase 9 observation weights, explicit convergence/finite checks, and no coefficient-dropping fallback per D-02. After fitting, compute `attack_mean` and `defence_mean` across the complete registered blocks, store `attack_centered = attack_raw - attack_mean` and `defence_centered = defence_raw - defence_mean`, and store `intercept_centered = intercept_raw + attack_mean + defence_mean`; use only these centered values for prediction so raw and centered linear predictors are invariant and each fitted team block sums to zero. Implement `predict_penalized_poisson_means()` with exact behavior: a known level uses its centered coefficient; an unseen attack or defence level contributes zero for that block; two unseen teams therefore use `intercept_centered + venue_term`, while a fixture with one unseen level retains the known counterpart's centered effect. Emit complete finite positive means plus per-team prior counts, shrinkage weights, and cold-start status per D-04.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_penalized_poisson_design.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Sparse matrix dimensions and penalty factors are asserted exactly.
    - Tests assert both centered coefficient-block means are zero, raw and centered linear predictors match at strict tolerance, and results are invariant to registered-level ordering.
    - No sparse/unseen identity is removed or causes NA/fixture loss, and this gate does not invoke tuning, Elo-offset, or manifest APIs owned by Task 2.
    - One- and two-unseen-team tests require zero centered effects/global-log-rate behavior and evidence fields rather than fabricated team coefficients.
  </acceptance_criteria>
  <done>The minimal team/venue mean model is identified, ridge-stabilized, and complete for unseen teams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nested Elo selection and prior-tournament hyperparameter tuning</name>
  <read_first>
    <file>R/forecast/penalized_poisson.R</file>
    <file>tests/testthat/test_statistical_penalized_poisson_tuning.R</file>
    <file>R/benchmark/runner.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/benchmark/cutoffs.R</file>
    <file>data/benchmark/phase10/tuning_editions.csv</file>
    <file>data/benchmark/phase10/tuning_grid.csv</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/benchmark/phase09/boundaries.csv</file>
    <file>data/processed/goal_training_features_hybrid.csv</file>
    <file>data/processed/elo_matches.csv</file>
  </read_first>
  <files>R/forecast/penalized_poisson.R, tests/testthat/test_statistical_penalized_poisson_tuning.R</files>
  <behavior>
    - The augmented fit uses the minimal log means as fixed offsets and can select the Elo increment to exactly zero without changing team effects.
    - The Elo increment accepts only adapter-supplied `elo_diff`, `elo_diff__value_present`, `elo_diff__source_present`, `elo_diff__source_date`, `elo_diff__imputed`, and `elo_diff__imputation_reason` as Elo value/provenance inputs.
    - Raw rating histories, a direct `data/processed/elo_ratings.csv` read, and nearest-date Elo reconstruction are rejected before fitting.
    - Team ridge lambda is selected first and Elo lasso lambda second from prior completed inner tournaments using equal-tournament updating RPS.
    - Largest-penalty ties are deterministic, and one selected pair is frozen for both outer tracks.
    - Poisoning outer outcomes cannot change selected penalties, means, or manifests.
  </behavior>
  <action>Implement `fit_penalized_elo_offset()` with the minimal model's `log(mu)` offset, no intercept, and `glmnet::glmnet(family = "poisson", alpha = 1)` over only the signed adapter-supplied `elo_diff`, so a zero selected coefficient exactly recovers the minimal parent per D-01/D-02. Ground its Elo contract in the live canonical table and existing runner/evidence code: require exactly `elo_diff`, `elo_diff__value_present`, `elo_diff__source_present`, `elo_diff__source_date`, `elo_diff__imputed`, and `elo_diff__imputation_reason` for Elo value/provenance, validate value/source/imputation consistency and `source_date &lt; evidence_cutoff_exclusive`, and fail before `glmnet` when any companion is absent or invalid. Do not accept raw-rating rows, home/away rating columns, rating-history/path/lookup arguments, direct access to `data/processed/elo_ratings.csv`, or any nearest-date reconstruction. Implement `select_penalized_poisson_hyperparameters()` to validate the hashed outer/inner registry and use only inner editions ending before the outer opener. Tournament labels should arrive from the adapter-prepared frame; if `data/processed/elo_matches.csv` is required solely to supply them, read only `match_id` and `tournament`, assert unique nonmissing `match_id` on both sides plus complete one-to-one match coverage, join only those two columns, and expose no score, team, date, or rating-history field to the model. Score each grid point through the shared G=40 distribution/market/scorer path, average each inner tournament before the objective, select team ridge then Elo lasso, and break ties toward larger penalties. Freeze one settings/hash row per outer fold and reuse it for frozen/updating tracks per D-03. Implement `penalized_poisson_manifest()` with grid/protocol/eligible-ID hashes, selected penalties, package version, convergence, active/dropped Elo status, cold-start counts, and strictly prior fit/source dates. In the task-scoped tuning test, add a static parsed-source scan proving the penalized module has no raw-rating-history read, direct rating-file access, or nearest-date lookup and that any tournament-map read is column restricted; add dynamic assertions that missing/invalid canonical companions, injected raw rating columns or lookup/path arguments, duplicate or unmatched tournament-map keys, and stale source dates are rejected before fitting, with a spy proving no history lookup callback runs.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_penalized_poisson_tuning.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_baselines.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Minimal and augmented models are demonstrably nested.
    - The Task 2 tuning tests statically reject raw-history/direct-rating-file/nearest-date source paths and dynamically reject every noncanonical Elo/provenance or non-one-to-one tournament-map input before model fitting.
    - Outer labels never enter tuning, and selected settings are identical across the two tracks.
    - Manifests name active/dropped Elo state and all chronology/settings parents.
  </acceptance_criteria>
  <done>Both D-01 variants produce deterministic, prior-tuned mean predictions ready for the common adapter.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `R/forecast/penalized_poisson.R` | `build_penalized_poisson_design()`, `fit_penalized_team_means()`, `fit_penalized_elo_offset()`, `select_penalized_poisson_hyperparameters()`, `predict_penalized_poisson_means()`, `penalized_poisson_manifest()` |
| `tests/testthat/test_statistical_penalized_poisson_design.R` | GREEN Task 1 sparse-design, centering, cold-start, symmetry, and completeness tests |
| `tests/testthat/test_statistical_penalized_poisson_tuning.R` | GREEN Task 2 nesting, canonical-Elo boundary, tuning, poisoning, and baseline-pairing tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tournament registry → tuner | Outer labels or misdated inner editions can leak into penalty selection. |
| Sparse design → model fit | Unknown levels or penalty-factor mistakes can erase teams or change attribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Information Disclosure | hyperparameter tuner | high | mitigate | Strict prior-edition registry, eligible-ID hashes, outer-label poisoning tests, and shared settings across tracks. |
| T-10-09 | Tampering | sparse team design | high | mitigate | Stable IDs, exact column/penalty tests, global fallback, and complete fixture assertions. |
| T-10-10 | Repudiation | fitted model | medium | mitigate | Package/grid/settings/eligible-ID hashes, convergence, active terms, and cold starts in manifests. |
</threat_model>

<verification>
Run the focused penalized-Poisson suite plus the existing Phase 9 baseline regression; do not execute historical outer folds.
</verification>

<success_criteria>
- D-01 through D-04 are implemented and tested.
- Both registered means are finite and complete for every synthetic fixture.
- Tuning is chronology-safe, deterministic, and track-shared.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-03-SUMMARY.md` when done.
</output>
