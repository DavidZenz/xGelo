---
phase: 10-statistical-goal-model-challengers
plan: "05"
type: execute
wave: 3
depends_on: [10-01, 10-10, 10-03]
files_modified:
  - R/forecast/score_dependence.R
  - tests/testthat/test_statistical_dependence_pmf.R
  - tests/testthat/test_statistical_dependence_parameters.R
autonomous: true
requirements: [STAT-03]

must_haves:
  truths:
    - "Independent, Dixon-Coles, and bivariate-Poisson variants consume exactly the same augmented penalized-Poisson fixture means."
    - "Each outer fold estimates one bounded global dependence parameter from prior training data and reuses it for every assessed fixture and both tracks."
    - "Every dependence grid is finite, nonnegative, normalized on G=40, preserves the registered marginal means, and feeds the common market/scoring path."
  artifacts:
    - path: "R/forecast/score_dependence.R"
      provides: "Shared-mean hashes, independent/DC/BP PMFs, fold parameter fitting, and manifests"
      exports: ["statistical_mean_prediction_hash", "independent_poisson_grid", "dixon_coles_grid", "bivariate_poisson_grid", "fit_fold_dependence_parameters", "validate_shared_mean_hash", "score_dependence_manifest"]
  key_links:
    - from: "fit_fold_dependence_parameters"
      to: "data/benchmark/phase10/tuning_editions.csv"
      via: "prior-only fold evidence"
      pattern: "outer_edition_id|eligible_match_ids_sha256"
    - from: "dixon_coles_grid"
      to: "R/benchmark/contracts.R"
      via: "G=40 distribution and shared market validation"
      pattern: "validate_benchmark_score_distributions|derive_benchmark_markets"
---

<objective>
Implement and test the two score-dependence corrections around one shared mean structure.

Purpose: Satisfy STAT-03 without confounding score dependence with different predictors, means, panels, or scoring.
Output: One dependence module and a GREEN PMF/chronology suite.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-09-SUMMARY.md
@.planning/phases/10-statistical-goal-model-challengers/10-03-SUMMARY.md

<interfaces>
- Plan 10-03 provides the centered, prior-tuned `poisson_team_ridge_elo` means consumed by Task 2; PMF utilities remain pure over checked `mu_home`/`mu_away` rows.
- Phase 9 requires complete long grids and validates them through `validate_benchmark_score_distributions()` and `derive_benchmark_markets()`.
- The representative selector is evaluation-layer work in Plan 10-07; this plan emits the evidence it consumes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stable independent, Dixon-Coles, and bivariate PMFs</name>
  <read_first>
    <file>R/forecast/score_dependence.R</file>
    <file>tests/testthat/test_statistical_dependence_pmf.R</file>
    <file>tests/testthat/helper_statistical_challengers.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/evaluation/proper_scores.R</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
  </read_first>
  <files>R/forecast/score_dependence.R, tests/testthat/test_statistical_dependence_pmf.R</files>
  <behavior>
    - Dixon-Coles adjusts only 0-0, 0-1, 1-0, and 1-1 and recovers independence at rho zero.
    - Bivariate Poisson uses a shared component while preserving each supplied marginal and recovers independence at q zero.
    - Every PMF is computed stably, normalized once on 0:40, and accepted by inherited distribution/market validators.
  </behavior>
  <action>Implement `statistical_mean_prediction_hash()` over outer fold, track, boundary, fixture, `mu_home`, and `mu_away`, explicitly excluding dependence ID. Implement `independent_poisson_grid()` as the reference. Implement `dixon_coles_grid()` with the standard four low-cell multipliers, a feasible rho interval computed across supplied means with an epsilon interior, log-space cells, finite/positivity rejection, and one G=40 normalization. Implement `bivariate_poisson_grid()` with `kappa = q * min(mu_home, mu_away)`, independent components equal to each registered marginal minus kappa, `lgamma()` plus log-sum-exp over the shared component, and exact marginal/mean checks per D-09. Do not add a `bivpois` runtime dependency; use the checked oracle fixtures and limiting cases.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_dependence_pmf.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Hand-computable oracle cells, independence limits, marginals, and unit mass pass at strict tolerances.
    - Invalid means, rho/q, negative cells, nonfinite values, and incomplete support fail explicitly.
    - All three siblings have identical mean hashes before grid generation, and this gate does not invoke Task 2 fold-parameter or manifest APIs.
  </acceptance_criteria>
  <done>The three dependence choices emit valid common-contract distributions from one mean structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estimate one prior-only global dependence parameter per fold</name>
  <read_first>
    <file>R/forecast/score_dependence.R</file>
    <file>tests/testthat/test_statistical_dependence_parameters.R</file>
    <file>R/forecast/penalized_poisson.R</file>
    <file>R/benchmark/cutoffs.R</file>
    <file>data/benchmark/phase10/tuning_editions.csv</file>
    <file>data/benchmark/phase10/selection_protocol.json</file>
    <file>data/benchmark/phase09/boundaries.csv</file>
  </read_first>
  <files>R/forecast/score_dependence.R, tests/testthat/test_statistical_dependence_parameters.R</files>
  <behavior>
    - One rho and one q are estimated from strictly prior training rows for each outer fold.
    - The same fold parameters and parent mean hashes are reused for frozen/updating tracks and every assessed fixture.
    - Poisoning assessed outcomes cannot change parameters, means, grids, or manifests.
  </behavior>
  <action>Implement `fit_fold_dependence_parameters()` using bounded `stats::optimize()` over prior-data log likelihood: the feasible data-dependent rho interval for Dixon-Coles and fixed `0 &lt;= q &lt; 0.95` for bivariate Poisson. Validate eligible IDs and dates against `tuning_editions.csv`, estimate one scalar per dependence family/outer fold, and freeze it for both tracks per D-10; forbid tournament-, era-, team-, match-, or track-specific values. Implement `validate_shared_mean_hash()` to require exact identity across independent/DC/BP rows before any grid is emitted. Implement `score_dependence_manifest()` with parameter/bounds/objective, training count/date, eligible-ID and mean hashes, optimization status, package/runtime versions, and no fallback. Extend poisoning and same-mean tests accordingly.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_dependence_parameters.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one valid parameter exists per outer-fold/dependence pair and no track-specific duplicate is permitted.
    - Outer-label poisoning and row reordering leave parameter and manifest hashes unchanged.
    - Mean-hash disagreement is a hard pre-grid failure.
  </acceptance_criteria>
  <done>D-09/D-10 dependence evidence is prior-only, fold-global, frozen, and ready for representative selection.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `R/forecast/score_dependence.R` | `statistical_mean_prediction_hash()`, `independent_poisson_grid()`, `dixon_coles_grid()`, `bivariate_poisson_grid()`, `fit_fold_dependence_parameters()`, `validate_shared_mean_hash()`, `score_dependence_manifest()` |
| `tests/testthat/test_statistical_dependence_pmf.R` | GREEN Task 1 PMF, marginal, shared-mean, limiting-case, and G=40 tests |
| `tests/testthat/test_statistical_dependence_parameters.R` | GREEN Task 2 prior-parameter, chronology, manifest, and poisoning tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared means → dependence PMF | A changed marginal would confound dependence and mean quality. |
| Prior rows → fold parameter | Assessed outcomes can leak into rho/q estimates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Tampering | mean/dependence identity | high | mitigate | Exact shared-mean hashes, marginal tests, and pre-grid rejection. |
| T-10-15 | Information Disclosure | dependence fitting | high | mitigate | Eligible-ID/date registry, fold-global scalar schema, and assessed-label poisoning tests. |
| T-10-16 | Denial of Service | PMF numerics | medium | mitigate | Bounded parameters, log-sum-exp, fixed G=40 cells, finite checks, and no general multivariate optimizer. |
</threat_model>

<verification>
Run the dependence suite and inherited contract suite; defer all historical folds to Plan 10-08.
</verification>

<success_criteria>
- D-09 and D-10 are implemented and tested.
- All PMFs preserve common means and inherited output contracts.
- Evidence required for D-11/D-12 representative selection is deterministic and complete.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-05-SUMMARY.md` when done.
</output>
