---
phase: 10-statistical-goal-model-challengers
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - R/benchmark/challenger_preflight.R
  - data/benchmark/phase10/glmnet_provenance.csv
  - _targets.R
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "`glmnet` 5.0 is installed only from a locally verified official-CRAN archive after repository metadata capture and dependency inventory; archive, metadata, dependency, and installed-content hashes persist into run manifests, while `bivpois` is not a production dependency."
  artifacts:
    - path: "R/benchmark/challenger_preflight.R"
      provides: "Official-CRAN provenance and fail-closed runtime environment checks"
      exports: ["capture_cran_package_provenance", "verify_cran_package_archive", "inventory_cran_dependencies", "hash_installed_package_contents", "require_challenger_environment"]
    - path: "data/benchmark/phase10/glmnet_provenance.csv"
      provides: "Official repository, archive checksum, dependency inventory, and installed package-content hashes"
      contains: "installed_content_sha256"
  key_links:
    - from: "data/benchmark/phase10/glmnet_provenance.csv"
      to: "_targets.R"
      via: "exact installed glmnet/Matrix environment and explicit source/package declarations"
      pattern: "glmnet|Matrix|challenger_preflight"
---

<objective>
Verify and freeze the Phase 10 optimizer/runtime provenance before registry or model work begins.

Purpose: Candidate fitting must use one audited optimizer environment with no unrelated dependency drift.
Output: One package/environment preflight module, one immutable provenance artifact, and targets package/source declarations.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-08-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md

<interfaces>
- Phase 9 row hashing uses `benchmark_row_sha256()`/canonical scalar ordering in `R/benchmark/registry.R`.
- Phase 9 bundle identity is `977e119dd17e1212d2bfc57da2e676b6ee9d16bfba8b6c9bdbf1a97d302db069` and must be verified from durable manifests before being copied into Phase 10 registrations.
- `_targets.R` uses namespace declarations in `tar_option_set(packages = ...)` and explicit source ordering.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify official CRAN provenance, install glmnet, and implement fail-fast preflights</name>
  <read_first>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md</file>
    <file>_targets.R</file>
    <file>R/benchmark/runner.R</file>
    <file>R/benchmark/registry.R</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/run_manifest.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/checksum_manifest.csv</file>
  </read_first>
  <files>R/benchmark/challenger_preflight.R, data/benchmark/phase10/glmnet_provenance.csv, _targets.R</files>
  <action>Use the approved official-CRAN audit in `10-RESEARCH.md`. Before any install, implement `capture_cran_package_provenance()` to fetch and persist the canonical repository URL, repository snapshot/index identity, index SHA-256, package/version, publication metadata, selected source-or-platform-binary artifact name/type, and official checksum for `glmnet` 5.0. Implement `inventory_cran_dependencies()` over `Depends`, `Imports`, and `LinkingTo`; persist the canonical inventory and its SHA-256, require every dependency to resolve from base/recommended, the existing library, or the same approved CRAN repository, and fail before installation on an unexpected dependency or system requirement. Download the selected archive without installing it, have `verify_cran_package_archive()` compare its checksum to official repository metadata and record an additional SHA-256, then install only that verified local archive into the constrained Phase 10 library with no unrelated package updates. Require exact version 5.0 and have `hash_installed_package_contents()` canonically hash relative installed-package paths plus file hashes. Persist all repository, metadata, dependency, archive, selected-artifact, and installed-content hashes in `glmnet_provenance.csv`; `require_challenger_environment()` must verify them and later Plans 10-07/10-08 must copy them into the canonical run manifest. Do not install or declare `bivpois`; checked analytical oracle fixtures are sufficient. Add `glmnet` and `Matrix` to `tar_option_set(packages = ...)`, source `challenger_preflight.R` before later Phase 10 modules, and retain the Phase 9 bundle SHA, approved-root, and offline-after-install checks. Registry, chronology, selection, and storage helpers belong exclusively to Plan 10-09.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'source("R/benchmark/challenger_preflight.R"); p &lt;- capture_cran_package_provenance(package = "glmnet", version = "5.0", install = TRUE, output_path = "data/benchmark/phase10/glmnet_provenance.csv"); stopifnot(isTRUE(p$valid), identical(as.character(utils::packageVersion("glmnet")), "5.0"), isTRUE(verify_cran_package_archive(p)), length(inventory_cran_dependencies(p)$unexpected) == 0L, grepl("^[0-9a-f]{64}$", hash_installed_package_contents("glmnet"))); x &lt;- require_challenger_environment(); stopifnot(isTRUE(x$valid)); targets::tar_manifest()'</automated>
  </verify>
  <acceptance_criteria>
    - Exact `glmnet` 5.0 is installed from the verified local official-CRAN archive and both package versions are declared in the target environment.
    - Official repository/index metadata, selected source/binary checksum, dependency inventory, archive SHA-256, and installed-content SHA-256 validate and are ready for run-manifest persistence.
    - Phase 9 bundle identity is checked before any diagnostic or candidate fit.
    - No package other than `glmnet` is installed; `bivpois` remains absent from production declarations.
  </acceptance_criteria>
  <done>The executor environment fails closed unless the audited optimizer and immutable parent are present.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols or contract |
|------|-------------------------|
| `R/benchmark/challenger_preflight.R` | `capture_cran_package_provenance()`, `verify_cran_package_archive()`, `inventory_cran_dependencies()`, `hash_installed_package_contents()`, `require_challenger_environment()` |
| `data/benchmark/phase10/glmnet_provenance.csv` | Official repository/index, dependency inventory, archive checksum/SHA-256, and installed package-content hashes |
| `_targets.R` | `glmnet`/`Matrix` package declarations and `challenger_preflight.R` source |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRAN package → R process | A wrong or substituted optimizer would alter candidate fits. |
| Phase 9 bundle → Phase 10 registry | A stale or tampered parent would invalidate every comparison. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | `glmnet` dependency | high | mitigate | Official CRAN index/metadata capture, selected archive checksum and SHA-256 verification, dependency inventory, constrained local-archive install, installed-content hash, exact 5.0 check, and run-manifest provenance. |
</threat_model>

<verification>
Verify the exact package, recompute repository/archive/dependency/installed-content hashes, and load the target manifest. Do not create Phase 10 registries or execute a benchmark fold in this plan.
</verification>

<success_criteria>
- Exact `glmnet` 5.0 and inherited package versions are available, declared, and backed by durable repository/archive/dependency/content provenance.
- The package/environment preflight is autonomous and file-disjoint from same-wave Plan 10-01; registry/storage protocol work is isolated in Plan 10-09.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-02-SUMMARY.md` when done.
</output>
