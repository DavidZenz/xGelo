---
phase: 10-statistical-goal-model-challengers
plan: "07"
type: execute
wave: 5
depends_on: [10-01, 10-11, 10-06]
files_modified:
  - R/evaluation/challenger_selection.R
  - R/benchmark/challenger_runner.R
  - tests/testthat/test_statistical_selection.R
  - tests/testthat/test_statistical_bundle.R
  - tests/testthat/test_statistical_targets.R
  - _targets.R
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "Every candidate is paired against all five Phase 9 baselines on exact declared comparison panels: 630 against each open baseline and 609 against production_hybrid_nb."
    - "Dependence representative, simpler non-inferior candidate, and best proper-score candidate are selected deterministically under the frozen research protocol without issuing a promotion/release decision."
    - "One cache-only runner reuses Phase 9 registries, adapters, validators, scorer, hashes, and atomic publication while retaining complete Phase 10 tuning/evidence lineage."
    - "The Phase 10 targets DAG is downstream-only and cannot access WC2026 outcomes or mutate Phase 9/dashboard outputs."
  artifacts:
    - path: "R/evaluation/challenger_selection.R"
      provides: "All-baseline pairing, dependence representative, and three-slot research shortlist"
      exports: ["challenger_all_baseline_comparisons", "select_dependence_representative", "build_statistical_shortlist", "validate_statistical_shortlist"]
    - path: "R/benchmark/challenger_runner.R"
      provides: "Phase 10 orchestration, bundle validation, hashing, and atomic publication"
      exports: ["phase10_output_paths", "load_phase09_parent_bundle", "run_statistical_challenger_benchmark", "write_statistical_challenger_bundle", "smoke_statistical_challenger_bundle", "validate_statistical_challenger_bundle"]
  key_links:
    - from: "challenger_all_baseline_comparisons"
      to: "R/evaluation/benchmark_scores.R"
      via: "exact-panel shared scorer and 12-tournament paired deltas"
      pattern: "make_paired_fold_comparisons|aggregate_benchmark_scores"
    - from: "run_statistical_challenger_benchmark"
      to: "R/benchmark/runner.R"
      via: "parent loading, sorting, hashing, validation, and staged install patterns"
      pattern: "benchmark_runner_|benchmark_output_paths"
---

<objective>
Wire all candidate outputs into exact all-baseline comparisons, a research-only shortlist, targets, and a deterministic publication runner.

Purpose: Turn tested model families into one reproducible Phase 10 evidence graph while preserving the Phase 12 promotion boundary.
Output: Selection service, runner/validator, pipeline tests, and six Phase 10 targets.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-RESEARCH.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-11-SUMMARY.md
@.planning/phases/10-statistical-goal-model-challengers/10-06-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-VERIFICATION.md

<interfaces>
- Phase 9 durable predictions/scores remain in `phase09-baselines-frozen` and are referenced, never copied or rerun.
- `benchmark_panel_fixture_ids()`/`select_benchmark_panel_predictions()` enforce exact denominators.
- `benchmark_runner_install_staged_bundle()` provides validated staging, rollback, and post-install validation.
- `evaluate_promotion()` is out of scope and must not be called from any Phase 10 source or target.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build exact candidate-by-five-baseline evidence and research shortlist</name>
  <read_first>
    <file>tests/testthat/test_statistical_selection.R</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/evaluation/promotion.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>data/benchmark/phase10/selection_protocol.json</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/panel_fixtures.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/scores/fixture_scores.csv</file>
  </read_first>
  <files>R/evaluation/challenger_selection.R, tests/testthat/test_statistical_selection.R</files>
  <behavior>
    - Seven candidates pair with four open baselines on exactly 630 fixtures and production_hybrid_nb on exactly 609, for both tracks and all 12 editions.
    - Dependence representative uses updating RPS first, all frozen supporting vetoes, and Dixon-Coles tie preference.
    - The independent mean remains preferred when neither dependence correction clears the meaningful-gain margin, while the better correction is still named representative.
    - Shortlist has three non-exclusive slots and no final winner/promotion/release field.
  </behavior>
  <action>Implement `challenger_all_baseline_comparisons()` by hash-validating and reading the Phase 9 durable scores, projecting candidate and baseline rows onto an explicit comparison panel, asserting fixture-ID equality before pairing, then reusing `make_paired_fold_comparisons()` and the registered 10,000-replicate tournament bootstrap. Emit candidate, baseline, panel, track, 12 fold rows, equal-tournament headline, pooled secondary, uncertainty, breadth, maximum regression, LOTO, active feature set, and parent hashes. Per D-11/D-12, implement `select_dependence_representative()` from independent/DC/BP evidence using the frozen -0.001 meaningful-gain, supporting vetoes, 0.0005 practical tie, and DC preference; name the better valid correction even when independent remains preferred. Per D-15/D-16, implement `build_statistical_shortlist()` with best updating equal-tournament RPS, simplest practically non-inferior active set, and dependence representative when distinct. Use research vocabulary only and validate no promotion/release/WC2026 decision fields or evaluator output is present.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_selection.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_scoring.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic matrix contains 7 x 5 x 2 comparison identities with exact 630/609 fixture sets and 12 fold deltas each.
    - D-11/D-12 tie, meaningful-gain, veto, and independent-retention cases are boundary-tested.
    - D-16 shortlist is deterministic, non-exclusive, evidence-linked, and explicitly not a promotion decision; this gate does not invoke Task 2 runner/publication or Task 3 targets APIs.
  </acceptance_criteria>
  <done>All required paired evidence and the Phase 12 research handoff are pure deterministic evaluation outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cache-only orchestration and atomic bundle validation</name>
  <read_first>
    <file>tests/testthat/test_statistical_bundle.R</file>
    <file>R/benchmark/challengers.R</file>
    <file>R/benchmark/challenger_protocol.R</file>
    <file>R/evaluation/challenger_selection.R</file>
    <file>R/benchmark/runner.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/benchmark/registry.R</file>
    <file>R/benchmark/cutoffs.R</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/benchmark/phase10/glmnet_provenance.csv</file>
    <file>data/benchmark/phase10/storage_preflight.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/checksum_manifest.csv</file>
  </read_first>
  <files>R/benchmark/challenger_runner.R, tests/testthat/test_statistical_bundle.R</files>
  <behavior>
    - Synthetic end-to-end execution validates parent inputs, runs registered candidates, scores exact panels, builds all-baseline comparisons/shortlist, and writes one checksummed bundle.
    - Normal and reversed candidate order produce identical non-runtime hashes in fresh sessions.
    - Invalid parent, tuning, evidence, panel, mean, storage, or WC2026 seal facts fail before atomic install.
  </behavior>
  <action>Implement exact output paths for run manifest, model/feature/fold-tuning/checksum manifests, predictions/distributions, fixture scores/summaries, all-baseline comparisons, shortlist, and markdown research report. `load_phase09_parent_bundle()` must validate the accepted bundle and exact parent graph before returning read-only baselines. `run_statistical_challenger_benchmark()` must require the Plan 10-02 environment preflight and Plan 10-09 registry/storage protocol, call the Phase 9 purpose guard before adapters, build strict frozen/updating states, run seven candidates with at most two independent model-track workers, retain every matchday refit, bind/validate feature and tuning evidence, use common scoring, build comparisons/shortlist, and never call the promotion evaluator. Copy the verified `glmnet_provenance.csv` repository-index, package-metadata, dependency-inventory, selected-artifact checksum/SHA-256, and installed-content SHA-256 fields into every canonical run manifest and parent/checksum graph; fail on any environment/provenance drift. `write_statistical_challenger_bundle()` must canonically sort/write/hash every artifact to a unique stage, reconcile normal/reversed runs, prevalidate, install with rollback via the Phase 9 atomic pattern, and postvalidate. Implement `smoke_statistical_challenger_bundle()` as a control-plane check designed for the 30-second task gate: validate the run/checksum manifests, Phase 9/10 parent identities, registry/settings/tuning/provenance hashes, small summaries/comparisons/shortlist tables, protected-boundary flags, declared counts, and one deterministic G=40 distribution sample per candidate without scanning every score row or running the full suite. `validate_statistical_challenger_bundle()` remains the deep phase gate and independently reconstructs complete schemas, counts, G=40, parent/settings/tuning/mean/evidence hashes, exact 630/609 panels, all comparisons, shortlist rules, seal/no-network facts, and checksum self/parent graph.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_bundle.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic bundles round-trip and reject each corrupted trust-boundary fact.
    - Two fresh-session/order runs have equal canonical hashes before publication.
    - Source scan and callback tests prove no network, WC2026 outcome, or promotion-evaluator path, and this gate does not require Task 3 target registration.
  </acceptance_criteria>
  <done>The Phase 10 bundle can be built and accepted atomically without weakening Phase 9 or crossing Phase 12 boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register isolated Phase 10 targets and expensive phase gates</name>
  <read_first>
    <file>_targets.R</file>
    <file>R/benchmark/challenger_runner.R</file>
    <file>R/benchmark/challenger_preflight.R</file>
    <file>R/benchmark/challenger_protocol.R</file>
    <file>R/benchmark/challengers.R</file>
    <file>R/evaluation/challenger_selection.R</file>
    <file>tests/testthat/test_statistical_targets.R</file>
    <file>tests/testthat/test_benchmark_pipeline.R</file>
    <file>.planning/phases/10-statistical-goal-model-challengers/10-VALIDATION.md</file>
  </read_first>
  <files>_targets.R, tests/testthat/test_statistical_targets.R</files>
  <behavior>
    - Target manifest contains registry, candidate, score, comparison, and bundle targets in dependency order.
    - Phase 10 ancestors are checked local Phase 9/10 inputs only; no dashboard, download, refresh, retrospective, WC2026 ledger, or promotion target is an ancestor.
    - Fast tests run per task/wave; the 12-tournament normal/reversed benchmark is reserved for Plan 10-08.
  </behavior>
  <action>Source package preflight, registry/storage protocol, penalized, dynamic, dependence, challenger adapter, selection, and runner modules in dependency order. Add file-oriented targets `benchmark_phase10_registry_files`, `benchmark_phase10_registries`, `benchmark_phase10_predictions`, `benchmark_phase10_scores`, `benchmark_phase10_comparisons`, and `benchmark_phase10_bundle_files`. Track all Phase 9 parent files and Phase 10 registries/preflight explicitly; pass the canonical goal-training feature input hash without making download/dashboard targets ancestors. Keep expensive canonical execution behind the final bundle target and make manifest tests inspect edges without running it. Add static/dynamic ancestry tests and per-wave focused test commands from `10-VALIDATION.md`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_targets.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); m &lt;- targets::tar_manifest(); stopifnot(all(c("benchmark_phase10_registry_files", "benchmark_phase10_registries", "benchmark_phase10_predictions", "benchmark_phase10_scores", "benchmark_phase10_comparisons", "benchmark_phase10_bundle_files") %in% m$name))'</automated>
  </verify>
  <acceptance_criteria>
    - Six exact target names load without executing the historical benchmark.
    - Dependency scans prove Phase 10 is downstream-only and holdout/promotion isolated.
    - Changes to any parent registry, tuning, model, evidence, panel, or protocol invalidate the correct downstream targets.
  </acceptance_criteria>
  <done>Phase 10 is reproducibly orchestrated with explicit fast and expensive validation boundaries.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/evaluation/challenger_selection.R` | `challenger_all_baseline_comparisons()`, `select_dependence_representative()`, `build_statistical_shortlist()`, `validate_statistical_shortlist()` |
| `R/benchmark/challenger_runner.R` | `phase10_output_paths()`, `load_phase09_parent_bundle()`, `run_statistical_challenger_benchmark()`, `write_statistical_challenger_bundle()`, `smoke_statistical_challenger_bundle()`, `validate_statistical_challenger_bundle()` |
| `tests/testthat/test_statistical_selection.R` | GREEN Task 1 all-baseline, dependence, and shortlist tests |
| `tests/testthat/test_statistical_bundle.R` | GREEN Task 2 runner, corruption, replay, no-promotion, and no-WC2026 tests |
| `tests/testthat/test_statistical_targets.R` | GREEN Task 3 target manifest, ancestry, and invalidation tests |
| `_targets.R` | `benchmark_phase10_registry_files`, `benchmark_phase10_registries`, `benchmark_phase10_predictions`, `benchmark_phase10_scores`, `benchmark_phase10_comparisons`, `benchmark_phase10_bundle_files` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable Phase 9 evidence → all-baseline comparisons | Different denominators or stale hashes can invalidate paired results. |
| Research shortlist → Phase 12 | Selection vocabulary or evaluator calls could prematurely become promotion. |
| Staged files → accepted bundle | Partial or order-dependent output must never replace the accepted Phase 10 bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-20 | Tampering | paired comparisons | high | mitigate | Exact panel projection, fixture set equality, 12-fold pairing, and Phase 9 parent validation. |
| T-10-21 | Elevation of Privilege | shortlist boundary | critical | mitigate | No evaluator call, no promotion/release fields, static source scan, and research-only schema validator. |
| T-10-22 | Tampering | atomic publication | high | mitigate | Unique staging, two-pass canonical reconciliation, pre/post validation, retained rollback, and checksum graph. |
| T-10-23 | Information Disclosure | targets ancestry | high | mitigate | Explicit local parents and forbidden dashboard/download/WC2026 ancestry tests. |
</threat_model>

<verification>
Run each of this plan's three task-scoped test files at its owning task, then the inherited benchmark contract/cutoff/scoring/pipeline/seal regressions and `targets::tar_manifest()` at the plan boundary; do not run the historical benchmark yet.
</verification>

<success_criteria>
- All five baselines are paired against every candidate on exact registered panels.
- D-11, D-12, and D-16 research selection semantics are deterministic and promotion-free.
- Synthetic end-to-end bundle validation and target isolation pass.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-07-SUMMARY.md` when done.
</output>
