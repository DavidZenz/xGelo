---
phase: 10-statistical-goal-model-challengers
plan: "06"
type: execute
wave: 4
depends_on: [10-01, 10-11, 10-03, 10-04, 10-05]
files_modified:
  - R/forecast/poisson.R
  - R/benchmark/challengers.R
  - tests/testthat/test_statistical_ablation_hierarchy.R
  - tests/testthat/test_statistical_adapter_dispatch.R
  - tests/testthat/test_statistical_ablation_selection.R
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04]

must_haves:
  truths:
    - "All seven registered candidates dispatch through one hard-coded adapter path and emit the inherited prediction, G=40 distribution, manifest, feature-evidence, panel, provenance, and seed schemas."
    - "The incumbent ablation compares the unchanged full open NB with one Elo-only sibling using identical fitter, weights, boundaries, and 630-fixture panel."
    - "Zero-coded xG/form columns remain formula-compatible but are explicitly source/value absent, imputed, and inactive; deeper ablation nodes remain auditable and unfit."
  artifacts:
    - path: "R/benchmark/challengers.R"
      provides: "Allowlisted dispatch, common adapter, ablation fit/evidence, and evidence normalization over Plan 10-09 validated registries"
      exports: ["fit_open_nb_elo_only_ablation", "fit_registered_challenger", "predict_registered_challenger", "run_registered_challenger_adapter", "challenger_ablation_evidence"]
    - path: "R/forecast/poisson.R"
      provides: "Named Elo-only incumbent predictor set only"
      exports: ["elo_only_goal_predictors"]
  key_links:
    - from: "run_registered_challenger_adapter"
      to: "R/benchmark/contracts.R"
      via: "all inherited validators before adapter return"
      pattern: "validate_benchmark_predictions|validate_benchmark_feature_evidence"
    - from: "R/benchmark/challengers.R"
      to: "benchmark_fit_two_sided_nb"
      via: "`fit_open_nb_elo_only_ablation()` adapter-layer wrapper using the same NB fitter and weights as the incumbent"
      pattern: "fit_open_nb_elo_only_ablation|benchmark_fit_two_sided_nb"
---

<objective>
Integrate every model family behind the common adapter and implement the controlled incumbent ablation.

Purpose: Complete STAT-04 and ensure STAT-01..03 differ only in registered statistical behavior, never in output/evidence/scoring contracts.
Output: One adapter module containing the ablation fit wrapper, a predictor-only incumbent helper extension, and GREEN ablation/adapter tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-statistical-goal-model-challengers/10-CONTEXT.md
@.planning/phases/10-statistical-goal-model-challengers/10-PATTERNS.md
@.planning/phases/10-statistical-goal-model-challengers/10-11-SUMMARY.md
@.planning/phases/10-statistical-goal-model-challengers/10-03-SUMMARY.md
@.planning/phases/10-statistical-goal-model-challengers/10-04-SUMMARY.md
@.planning/phases/10-statistical-goal-model-challengers/10-05-SUMMARY.md
@.planning/phases/09-rolling-tournament-benchmark-harness/09-08-SUMMARY.md

<interfaces>
- Phase 9 adapter return is `list(predictions, distributions, manifests, feature_coverage)` from `run_registered_baseline_adapter()`.
- `build_registered_feature_coverage()` and `validate_benchmark_feature_evidence()` preserve source/value/imputation/active dimensions.
- Plans 10-03..05 provide mean, state, PMF, tuning, and manifest functions; this plan owns only registry dispatch and schema translation.
- Plan 10-09 owns `load_and_validate_challenger_protocol()` plus all canonical registry, chronology, selection, ablation, and storage validation; this plan consumes that validated object and does not redefine those gates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hierarchical incumbent ablation with inactive-feature provenance</name>
  <read_first>
    <file>R/forecast/poisson.R</file>
    <file>R/benchmark/challengers.R</file>
    <file>R/benchmark/challenger_protocol.R</file>
    <file>tests/testthat/test_statistical_ablation_hierarchy.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/weights.R</file>
    <file>data/benchmark/phase09/model_registry.csv</file>
    <file>data/benchmark/phase09/feature_contract.csv</file>
    <file>data/benchmark/phase10/ablation_registry.csv</file>
    <file>outputs/benchmarks/rolling_tournaments/phase09-baselines-frozen/manifests/feature_coverage.csv</file>
  </read_first>
  <files>R/forecast/poisson.R, R/benchmark/challengers.R, tests/testthat/test_statistical_ablation_hierarchy.R</files>
  <behavior>
    - Elo-only ablation uses the incumbent two-sided NB fitter, observation weights, open panel, and boundaries with only the registered active Elo/venue predictors.
    - Full-incumbent compatibility columns remain present, but xG/form evidence records source/value absent, imputed, inactive, and a zero-coverage reason.
    - Deeper attack-xG, defence-xG, xGD, and form nodes are not fit when their registered coverage gate is zero.
  </behavior>
  <action>Add only `elo_only_goal_predictors()` to `R/forecast/poisson.R`; keep `baseline_goal_predictors()`, `hybrid_goal_predictors()`, incumbent formulas, existing fitters, and all benchmark adapter wrappers out of that forecast helper file. Implement `fit_open_nb_elo_only_ablation()` in `R/benchmark/challengers.R`, routing it through `benchmark_fit_two_sided_nb()` with the same Phase 9 observation weights and open-core inputs. Per D-13, compare this level-one sibling first and leave deeper nodes at their registered non-activated status because durable source/value coverage is zero. Per D-14, retain the incumbent's zero-coded xG/form formula columns and emit `source_present = FALSE`, `value_present = FALSE`, `imputed = TRUE`, `active_in_fit = FALSE`, and `point_in_time_source_coverage_zero` rather than representing measured zero or predictive value. Tests must prove identical panels/weights/fitter, verify the fit wrapper is owned by `challengers.R` while `poisson.R` contains only the predictor helper, and prove no deeper fit invocation.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_ablation_hierarchy.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_baselines.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Existing incumbent predictor helpers and baseline regressions remain unchanged.
    - Elo-only and full incumbent use exact common fixtures, cutoffs, weights, family, and score path.
    - Every unavailable block is explicit in manifests/evidence and no child model is silently fitted; this gate does not invoke Task 2 adapter dispatch or Task 3 non-inferiority APIs.
  </acceptance_criteria>
  <done>The controlled D-13/D-14 ablation is attributable and provenance-correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement allowlisted seven-candidate adapter dispatch</name>
  <read_first>
    <file>R/benchmark/challengers.R</file>
    <file>tests/testthat/test_statistical_adapter_dispatch.R</file>
    <file>R/benchmark/baselines.R</file>
    <file>R/benchmark/contracts.R</file>
    <file>R/forecast/penalized_poisson.R</file>
    <file>R/forecast/dynamic_goal_ability.R</file>
    <file>R/forecast/score_dependence.R</file>
    <file>R/forecast/poisson.R</file>
    <file>data/benchmark/phase10/model_registry.csv</file>
    <file>data/benchmark/phase10/feature_contract.csv</file>
  </read_first>
  <files>R/benchmark/challengers.R, tests/testthat/test_statistical_adapter_dispatch.R</files>
  <behavior>
    - Exactly seven candidate IDs resolve through a hard-coded adapter allowlist; unknown or tampered IDs/settings fail before fitting.
    - Every adapter emits exact fixture/boundary/seed identities, complete G=40 grids, inherited manifests, and exact registered feature groups.
    - Dependence siblings consume and attest the same augmented penalized means.
  </behavior>
  <action>Call Plan 10-09's `load_and_validate_challenger_protocol()` before any dispatch and accept only its canonical validated object; do not reload raw Phase 10 registries or duplicate parent/hash/grid/chronology/ablation/selection/storage validation in this file. Implement `fit_registered_challenger()` and `predict_registered_challenger()` as explicit `switch()` dispatch over the exact seven registrations; never evaluate formulas or function names from CSV text. Implement `run_registered_challenger_adapter()` by matching `run_registered_baseline_adapter()` output shape, calling the Plan 10-03..05 model services, deriving all markets only through `derive_benchmark_markets()`, producing exact feature evidence and manifests, and invoking all Phase 9 validators before return. Namespace distribution/evidence IDs by candidate/track and enforce shared mean hashes for the dependence siblings.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_adapter_dispatch.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Unknown adapter IDs, parent drift, settings drift, and registry code injection fail before model callbacks.
    - All seven adapters pass the same inherited validators and preserve exact fixtures.
    - No candidate-specific scorer, market formula, panel discovery, or output schema exists, and this gate does not invoke Task 3 non-inferiority evidence.
  </acceptance_criteria>
  <done>Every statistical candidate conforms to one immutable common adapter/evidence boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce practical non-inferiority evidence and all-family adapter completeness</name>
  <read_first>
    <file>R/benchmark/challengers.R</file>
    <file>R/forecast/poisson.R</file>
    <file>tests/testthat/test_statistical_ablation_selection.R</file>
    <file>data/benchmark/phase10/selection_protocol.json</file>
    <file>data/benchmark/phase10/ablation_registry.csv</file>
    <file>R/evaluation/benchmark_scores.R</file>
    <file>R/benchmark/contracts.R</file>
  </read_first>
  <files>R/benchmark/challengers.R, tests/testthat/test_statistical_ablation_selection.R</files>
  <behavior>
    - Simpler-incumbent evidence uses equal-tournament updating RPS delta at most +0.001 plus supporting/calibration/fold vetoes.
    - Coefficient p-values are absent from selection inputs and cannot activate deeper ablations.
    - All seven synthetic adapter runs preserve exact predictions, evidence groups, and G=40 support.
  </behavior>
  <action>Add `challenger_ablation_evidence()` to construct the registered full-versus-Elo-only comparison input from shared score outputs and the selection protocol. Per D-15, label the smaller set practically non-inferior only when updating equal-tournament RPS delta is at most +0.001 and Brier/log/calibration/worst-fold vetoes all pass; never inspect coefficient significance. Persist both the decision inputs and the complete active/inactive feature sets for later shortlist use. Complete the test matrix over every candidate, both tracks, cold starts, inactive xG/form rows, dependence mean hashes, and exact prediction-to-feature foreign keys.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_statistical_ablation_selection.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_contracts.R", reporter = "summary", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>
    - Non-inferiority is boundary-tested at +0.001 and every supporting veto.
    - Selection inputs contain no p-value/significance field.
    - Seven candidates x two tracks pass complete synthetic adapter contracts.
  </acceptance_criteria>
  <done>STAT-04 and the common seven-candidate adapter contract are complete.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New/modified symbols or contract |
|------|----------------------------------|
| `R/forecast/poisson.R` | `elo_only_goal_predictors()` only; incumbent helpers remain unchanged |
| `R/benchmark/challengers.R` | `fit_open_nb_elo_only_ablation()`, `fit_registered_challenger()`, `predict_registered_challenger()`, `run_registered_challenger_adapter()`, `challenger_ablation_evidence()`; canonical registry/protocol validation remains owned by Plan 10-09 |
| `tests/testthat/test_statistical_ablation_hierarchy.R` | GREEN Task 1 provenance, hierarchy, and zero-coverage activation tests |
| `tests/testthat/test_statistical_adapter_dispatch.R` | GREEN Task 2 injection, seven-adapter, and shared-contract tests |
| `tests/testthat/test_statistical_ablation_selection.R` | GREEN Task 3 practical non-inferiority and retained-feature evidence tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry text → adapter execution | Executable or unregistered IDs could bypass candidate contracts. |
| Zero-coded inputs → evidence | Missing inactive predictors can be misrepresented as genuine measured zeroes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-17 | Elevation of Privilege | challenger dispatch | high | mitigate | Hard-coded switch, exact seven-ID registry, no eval/parse, and callback non-invocation tests. |
| T-10-18 | Tampering | ablation evidence | high | mitigate | Separate source/value/imputation/active fields and durable zero-coverage reason. |
| T-10-19 | Repudiation | common adapter | high | mitigate | Inherited validators, exact foreign keys, settings/parent hashes, and complete manifests before return. |
</threat_model>

<verification>
Run all four Phase 10 model tests plus inherited baseline/contract tests; no historical benchmark execution occurs in this plan.
</verification>

<success_criteria>
- D-13 through D-15 are implemented and tested.
- All seven candidates pass one common adapter/schema/evidence contract.
- No Phase 9 source, registry, bundle, or scorer is modified.
</success_criteria>

<output>
Create `.planning/phases/10-statistical-goal-model-challengers/10-06-SUMMARY.md` when done.
</output>
