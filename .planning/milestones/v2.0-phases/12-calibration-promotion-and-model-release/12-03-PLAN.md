---
phase: 12-calibration-promotion-and-model-release
plan: 03
type: execute
wave: 3
depends_on:
  - 12-00
  - 12-01
  - 12-02
files_modified:
  - R/calibration/calibration_selection.R
  - outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv
  - tests/testthat/test_phase12_calibration.R
autonomous: true
requirements:
  - CAL-02
must_haves:
  truths:
    - "Raw and calibrated 1X2 predictions are scored on the exact same fixture IDs, editions, tracks, and proper-score functions; no denominator or G=40 distribution changes when the calibrated view is added (D-01, D-04)."
    - "The development gate persists calibration error, RPS, Brier, log-loss, fold-stability, coverage, and ordered veto evidence for every candidate/track, then selects calibrated output only when calibration improves and all frozen vetoes pass."
    - "Raw probabilities remain primary when calibration support is insufficient, calibration does not improve, any supporting veto fails, or exact fixture/coverage identity differs."
    - "The selected `primary_probability_view` is explicit and downstream consumers can distinguish calibrated 1X2 from the unchanged fitted goal distribution."
    - "FLAGGED ASSUMPTION [resolved edge contract CAL-01]: development selection consumes only the freeze-validated chronology-safe calibration artifacts from Plan 02."
    - "FLAGGED ASSUMPTION [resolved edge contract CAL-02]: calibration promotion means strictly lower fixed-bin calibration error with all inherited proper-score/stability/coverage vetoes passing."
    - "FLAGGED ASSUMPTION [resolved edge contract PROMO-02]: development raw-versus-calibrated selection is not final promotion, cannot open WC2026 labels, and cannot change the Phase 9 policy evaluator."
  artifacts:
    - path: R/calibration/calibration_selection.R
      provides: "Shared raw/calibrated scoring, development gate, veto evidence, and primary-view selection"
    - path: tests/testthat/test_phase12_calibration.R
      provides: "CAL-02 identical-fixture, shared-score, unchanged-distribution, regression, and raw-fallback tests"
    - path: outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv
      provides: "Durable raw-versus-calibrated comparison and selected primary-view rows"
  key_links:
    - from: R/calibration/probability_calibration.R::apply_phase12_1x2_calibrator
      to: R/calibration/calibration_selection.R::compare_phase12_raw_calibrated
      via: "Raw and derived calibrated 1X2 vectors enter one identical fixture-scoring contract"
    - from: R/evaluation/benchmark_scores.R::score_benchmark_fixtures
      to: R/evaluation/benchmark_scores.R::aggregate_benchmark_scores
      via: "Both views retain the equal-tournament 12-edition estimand and exact fixture coverage"
    - from: R/calibration/calibration_selection.R::select_phase12_primary_probability_view
      to: data/benchmark/phase12/freeze_manifest.csv
      via: "Selection is valid only under the frozen recipe, thresholds, and parent identities"
---

<objective>
Compare raw and calibrated derived 1X2 probabilities under the inherited benchmark scorer and freeze the development primary-view decision.

Purpose: CAL-02 requires calibration evidence without conflating it with scoreline calibration or final promotion. The development gate must retain the raw fallback whenever any required evidence or veto is unfavorable.
Output: `R/calibration/calibration_selection.R`, durable raw/calibrated gate rows, and focused CAL-02 regression tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md
@data/benchmark/phase09/promotion_protocol.json
@R/evaluation/benchmark_scores.R
@R/evaluation/promotion.R
@R/calibration/inner_oof.R
@R/calibration/probability_calibration.R
@tests/testthat/test_benchmark_scoring.R
@tests/testthat/test_benchmark_promotion.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one frozen candidate through raw-versus-calibrated development scoring</name>
  <read_first>R/evaluation/benchmark_scores.R, R/evaluation/proper_scores.R, R/evaluation/promotion.R, data/benchmark/phase09/promotion_protocol.json, R/calibration/inner_oof.R, R/calibration/probability_calibration.R, R/release/freeze_manifest.R, tests/testthat/test_benchmark_scoring.R</read_first>
  <files>R/calibration/calibration_selection.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, tests/testthat/test_phase12_calibration.R</files>
  <action>Require the validated freeze and completed calibrator artifacts before comparison. Implement `compare_phase12_raw_calibrated(raw_predictions, calibrated_predictions, fixtures, distributions, expected_fixture_ids, expected_editions)` for one frozen candidate/track. Require identical fixture, edition, track, score-distribution, auxiliary-market, and prediction-status identities; allow only derived 1X2 values to differ. Reuse the exact scorer and fixed-bin calibration services in `R/evaluation/benchmark_scores.R` and `R/evaluation/proper_scores.R`, plus the inherited paired-comparison shape; call raw and calibrated paths with identical fixture IDs and 12-edition inputs. Add `select_phase12_primary_probability_view()` with strict calibration improvement and inherited supporting vetoes, without duplicating `evaluate_promotion()` from `R/evaluation/promotion.R`. Follow canonical durable-output/read-back patterns in `R/benchmark/runner.R`, preserve raw output on failure, and set exactly `calibrated_1x2` or `raw_1x2`. Implement D-01 and D-04 and satisfy validation-map entry 12-03-01.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_calibration.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>A synthetic candidate receives raw and calibrated score rows over identical fixtures; both views use shared RPS/Brier/log-loss and fixed-bin calibration services; score distributions are identical; calibrated output is selected only on strict calibration improvement with all vetoes passing; and raw fallback is explicit otherwise.</acceptance_criteria>
  <done>The development decision for one candidate/track is a tested, scorer-backed primary-view result with no final-holdout access.</done>
</task>

<task type="auto">
  <name>Task 2: Persist all raw/calibrated comparison and regression evidence</name>
  <read_first>R/calibration/calibration_selection.R, R/evaluation/benchmark_scores.R, R/evaluation/proper_scores.R, R/evaluation/promotion.R, R/benchmark/runner.R, data/benchmark/phase09/promotion_protocol.json, tests/testthat/test_phase12_calibration.R, .planning/phases/12-calibration-promotion-and-model-release/12-VALIDATION.md</read_first>
  <files>R/calibration/calibration_selection.R, outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv, tests/testthat/test_phase12_calibration.R</files>
  <action>Complete `phase12_calibration_gate_rows()` and `write_phase12_calibration_gate()` so each candidate/track row records raw and calibrated headline/tournament metrics, class-specific and aggregate calibration errors, fold deltas/stability, coverage numerator/denominator, `primary_probability_view`, `calibration_promoted`, and ordered reason codes. Use equal-tournament aggregation and `make_paired_fold_comparisons()` from `R/evaluation/benchmark_scores.R`, validators from `R/evaluation/proper_scores.R`, protocol operators from `R/evaluation/promotion.R`, and canonical durable-output/read-back patterns from `R/benchmark/runner.R`. Do not add a threshold table or call `select_promoted_candidate()` for this development view. Test strict equality/touching thresholds, incomplete coverage, sparse support, tied metrics, unchanged distributions, and repeated ordering. Preserve all nine candidate identities and inactive no-score rows. Cite D-04 through D-08 and satisfy validation-map entry 12-03-02.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_calibration.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_file("tests/testthat/test_benchmark_scoring.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The durable gate contains raw/calibrated score and calibration evidence for every candidate/track state, ordered fallback reasons, exact fixture coverage, and an explicit primary view; inherited scoring tests remain green and no WC2026 source is read.</acceptance_criteria>
  <done>CAL-02 is fully evidenced and ready for the final pre-label gate, with raw fallback behavior available for incumbent and challenger release paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw/calibrated prediction rows → shared scoring | A view mismatch could change the estimand or hide missing fixtures. |
| Protocol JSON → development selection | Threshold drift could make calibrated output appear eligible under a different policy. |
| Development gate → final promotion | Development evidence must not grant holdout or release authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-03-01 | Tampering | Raw/calibrated fixture pairing | high | mitigate | Require exact fixture, edition, track, status, distribution, market, and coverage identity before shared scoring. |
| T-12-03-02 | Tampering | Score and calibration metric semantics | high | mitigate | Delegate to `score_benchmark_fixtures()`, `aggregate_benchmark_scores()`, `fixed_benchmark_calibration()`, and existing paired-comparison helpers. |
| T-12-03-03 | Elevation of privilege | Raw/calibrated primary-view selector | high | mitigate | Read supporting limits from the locked protocol, record reason codes, preserve raw fallback, and prohibit any promotion-policy implementation here. |
| T-12-03-04 | Information disclosure | Development data and holdout labels | critical | mitigate | Run `guard_benchmark_purpose(..., "calibration")` upstream and use only the frozen pre-2026 development rows. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any proposed install requires legitimacy review and a blocking human gate. |
</threat_model>

<verification>
Run the focused calibration file after each task, the inherited scoring and promotion files after the wave, and the full suite before final-evaluation planning is executed. Inspect raw/calibrated CSV identity and confirm no scoreline distribution column changes.
</verification>

<success_criteria>
CAL-02 is satisfied by identical-fixture raw/calibrated proper scoring, fixed-bin calibration and fold-stability diagnostics, persisted veto evidence, and deterministic raw fallback when calibrated output is not admissible.
</success_criteria>

### Artifacts this phase produces

- Functions: `compare_phase12_raw_calibrated()`, `select_phase12_primary_probability_view()`, `phase12_calibration_gate_rows()`, and `write_phase12_calibration_gate()`.
- Files: `R/calibration/calibration_selection.R` and the expanded `tests/testthat/test_phase12_calibration.R`.
- Manifest/output: `outputs/benchmarks/rolling_tournaments/phase12-calibration-release/calibration/calibration_gate.csv` plus candidate/track raw/calibrated score and calibration rows.
- Contract: explicit `primary_probability_view`, `calibrated_1x2`/`raw_1x2` selection, unchanged `score_distribution_id`, and ordered veto reasons.
- CLI/test surface: focused Phase 12 calibration command plus inherited scoring command.

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-03-SUMMARY.md` when done.
</output>
