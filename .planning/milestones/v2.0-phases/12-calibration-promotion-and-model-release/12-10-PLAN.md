---
phase: 12-calibration-promotion-and-model-release
plan: 10
type: execute
wave: 10
depends_on: [12-09]
files_modified:
  - R/release/release_contract.R
  - R/release/release_bundle.R
  - R/visualization/worldcup_dashboard.R
  - tests/testthat/test_phase12_release.R
  - tests/testthat/test_worldcup_dashboard.R
autonomous: true
gap_closure: true
requirements: [PROMO-03]
must_haves:
  truths:
    - "A release root, root-level candidate, or immediate-child candidate that uses a symlink or normalizes outside the trusted root is rejected before bundle validation, readRDS(), or forecast work (D-15)."
    - "resolve_phase12_approved_release() independently establishes the current trusted-root preflight even when validated_preflight is supplied, and a forged or stale handoff cannot select a different release before model loading (D-09, D-15)."
    - "The embedded decision identity changes when canonical candidate evidence, the exact raw release_decision token (\"challenger approved\" or \"incumbent retained\"), or selected model ID changes; status normalization is used only for manifest/authority comparisons, while final_evaluation_manifest.csv promotion_decision_sha256 remains a separate uniform external-file provenance field (D-12, D-15)."
    - "Contract model_artifact and calibrator_artifact are distinct canonical intended identities, each maps to its intended manifest row, and refreshed hashes cannot authorize a model/calibrator path swap before readRDS() (D-14, D-15)."
    - "New contracts require freeze_id and freeze_self_sha256, and final-evaluation freeze_id/freeze_self_sha256 cross-links agree with the release manifest and both freeze/final-evaluation evidence for approved and incumbent-retained releases; the immutable current incumbent-retained contract is accepted only through an explicit raw-only compatibility derivation, and only candidate eligibility checks are status-specific (D-08, D-12, D-15)."
    - "Loaded model and calibrator objects carry required identity fields matching the contract; the accepted incumbent-retained raw artifact shape is supported only through an explicit raw-only compatibility rule, never through missing-identity acceptance (D-13, D-16)."
    - "A calibrated_1x2 release derives one calibrated per-match outcome view from forecast_dashboard_matches(), passes that view through group/stage and knockout consumers, uses calibrated outcome categories for points/ranking and advancement, and retains raw scoreline distributions for goal bookkeeping and scoreline-derived auxiliary markets (D-01, D-04)."
    - "An unsupported or incomplete calibrated release is rejected before forecast generation, and a valid incumbent-retained raw release preserves the current dashboard/export payload and UI behavior (D-13, D-16)."
    - "Focused tests prove symlink, forged-handoff, approved and incumbent-retained hash-sensitivity, refreshed-hash swap, unconditional freeze/final-evaluation links, loaded-identity, calibrated-output data flow, and preflight-before-readRDS/forecast invariants without changing durable generated outputs."
  artifacts:
    - path: R/release/release_contract.R
      provides: "Symlink-safe trusted-root discovery, fresh preflight handoff validation, candidate authority, freeze cross-links, exact decision identity recomputation, and strict resolver identity checks"
    - path: R/release/release_bundle.R
      provides: "Canonical contract artifact identity validation, freeze-linked evidence checks, and fail-closed loaded model/calibrator identity validation"
    - path: R/visualization/worldcup_dashboard.R
      provides: "Release-calibrator wiring for match/group/stage/knockout derived 1X2 outputs with unchanged scoreline distributions"
    - path: tests/testthat/test_phase12_release.R
      provides: "Temporary release-boundary regressions for topology, handoffs, hashes, path swaps, freeze links, identities, and ordering"
    - path: tests/testthat/test_worldcup_dashboard.R
      provides: "Calibrated consumer behavior and retained-release UI regression coverage"
  key_links:
    - from: trusted root and candidate paths
      to: preflight_phase12_approved_release()
      via: "raw symlink checks plus normalized manifest/root containment before validate_phase12_complete_release_bundle(load_models = FALSE)"
    - from: resolve_phase12_approved_release()
      to: preflight_phase12_approved_release()
      via: "fresh preflight for every resolver call, with any supplied handoff compared to the fresh trusted-root/path/identity result"
    - from: model_contract.json
      to: release_manifest.csv and readRDS()
      via: "canonical model/calibrator artifact identities, one-to-one manifest rows, byte hashes, then loaded-object identity checks"
    - from: benchmark_report.md candidate evidence
      to: embedded decision_sha256
      via: "promotion_report.R-compatible canonical CSV serialization plus the exact raw release-decision token (challenger approved or incumbent retained) and selected ID; status normalization is comparison-only"
    - from: contract freeze_id
      to: freeze_manifest.csv and final_evaluation_manifest.csv
      via: "new contracts bind contract freeze_id plus freeze_self_sha256 to the freeze row; the immutable retained fixture uses an explicit raw-only compatibility derivation from that row and the release-manifest freeze artifact hash; final-evaluation freeze_id/freeze_self_sha256 and track cross-links are required, while G is derived from the authoritative freeze row because the current final-evaluation schema has no score_support_g column"
    - from: resolver-returned calibrator and primary_probability_view
      to: dashboard match/group/stage/knockout outputs
      via: "derived 1X2 calibration only; raw scoreline distributions remain the simulation/evidence source"
---

<objective>
Close every remaining PROMO-03 release-consumer blocker identified by fresh verification and deep review: eliminate trusted-root escapes and forgeable preflight handoffs, bind every release identity and evidence hash fail-closed, and make calibrated releases truthful across all dashboard-derived 1X2 outputs while preserving the accepted incumbent-retained release.

Purpose: The current focused suites are green but adversarial probes still show security and correctness failures at the release boundary. This plan repairs those root causes and adds the missing regression evidence without reopening the holdout, changing the accepted release contract shape beyond required compatibility fields, editing direct callers, or producing durable generated outputs.
Output: Hardened release contract/bundle validation, calibrated dashboard consumer wiring, and temporary-fixture regression coverage for all listed blockers.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md
@.planning/phases/12-calibration-promotion-and-model-release/12-RESEARCH.md
@.planning/phases/12-calibration-promotion-and-model-release/12-VERIFICATION.md
@.planning/phases/12-calibration-promotion-and-model-release/12-REVIEW.md
@.planning/phases/12-calibration-promotion-and-model-release/12-09-PLAN.md
@.planning/phases/12-calibration-promotion-and-model-release/12-09-SUMMARY.md
@R/release/release_contract.R
@R/release/release_bundle.R
@R/release/release_install.R
@R/release/promotion_report.R
@R/calibration/probability_calibration.R
@R/visualization/worldcup_dashboard.R
@tests/testthat/test_phase12_release.R
@tests/testthat/test_worldcup_dashboard.R
@_targets.R
@scripts/update_worldcup_dashboard.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-establish the trusted release-to-resolver integrity path</name>
  <read_first>R/release/release_contract.R, R/release/release_bundle.R, R/release/release_install.R, R/release/promotion_report.R, R/benchmark/runner.R, tests/testthat/test_phase12_release.R, _targets.R, scripts/update_worldcup_dashboard.R, .planning/phases/12-calibration-promotion-and-model-release/12-VERIFICATION.md, .planning/phases/12-calibration-promotion-and-model-release/12-REVIEW.md, .planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md</read_first>
  <files>R/release/release_contract.R, R/release/release_bundle.R, tests/testthat/test_phase12_release.R</files>
  <action>Implement the release-boundary fixes required by D-09, D-12, D-13, D-14, D-15, and D-16 while preserving the current model_contract.json field names and the two model rows whose artifact_role is model. In trusted-root discovery, reject a symlinked/non-directory trusted root, symlinked root release_manifest.csv, symlinked immediate-child release directory, or symlinked child release_manifest.csv; normalize every candidate and require both its manifest and release directory to remain inside the normalized trusted root before any bundle validator call. Make resolve_phase12_approved_release() obtain a fresh preflight on every call; when validated_preflight is supplied, compare its normalized trusted_root, release_root, pinned manifest path, and authoritative metadata/candidate identity to the fresh result and reject any forged, stale, or alternate handoff before validate_phase12_complete_release_bundle(load_models = TRUE). Keep _targets.R and scripts/update_worldcup_dashboard.R unchanged so their direct resolver calls inherit this ordering.</action>
  <action>Following the checksum/containment pattern in R/benchmark/runner.R:656-712, implement the release evidence cross-link contract in validate_phase12_release_bundle() and phase12_release_contract_validate_candidate_authority(). Require contract freeze_id and exact agreement with the release manifest for every status. For newly staged releases, write and require contract freeze_self_sha256 and bind it to the unique freeze row's freeze_id/freeze_self_sha256; for the immutable current incumbent-retained fixture, allow the absent contract field only through an explicit raw-release compatibility branch that derives the same authoritative freeze_self_sha256 from the copied freeze row and verifies the release-manifest row for manifests/freeze_manifest.csv protects that copied file, never by accepting a missing identity. Require freeze_manifest.csv to contain one matching freeze_id/freeze_self_sha256, selected_g/score_support_g = 40, and its authoritative candidate fields; require final_evaluation_manifest.csv to contain matching freeze_id/freeze_self_sha256 on every row and a single track_id equal to the release track. The current final-evaluation schema has no score_support_g column: derive G=40 from the authoritative freeze row and release manifest, and if a future final-evaluation schema supplies score_support_g, require every value to equal that same authoritative 40. Apply these unconditional evidence checks to approved and incumbent-retained releases before candidate authority; keep active/scored selected-candidate eligibility checks status-specific, so incumbent-retained releases do not need a selected challenger row but cannot bypass the evidence links. Use the exact intended model/calibrator identities in addition to distinct safe paths, one-to-one artifact rows, artifact names, roles, and current-byte hashes; a path swap followed by refreshed manifest/self hashes must still fail. Recompute the embedded decision identity exactly as promotion_report.R does: write the raw candidate-evidence rows with utils::write.csv(..., row.names = FALSE, na = "", quote = TRUE), obtain the raw release_decision token from the candidate-evidence release_decision field or equivalent promotion result, require exactly "challenger approved" for an approved release or "incumbent retained" for an incumbent-retained release, concatenate that token and the selected model ID with the evidence serialization using newline separators, then SHA-256 the resulting string. Normalize only the report status for manifest/authority comparisons; never normalize the token used for the hash. Compare the recomputed identity independently across benchmark report, release manifest, contract, and provenance, with both approved and incumbent-retained fixtures. Continue treating final_evaluation_manifest.csv promotion_decision_sha256 only as a single uniform 64-hex external promotion-report provenance field, separate from the embedded identity. Use the established staged-validation pattern in R/benchmark/runner.R for temporary fixture mutation and rollback-safe validation.</action>
  <action>After the metadata gate, require the loaded model object to contain model_id equal to contract$selected_model_id and the loaded calibrator to contain candidate_id equal to that same identity; missing fields and mismatches fail before the resolver returns. Explicitly document in code and tests the only compatibility branch: the currently accepted incumbent-retained raw_1x2 calibrator shape may omit fitted-calibration internals but must still carry schema_version, candidate_id, track_id, fit_status, primary_probability_view, and distribution_unchanged with matching identity; a calibrated_1x2 contract must carry the complete Phase 12 calibrator identity needed by apply_phase12_1x2_calibrator(). Add tracer assertions for valid approved and incumbent-retained paths and for invalid symlink, forged-handoff, refreshed-hash path-swap, approved raw-token/status drift, incumbent-retained raw-token/status drift, selected-ID hash drift, unconditional freeze/final-evaluation cross-link, and loaded-identity cases, with readRDS() activity proving all failures occur first.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure=TRUE, stop_on_warning=TRUE)'</automated>
  </verify>
  <acceptance_criteria>The trusted-root preflight rejects root and immediate-child symlink escapes; every resolver call performs or validates a fresh preflight before any RDS read; refreshed metadata cannot authorize swapped contract paths; embedded decision hashes change for evidence/status/selected-ID drift; freeze links and loaded identities are mandatory; and the valid incumbent-retained fixture still resolves with its accepted contract shape.</acceptance_criteria>
  <done>Release metadata and resolver authority are structurally bound to the trusted root, canonical artifacts, freeze/evaluation evidence, exact decision identity, and loaded object identities, with no direct-caller or durable-output changes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply calibrated 1X2 output through the dashboard forecast graph</name>
  <read_first>R/visualization/worldcup_dashboard.R, R/calibration/probability_calibration.R, R/forecast/monte_carlo.R, R/benchmark/runner.R, tests/testthat/test_worldcup_dashboard.R, tests/testthat/test_phase12_release.R, .planning/phases/12-calibration-promotion-and-model-release/12-CONTEXT.md, .planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md</read_first>
  <files>R/visualization/worldcup_dashboard.R, tests/testthat/test_worldcup_dashboard.R</files>
  <action>Following the existing forecast_dashboard_matches(), make_knockout_route_estimator(), and build_worldcup_dashboard_data() consumer analogs mapped in 12-PATTERNS.md, extend the resolver-returned release wiring per D-01, D-04, D-13, D-15, and D-16 so the dashboard passes the calibrator and primary_probability_view explicitly through forecast_dashboard_matches(), make_knockout_route_estimator(), simulate_group_stage_dashboard(), and build_bracket_paths(). For a calibrated_1x2 contract, use the existing Phase 12 calibrator API to transform each open match's raw derived home/draw/away vector, update only the match 1X2 fields and predicted outcome, and leave the scoreline_distribution rows, expected goals, top scorelines, totals, and both-teams-to-score values unchanged. Reject a calibrated contract before forecast generation if the resolver did not return a structurally valid fitted/fallback-compatible calibrator.</action>
  <action>Define an explicit calibrated per-match outcome view as a data-flow contract: forecast_dashboard_matches() derives each match's raw outcome vector, applies the resolver-returned calibrator, and emits the calibrated home/draw/away probabilities and outcome category alongside the unchanged scoreline_distribution. Pass that view explicitly into simulate_group_stage_dashboard(), make_knockout_route_estimator(), and build_bracket_paths(); do not make group/stage consumers infer calibrated probabilities by sampling raw scoreline distributions. In simulate_group_stage_dashboard(), sample the calibrated outcome category for group points and ranking, while using the unchanged raw scoreline distribution only for goal bookkeeping and scoreline-derived evidence under the existing deterministic seed contract. Make knockout route estimation transform the raw regulation 1X2 vector, use the calibrated draw probability for extra-time/penalty allocation, and derive advancement probabilities from calibrated regulation and tiebreak components without altering the returned scoreline summary or scoreline support. Ensure bracket paths receive those calibrated route values, and keep raw_1x2/incumbent-retained behavior byte-for-byte compatible at the semantic field level. Do not add raw model-path or baseline authority and do not modify _targets.R, scripts/update_worldcup_dashboard.R, or any generated output.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_dashboard.R", stop_on_failure=TRUE, stop_on_warning=TRUE)'</automated>
  </verify>
  <acceptance_criteria>A valid calibrated fixture produces changed derived match, group/stage, and knockout 1X2/advancement probabilities when the calibrator changes the raw vector; its scoreline distributions and scoreline-derived auxiliary fields remain unchanged; invalid calibrated contracts fail before forecast work; and the retained raw release continues to pass the existing dashboard/UI regressions.</acceptance_criteria>
  <done>Dashboard and export consumers truthfully implement calibrated_1x2 releases across all required derived probability surfaces while preserving the accepted raw incumbent behavior and scoreline evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Complete adversarial release and consumer regression coverage</name>
  <read_first>tests/testthat/test_phase12_release.R, tests/testthat/test_worldcup_dashboard.R, R/release/release_contract.R, R/release/release_bundle.R, R/visualization/worldcup_dashboard.R, R/benchmark/runner.R, _targets.R, scripts/update_worldcup_dashboard.R, .planning/phases/12-calibration-promotion-and-model-release/12-VERIFICATION.md, .planning/phases/12-calibration-promotion-and-model-release/12-REVIEW.md, .planning/phases/12-calibration-promotion-and-model-release/12-PATTERNS.md</read_first>
  <files>tests/testthat/test_phase12_release.R, tests/testthat/test_worldcup_dashboard.R</files>
  <action>Using the temporary-bundle, checksum-mutation, staged-validation, and fail-closed sentinel patterns in R/benchmark/runner.R and the test analogs mapped in 12-PATTERNS.md, expand the two existing focused test files using only temporary copies outside the accepted release root and no durable generated outputs. Add fail-closed cases for symlinked trusted roots, symlinked root/immediate-child manifests and directories, forged validated_preflight objects that point to an alternate valid bundle, exact approved and incumbent-retained raw release_decision token hash fixtures plus status/selected-ID decision-hash sensitivity, independent malformed/non-uniform final-evaluation external provenance, contract model/calibrator path swaps with refreshed artifact and self hashes, missing/different freeze_id and freeze-self/final-evaluation cross-links for both release statuses, the current retained fixture's explicit absent-contract-freeze-self compatibility rule, missing or mismatched loaded model/calibrator identities, and calibrated contracts with absent or incompatible calibrators.</action>
  <action>Add ordering spies around preflight_phase12_approved_release(), readRDS(), validate/load paths, forecast_dashboard_matches(), simulate_group_stage_dashboard(), and knockout route generation so every invalid release fails before model or forecast work and every unchanged direct caller remains preflight-first. Add a calibrated synthetic fixture that asserts forecast_dashboard_matches() emits the calibrated per-match outcome view, simulate_group_stage_dashboard() uses calibrated outcome categories for points/ranking while raw scorelines provide goal bookkeeping, and match/group/stage/knockout derived 1X2 outputs change while scoreline distributions and scoreline-derived fields remain unchanged; also assert valid incumbent-retained payload/UI behavior. Retain the existing semantic tab/search/bracket/accessibility, raw-argument rejection, release metadata, and no-label assertions. Include a static assertion that _targets.R and scripts/update_worldcup_dashboard.R still call resolve_phase12_approved_release() rather than selecting raw model paths, without editing those callers.</action>
  <verify>
    <automated>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_phase12_release.R", stop_on_failure=TRUE, stop_on_warning=TRUE); testthat::test_file("tests/testthat/test_worldcup_dashboard.R", stop_on_failure=TRUE, stop_on_warning=TRUE)'</automated>
  </verify>
  <acceptance_criteria>The focused suites independently cover every review/verification blocker and ordering claim, exact approved/incumbent hash semantics, unconditional freeze/evaluation links, calibrated per-match-to-group/stage data flow, calibrated and raw consumer behavior, direct-caller preservation, and incumbent UI regressions; all fixtures are temporary; and the 12-10 execution delta contains only the five planned source/test files, regardless of pre-existing dirty worktree paths.</acceptance_criteria>
  <done>PROMO-03 has executable regression evidence for all required security, integrity, calibration, ordering, and presentation truths, with no durable release/output mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Filesystem/trusted release root -> metadata preflight | Untrusted symlinks, paths, manifests, contract metadata, and copied evidence attempt to become release authority. |
| Metadata preflight -> full resolver/readRDS | Only independently validated release metadata may authorize model and calibrator bytes. |
| Loaded model/calibrator -> dashboard forecast graph | Artifact identity and calibration semantics cross into match, group, stage, knockout, and export probabilities. |
| Forecast payload -> HTML/JSON/export UI | Derived probabilities and release provenance cross into presentation while holdout labels remain excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-10-01 | Tampering | Trusted-root candidate discovery | critical | mitigate | Reject raw symlink roots/directories/manifests and normalized paths outside the trusted root before metadata validation. |
| T-12-10-02 | Spoofing | validated_preflight handoff | critical | mitigate | Recompute a fresh preflight and compare normalized root, pinned manifest, metadata, and candidate authority before loading. |
| T-12-10-03 | Tampering | Embedded promotion decision identity | critical | mitigate | Reuse promotion_report.R canonical evidence serialization and bind the exact raw release-decision token plus selected ID; normalize status only for authority comparisons and keep external final-evaluation provenance separate. |
| T-12-10-04 | Tampering | Contract artifact paths and freeze evidence | critical | mitigate | Require canonical intended model/calibrator identities, byte hashes, contract freeze_id, freeze self-hash, and final-evaluation cross-links before readRDS(). |
| T-12-10-05 | Elevation of privilege | Loaded model/calibrator identity | high | mitigate | Require non-missing model_id/candidate_id equality and explicit raw-incumbent compatibility fields; reject silent legacy identity absence. |
| T-12-10-06 | Tampering | Calibrated dashboard probabilities | high | mitigate | Validate calibrated contracts and apply the resolver-returned calibrator across every derived 1X2 surface without altering scoreline distributions. |
| T-12-10-07 | Information disclosure | Consumer payloads | high | mitigate | Retain no-label release checks and existing dashboard tests while resolving models only through the approved contract. |
| T-12-10-08 | Denial of service | Invalid release/test fixtures | medium | mitigate | Fail closed before model/forecast work and keep all adversarial fixtures temporary and isolated. |
| T-12-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is planned; any attempted dependency change remains outside this plan and requires the package legitimacy gate. |
</threat_model>

<verification>
Before execution, capture the git status and complete path inventory (including staged, unstaged, and untracked paths) as the baseline. Run the release and dashboard focused suites with warnings treated as failures, then run the full repository test suite and a fresh-process retained-release validation. Confirm symlink and forged-handoff probes fail before readRDS()/forecast sentinels, refreshed-hash path swaps fail, exact approved and incumbent-retained raw decision-token hash fixtures pass while status/selected-ID mutations change the embedded identity, external promotion provenance remains independently uniform, freeze/final-evaluation links and loaded identities are strict for both release statuses, calibrated outputs change only the derived 1X2 surfaces, calibrated outcome categories drive group ranking, raw scoreline distributions remain identical, and valid incumbent-retained UI behavior remains green. Compare the post-execution status/path inventory against the baseline and verify that only the five planned source/test paths plus the new `.planning/phases/12-calibration-promotion-and-model-release/12-10-SUMMARY.md` planning artifact were added or modified by 12-10; separately compare before/after durable release, benchmark, dashboard, and other generated-output inventories and require no durable output changes. Do not require a clean global worktree or treat unrelated pre-existing paths as 12-10 changes.
</verification>

<success_criteria>
PROMO-03 is closed when trusted-root topology, preflight handoffs, contract paths, embedded/external decision identities, freeze/evaluation evidence, and loaded object identities are all fail-closed; calibrated_1x2 consumers are correctly applied or rejected before use across match/group/stage/knockout outputs with unchanged scoreline distributions; direct resolver callers remain preflight-first; every requested adversarial and ordering regression passes; and the accepted incumbent-retained dashboard/export/UI behavior remains intact without durable generated-output changes.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Freeze, calibrate, evaluate once, and release only an eligible model | 12-01..12-10 | COVERED | Prior plans cover freeze/calibration/holdout/promotion; this gap plan closes the remaining consumer integrity boundary. |
| REQ | CAL-01 | Inner-OOF calibration without outer-holdout leakage | 12-02 | COVERED | Existing verified implementation; this plan consumes its calibrator only. |
| REQ | CAL-02 | Shared raw/calibrated proper-score comparison | 12-03 | COVERED | Existing verified implementation; consumer wiring preserves the scoreline distribution contract. |
| REQ | PROMO-01 | Freeze identities and thresholds before final evaluation | 12-01 | COVERED | This plan adds release-to-freeze cross-link validation. |
| REQ | PROMO-02 | One-shot evaluation and incumbent fallback | 12-04..12-05 | COVERED | This plan preserves final-evaluation external provenance semantics. |
| REQ | PROMO-03 | Versioned release, reports, provenance, and dashboard regressions | 12-06..12-10 | COVERED | All fresh verification/review blockers and requested tests are assigned here. |
| RESEARCH | Trusted topology | 12-10 Task 1 | COVERED | Root and immediate-child symlink/file/directory escapes are rejected before validation/loading. |
| RESEARCH | Metadata-only consumer boundary | 12-09, 12-10 Task 1 | COVERED | Fresh preflight remains ahead of full resolver reads. |
| RESEARCH | Independent promotion hash semantics | 12-10 Task 1 and Task 3 | COVERED | Embedded identity reproduces promotion_report.R's raw candidate-evidence CSV serialization plus the exact raw `challenger approved` or `incumbent retained` token and selected ID; normalization is limited to manifest/authority comparisons, and the external field stays provenance-only. |
| RESEARCH | Contract artifact authority | 12-10 Task 1 and Task 3 | COVERED | Canonical intended paths and refreshed-hash swaps are tested. |
| RESEARCH | Calibrated consumer semantics | 12-10 Task 2 and Task 3 | COVERED | forecast_dashboard_matches() emits the calibrated per-match outcome view; calibrated categories drive group/stage points/ranking and knockout advancement while raw scoreline distributions remain goal-bookkeeping evidence. |
| CONTEXT | D-01, D-04 | 12-10 Task 2 | COVERED | Calibrated derived 1X2 branch follows the frozen gate and preserves score distributions. |
| CONTEXT | D-08, D-09, D-12 | 12-10 Task 1 and Task 3 | COVERED | Freeze/evaluation links are unconditional for approved and incumbent-retained releases, exact raw decision tokens are hashed, and preflight-before-load ordering is enforced. |
| CONTEXT | D-13, D-14, D-15, D-16 | 12-10 Tasks 1-3 | COVERED | Incumbent fallback, complete release contract, manifest-only authority, strict loading, and UI preservation are covered. |
</source_audit>

<artifacts>
## Artifacts this phase produces

- Hardened `R/release/release_contract.R` trusted-root, preflight-handoff, decision-identity, freeze-link, artifact-identity, and loaded-identity behavior.
- Hardened `R/release/release_bundle.R` canonical contract-path and evidence-link validation.
- Calibrated release-consumer wiring in `R/visualization/worldcup_dashboard.R` with unchanged scoreline distributions.
- Focused temporary-fixture regressions in `tests/testthat/test_phase12_release.R` and `tests/testthat/test_worldcup_dashboard.R`.
- No durable release, benchmark, dashboard, generated output, `_targets.R`, or `scripts/update_worldcup_dashboard.R` changes.
</artifacts>

<output>
Create `.planning/phases/12-calibration-promotion-and-model-release/12-10-SUMMARY.md` when done.
</output>
