---
phase: 08-forecast-ledger-and-wc-2026-retrospective
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - R/evaluation/proper_scores.R
  - R/evaluation/worldcup_retrospective.R
  - tests/testthat/test_worldcup_scoring.R
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]
user_setup: []
must_haves:
  truths:
    - "D-09: Regulation-time RPS is primary; Brier and log loss support it; accuracy is descriptive."
    - "D-10: Headline aggregates weight fixtures equally and stage/team-balanced cuts remain diagnostics."
    - "D-11: Joint and marginal goal distributions, totals, and BTTS receive proper scores without distribution synthesis."
    - "D-12: Deterministic bootstrap intervals and sparse-calibration warnings quantify uncertainty."
  artifacts:
    - path: R/evaluation/proper_scores.R
      provides: Pure proper-score, calibration, and bootstrap helpers
    - path: R/evaluation/worldcup_retrospective.R
      provides: Match, advancement, stage-reach, and aggregate evaluation
    - path: tests/testthat/test_worldcup_scoring.R
      provides: Formula and tournament-contract tests
  key_links:
    - from: R/evaluation/worldcup_retrospective.R
      to: outputs/evaluation/wc2026/forecast_ledger.rds
      via: strict and exploratory selected views
---

<objective>
Score match outcomes, goal distributions, advancement, and stage reach with a
single deterministic evaluation contract and honest uncertainty.

Purpose: Turn the verified ledger into comparable evidence without selection,
aggregation, or calibration leakage.
Output: Scoring modules, metric tables, uncertainty summaries, and tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-RESEARCH.md
@.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-01-SUMMARY.md
@R/forecast/monte_carlo.R
@R/forecast/calibration.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure proper-score functions</name>
  <files>R/evaluation/proper_scores.R, tests/testthat/test_worldcup_scoring.R</files>
  <read_first>R/forecast/calibration.R, R/forecast/monte_carlo.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-RESEARCH.md</read_first>
  <action>Create validated pure functions `multiclass_brier()`, `ranked_probability_score()`, `log_score()`, `binary_brier()`, `discrete_rps()`, `score_scoreline_distribution()`, and `derive_binary_markets()`. Fix the 1X2 order to home/draw/away, normalize RPS by two, require probability sums within `1e-6`, and expose `epsilon = 1e-15` only for log calculations. Derive home/away marginals, over 2.5, and BTTS directly from stored scoreline probabilities. Add hand-calculated perfect, uniform, confidently wrong, missing observed cell, and invalid-distribution tests.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_scoring.R")'</verify>
  <acceptance_criteria>
    - Perfect forecasts return zero RPS and Brier score.
    - Uniform and confidently wrong fixture expectations match hand-calculated values within `1e-12`.
    - Invalid or non-normalized distributions fail with named contract errors.
    - Goal-market probabilities are derived only from stored scoreline rows.
  </acceptance_criteria>
  <done>All formula and distribution contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Score fixtures and build aggregates, calibration, and intervals</name>
  <files>R/evaluation/worldcup_retrospective.R, tests/testthat/test_worldcup_scoring.R</files>
  <read_first>R/evaluation/proper_scores.R, R/evaluation/worldcup_ledger.R, .planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-CONTEXT.md</read_first>
  <action>Implement `score_worldcup_matches()`, `aggregate_worldcup_scores()`, `make_calibration_bins()`, and `bootstrap_worldcup_scores()`. Produce one row per fixture/view/metric with eligibility and metric-specific coverage. Headline strict `latest_valid` regulation-time RPS and retain Brier/log loss, goal scores, totals, BTTS, expected-goal errors, exact-score hit, accuracy, stage cuts, outcome-class cuts, confidence cuts, and team-balanced diagnostics. Use equal fixture weights. Build deterministic quantile calibration bins with `min_bin_size = 5`, `max_bins = 10`; flag sparse cuts. Use fixture bootstrap with default `reps = 2000`, `conf = 0.95`, `seed = 20260720`, including paired first/latest deltas only where both exist.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_scoring.R")'</verify>
  <acceptance_criteria>
    - Every aggregate includes `sample`, `view`, `metric`, `n_scored`, `n_official`, `coverage`, estimate, and interval fields.
    - Strict and exploratory records never share an aggregate row.
    - Headline weighting equals the arithmetic mean of fixture-level RPS values.
    - Bootstrap output is identical for repeated runs with seed `20260720`.
    - Calibration bins with fewer than five observations are marked sparse.
  </acceptance_criteria>
  <done>Aggregate, calibration, and deterministic uncertainty tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Score advancement and anchored stage-reach forecasts</name>
  <files>R/evaluation/worldcup_retrospective.R, tests/testthat/test_worldcup_scoring.R</files>
  <read_first>R/evaluation/worldcup_retrospective.R, R/evaluation/worldcup_ledger.R, outputs/dashboard/worldcup_stage_probabilities.csv, outputs/dashboard/worldcup_bracket_prematch_forecasts.csv</read_first>
  <action>Add `score_knockout_advancement()`, `select_stage_reach_anchors()`, `score_stage_reach()`, and `write_worldcup_score_bundle()`. Score knockout match advancement against the actual advancing team. For stage reach, use the earliest valid tournament snapshot as the pre-tournament anchor and the latest valid snapshot strictly before each stage starts as the updated anchor; never count hourly revisions as independent observations. Write match, aggregate, calibration, advancement, and stage-reach CSVs with checksums and update the bundle manifest.</action>
  <verify>Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_scoring.R")'</verify>
  <acceptance_criteria>
    - All scored knockout fixtures use advancement rather than regulation outcome as the binary target.
    - Stage anchors are deterministic and strictly precede the event boundary they forecast.
    - Repeated hourly snapshots do not inflate stage-reach sample size.
    - Metric files expose strict/exploratory sample labels and target-specific coverage.
  </acceptance_criteria>
  <done>All EVAL-01, EVAL-02, and EVAL-03 scoring artifacts are reproducible.</done>
</task>

</tasks>

<verification>
- [ ] `Rscript --vanilla -e 'testthat::test_file("tests/testthat/test_worldcup_scoring.R")'` passes.
- [ ] Headline RPS is an equal-weight fixture mean over strict latest forecasts.
- [ ] Every goal metric reports independent coverage and uses no synthesized distribution.
- [ ] Advancement and stage-reach anchors predate their target events.
</verification>

<success_criteria>
- All proper scores agree with hand-calculated fixtures.
- Strict/exploratory, first/latest, stage, class, and confidence cuts are explicit.
- Bootstrap intervals and calibration diagnostics are deterministic.
</success_criteria>

## Artifacts This Phase Produces

- `R/evaluation/proper_scores.R`: `multiclass_brier()`,
  `ranked_probability_score()`, `log_score()`, `binary_brier()`,
  `discrete_rps()`, `score_scoreline_distribution()`, and
  `derive_binary_markets()`.
- `R/evaluation/worldcup_retrospective.R`: `score_worldcup_matches()`,
  `aggregate_worldcup_scores()`, `make_calibration_bins()`,
  `bootstrap_worldcup_scores()`, `score_knockout_advancement()`,
  `select_stage_reach_anchors()`, `score_stage_reach()`, and
  `write_worldcup_score_bundle()`.
- `tests/testthat/test_worldcup_scoring.R`.
- `outputs/evaluation/wc2026/{match_scores.csv,aggregate_scores.csv,calibration_bins.csv,advancement_scores.csv,stage_reach_scores.csv}`.

<output>
After completion, create `.planning/phases/08-forecast-ledger-and-wc-2026-retrospective/08-02-SUMMARY.md`.
</output>
