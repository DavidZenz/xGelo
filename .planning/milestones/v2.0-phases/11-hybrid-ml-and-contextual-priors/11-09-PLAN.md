---
phase: 11-hybrid-ml-and-contextual-priors
plan: 09
type: execute
wave: 6
depends_on:
  - 11-06
files_modified:
  - tests/testthat/test_hybrid_context_features.R
autonomous: true
gap_closure: true
requirements:
  - HYBRID-02

must_haves:
  truths:
    - "The context feature contract test expects the production nine-candidate Phase 11 registry, including the xG-gated and structural candidates."
    - "The six context ablations are asserted separately from the two non-context candidates, so registry maintenance cannot turn xG or structural entries into ablations."
    - "The focused target test and the complete testthat suite pass with warnings treated as failures, without changing production registry or promotion behavior."
  artifacts:
    - path: tests/testthat/test_hybrid_context_features.R
      provides: "Registry-aligned candidate and ablation expectations"
  key_links:
    - from: data/benchmark/phase11/model_registry.csv
      to: tests/testthat/test_hybrid_context_features.R
      via: "Nine production candidate IDs and six context-only ablation IDs"
    - from: tests/testthat/test_hybrid_context_features.R
      to: tests/testthat/test_hybrid_targets.R
      via: "Focused context contract and target-DAG regression coverage"
---

<objective>
Remove the two stale expected-candidate assertions in the context contract test and prove the corrected expectation through the targeted target test and complete suite.

Purpose: The production registry already contains nine candidates; the test still encodes the earlier seven-candidate list and can obscure the actual inactive-but-registered xG and structural contracts.
Output: One bounded test-maintenance change and failure-propagating focused/full-suite evidence; no production candidate, bundle, or promotion change.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-VALIDATION.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-VERIFICATION.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-05-SUMMARY.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-06-SUMMARY.md
@data/benchmark/phase11/model_registry.csv
@tests/testthat/test_hybrid_context_features.R
@tests/testthat/test_hybrid_targets.R
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Align context expectations with the nine-candidate registry</name>
  <files>tests/testthat/test_hybrid_context_features.R</files>
  <action>Update the stale expected-candidate assertions at the reported lines 77-78 to include the exact production IDs for the open RF base, six context/drop-one candidates, `phase11_rf_dynamic_elo_context_xg_gated_open`, and `phase11_structural_sparse_prior_open`. Keep the candidate order and spelling identical to `data/benchmark/phase11/model_registry.csv`. Replace the positional ablation expectation with an explicit six-ID context-only vector so the xG-gated and structural candidates remain registered but are not treated as context ablations. This maintenance change implements D-05 and preserves D-09, D-12, D-15, and D-16: it changes assertions only, does not activate inactive candidates, and does not add any Phase 12 or promotion call.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_context_features.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <done>The focused context contract passes with nine expected candidates and exactly six context-ablation IDs, while xG and structural entries remain distinct registered candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Run bounded target and complete-suite regression checks</name>
  <files>tests/testthat/test_hybrid_context_features.R</files>
  <action>Run the Phase 11 target-DAG contract test and then the complete `tests/testthat` suite using the repository’s failure-propagating commands with warnings treated as failures. Confirm the registry remains nine candidates, the target test remains green, and the suite change is limited to the stale expectation maintenance. Keep the sealed-WC2026, research-only, fail-closed, and Phase 12-only promotion boundaries unchanged under D-04, D-07, D-12, D-15, and D-16.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_targets.R", stop_on_failure = TRUE, stop_on_warning = TRUE); testthat::test_dir("tests/testthat", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <done>The target-specific regression and full testthat suite pass without warnings, and the only implementation delta is the registry-aligned expectation in `test_hybrid_context_features.R`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Production registry → test expectations | Registry drift can create false failures or false confidence if assertions are incomplete. |
| Test suite → Phase 11 handoff | A passing suite must not grant candidate freeze or promotion authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-09-01 | Tampering | `test_hybrid_context_features.R` candidate list | medium | mitigate | Compare every expected ID to the checked-in model registry and assert a separate explicit six-ID ablation set. |
| T-11-09-02 | Spoofing | Test pass reported without target/full-suite execution | medium | mitigate | Use failure-propagating focused and full `testthat` commands with warnings treated as failures. |
| T-11-09-03 | Elevation of privilege | Test maintenance and promotion boundary | high | mitigate | Keep registry, bundle, candidate activation, WC2026 seal, and Phase 12 promotion authority untouched. |
| T-11-09-SC | Tampering | Package installation surface | high | mitigate | No package installation is planned; any later install remains outside this plan and must pass the package legitimacy gate and a blocking human review. |
</threat_model>

<verification>
The focused context test, Phase 11 target test, and complete testthat suite all exit successfully with warning failures enabled. Inspect the final diff to confirm only the stale candidate expectation changed.
</verification>

<success_criteria>
Both stale assertions are aligned with the nine-candidate production registry, context ablations remain six explicit IDs, and the target plus complete suite pass without changing candidate activation or promotion authority.
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-09-SUMMARY.md` when done.
</output>
