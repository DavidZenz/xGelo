---
phase: 11-hybrid-ml-and-contextual-priors
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - R/forecast/context_features.R
  - R/benchmark/hybrid_protocol.R
  - R/benchmark/hybrid_adapters.R
  - R/benchmark/hybrid_runner.R
  - data/benchmark/phase11/country_centroids.csv
  - data/benchmark/phase11/country_centroids_metadata.csv
  - data/benchmark/phase11/model_registry.csv
  - data/benchmark/phase11/feature_contract.csv
  - data/benchmark/phase11/context_ablation_registry.csv
  - tests/testthat/test_hybrid_context_features.R
autonomous: true
requirements:
  - HYBRID-02
must_haves:
  truths:
    - "The open-context feature set contains host, neutral venue, rest, travel, and tournament-stage features with provenance and missingness."
    - "Individual ablations identify incremental context value without changing the established 630 open denominator."
  artifacts:
    - "R/forecast/context_features.R"
    - "data/benchmark/phase11/country_centroids.csv"
    - "data/benchmark/phase11/country_centroids_metadata.csv"
    - "data/benchmark/phase11/model_registry.csv"
    - "data/benchmark/phase11/context_ablation_registry.csv"
  key_links:
    - "fixture metadata/history + centroid registry -> context evidence companions -> feature contract -> model registry rows -> ablation registry -> hybrid adapter dispatch -> hybrid runner/scorer"
    - "context bundle and each registered context ablation model ID -> R/benchmark/hybrid_adapters.R -> R/benchmark/hybrid_runner.R -> common G=40 benchmark scores"
  prohibitions:
    - "No WC2026 opening, scoring, leakage, tuning, calibration, or candidate selection against sealed 2026 holdout outputs."
    - "No silent optional-data activation; xG, structural, enriched squad, and external market modes must fail closed or publish explicit inactive reasons."
    - "No raw restricted-data publication; committed outputs may include only permitted open data, derived aggregates, and licensed metadata or probabilities."
    - "No automated bookmaker, Transfermarkt, or FotMob scraping or live collection paths."
    - "No Phase 12 promotion, release, dashboard-update, or final-selection authority; Phase 11 remains research_only evidence."
---

<objective>
Add the named open-context feature set and its individual ablations.

Purpose: Test tournament context as open-data evidence while preserving the frozen benchmark estimand and transparent feature attribution.
Output: Context feature builder, feature-contract rows, and ablation registry.
</objective>

<execution_context>
@/Users/davidzenz/.codex/gsd-core/workflows/execute-plan.md
@/Users/davidzenz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-CONTEXT.md
@.planning/phases/11-hybrid-ml-and-contextual-priors/11-PATTERNS.md
@R/forecast/features.R
@R/benchmark/contracts.R
@data/benchmark/phase09/fixtures.csv
@data/benchmark/phase09/panel_fixtures.csv
@tests/testthat/test_hybrid_context_features.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive open-context feature evidence</name>
  <files>R/forecast/context_features.R, data/benchmark/phase11/country_centroids.csv, data/benchmark/phase11/country_centroids_metadata.csv, tests/testthat/test_hybrid_context_features.R</files>
  <read_first>R/forecast/features.R; R/benchmark/contracts.R; data/benchmark/phase09/fixtures.csv</read_first>
  <behavior>
    - D-05 / HYBRID-02: `build_open_context_features()` returns host, neutral, rest, travel, and stage values.
    - D-08: rest derives from prior match dates; travel derives from prior venue country or host-country centroid to current venue/host centroid; stage derives from checked fixture metadata; every feature records source, vintage, derivation, and missingness.
  </behavior>
  <action>Implement `R/forecast/context_features.R` with helpers for `phase11_context_required_columns()`, `load_phase11_country_centroids()`, `build_open_context_features()`, and `validate_open_context_feature_evidence()`. Create committed centroid inputs at `data/benchmark/phase11/country_centroids.csv` and `data/benchmark/phase11/country_centroids_metadata.csv` before travel execution. Source rule: freeze WGS84 country proxy coordinates derived from Natural Earth Admin 0 public-domain country geometries or CEPII GeoDist country-coordinate fields, normalized to ISO3 with `countrycode`; include `country_iso3`, `country_name`, `latitude`, `longitude`, `coordinate_role` (`admin0_centroid` or `country_proxy`), `source_name`, `source_url_or_label`, `source_vintage`, `license_class`, `derivation_rule`, `parent_source_sha256`, and `row_sha256`. Use `geosphere::distGeo()` against this committed registry for prior-location-to-current-location travel, using venue-country rows when present and host-country centroid fallback only when the fixture history lacks a prior venue country. Validate parent hashes before computing `travel_km`; benchmark execution must be network-free and must fail if a required venue or host country is absent from the registry. Preserve `add_forecast_feature_evidence()` companion semantics. Do not describe travel as stadium-level unless a checked stadium source is actually present.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_context_features.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_benchmark_contracts.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>Context features are deterministic, centroid registry parent hashes validate, source-dated evidence is strictly before cutoff where applicable, travel is computed from committed country proxies without network access, derived fixture evidence is distinct from source-backed evidence, and missing common-panel values are rejected.</acceptance_criteria>
  <done>Open-context features are available as point-in-time evidence for RF/context candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register context bundle and ablations</name>
  <files>R/benchmark/hybrid_protocol.R, R/benchmark/hybrid_adapters.R, R/benchmark/hybrid_runner.R, data/benchmark/phase11/model_registry.csv, data/benchmark/phase11/country_centroids.csv, data/benchmark/phase11/country_centroids_metadata.csv, data/benchmark/phase11/feature_contract.csv, data/benchmark/phase11/context_ablation_registry.csv, tests/testthat/test_hybrid_context_features.R</files>
  <read_first>R/benchmark/challenger_protocol.R; R/benchmark/hybrid_adapters.R; R/benchmark/hybrid_runner.R; data/benchmark/phase11/model_registry.csv; data/benchmark/phase09/feature_contract.csv; data/benchmark/phase09/panel_fixtures.csv</read_first>
  <behavior>
    - D-05: The full context bundle and one-feature-drop ablations are named and registered.
    - D-06 / D-07: Primary headline comparison uses the established open core; supplemental regional diagnostics are labelled and cannot alter the denominator.
  </behavior>
  <action>Extend `hybrid_protocol.R` with canonical context feature-contract rows, centroid-registry validators, and `canonical_phase11_context_ablation_registry()`. Register the combined `open_context` feature set plus individual ablations for host, neutral, rest, travel, and stage. Add explicit HYBRID-02 model rows to `data/benchmark/phase11/model_registry.csv`: one full context bundle candidate such as `phase11_rf_dynamic_elo_context_open`, plus one registered drop-one ablation for each named feature, for example `phase11_rf_dynamic_elo_context_drop_host_open`, `phase11_rf_dynamic_elo_context_drop_neutral_open`, `phase11_rf_dynamic_elo_context_drop_rest_open`, `phase11_rf_dynamic_elo_context_drop_travel_open`, and `phase11_rf_dynamic_elo_context_drop_stage_open`. Each row must declare `mode=open_default`, `panel=open_core`, `open_fixture_count=630`, `rich_fixture_count=609`, `score_support_g=40`, the inherited RF/NB adapter family from 11-02, feature set ID, removed-feature rule when applicable, settings hash, seed ID, source parent hashes, `research_only=TRUE`, and `wc2026_sealed=TRUE`. Wire those candidate IDs in `R/benchmark/hybrid_adapters.R` so dispatch builds the context feature matrix, drops only the registered ablation feature for ablation IDs, rejects unavailable common-panel context values instead of imputing them, then emits the same G=40 score-distribution schema as the 11-02 RF candidate. Extend `R/benchmark/hybrid_runner.R` so `run_hybrid_challenger_benchmark()` expands and scores the context bundle and every registered context ablation through the common Phase 09/10 fold, panel, prediction, and proper-score path; persist per-candidate coverage, feature-parent hashes, ablation metadata, and inactive/error reasons in runner evidence. Include centroid artifact SHA-256 and metadata SHA-256 as parents for `travel_km`, license class `open-or-derived-open`, availability rule, derivation rule, missingness rule, and row hashes. Keep Copa America/AFCON out of the primary panel; if any supplemental diagnostic hooks are added, label them `supplemental_training_or_regional_diagnostic` and exclude them from open-core candidate selection.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_hybrid_context_features.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test_benchmark_contracts.R", stop_on_failure = TRUE, stop_on_warning = TRUE)'</automated>
  </verify>
  <acceptance_criteria>The context registry validates; `model_registry.csv` contains one full context bundle model ID and one model ID per registered context ablation; `hybrid_adapters.R` dispatches each ID to the registered feature set or drop-one feature set; `hybrid_runner.R` includes those IDs in benchmark scoring; `travel_km` declares the centroid registry parent hashes; every ablation has one parent and one named removed feature; headline panel remains open_core=630; unavailable context cannot be silently imputed for headline comparisons.</acceptance_criteria>
  <done>HYBRID-02 context bundle and ablations are registry-backed, adapter-dispatched, runner-scored, and test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture/history data -> context builder | Open fixture and prior-match facts become model features. |
| Context evidence -> model registry | Feature availability controls candidate identity and panel eligibility. |

## Phase-Wide Risk Coverage

| Risk | Applicability | Plan Treatment |
|------|---------------|----------------|
| Historical leakage | applicable | Rest, travel, stage, host, and neutral features validate prior evidence, cutoff dates, and fixture-derived source rules. |
| Silent activation | applicable | Context candidates require complete open-context evidence and reject unavailable headline-panel values rather than activating with implicit imputation. |
| Restricted-data redistribution | not applicable | This plan uses open fixture data and committed open geography centroids only. |
| Registry/bundle drift | applicable | Feature-contract rows and context ablation registry include source artifact hashes, centroid parent hashes, and row hashes. |
| Manual snapshot contamination | applicable | The centroid registry is a manual/frozen geography input and must include source vintage, license, parent checksum, and row hashes before travel use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Tampering | `R/forecast/context_features.R` | high | mitigate | Validate all source dates and derivation metadata, rejecting same-day/future rest/travel/stage evidence per D-08. |
| T-11-11 | Tampering | `context_ablation_registry.csv` | medium | mitigate | Register individual ablations so combined-context gains cannot hide one feature's effect per D-05. |
| T-11-12 | Repudiation | Context feature provenance | medium | mitigate | Store source artifact hashes, license class, derivation rule, and missingness rule in the feature contract. |
| T-11-13 | Denial of Service | Geography dependency | low | mitigate | Fail early with dependency/source messages for countrycode/geosphere/geography facts instead of partial travel output. |
| T-11-SC | Tampering | Package installs | low | accept | No package-manager install is performed by this plan. |
</threat_model>

<verification>
Run the context feature test and the inherited benchmark contract test after edits.
</verification>

<success_criteria>
HYBRID-02 is implemented as a named open-data feature set with strict provenance and ablation attribution.
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ml-and-contextual-priors/11-03-SUMMARY.md` when done.
</output>
